# 2026-04-24

- Reinforced a durable skill-authoring rule: do not invent future schema/template structure in skills or docs. If something is unresolved, keep it explicit as an open gap instead of pretending it is already standardized.
- Tightened that rule further for active skills and project docs: remove parked `Not yet defined` placeholders and replace them with concrete content, explicit follow-up tasks, or `None.` empties where appropriate.
- Consolidated the local development skill surface around `skills/openclaw/project_development` and `skills/codex/project_execution`, with `skill-map.md` references and tighter docs-boundary ownership.
- Standardized shared default vocabularies in `universal-catalog` for `task_lifecycle_state`, `review_readiness`, `acceptance_outcome`, `test_status`, `maintenance_status`, and `docs_status`, and updated skill templates to use concrete JSON examples.
- Ratified the current `universal-catalog` boundary: markdown docs/prompt templates stay in skills; `storage/templates/` is reserved for real catalog-owned `output` templates; `script` entries are full source-file addresses; `term` entries are approved definitions.
- Added the first secret-alias config model to `universal-catalog`: `kind = config` may store a reviewed secret alias reference, but never a secret value or raw secret file contents. Added a read-only secret resolver helper under `universal-catalog/src/`.
- Established the local secret alias pattern for this host: catalog config key -> secret alias -> `/root/secrets/registry.json` -> local secret file. `TOOLS.md` now records this storage/lookup/use rule.
- Registered `NETWORK_FRAMEWORK_COMPANION_TOKEN_SECRET_ALIAS` in `universal-catalog` with payload `network-framework/companion-token`, and added the matching alias entry to `/root/secrets/registry.json`.
- Re-aligned `network-framework` to its fixed docs spine and ratified the ordered runtime-function model: shared config/bootstrap compatibility, desktop entry/UI control, local onboarding actions, local workstation status probing, and a read-only remote status companion.
- Ratified the phase-1 `network-framework` remote companion boundary as authenticated read-only `GET /v1/status` over tailnet-scoped HTTPS, grounded on normalized `openclaw status --json`, with the companion bearer token resolved through a local secret alias instead of committed config.
- Implemented and pushed the phase-1 `network-framework` desktop/runtime/companion slice while preserving the existing bootstrap path. The repo now includes `src/lib/`, `src/desktop/`, `src/runtime/`, and `src/companion/` ownership boundaries plus dedicated `test:desktop`, `test:runtime`, and `test:companion` commands.
- Recorded a durable review rule for future acceptance: reject implementations that reintroduce HTTP fallback where docs require HTTPS-only, or that treat newly invented permanent names as already-ratified without OpenClaw review.
- Updated `AGENTS.md` so development-oriented new sessions should read `skills/openclaw/project_development/SKILL.md` immediately unless a more specific skill clearly overrides it.
- Raised the local OpenClaw LLM idle-timeout setting to 300 seconds in `~/.openclaw/openclaw.json` and verified the gateway came back successfully after restart.
# 2026-04-24

- Reinforced a durable skill-authoring rule: do not invent future schema/template structure in skills or docs. If something is unresolved, keep it explicit as an open gap instead of pretending it is already standardized.
- Tightened that rule further for active skills and project docs: remove parked `Not yet defined` placeholders and replace them with concrete content, explicit follow-up tasks, or `None.` empties where appropriate.
- Consolidated the local development skill surface around `skills/openclaw/project_development` and `skills/codex/project_execution`, with `skill-map.md` references and tighter docs-boundary ownership.
- Standardized shared default vocabularies in `universal-catalog` for `task_lifecycle_state`, `review_readiness`, `acceptance_outcome`, `test_status`, `maintenance_status`, and `docs_status`, and updated skill templates to use concrete JSON examples.
- Ratified the current `universal-catalog` boundary: markdown docs/prompt templates stay in skills; `storage/templates/` is reserved for real catalog-owned `output` templates; `script` entries are full source-file addresses; `term` entries are approved definitions.
- Added the first secret-alias config model to `universal-catalog`: `kind = config` may store a reviewed secret alias reference, but never a secret value or raw secret file contents. Added a read-only secret resolver helper under `universal-catalog/src/`.
- Established the local secret alias pattern for this host: catalog config key -> secret alias -> `/root/secrets/registry.json` -> local secret file. `TOOLS.md` now records this storage/lookup/use rule.
- Registered `NETWORK_FRAMEWORK_COMPANION_TOKEN_SECRET_ALIAS` in `universal-catalog` with payload `network-framework/companion-token`, and added the matching alias entry to `/root/secrets/registry.json`.
- Re-aligned `network-framework` to its fixed docs spine and ratified the ordered runtime-function model: shared config/bootstrap compatibility, desktop entry/UI control, local onboarding actions, local workstation status probing, and a read-only remote status companion.
- Ratified the phase-1 `network-framework` remote companion boundary as authenticated read-only `GET /v1/status` over tailnet-scoped HTTPS, grounded on normalized `openclaw status --json`, with the companion bearer token resolved through a local secret alias instead of committed config.
- Implemented and pushed the phase-1 `network-framework` desktop/runtime/companion slice while preserving the existing bootstrap path. The repo now includes `src/lib/`, `src/desktop/`, `src/runtime/`, and `src/companion/` ownership boundaries plus dedicated `test:desktop`, `test:runtime`, and `test:companion` commands.
- Recorded a durable review rule for future acceptance: reject implementations that reintroduce HTTP fallback where docs require HTTPS-only, or that treat newly invented permanent names as already-ratified without OpenClaw review.
- Updated `AGENTS.md` so development-oriented new sessions should read `skills/openclaw/project_development/SKILL.md` immediately unless a more specific skill clearly overrides it.
- Raised the local OpenClaw LLM idle-timeout setting to 300 seconds in `~/.openclaw/openclaw.json` and verified the gateway came back successfully after restart.
- User clarified the preferred future `network-framework` packaged-client model: the Windows installer should ask only for node name and install path, then auto-configure the rest.
- User prefers VPN and OpenClaw node to be installed as services but started and stopped manually from the desktop app rather than auto-starting.
- User wants SMB treated as a persistent mounted baseline, not an app-controlled service toggle; the app should observe it and offer open/repair affordances rather than own its lifecycle.
- User requested a durable standard that all projects emphasize clear maintained-file boundaries, reduced overlap, and explicit rules for which information belongs in which files, including memory files. This was implemented across `AGENTS.md`, `skills/openclaw/memory_management/references/routing.md`, and `skills/openclaw/project_development/SKILL.md`, then committed/pushed as `04c6e5f Strengthen file boundary standards`.
- OpenClaw model configuration was corrected to match available authentication: only `openai-codex` is authenticated on this host. Default/current model was moved to `openai-codex/gpt-5.5-pro`; `openai-codex/gpt-5.5` and `openai-codex/gpt-5.4` remain configured/available; unauthenticated `openai/gpt-5.5` was removed from configured models.
- User asked for the expired/inactive memory-core dreaming narrative session cleanup policy. We found four completed `dreaming-narrative-*` sessions still registered after 13–17 hours because current OpenClaw cleanup archives orphan transcripts but does not TTL-remove completed sessions whose transcripts still exist.

## 2026-04-24 late-session updates

- User asked to set OpenClaw default model back to `openai-codex/gpt-5.5`; it was confirmed available/authenticated along with `openai-codex/gpt-5.5-pro`.
- User then asked to switch Codex/OpenClaw usage to `openai-codex/gpt-5.5-pro` and smoke-test whether it can respond. Model status output showed `openai-codex/gpt-5.5-pro` as default/configured/authenticated, but the subagent smoke test completed with no visible output/tokens, so the practical response test was inconclusive and should be rechecked after compaction.
- Added manual expired dreaming narrative session cleanup to the memory-management skill: `skills/openclaw/memory_management/references/dreaming-cleanup.md` and `scripts/cleanup_expired_dreaming_sessions.py`. The cleanup is scoped to `dreaming-narrative-*`, defaults to dry-run, requires explicit `--execute`, uses a 6h TTL by default, and archives files/store backup under `.openclaw-repair/dreaming-session-cleanup/<timestamp>/` instead of hard-deleting.
- Ran the dreaming cleanup against main sessions and reduced `dreaming-narrative-*` store entries from 4 to 0; archived transcripts/trajectory sidecars and `sessions.json.backup` under `/root/.openclaw/workspace/.openclaw-repair/dreaming-session-cleanup/2026-04-24T20-26-13Z/`. Workspace commit reported: `2bc090c Add dreaming session cleanup to memory skill`.

## network-framework packaged-client control model accepted

- Accepted and pushed `network-framework` commit `898dda1 Implement packaged client control model`.
- Implemented NSIS installer artifact, catalog-backed packaged defaults, hidden Windows command execution, explicit VPN/OpenClaw Node service start/stop controls, and SMB check/open/repair without SMB service lifecycle semantics.
- Added command-result redaction so auth keys and SMB passwords are not exposed through structured action results.
- Verification passed: `git diff --check`, `npm test`, `npm run test:desktop`, `npm run test:runtime`, `npm run test:companion`, `npm run test:installer`, `node src/cli.js build --config config/server.example.json --output dist/network-framework.ps1`, and docs spine file check.


## Pre-compaction flush — 2026-04-24 18:12 EDT

- Project data storage policy was clarified and written into the project-development skill:
  - SQL is not the default destination for all data.
  - SQL requires long-term structured value, repeated query/share needs, constraints, transactions, indexes/joins, or large stable schema datasets.
  - Logs/traces/runtime noise use retention; blobs/media/generated artifacts use file/object storage; CSV is for human-reviewed table artifacts; small isolated structured data should usually use CSV/JSON.
  - Dedicated SQL tables require justification that SQL is better than CSV/JSON/file/object/log/cache/generated storage.
  - Related commits reported during the session: `ce4b7c2`, `96ba128`, `c256cbd`.
- `universal-catalog/src/` was inspected after the storage-policy discussion. No rewrite was needed because helpers are catalog-specific, read-only, query-executor-injected, not connection owners, not bulk-data readers, and tests passed. Future large tables such as stock bars should get dedicated table/helper/query layers rather than expanding `catalog-reader` into a generic data access layer.
- Returned to `/root/projects/network-framework` and implemented the ready packaged-client task. Accepted and pushed commit: `898dda1 Implement packaged client control model`.
- `network-framework` packaged-client implementation details now include:
  - NSIS installer artifact at `installer/network-framework.nsi` plus `installer/README.md`.
  - Minimal installer inputs: install path and OpenClaw node name.
  - No VPN/OpenClaw service auto-start during install.
  - No Windows Registry-backed settings layer for this slice.
  - Catalog-backed packaged defaults in `src/lib/catalog-defaults.js`.
  - Hidden command execution via `windowsHide: true` and hidden PowerShell args while preserving GUI/tray surfaces.
  - Explicit VPN and OpenClaw Node service start/stop/status controls.
  - SMB remains persistent baseline mount with check/open/repair only; no SMB service toggle.
- Important acceptance fix before accepting `network-framework`: structured command results now redact secret-bearing args such as Tailscale auth keys and SMB passwords so UI/status surfaces do not expose secrets.
- `network-framework` verification evidence for accepted packaged-client work:
  - `git diff --check`
  - `npm test` → 25 pass
  - `npm run test:desktop` → 3 pass
  - `npm run test:runtime` → 7 pass
  - `npm run test:companion` → 8 pass
  - `npm run test:installer` → 2 pass
  - `node src/cli.js build --config config/server.example.json --output dist/network-framework.ps1`
  - `find docs -maxdepth 1 -type f | sort`
- Current durable project state: `network-framework` repo was clean and pushed to GitHub after commit `898dda1`.

## network-framework Tailscale reuse-first boundary

- User clarified that if Tailscale is already installed, `network-framework` should not download/install it again.
- User's current Tailscale setup uses GitHub login; public project docs were generalized to preserve existing authenticated identity-provider sessions, including GitHub login, without exposing unnecessary personal framing.
- Pushed `network-framework` commit `607e97b Define Tailscale reuse-first setup boundary`.
- Docs now require Tailscale setup to detect and reuse existing install/auth state, avoid forced auth-key login when already authenticated, and reject re-download/reinstall behavior for valid local Tailscale installations.
- `docs/04_task.md` now has ready next task `task_packaged_client_binary_build_definition` for the next packaged-client binary build slice.

## network-framework server-side Tailscale prerequisite boundary

- User approved adding server-side Tailscale installation/configuration steps to the `network-framework` docs/task boundary.
- Pushed `network-framework` commit `e96a5ff Define server Tailscale prerequisite boundary`.
- Docs now treat server-side Tailscale as an explicit prerequisite before Windows clients can reach OpenClaw gateway, SMB, dashboard, or companion endpoints over the tailnet.
- The accepted boundary allows detecting/installing/starting the server Tailscale client only as an operator-approved setup action.
- Tailscale identity-provider/browser login, tailnet approval, auth keys, preauth keys, login URLs, and tokens must remain human-controlled and out of committed project files.
- Added ready task `task_server_tailscale_prerequisite_setup`; `docs/04_task.md` now has two ready dispatches: server Tailscale prerequisite setup and packaged-client binary build definition.

## network-framework prerequisite install-assist boundary

- User clarified that the whole program should prepare all required software/apps, integrate client-side prerequisites into installer/setup where needed, help configure them, and skip anything already installed.
- Pushed `network-framework` commit `8a501f4 Define prerequisite install-assist boundary`.
- New accepted principle: required software setup is detect-first, reuse-first, and idempotent.
- Packaged-client setup must check each required app/service first, skip valid existing installs, install/bundle/guide only missing prerequisites, and keep installation separate from user-controlled login/start/connect/approval actions.
- This expands the next packaged-client binary task boundary to preserve prerequisite detect-first/install-assist behavior alongside minimal-input, no-Registry, hidden-command, explicit-service-control, SMB-baseline, and Tailscale reuse-first rules.

## network-framework packaged-client default choices

- User delegated remaining `network-framework` setup/binary-build choices to OpenClaw judgment.
- Chosen defaults:
  - packaged desktop runtime: Electron
  - installer: NSIS
  - client runtime policy: bundle Electron runtime so users do not install Node.js separately
  - local config path: `%LOCALAPPDATA%\\NetworkFramework\\config\\packaged-client.json`
  - prerequisite policy: detect-first, reuse-first, install-assist
  - privilege policy: installer may elevate; normal runtime stays user-level except explicit privileged service prompts
  - server companion deployment: systemd service
  - companion TLS: Tailscale cert
  - companion default port: `18790`
  - server Tailscale login mode: operator browser/identity-provider login
- Registered these defaults in `universal-catalog` via migration `004_register_network_framework_packaging_defaults.sql`; pushed commit `4b4aca6 Register network framework packaging defaults`.
- Updated and pushed `network-framework` docs/README with commit `1f0ea5f Select packaged client binary defaults`.
- Applied catalog migrations and verified helper tests. Active `universal_catalog` now has `114` rows, `114` unique keys, `114` unique ids; `schema_migrations` includes 001-004.
- `network-framework` verification passed after docs/default choices: `git diff --check`, docs spine file check, `npm test`, `npm run test:desktop`, `npm run test:runtime`, `npm run test:companion`, `npm run test:installer`, and bootstrap build.

## network-framework server Tailscale prerequisite accepted

- Server-side Tailscale setup completed on host `openclaw` after user completed browser/identity-provider approval.
- Installed Tailscale `1.96.4`, enabled/started `tailscaled`, and verified status: service active, `BackendState=Running`, host online, DNS name present, tailnet IPs present.
- Did not commit auth keys, login URLs, tokens, or tailnet secrets.
- Updated `network-framework/docs/04_task.md` to mark `task_server_tailscale_prerequisite_setup` accepted and leave `task_packaged_client_binary_build_implementation` as the only ready dispatch.
- Pushed `network-framework` commit `5fb0c64 Accept server Tailscale prerequisite setup`.

## network-framework packaged-client binary build accepted

- Implemented and accepted `task_packaged_client_binary_build_implementation` in `/root/projects/network-framework`.
- Pushed commit `18ea977 Implement packaged client binary build`.
- Added Electron desktop entry/preload/window shell, `npm run build:desktop`, package lock with Electron `^41.3.0`, NSIS alignment for `%LOCALAPPDATA%\NetworkFramework\config\packaged-client.json`, detect-first prerequisite status helpers, and catalog-default tests using registered keys.
- Review correction: rejected/rewrote Codex-introduced unregistered names (`NETWORK_FRAMEWORK_RUNTIME_BUNDLE`, `NETWORK_FRAMEWORK_PRIVILEGE_POLICY`, companion HTTPS/default-port aliases) back to registered catalog keys (`NETWORK_FRAMEWORK_CLIENT_RUNTIME_BUNDLE_POLICY`, `NETWORK_FRAMEWORK_SERVICE_PRIVILEGE_POLICY`, `NETWORK_FRAMEWORK_COMPANION_TLS_PROVIDER`, `NETWORK_FRAMEWORK_COMPANION_PORT`, `NETWORK_FRAMEWORK_SERVER_TAILSCALE_LOGIN_MODE`) and restored `NETWORK_FRAMEWORK_LOCAL_CONFIG_STORE = project_config_boundary`.
- Verification passed: `git diff --check`, docs spine listing, `npm test`, `npm run test:desktop`, `npm run test:runtime`, `npm run test:companion`, `npm run test:installer`, bootstrap build, `npm run build:desktop`, and `npm audit --omit=optional`.
- `npm run build:desktop` prepared `dist/desktop` and copied the locally installed Electron runtime into `dist/desktop/electron-runtime`; final Windows NSIS executable production remains host/toolchain-dependent.
- No secrets, login URLs, or Windows Registry-backed settings were added.

## network-framework Windows installer artifact produced

- Implemented and accepted `task_windows_installer_artifact_production` in `/root/projects/network-framework`.
- Installed/verified NSIS `makensis` on the OpenClaw host and produced a real generated installer artifact at `dist/network-framework-setup.exe` (136M, ignored by git).
- Added `npm run build:installer` via `scripts/build-installer.js`; it prepares a Windows x64 Electron runtime bundle, invokes NSIS, verifies `dist/network-framework-setup.exe`, and records manifest status.
- Updated `scripts/prepare-desktop-bundle.js` to support explicit Electron platform/arch targets, Windows runtime download/preparation via `@electron/get` + `extract-zip`, runtime source injection for tests, and manifest fields for runtime/platform/installer status.
- Fixed NSIS cross-host compilation details: `!cd ".."`, forward-slash source/output paths, and `ReadEnvStr $NodeName "COMPUTERNAME"` instead of the previously unresolved `$COMPUTERNAME` constant.
- Pushed `network-framework` commit `6531dcf Produce Windows installer artifact`.
- Verification passed: `git diff --check`, docs spine listing, `npm test`, `npm run test:desktop`, `npm run test:runtime`, `npm run test:companion`, `npm run test:installer`, bootstrap build, `npm run build:desktop`, `npm run build:installer`, and `npm audit --omit=optional`.
- Final artifact manifest evidence: Electron `41.3.0`, `electronPlatform=win32`, `electronArch=x64`, `electronRuntimeBundled=true`, `electronRuntimePresent=true`, `nsisCommand=makensis`, `installerArtifactStatus=produced`.
- No secrets, login URLs, Registry settings, Tailscale auth keys, bearer tokens, or unattended Tailscale login behavior were added.

## network-framework packaged app icon added

- Added and accepted `task_packaged_client_app_icon` in `/root/projects/network-framework`.
- Pushed commit `1f89943 Add packaged app icon`.
- Added maintained icon assets: `assets/app-icon.svg` as canonical source, `assets/app-icon.png` for Electron window/tray, and `assets/app-icon.ico` for NSIS/Windows installer icon.
- Added `scripts/generate-icons.js` and `npm run build:icons` to regenerate PNG/ICO outputs.
- Wired the icon into Electron BrowserWindow/tray via `src/desktop/electron-main.js`, copied assets into `dist/desktop`, and added NSIS `Icon`/`UninstallIcon` directives.
- Verification passed: `git diff --check`, docs spine listing, `npm run build:icons`, `npm test`, `npm run test:desktop`, `npm run test:runtime`, `npm run test:companion`, `npm run test:installer`, bootstrap build, `npm run build:desktop`, `npm run build:installer`, and `npm audit --omit=optional`.
- Final installer manifest includes `appIconPath=assets/app-icon.png` and `installerIconPath=assets/app-icon.ico`; generated installer artifact still produced successfully.

## network-framework app icon replaced with user-approved artwork

- User supplied the final app icon artwork and asked to use it.
- Replaced the generated NF icon with the user-approved PNG as canonical source: `assets/app-icon-source.png`.
- Removed stale `assets/app-icon.svg` and updated `assets/README.md` so the canonical icon source is now the user-approved raster image.
- Reworked `scripts/generate-icons.js` to decode/resize `app-icon-source.png` and regenerate `assets/app-icon.png` plus `assets/app-icon.ico` without external image tools.
- Pushed `network-framework` commit `164f7b9 Use approved app icon artwork`.
- Verification passed: `git diff --check`, docs spine listing, `npm run build:icons`, `npm test`, `npm run test:desktop`, `npm run test:runtime`, `npm run test:companion`, `npm run test:installer`, bootstrap build, `npm run build:desktop`, `npm run build:installer`, and `npm audit --omit=optional`.
- Final generated installer artifact still produced successfully and manifest still includes `appIconPath=assets/app-icon.png` and `installerIconPath=assets/app-icon.ico`.

## network-framework clean tabbed desktop GUI accepted

- Implemented and accepted `task_packaged_client_clean_tabbed_gui` in `/root/projects/network-framework`.
- Pushed commit `3b9b341 Implement clean tabbed desktop layout`.
- Replaced the JSON-first Electron shell with a cleaner desktop console layout: branded top bar, persistent quick-control bar for VPN start/stop, OpenClaw Node start/stop, and refresh, plus tabs for Overview, Network, Node, Files, and Remote.
- Added card-based status panels and per-tab contextual actions while preserving explicit action routing through the existing runtime boundary.
- Updated tests to assert top quick controls, tab IDs, icon boundary, and Electron shell quick-control/tab markup.
- Also corrected `docs/04_task.md` to describe the current user-approved app icon source as `assets/app-icon-source.png` rather than stale `assets/app-icon.svg`.
- Verification passed: `git diff --check`, docs spine listing, `npm test`, `npm run test:desktop`, `npm run test:runtime`, `npm run test:companion`, `npm run test:installer`, bootstrap build, `npm run build:icons`, `npm run build:desktop`, `npm run build:installer`, and `npm audit --omit=optional`.
# 2026-04-24

- Reinforced a durable skill-authoring rule: do not invent future schema/template structure in skills or docs. If something is unresolved, keep it explicit as an open gap instead of pretending it is already standardized.
- Tightened that rule further for active skills and project docs: remove parked `Not yet defined` placeholders and replace them with concrete content, explicit follow-up tasks, or `None.` empties where appropriate.
- Consolidated the local development skill surface around `skills/openclaw/project_development` and `skills/codex/project_execution`, with `skill-map.md` references and tighter docs-boundary ownership.
- Standardized shared default vocabularies in `universal-catalog` for `task_lifecycle_state`, `review_readiness`, `acceptance_outcome`, `test_status`, `maintenance_status`, and `docs_status`, and updated skill templates to use concrete JSON examples.
- Ratified the current `universal-catalog` boundary: markdown docs/prompt templates stay in skills; `storage/templates/` is reserved for real catalog-owned `output` templates; `script` entries are full source-file addresses; `term` entries are approved definitions.
- Added the first secret-alias config model to `universal-catalog`: `kind = config` may store a reviewed secret alias reference, but never a secret value or raw secret file contents. Added a read-only secret resolver helper under `universal-catalog/src/`.
- Established the local secret alias pattern for this host: catalog config key -> secret alias -> `/root/secrets/registry.json` -> local secret file. `TOOLS.md` now records this storage/lookup/use rule.
- Registered `NETWORK_FRAMEWORK_COMPANION_TOKEN_SECRET_ALIAS` in `universal-catalog` with payload `network-framework/companion-token`, and added the matching alias entry to `/root/secrets/registry.json`.
- Re-aligned `network-framework` to its fixed docs spine and ratified the ordered runtime-function model: shared config/bootstrap compatibility, desktop entry/UI control, local onboarding actions, local workstation status probing, and a read-only remote status companion.
- Ratified the phase-1 `network-framework` remote companion boundary as authenticated read-only `GET /v1/status` over tailnet-scoped HTTPS, grounded on normalized `openclaw status --json`, with the companion bearer token resolved through a local secret alias instead of committed config.
- Implemented and pushed the phase-1 `network-framework` desktop/runtime/companion slice while preserving the existing bootstrap path. The repo now includes `src/lib/`, `src/desktop/`, `src/runtime/`, and `src/companion/` ownership boundaries plus dedicated `test:desktop`, `test:runtime`, and `test:companion` commands.
- Recorded a durable review rule for future acceptance: reject implementations that reintroduce HTTP fallback where docs require HTTPS-only, or that treat newly invented permanent names as already-ratified without OpenClaw review.
- Updated `AGENTS.md` so development-oriented new sessions should read `skills/openclaw/project_development/SKILL.md` immediately unless a more specific skill clearly overrides it.
- Raised the local OpenClaw LLM idle-timeout setting to 300 seconds in `~/.openclaw/openclaw.json` and verified the gateway came back successfully after restart.
# 2026-04-24

- Reinforced a durable skill-authoring rule: do not invent future schema/template structure in skills or docs. If something is unresolved, keep it explicit as an open gap instead of pretending it is already standardized.
- Tightened that rule further for active skills and project docs: remove parked `Not yet defined` placeholders and replace them with concrete content, explicit follow-up tasks, or `None.` empties where appropriate.
- Consolidated the local development skill surface around `skills/openclaw/project_development` and `skills/codex/project_execution`, with `skill-map.md` references and tighter docs-boundary ownership.
- Standardized shared default vocabularies in `universal-catalog` for `task_lifecycle_state`, `review_readiness`, `acceptance_outcome`, `test_status`, `maintenance_status`, and `docs_status`, and updated skill templates to use concrete JSON examples.
- Ratified the current `universal-catalog` boundary: markdown docs/prompt templates stay in skills; `storage/templates/` is reserved for real catalog-owned `output` templates; `script` entries are full source-file addresses; `term` entries are approved definitions.
- Added the first secret-alias config model to `universal-catalog`: `kind = config` may store a reviewed secret alias reference, but never a secret value or raw secret file contents. Added a read-only secret resolver helper under `universal-catalog/src/`.
- Established the local secret alias pattern for this host: catalog config key -> secret alias -> `/root/secrets/registry.json` -> local secret file. `TOOLS.md` now records this storage/lookup/use rule.
- Registered `NETWORK_FRAMEWORK_COMPANION_TOKEN_SECRET_ALIAS` in `universal-catalog` with payload `network-framework/companion-token`, and added the matching alias entry to `/root/secrets/registry.json`.
- Re-aligned `network-framework` to its fixed docs spine and ratified the ordered runtime-function model: shared config/bootstrap compatibility, desktop entry/UI control, local onboarding actions, local workstation status probing, and a read-only remote status companion.
- Ratified the phase-1 `network-framework` remote companion boundary as authenticated read-only `GET /v1/status` over tailnet-scoped HTTPS, grounded on normalized `openclaw status --json`, with the companion bearer token resolved through a local secret alias instead of committed config.
- Implemented and pushed the phase-1 `network-framework` desktop/runtime/companion slice while preserving the existing bootstrap path. The repo now includes `src/lib/`, `src/desktop/`, `src/runtime/`, and `src/companion/` ownership boundaries plus dedicated `test:desktop`, `test:runtime`, and `test:companion` commands.
- Recorded a durable review rule for future acceptance: reject implementations that reintroduce HTTP fallback where docs require HTTPS-only, or that treat newly invented permanent names as already-ratified without OpenClaw review.
- Updated `AGENTS.md` so development-oriented new sessions should read `skills/openclaw/project_development/SKILL.md` immediately unless a more specific skill clearly overrides it.
- Raised the local OpenClaw LLM idle-timeout setting to 300 seconds in `~/.openclaw/openclaw.json` and verified the gateway came back successfully after restart.
- User clarified the preferred future `network-framework` packaged-client model: the Windows installer should ask only for node name and install path, then auto-configure the rest.
- User prefers VPN and OpenClaw node to be installed as services but started and stopped manually from the desktop app rather than auto-starting.
- User wants SMB treated as a persistent mounted baseline, not an app-controlled service toggle; the app should observe it and offer open/repair affordances rather than own its lifecycle.
- User requested a durable standard that all projects emphasize clear maintained-file boundaries, reduced overlap, and explicit rules for which information belongs in which files, including memory files. This was implemented across `AGENTS.md`, `skills/openclaw/memory_management/references/routing.md`, and `skills/openclaw/project_development/SKILL.md`, then committed/pushed as `04c6e5f Strengthen file boundary standards`.
- OpenClaw model configuration was corrected to match available authentication: only `openai-codex` is authenticated on this host. Default/current model was moved to `openai-codex/gpt-5.5-pro`; `openai-codex/gpt-5.5` and `openai-codex/gpt-5.4` remain configured/available; unauthenticated `openai/gpt-5.5` was removed from configured models.
- User asked for the expired/inactive memory-core dreaming narrative session cleanup policy. We found four completed `dreaming-narrative-*` sessions still registered after 13–17 hours because current OpenClaw cleanup archives orphan transcripts but does not TTL-remove completed sessions whose transcripts still exist.

## 2026-04-24 late-session updates

- User asked to set OpenClaw default model back to `openai-codex/gpt-5.5`; it was confirmed available/authenticated along with `openai-codex/gpt-5.5-pro`.
- User then asked to switch Codex/OpenClaw usage to `openai-codex/gpt-5.5-pro` and smoke-test whether it can respond. Model status output showed `openai-codex/gpt-5.5-pro` as default/configured/authenticated, but the subagent smoke test completed with no visible output/tokens, so the practical response test was inconclusive and should be rechecked after compaction.
- Added manual expired dreaming narrative session cleanup to the memory-management skill: `skills/openclaw/memory_management/references/dreaming-cleanup.md` and `scripts/cleanup_expired_dreaming_sessions.py`. The cleanup is scoped to `dreaming-narrative-*`, defaults to dry-run, requires explicit `--execute`, uses a 6h TTL by default, and archives files/store backup under `.openclaw-repair/dreaming-session-cleanup/<timestamp>/` instead of hard-deleting.
- Ran the dreaming cleanup against main sessions and reduced `dreaming-narrative-*` store entries from 4 to 0; archived transcripts/trajectory sidecars and `sessions.json.backup` under `/root/.openclaw/workspace/.openclaw-repair/dreaming-session-cleanup/2026-04-24T20-26-13Z/`. Workspace commit reported: `2bc090c Add dreaming session cleanup to memory skill`.

## network-framework packaged-client control model accepted

- Accepted and pushed `network-framework` commit `898dda1 Implement packaged client control model`.
- Implemented NSIS installer artifact, catalog-backed packaged defaults, hidden Windows command execution, explicit VPN/OpenClaw Node service start/stop controls, and SMB check/open/repair without SMB service lifecycle semantics.
- Added command-result redaction so auth keys and SMB passwords are not exposed through structured action results.
- Verification passed: `git diff --check`, `npm test`, `npm run test:desktop`, `npm run test:runtime`, `npm run test:companion`, `npm run test:installer`, `node src/cli.js build --config config/server.example.json --output dist/network-framework.ps1`, and docs spine file check.


## Pre-compaction flush — 2026-04-24 18:12 EDT

- Project data storage policy was clarified and written into the project-development skill:
  - SQL is not the default destination for all data.
  - SQL requires long-term structured value, repeated query/share needs, constraints, transactions, indexes/joins, or large stable schema datasets.
  - Logs/traces/runtime noise use retention; blobs/media/generated artifacts use file/object storage; CSV is for human-reviewed table artifacts; small isolated structured data should usually use CSV/JSON.
  - Dedicated SQL tables require justification that SQL is better than CSV/JSON/file/object/log/cache/generated storage.
  - Related commits reported during the session: `ce4b7c2`, `96ba128`, `c256cbd`.
- `universal-catalog/src/` was inspected after the storage-policy discussion. No rewrite was needed because helpers are catalog-specific, read-only, query-executor-injected, not connection owners, not bulk-data readers, and tests passed. Future large tables such as stock bars should get dedicated table/helper/query layers rather than expanding `catalog-reader` into a generic data access layer.
- Returned to `/root/projects/network-framework` and implemented the ready packaged-client task. Accepted and pushed commit: `898dda1 Implement packaged client control model`.
- `network-framework` packaged-client implementation details now include:
  - NSIS installer artifact at `installer/network-framework.nsi` plus `installer/README.md`.
  - Minimal installer inputs: install path and OpenClaw node name.
  - No VPN/OpenClaw service auto-start during install.
  - No Windows Registry-backed settings layer for this slice.
  - Catalog-backed packaged defaults in `src/lib/catalog-defaults.js`.
  - Hidden command execution via `windowsHide: true` and hidden PowerShell args while preserving GUI/tray surfaces.
  - Explicit VPN and OpenClaw Node service start/stop/status controls.
  - SMB remains persistent baseline mount with check/open/repair only; no SMB service toggle.
- Important acceptance fix before accepting `network-framework`: structured command results now redact secret-bearing args such as Tailscale auth keys and SMB passwords so UI/status surfaces do not expose secrets.
- `network-framework` verification evidence for accepted packaged-client work:
  - `git diff --check`
  - `npm test` → 25 pass
  - `npm run test:desktop` → 3 pass
  - `npm run test:runtime` → 7 pass
  - `npm run test:companion` → 8 pass
  - `npm run test:installer` → 2 pass
  - `node src/cli.js build --config config/server.example.json --output dist/network-framework.ps1`
  - `find docs -maxdepth 1 -type f | sort`
- Current durable project state: `network-framework` repo was clean and pushed to GitHub after commit `898dda1`.

## network-framework Tailscale reuse-first boundary

- User clarified that if Tailscale is already installed, `network-framework` should not download/install it again.
- User's current Tailscale setup uses GitHub login; public project docs were generalized to preserve existing authenticated identity-provider sessions, including GitHub login, without exposing unnecessary personal framing.
- Pushed `network-framework` commit `607e97b Define Tailscale reuse-first setup boundary`.
- Docs now require Tailscale setup to detect and reuse existing install/auth state, avoid forced auth-key login when already authenticated, and reject re-download/reinstall behavior for valid local Tailscale installations.
- `docs/04_task.md` now has ready next task `task_packaged_client_binary_build_definition` for the next packaged-client binary build slice.

## network-framework server-side Tailscale prerequisite boundary

- User approved adding server-side Tailscale installation/configuration steps to the `network-framework` docs/task boundary.
- Pushed `network-framework` commit `e96a5ff Define server Tailscale prerequisite boundary`.
- Docs now treat server-side Tailscale as an explicit prerequisite before Windows clients can reach OpenClaw gateway, SMB, dashboard, or companion endpoints over the tailnet.
- The accepted boundary allows detecting/installing/starting the server Tailscale client only as an operator-approved setup action.
- Tailscale identity-provider/browser login, tailnet approval, auth keys, preauth keys, login URLs, and tokens must remain human-controlled and out of committed project files.
- Added ready task `task_server_tailscale_prerequisite_setup`; `docs/04_task.md` now has two ready dispatches: server Tailscale prerequisite setup and packaged-client binary build definition.

## network-framework prerequisite install-assist boundary

- User clarified that the whole program should prepare all required software/apps, integrate client-side prerequisites into installer/setup where needed, help configure them, and skip anything already installed.
- Pushed `network-framework` commit `8a501f4 Define prerequisite install-assist boundary`.
- New accepted principle: required software setup is detect-first, reuse-first, and idempotent.
- Packaged-client setup must check each required app/service first, skip valid existing installs, install/bundle/guide only missing prerequisites, and keep installation separate from user-controlled login/start/connect/approval actions.
- This expands the next packaged-client binary task boundary to preserve prerequisite detect-first/install-assist behavior alongside minimal-input, no-Registry, hidden-command, explicit-service-control, SMB-baseline, and Tailscale reuse-first rules.

## network-framework packaged-client default choices

- User delegated remaining `network-framework` setup/binary-build choices to OpenClaw judgment.
- Chosen defaults:
  - packaged desktop runtime: Electron
  - installer: NSIS
  - client runtime policy: bundle Electron runtime so users do not install Node.js separately
  - local config path: `%LOCALAPPDATA%\\NetworkFramework\\config\\packaged-client.json`
  - prerequisite policy: detect-first, reuse-first, install-assist
  - privilege policy: installer may elevate; normal runtime stays user-level except explicit privileged service prompts
  - server companion deployment: systemd service
  - companion TLS: Tailscale cert
  - companion default port: `18790`
  - server Tailscale login mode: operator browser/identity-provider login
- Registered these defaults in `universal-catalog` via migration `004_register_network_framework_packaging_defaults.sql`; pushed commit `4b4aca6 Register network framework packaging defaults`.
- Updated and pushed `network-framework` docs/README with commit `1f0ea5f Select packaged client binary defaults`.
- Applied catalog migrations and verified helper tests. Active `universal_catalog` now has `114` rows, `114` unique keys, `114` unique ids; `schema_migrations` includes 001-004.
- `network-framework` verification passed after docs/default choices: `git diff --check`, docs spine file check, `npm test`, `npm run test:desktop`, `npm run test:runtime`, `npm run test:companion`, `npm run test:installer`, and bootstrap build.

## network-framework server Tailscale prerequisite accepted

- Server-side Tailscale setup completed on host `openclaw` after user completed browser/identity-provider approval.
- Installed Tailscale `1.96.4`, enabled/started `tailscaled`, and verified status: service active, `BackendState=Running`, host online, DNS name present, tailnet IPs present.
- Did not commit auth keys, login URLs, tokens, or tailnet secrets.
- Updated `network-framework/docs/04_task.md` to mark `task_server_tailscale_prerequisite_setup` accepted and leave `task_packaged_client_binary_build_implementation` as the only ready dispatch.
- Pushed `network-framework` commit `5fb0c64 Accept server Tailscale prerequisite setup`.

## network-framework packaged-client binary build accepted

- Implemented and accepted `task_packaged_client_binary_build_implementation` in `/root/projects/network-framework`.
- Pushed commit `18ea977 Implement packaged client binary build`.
- Added Electron desktop entry/preload/window shell, `npm run build:desktop`, package lock with Electron `^41.3.0`, NSIS alignment for `%LOCALAPPDATA%\NetworkFramework\config\packaged-client.json`, detect-first prerequisite status helpers, and catalog-default tests using registered keys.
- Review correction: rejected/rewrote Codex-introduced unregistered names (`NETWORK_FRAMEWORK_RUNTIME_BUNDLE`, `NETWORK_FRAMEWORK_PRIVILEGE_POLICY`, companion HTTPS/default-port aliases) back to registered catalog keys (`NETWORK_FRAMEWORK_CLIENT_RUNTIME_BUNDLE_POLICY`, `NETWORK_FRAMEWORK_SERVICE_PRIVILEGE_POLICY`, `NETWORK_FRAMEWORK_COMPANION_TLS_PROVIDER`, `NETWORK_FRAMEWORK_COMPANION_PORT`, `NETWORK_FRAMEWORK_SERVER_TAILSCALE_LOGIN_MODE`) and restored `NETWORK_FRAMEWORK_LOCAL_CONFIG_STORE = project_config_boundary`.
- Verification passed: `git diff --check`, docs spine listing, `npm test`, `npm run test:desktop`, `npm run test:runtime`, `npm run test:companion`, `npm run test:installer`, bootstrap build, `npm run build:desktop`, and `npm audit --omit=optional`.
- `npm run build:desktop` prepared `dist/desktop` and copied the locally installed Electron runtime into `dist/desktop/electron-runtime`; final Windows NSIS executable production remains host/toolchain-dependent.
- No secrets, login URLs, or Windows Registry-backed settings were added.

## network-framework Windows installer artifact produced

- Implemented and accepted `task_windows_installer_artifact_production` in `/root/projects/network-framework`.
- Installed/verified NSIS `makensis` on the OpenClaw host and produced a real generated installer artifact at `dist/network-framework-setup.exe` (136M, ignored by git).
- Added `npm run build:installer` via `scripts/build-installer.js`; it prepares a Windows x64 Electron runtime bundle, invokes NSIS, verifies `dist/network-framework-setup.exe`, and records manifest status.
- Updated `scripts/prepare-desktop-bundle.js` to support explicit Electron platform/arch targets, Windows runtime download/preparation via `@electron/get` + `extract-zip`, runtime source injection for tests, and manifest fields for runtime/platform/installer status.
- Fixed NSIS cross-host compilation details: `!cd ".."`, forward-slash source/output paths, and `ReadEnvStr $NodeName "COMPUTERNAME"` instead of the previously unresolved `$COMPUTERNAME` constant.
- Pushed `network-framework` commit `6531dcf Produce Windows installer artifact`.
- Verification passed: `git diff --check`, docs spine listing, `npm test`, `npm run test:desktop`, `npm run test:runtime`, `npm run test:companion`, `npm run test:installer`, bootstrap build, `npm run build:desktop`, `npm run build:installer`, and `npm audit --omit=optional`.
- Final artifact manifest evidence: Electron `41.3.0`, `electronPlatform=win32`, `electronArch=x64`, `electronRuntimeBundled=true`, `electronRuntimePresent=true`, `nsisCommand=makensis`, `installerArtifactStatus=produced`.
- No secrets, login URLs, Registry settings, Tailscale auth keys, bearer tokens, or unattended Tailscale login behavior were added.

## network-framework packaged app icon added

- Added and accepted `task_packaged_client_app_icon` in `/root/projects/network-framework`.
- Pushed commit `1f89943 Add packaged app icon`.
- Added maintained icon assets: `assets/app-icon.svg` as canonical source, `assets/app-icon.png` for Electron window/tray, and `assets/app-icon.ico` for NSIS/Windows installer icon.
- Added `scripts/generate-icons.js` and `npm run build:icons` to regenerate PNG/ICO outputs.
- Wired the icon into Electron BrowserWindow/tray via `src/desktop/electron-main.js`, copied assets into `dist/desktop`, and added NSIS `Icon`/`UninstallIcon` directives.
- Verification passed: `git diff --check`, docs spine listing, `npm run build:icons`, `npm test`, `npm run test:desktop`, `npm run test:runtime`, `npm run test:companion`, `npm run test:installer`, bootstrap build, `npm run build:desktop`, `npm run build:installer`, and `npm audit --omit=optional`.
- Final installer manifest includes `appIconPath=assets/app-icon.png` and `installerIconPath=assets/app-icon.ico`; generated installer artifact still produced successfully.

## network-framework app icon replaced with user-approved artwork

- User supplied the final app icon artwork and asked to use it.
- Replaced the generated NF icon with the user-approved PNG as canonical source: `assets/app-icon-source.png`.
- Removed stale `assets/app-icon.svg` and updated `assets/README.md` so the canonical icon source is now the user-approved raster image.
- Reworked `scripts/generate-icons.js` to decode/resize `app-icon-source.png` and regenerate `assets/app-icon.png` plus `assets/app-icon.ico` without external image tools.
- Pushed `network-framework` commit `164f7b9 Use approved app icon artwork`.
- Verification passed: `git diff --check`, docs spine listing, `npm run build:icons`, `npm test`, `npm run test:desktop`, `npm run test:runtime`, `npm run test:companion`, `npm run test:installer`, bootstrap build, `npm run build:desktop`, `npm run build:installer`, and `npm audit --omit=optional`.
- Final generated installer artifact still produced successfully and manifest still includes `appIconPath=assets/app-icon.png` and `installerIconPath=assets/app-icon.ico`.

## network-framework clean tabbed desktop GUI accepted

- Implemented and accepted `task_packaged_client_clean_tabbed_gui` in `/root/projects/network-framework`.
- Pushed commit `3b9b341 Implement clean tabbed desktop layout`.
- Replaced the JSON-first Electron shell with a cleaner desktop console layout: branded top bar, persistent quick-control bar for VPN start/stop, OpenClaw Node start/stop, and refresh, plus tabs for Overview, Network, Node, Files, and Remote.
- Added card-based status panels and per-tab contextual actions while preserving explicit action routing through the existing runtime boundary.
- Updated tests to assert top quick controls, tab IDs, icon boundary, and Electron shell quick-control/tab markup.
- Also corrected `docs/04_task.md` to describe the current user-approved app icon source as `assets/app-icon-source.png` rather than stale `assets/app-icon.svg`.
- Verification passed: `git diff --check`, docs spine listing, `npm test`, `npm run test:desktop`, `npm run test:runtime`, `npm run test:companion`, `npm run test:installer`, bootstrap build, `npm run build:icons`, `npm run build:desktop`, `npm run build:installer`, and `npm audit --omit=optional`.

## network-framework app controls and SMB UI surface refined

- User clarified that separate `Start VPN` / `Stop VPN` controls should become a single toggle-style button, and that SMB should stay in broader setup/config but not appear in the app UI because it is not controlled by program start/stop switches.
- Implemented and accepted the app-control refinement in `/root/projects/network-framework`; pushed commit `ee3d583 Refine app controls and SMB surface`.
- Top quick controls are now status-aware toggles for VPN and OpenClaw Node plus Refresh. The toggle controls choose start/stop action from current local status while preserving explicit user-triggered runtime action routing and avoiding hidden auto-start/connect behavior.
- SMB was removed from visible desktop app surfaces, including the Files tab and tray menu. SMB configuration, probe/setup/repair runtime support, and generated bootstrap/config boundaries remain available outside the app's start/stop UI.
- Local status now carries service `running` fields for VPN and OpenClaw Node so UI toggle labels can reflect current state; VPN connected remains distinct from service-running state.
- Verification for commit `ee3d583` passed: `git diff --check`, `npm test`, `npm run test:desktop`, `npm run test:runtime`, `npm run test:companion`, `npm run test:installer`, `npm run build:icons`, `npm run build:desktop`, `npm run build:installer`, and `npm audit --omit=optional`.

## network-framework packaged client setup functionality
- Accepted and pushed `network-framework` commit `5c418ef Make packaged client setup functional`.
- Setup tab is now functional rather than read-only: saves validated VPN/gateway/dashboard/companion config to `%LOCALAPPDATA%\NetworkFramework\config\packaged-client.json`, keeps secret values out of config by storing only env var names, exposes explicit Tailscale login (`tailscale up`), registers OpenClaw node through the existing command boundary, opens dashboard, and fetches read-only companion status from an explicit status URL/token env.
- Verification passed: `git diff --check`, `npm run test:desktop`, `npm run test:runtime`, `npm test`, bootstrap build, `npm run build:desktop`, `npm run build:installer`, `npm audit --omit=optional`.
- New installer copied to `/root/transfer/network-framework-setup.exe`; SHA-256 `a830d18be73abd78acc5aa01b2f97b593eff74f582618a90cfb4b66f4467a97b`.

## Pre-compaction flush — 2026-04-24 22:33 EDT

- Diagnosed Windows/server transfer speed after Syncthing looked slow. Server Syncthing was reconfigured to treat Tailscale as LAN/local (`100.64.0.0/10`, `fd7a:115c:a1e0::/48`), with LAN bandwidth limiting disabled and global announce/relays/NAT still disabled. After restart, Syncthing connected to Windows over Tailscale as LAN (`100.96.248.33:22000`, `tcp-client`, `isLocal=true`).
- Tailscale HTTP download test for `network-framework-setup.exe` from `100.104.174.7:18080` was very slow from Windows (~68,430 bytes/s, ~34 min ETA), showing that Syncthing was not the only bottleneck on that path. The temporary HTTP test service was later stopped.
- SCP test from Windows completed the ~137MB installer in `25.756s` (~5.3 MB/s), much faster than the Tailscale HTTP/Syncthing path. Practical transfer policy: use SCP (or GitHub release/CDN later) for immediate large installer downloads; keep Syncthing for background directory sync, not urgent installer distribution.
- Avoid downloading large files directly into `D:\transfer` while Syncthing is watching it. A repeated SCP into `D:\transfer\network-framework-setup.exe` slowed to ~128.6KB/s, likely because Syncthing was scanning/syncing the partially written file. Prefer Windows Desktop or `C:\temp` for direct SCP downloads, then copy into the sync folder only after completion if needed.
- User pushed back that repeated downloads of an empty shell waste time; durable expectation: do not ask them to redownload the packaged client until the app has real setup/functionality worth testing. The subsequent accepted functional setup commit is `5c418ef Make packaged client setup functional`, with installer SHA-256 `a830d18be73abd78acc5aa01b2f97b593eff74f582618a90cfb4b66f4467a97b` already recorded in this memory file.

## network-framework Tailscale wording fix
- User clarified that by "VPN" they meant a China/Google-access Internet VPN, while `network-framework` only controls Tailscale private-network access to the server. The app should not imply it can provide Google/Internet proxy access.
- Accepted and pushed `network-framework` commit `3887f58 Clarify Tailscale private network wording`.
- User-facing packaged-client labels now say Tailscale/private network instead of generic VPN; docs/README clarify that the project does not provide an Internet/Google proxy VPN.
- New installer built as `dist/network-framework-setup-9b99c31.exe`; SHA-256 `9b99c319ff61e8f37c0001c30e14a05126d826d86434018b5b469f894465f011`.

## network-framework Internet proxy VPN and Syncthing cleanup
- User requested changing `network-framework` from Tailscale-private-network-only behavior into an Internet proxy VPN and uninstalling Syncthing/removing `/root/transfer`.
- Syncthing was stopped, disabled, purged via apt, and autoremove completed. `/root/transfer` was removed from its live path by moving it recoverably to `/root/.openclaw-repair/removed-transfer-20260425T025005Z/`.
- Registered Internet proxy defaults in `universal-catalog` and pushed commit `3dba81b Register network framework internet proxy defaults`. New catalog rows bring the register to 118 rows/keys/ids and add Tailscale exit-node provider/default config names.
- Implemented and pushed `network-framework` commit `92a2bcd Add Tailscale exit node internet proxy controls`.
- The packaged app now has explicit Internet Proxy VPN support through Tailscale exit-node routing: setup config for provider/exit node/LAN access, local status, GUI/tray enable/disable actions, installer defaults, validation, and tests. It does not add a custom proxy engine and does not enable Internet routing without explicit user action.
- Server exit-node prep was applied on OpenClaw: `/etc/sysctl.d/99-network-framework-tailscale-exit-node.conf` enables `net.ipv4.ip_forward=1` and `net.ipv6.conf.all.forwarding=1`; `tailscale set --advertise-exit-node=true` caused advertised default routes (`0.0.0.0/0`, `::/0`). Tailnet/admin-console approval may still be required before Windows can select `openclaw` as an exit node.
- Verification passed for `network-framework`: `git diff --check`, docs spine listing, `npm run build:icons`, `npm run test:desktop`, `npm run test:runtime`, `npm run test:companion`, `npm run test:installer`, `npm test`, bootstrap build, `npm run build:desktop`, `npm run build:installer`, and `npm audit --omit=optional`.
- New installer is `/root/projects/network-framework/dist/network-framework-setup-62d397e.exe` with SHA-256 `62d397e933f5e267d0aa60074492141e737ce49d1990ae33cfc4472a72c79603`.


## Pre-compaction flush — 2026-04-24 23:58 EDT

- User accepted that the Internet Proxy VPN only needs to be usable for now and asked to stop deep-diving proxy performance; focus shifted to completing OpenClaw-related capabilities in the packaged app, including OpenClaw Node and Control Dashboard affordances.
- OpenClaw server status was inspected: Gateway is installed/enabled/running, default agent is `main`, workspace is `/root/.openclaw/workspace`, and Node service is not installed/enabled/running. Gateway initially listened only on loopback (`127.0.0.1:18789` and `127.0.0.1:18791`), with server Tailscale IP `100.104.174.7`.
- OpenClaw docs confirmed the safer access pattern: keep Gateway bound to loopback and expose dashboard/WebSocket to the tailnet with Tailscale Serve/Funnel instead of rebinding Gateway publicly. Local config `/root/.openclaw/openclaw.json` contains sensitive tokens/API keys and must not be quoted verbatim in user-facing output or committed docs.
- Tailscale Serve was configured so `https://openclaw.tailc5b89d.ts.net` proxies to `http://127.0.0.1:18789`; later listener evidence showed Gateway on loopback and tailscaled listening on tailnet HTTPS (`100.104.174.7:443` / IPv6 tailnet address). This gives Windows a tailnet dashboard URL while keeping Gateway loopback-bound.
- User requested a one-click app action equivalent to `ssh -N -L 18789:127.0.0.1:18789 root@openclaw` followed by opening the dashboard, so they do not need to type the command manually.
- Implemented and pushed a packaged app control for `Start SSH Tunnel + Open Dashboard`. It backgrounds an SSH local tunnel using `ssh -N -L 18789:127.0.0.1:18789 -o BatchMode=yes -o ExitOnForwardFailure=yes root@openclaw`, then opens `http://127.0.0.1:18789/`. The action intentionally requires pre-existing Windows SSH key auth to `root@openclaw`; it must fail fast rather than hide a password prompt in the background.
- Built and provided installer `/root/projects/network-framework/dist/network-framework-setup-5f45630.exe` with SHA-256 `5f45630a65ffb9c197c58dfb7ca152bcfcabb947980ff0de283572df81dcd339`. Download command given: `scp root@openclaw:/root/projects/network-framework/dist/network-framework-setup-5f45630.exe "$env:USERPROFILE\Desktop\network-framework-setup-5f45630.exe"`.
- Verification for the SSH tunnel/dashboard button build was reported as passing: desktop/runtime/installer tests, full `npm test`, desktop build, installer build, and audit.

## network-framework Windows OpenClaw shim fix — 2026-04-25 01:02 EDT
- User reported `be9a63a` packaged app output: `openclaw.node.install` failed with `openclaw.exe is not installed or is not available in PATH` while SSH fallback status proved server OpenClaw was reachable.
- Diagnosis: app was hard-requiring `openclaw.exe`; Windows npm/pnpm global installs commonly expose `openclaw.cmd` under `%APPDATA%\npm`, and Electron launched from Explorer may not inherit that PATH.
- Implemented `network-framework` commit `379174a Support Windows OpenClaw command shims`:
  - detection now uses `where openclaw`, not `where openclaw.exe`
  - app-owned OpenClaw child processes prepend `%APPDATA%\npm` to PATH
  - OpenClaw actions/probes run via `cmd.exe /d /s /c openclaw ...` so `.cmd` shims work
  - retained configured `OPENCLAW_GATEWAY_URL` injection for app-owned OpenClaw commands
- Verification passed: `git diff --check`, `npm run test:runtime`, `npm run test:desktop`, `npm run test:installer`, `npm test` (54 pass), bootstrap build, `npm run build:desktop`, `npm run build:installer`, `npm audit --omit=optional`.
- Latest installer artifact: `/root/projects/network-framework/dist/network-framework-setup-33e7ce0.exe`, SHA-256 `33e7ce001aa170cfad0ff9852fa145485cadeb5895fdfd9d7737b67ec24a5263`.
