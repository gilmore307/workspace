# 2026-05-24

## Pre-compaction flush - Layer 5/6 training granularity

- Chentong rejected a candidate-only interpretation for Layer 6 training because downstream Layer 7 add/reduce/maintain decisions need minute-level risk context even if live components are triggered on demand. Corrected `trading-model` so Layer 6 supports minute-level policy rows and no longer requires every row to have `target_candidate_id`.
- Layer 6 durable decision: training should use every eligible minute of risk-policy context, while live inference may remain demand-triggered. Implemented/recorded `policy_scope` values: `global`, `target_candidate`, and `active_position`. `global` rows cover minute-level portfolio/account/market policy state; target/position rows condition that state for candidates or existing positions.
- Layer 6 verification passed before compaction: `tests.test_dynamic_risk_policy_model`, `tests.test_layer_04_10_script_entrypoints`, full `unittest discover tests` with 235 tests OK, and `git diff --check` clean. Commit pushed in `trading-model`: `1e61658 Support minute-level Layer 6 policy rows`.
- Chentong then rejected pre-screened/candidate-only Layer 5 training because real use will expose the model to many states that candidate filtering would have removed. Corrected Layer 5 to train on dense minute target-state rows rather than only accepted candidates.
- Layer 5 durable decision: training data should include every eligible minute from Layer 3 accepted anonymous target-state rows, including strong signals, weak signals, no-edge states, conflicts, event-risk-suppressed states, poor paths, and weak tradability. Layer 4 `event_failure_risk_vector` is optional conditioning input, not a prerequisite for sample generation.
- Layer 5 route decisions such as `pass_to_layer6`, `watch_only`, and `reject` are post-score calibration/routing thresholds, not pre-training filters. This avoids trigger/selection bias and lets Layer 5 learn the distribution it will see in production.
- Layer 5 verification passed before compaction: `tests.test_alpha_confidence_model`, `tests.test_layer_04_10_script_entrypoints`, full `unittest discover tests` with 236 tests OK, and `git diff --check` clean. Commit pushed in `trading-model`: `0750fc6 Train Layer 5 on dense minute rows`.

## Layer 8 SQL input prefix fix

- Reviewed current `trading-model` Layer 8 route: repository docs define Layer 8 as `UnderlyingActionModel` with physical surface `model_08_underlying_action`; Layer 9 remains optional `TradingGuidanceModel / OptionExpressionModel`.
- Found and fixed a narrow database-generation bug in `scripts/models/model_08_underlying_action/generate_model_08_underlying_action.py`: SQL input assembly read `model_07_position_projection` rows but copied only `6_` prefixed projection fields into `position_projection_vector`, so database runs could lose Layer 7 target exposure/confidence fields and fall back to defaults.
- Added a script-entrypoint test proving Layer 8 database input preserves `7_` projection fields and excludes stray `6_` fields.
- Verification passed in `trading-model`: `PYTHONPATH=src python3 -m unittest tests.test_underlying_action_model tests.test_layer_04_10_script_entrypoints`, full `PYTHONPATH=src python3 -m unittest discover tests` with 237 tests OK, and `git diff --check` clean.
- Commit pushed in `trading-model`: `6ebd2c5 Fix Layer 8 projection SQL input prefix`.
