# 2026-05-24

## Pre-compaction flush - Layer 5/6 training granularity

- Chentong rejected a candidate-only interpretation for Layer 6 training because downstream Layer 7 add/reduce/maintain decisions need minute-level risk context even if live components are triggered on demand. Corrected `trading-model` so Layer 6 supports minute-level policy rows and no longer requires every row to have `target_candidate_id`.
- Layer 6 durable decision: training should use every eligible minute of risk-policy context, while live inference may remain demand-triggered. Implemented/recorded `policy_scope` values: `global`, `target_candidate`, and `active_position`. `global` rows cover minute-level portfolio/account/market policy state; target/position rows condition that state for candidates or existing positions.
- Layer 6 verification passed before compaction: `tests.test_dynamic_risk_policy_model`, `tests.test_layer_04_10_script_entrypoints`, full `unittest discover tests` with 235 tests OK, and `git diff --check` clean. Commit pushed in `trading-model`: `1e61658 Support minute-level Layer 6 policy rows`.
- Chentong then rejected pre-screened/candidate-only Layer 5 training because real use will expose the model to many states that candidate filtering would have removed. Corrected Layer 5 to train on dense minute target-state rows rather than only accepted candidates.
- Layer 5 durable decision: training data should include every eligible minute from Layer 3 accepted anonymous target-state rows, including strong signals, weak signals, no-edge states, conflicts, event-risk-suppressed states, poor paths, and weak tradability. Layer 4 `event_failure_risk_vector` is optional conditioning input, not a prerequisite for sample generation.
- Layer 5 route decisions such as `pass_to_layer6`, `watch_only`, and `reject` are post-score calibration/routing thresholds, not pre-training filters. This avoids trigger/selection bias and lets Layer 5 learn the distribution it will see in production.
- Layer 5 verification passed before compaction: `tests.test_alpha_confidence_model`, `tests.test_layer_04_10_script_entrypoints`, full `unittest discover tests` with 236 tests OK, and `git diff --check` clean. Commit pushed in `trading-model`: `0750fc6 Train Layer 5 on dense minute rows`.

## Layer 8 SQL input prefix fix

- Reviewed current `trading-model` Layer 8 route: repository docs define Layer 8 as `UnderlyingActionModel` with physical surface `model_08_underlying_action`; Layer 9 remains optional `TradingGuidanceModel / OptionExpressionModel`.
- Found and fixed a narrow database-generation bug in `scripts/models/model_08_underlying_action/generate_model_08_underlying_action.py`: SQL input assembly read `model_07_position_projection` rows but copied only `6_` prefixed projection fields into `position_projection_vector`, so database runs could lose Layer 7 target exposure/confidence fields and fall back to defaults.
- Added a script-entrypoint test proving Layer 8 database input preserves `7_` projection fields and excludes stray `6_` fields.
- Verification passed in `trading-model`: `PYTHONPATH=src python3 -m unittest tests.test_underlying_action_model tests.test_layer_04_10_script_entrypoints`, full `PYTHONPATH=src python3 -m unittest discover tests` with 237 tests OK, and `git diff --check` clean.
- Commit pushed in `trading-model`: `6ebd2c5 Fix Layer 8 projection SQL input prefix`.

## Layer 9 option-surface bypass settled

- Chentong accepted the Layer 9 route: keep one minute-level Layer 9 status/training row for every eligible minute with a Layer 8 thesis, including non-optionable underlyings such as BTC.
- Implemented Layer 9 option-surface statuses in `trading-model`: `optionable_chain_available`, `optionable_chain_missing`, and `non_optionable_underlying`.
- `non_optionable_underlying` now bypasses option-chain scoring, emits `underlying_only_expression` when the Layer 8 thesis is actionable, clears selected contract fields, and records `non_optionable_underlying` plus `layer_9_bypassed_option_expression_scoring` reason codes.
- Fixed Layer 9 handoff fallbacks to read Layer 8 resolved fields (`8_resolved_action_side`, `8_resolved_dominant_horizon`) instead of stale `9_resolved_*` names.
- Updated Layer 9 docs and package README for dense minute-level status rows and non-optionable bypass semantics; corrected docs from stale `8_resolved_*` expression fields to `9_resolved_*`.
- Added `trading-manager` registry migration `639_register_layer_nine_option_surface_status.sql`, updated `scripts/registry/current.csv`, and registered the option-surface status vocabulary plus the new `9_resolved_option_surface_status` field.
- Verification passed: `trading-model` full `PYTHONPATH=src python3 -m unittest discover tests` with 240 tests OK; `trading-manager` targeted registry/Layer 9 tests 58 tests OK; registry snapshot OK; registry dry-run no pending migrations; `git diff --check` clean for both repos.

## Pre-compaction flush - Layer 9 implementation commits

- Layer 9 option-surface bypass work was pushed as `trading-model` commit `8e49df6 Add Layer 9 option surface bypass status` and `trading-manager` commit `3faf7a4 Register Layer 9 option surface status`.
- Workspace daily-memory/log commit pushed: `021fcdf Record Layer 9 bypass decision`.
- Known unrelated workspace dirt remained after the work and was intentionally left untouched: `DREAMS.md`, `MEMORY.md`, `memory/2026-05-21.md`, `memory/2026-05-22.md`, and previously untracked/dirty daily memory around `memory/2026-05-23.md`.

## Ten-layer model stack rescan

- Rescanned the active `trading-model` ten-layer model stack across docs, source model packages, script model entrypoints, tests, and `trading-manager` registry current snapshot after the Layer 8/9 decisions.
- Confirmed all ten model directories and script entrypoint directories exist from `model_01_market_regime` through `model_10_event_risk_governor`, with docs `10_layer_01_market_regime.md` through `19_layer_10_event_risk_governor.md`.
- Cleaned remaining current-surface drift: Layer 8 docs now use `8_resolved_*` plan fields and `8A` through `8I` internal components; Layer 10 docs now use `10A` through `10C` internal components.
- Added `trading-manager` registry migration `640_clean_layer_eight_nine_registry_writing.sql` and regenerated `scripts/registry/current.csv` so active Layer 8/9 registry rows no longer mention stale `layer_7_full_contract_fit_model`, Layer 7 target-range guardrail payloads, Layer 7 path thesis, or a Layer 6 generator for `model_08_underlying_action`.
- Verification passed: `trading-model` full `PYTHONPATH=src python3 -m unittest discover tests` with 240 tests OK; `trading-manager` targeted registry/Layer 9 tests 58 tests OK; registry snapshot OK; registry dry-run no pending migrations; `git diff --check` clean for both repos.

## Execution component sequence numbering

- Chentong asked for live-trading components to be numbered like model layers so the intraday process order is obvious and names are shorter.
- Implemented concise component sequence fields in `trading-execution` runtime graph output: `C01 Allocation`, `C02 Entry`, `C03 Lifecycle`, `C04 Option Review`, `C05 Failure Review`, `C06 Order Intent`, and `C07 Execution Gate`.
- Kept existing stable `component_id` values such as `opportunity_risk_allocation_engine` as contract-facing interface names to avoid breaking downstream callers; `component_step` and `component_name` are display/order fields.
- Updated `trading-execution` architecture, decision, runtime component docs, component implementation, and runtime component tests.
- Added `trading-manager` registry migration `641_register_execution_component_sequence.sql` and regenerated `scripts/registry/current.csv` to register `EXECUTION_RUNTIME_COMPONENT_SEQUENCE` plus the updated component graph policy.
- Verification passed: `trading-execution` full `PYTHONPATH=src python3 -m unittest discover tests` with 80 tests OK; `python3 -m compileall -q src scripts` OK; `trading-manager` registry tests 52 OK; registry snapshot OK; registry dry-run no pending migrations; `git diff --check` clean for both repos.

## Pre-compaction flush - execution component numbering commits

- Execution component numbering was pushed as `trading-execution` commit `bc6b726 Number execution runtime components` and `trading-manager` commit `336c35e Register execution component sequence`.
- Workspace daily-memory/log commit pushed: `246627c Record execution component numbering`.
- Current component sequence for future discussion: `C01 Allocation`, `C02 Entry`, `C03 Lifecycle`, `C04 Option Review`, `C05 Failure Review`, `C06 Order Intent`, and `C07 Execution Gate`.
- Stable contract-facing component ids remain unchanged beneath those short names, including `opportunity_risk_allocation_engine`, `entry_decision_engine`, `position_lifecycle_controller`, `option_reexpression_review`, `failure_explanation_component`, `order_intent_builder`, and `execution_gate_adapter`.

## Trading model sequence M prefix

- Chentong decided that execution components use `C` prefixes and trading models should use matching `M` prefixes for the intraday model order.
- Implemented `M01` through `M10` as display/order labels in `trading-model`, with `src/models/model_sequence.py` owning the canonical sequence and docs showing concise model names.
- Stable physical and contract-facing surfaces remain unchanged as `model_01_market_regime` through `model_10_event_risk_governor`; the `M` labels do not rename package directories, script directories, SQL fields, or registry contract ids.
- Current model sequence for future discussion: `M01 Market Regime`, `M02 Sector Context`, `M03 Target State`, `M04 Event Failure Risk`, `M05 Alpha Confidence`, `M06 Dynamic Risk Policy`, `M07 Position Projection`, `M08 Underlying Action`, `M09 Option Expression`, and `M10 Event Risk Governor`.
- Added `trading-manager` registry migration `642_register_trading_model_sequence.sql` and regenerated `scripts/registry/current.csv` to register `TRADING_MODEL_SEQUENCE` plus the layer naming policy update.
- Verification passed: `trading-model` full `PYTHONPATH=src python3 -m unittest discover tests` with 242 tests OK; `python3 -m compileall -q src scripts` OK; `trading-manager` registry tests 52 OK; registry snapshot OK; registry dry-run no pending migrations; `git diff --check` clean for both repos.

## Execution component physical id alignment

- Chentong clarified the stricter naming rule: because models use physical ids like `model_10_event_risk_governor`, execution components should use matching physical ids like `component_01_*`, not only `C01` display labels.
- Updated `trading-execution` runtime component ids to `component_01_allocation`, `component_02_entry`, `component_03_lifecycle`, `component_04_option_review`, `component_05_failure_review`, `component_06_order_intent`, and `component_07_execution_gate`.
- Updated execution docs and tests so `C01-C07` remain the display/order labels while `component_01_*` through `component_07_*` are the stable component ids.
- Added `trading-manager` registry migration `643_align_execution_component_ids.sql` and regenerated `scripts/registry/current.csv`; active registry no longer uses the old component ids `opportunity_risk_allocation_engine`, `entry_decision_engine`, `position_lifecycle_controller`, `option_reexpression_review`, `failure_explanation_component`, `order_intent_builder`, or `execution_gate_adapter`.
- Verification passed: `trading-execution` full `PYTHONPATH=src python3 -m unittest discover tests` with 80 tests OK; `python3 -m compileall -q src scripts` OK; `trading-manager` registry tests 52 OK; registry snapshot OK; registry dry-run no pending migrations; `git diff --check` clean for both repos.

## High-risk options stop-source policy

- Chentong defined the execution account as high-risk and options-first: large option premium drawdowns are allowed, and normal stop/exit management should be based on the underlying stock/asset path rather than option mark-to-market loss.
- Accepted policy: execution must strictly follow the model-provided underlying thesis invalidation and hard stop lines. It must not replace model stops with a fixed percentage stop.
- `max_loss_usd` and `max_loss_pct` remain required pre-order risk-budget fields, but they size capital/risk budget and catastrophic gates; they are not standalone ordinary exit triggers for options positions.
- If a trade lacks enforceable model stop/invalidation evidence, execution must reject the order instead of filling in a default fixed stop.
- Updated `trading-execution` risk-cap, decision, and runtime component docs; added `trading-manager` registry migration `644_register_underlying_thesis_stop_policy.sql` and regenerated `scripts/registry/current.csv`.
- Verification passed: `trading-manager` registry tests 52 OK; registry snapshot OK; registry dry-run no pending migrations; `git diff --check` clean for `trading-execution`, `trading-manager`, and this memory entry.

## C01 narrowed to execution intake

- Chentong rejected placing position and risk management in C01. Accepted boundary: C01 only reads account balance state, current holdings, and watch targets; if an account sleeve has available balance, C01 may pass watch targets forward.
- Renamed `C01 Allocation / component_01_allocation` to `C01 Intake / component_01_intake` and replaced `target_allocation_snapshot` with `execution_intake_snapshot`.
- C01 no longer calls M06, no longer consumes `account_sleeve_risk_budget_snapshot`, and no longer emits `risk_budget`. It emits `available_balance_usd`, `new_position_balance_status`, `watch_targets`, `blocked_targets`, `open_position_refs`, and model refs for M01/M02/M03.
- Downstream ownership: C02 decides whether to open a position, C03 manages existing positions, C04 reviews option expression, C06 builds order intents, and C07 enforces execution gates. C01 must not block for concentration, position size, stop distance, or risk-budget reasons.
- Added `trading-manager` migrations `645_register_execution_intake_component.sql` and `646_clean_execution_intake_registry_note.sql`, regenerated `scripts/registry/current.csv`, and updated manager registry tests.
- Verification passed: `trading-execution` full tests 81 OK; `python3 -m compileall -q src scripts` OK; `trading-manager` registry tests 52 OK; registry snapshot OK; registry dry-run no pending migrations; stale active reference scan found no old `target_allocation_snapshot` / `component_01_allocation` references in active docs/source/tests/current registry.

## Pre-compaction flush - trading model M-prefix commits

- Trading model M-prefix sequence work was pushed as `trading-model` commit `54b0810 Add M-prefixed model sequence` and `trading-manager` commit `3774cc7 Register trading model sequence`.
- Workspace daily-memory/log commit pushed: `0bf36f7 Record model sequence prefix`.
- `trading-model` and `trading-manager` were clean after the work. Existing unrelated workspace dirty files remained untouched: `DREAMS.md`, `MEMORY.md`, `memory/2026-05-21.md`, `memory/2026-05-22.md`, and `memory/2026-05-23.md`.

## C01 sector opportunity mix

- Chentong decided multi-sector opportunity proportions should belong naturally in C01 Intake, but only as an opportunity map, not final position sizing or risk allocation.
- C01 now emits `sector_opportunity_mix` from M02 sector context: it includes every sufficiently strong sector/theme whose strength clears the accepted threshold and normalizes weights by relative strength. It does not force a fixed top-three list.
- Example policy: software 35%, semiconductors 35%, healthcare 30% is valid when those are the sufficiently strong sectors and their relative scores normalize to that mix; if another sector is also strong enough, it is included too.
- Downstream boundary remains intact: C02 uses the opportunity map when considering entries, while final target selection, position sizing, stop/risk handling, order construction, and execution gates remain downstream.
- Added `trading-manager` migration `647_register_c01_sector_opportunity_mix.sql`, regenerated `scripts/registry/current.csv`, and updated `trading-execution` docs/source/tests for `sector_opportunity_mix`.
- Verification passed: `trading-execution` full tests 82 OK; `python3 -m compileall -q src scripts` OK; `trading-manager` registry tests 52 OK; registry snapshot OK; registry dry-run no pending migrations; `git diff --check` clean for both repos.

## C01 dynamic remaining sector opportunity mix

- Chentong clarified that C01's sector opportunity mix must be dynamic against current holdings. If desired mix is software 35%, semiconductors 35%, healthcare 30% and current holdings already fill semiconductors 35%, C01 should stop asking C02 to focus on semiconductors until that exposure falls below target.
- Updated `sector_opportunity_mix` rows to include `target_mix_weight`, `current_mix_weight`, `remaining_mix_weight`, and residual-normalized `opportunity_mix_weight`.
- This remains C01 candidate-focus management, not final position sizing or risk allocation. C02 consumes the remaining opportunity map; later components still own sizing, risk caps, order intent, and execution gates.
- Added `trading-manager` migration `648_register_c01_dynamic_sector_opportunity_mix.sql` and regenerated `scripts/registry/current.csv`.
- Verification passed: `trading-execution` full tests 83 OK; `python3 -m compileall -q src scripts` OK; `trading-manager` registry tests 52 OK; registry snapshot OK; registry dry-run no pending migrations; `git diff --check` clean for both repos.

## C01 candidate pool sources

- Chentong decided C01 should maintain the equity/options target pool from three accepted sources: targets in remaining strong-sector opportunity buckets, recent high-trading-volume targets, and recent news/earnings catalyst targets.
- If a sector such as semiconductors is already filled versus C01 target mix, C01 excludes related companies from `watch_targets` before C02 sees them.
- Implemented C01 watch-target annotations via `candidate_reasons` and blocked-target reasons such as `sector_opportunity_already_filled` and `not_in_c01_candidate_source_pool`.
- Added `trading-manager` migration `649_register_c01_candidate_pool_policy.sql`, regenerated `scripts/registry/current.csv`, and updated execution docs/source/tests.
- Verification passed: `trading-execution` full tests 84 OK; `python3 -m compileall -q src scripts` OK; `trading-manager` registry tests 52 OK; registry snapshot OK; registry dry-run no pending migrations; `git diff --check` clean for both repos.

## C01 candidate source union refined

- Chentong corrected that a target such as NVDA should not be excluded merely because semiconductors are filled when it is also a hotspot/high-volume/catalyst name.
- Refined C01 rule: a filled sector removes only the `remaining_strong_sector_opportunity` reason. The target still enters `watch_targets` if it has high-volume, abnormal-volume, news, or earnings catalyst evidence. Only targets with none of the accepted source reasons are filtered out.
- Clarified thresholds: `recent_high_trading_volume` requires a reviewed high-volume flag or absolute volume/dollar-volume score or percentile >= 0.80; `recent_abnormal_volume` requires a reviewed abnormal-volume flag, relative/abnormal volume score >= 0.80, relative volume >= 2.0x, or volume z-score >= 2.0.
- Added `trading-manager` migration `650_refine_c01_candidate_pool_union_policy.sql`, regenerated `scripts/registry/current.csv`, and updated execution docs/source/tests.
- Verification passed: `trading-execution` full tests 84 OK; `python3 -m compileall -q src scripts` OK; `trading-manager` registry tests 52 OK; registry snapshot OK; registry dry-run no pending migrations; `git diff --check` clean for both repos.

## C02 entry thesis boundary

- Chentong accepted C02 as underlying entry-thesis suitability only. It consumes C01 `watch_targets`, does not check account balance, does not choose option versus stock expression, does not select option contracts, does not size positions, and does not directly authorize C06 order intents.
- C02 no longer calls M09. It uses M03 target state, M04 event failure risk, M05 alpha confidence, M06 dynamic risk policy, and M08 underlying action.
- `entry_decision` now emits `entry_thesis_status` as `suitable`, `deferred`, or `rejected`. Only `suitable` routes to C04 for expression review.
- A suitable thesis requires underlying direction, entry zone, target or take-profit zone, model invalidation price, hard stop price, expected horizon when available, and an `entry_suitability_score`.
- Direct order intent from `entry_decision` is intentionally non-executable; C06 must consume later executable decisions from expression/lifecycle review instead.
- Added `trading-manager` migration `651_register_c02_entry_thesis_boundary.sql`, regenerated `scripts/registry/current.csv`, and updated execution docs/source/tests.
- Verification passed: `trading-execution` full tests 84 OK; `python3 -m compileall -q src scripts` OK; `trading-manager` registry tests 52 OK; registry snapshot OK; registry dry-run no pending migrations; `git diff --check` clean for both repos.

## C03 lifecycle boundary pending implementation

- Discussion moved to C03 after C02 was accepted. Proposed C03 identity: `C03 Lifecycle / component_03_lifecycle`, responsible only for already-open position lifecycle decisions.
- Accepted intended boundary: C03 manages existing position thesis state, not new target screening, new entry suitability, option contract selection, order sizing, or order construction.
- C03 should read current positions, associated underlying exposure, cost/current price context, and the C02/M08 underlying thesis for those positions.
- C03 should decide lifecycle actions such as `hold`, `add`, `reduce`, `exit`, `stop`, and `take_profit`, then emit `position_lifecycle_decision` for downstream components. It does not submit orders.
- C03 should use M04 event failure risk, M05 alpha confidence, M06 dynamic risk policy, M07 position projection, and M08 underlying action.
- C03 should not call M09; option strike/expiry/roll/repair/liquidity/IV/DTE decisions belong to C04. C03 should not call M10; failure explanation/governor work remains C05 after abnormal failures.
- Important implementation warning found before compaction: current C03 still appears to use `max_position_loss_pct` as a fixed loss stop. That conflicts with the high-risk options stop-source policy. When C03 is implemented/refined, remove ordinary fixed-percentage loss-stop behavior and require underlying hard stop / model invalidation evidence from M08 instead.

## C03 underlying-first lifecycle and review gate

- Chentong accepted the architecture rule that all open, add, reduce, close, stop, and take-profit calculations are first computed against the underlying/stock thesis. For options, the next layer translates that underlying action into option expression.
- C03 owns existing-position lifecycle only: it decides whether the underlying exposure should hold, add, reduce, stop, exit, or take profit. It does not select contracts, size orders, or execute trades.
- C04 owns option expression translation, including whether a suitable option exists, whether to roll/repair, whether stock fallback is better, or whether to skip expression.
- Because the account is options-first and high risk, C03 should not stop merely because option mark-to-market loss or a fixed P/L percentage is large. Ordinary lifecycle stops use underlying hard stop/model invalidation, critical event risk, or explicit M08 action.
- Chentong corrected that C03 should not introduce complex fee/PDT/churn formulas. Every operation should have explicit, persuasive reason evidence, and live orders require final agent review at C07.
- Chentong marked agent final review as a hard line: open, add, reduce, close/exit, stop, take-profit, option roll, or stock fallback orders must pass C07 agent review before live broker submission.
- C03 add decisions must also obey the upstream sector/opportunity mix and portfolio exposure constraints from C01/M07; the system must not keep adding aggressively when the relevant sector or exposure bucket is already filled.
- After checking the 2026-06-04 PDT framework change, Chentong decided to delete the dedicated PDT/day-trade gate from C03 because the system is unlikely to go live before the change. Current accepted rule: no C03 PDT formula or PDT gate; cautious add/reduce management still stands through explicit reason evidence, C01/M07 sector/opportunity constraints, and C07 final agent review.

## Pre-compaction flush - C03 lifecycle policy commits

- C03 lifecycle policy work was pushed in three batches:
  - `trading-execution` `53a2ba3 Refine C03 lifecycle cost controls`, `be07cac Move C03 churn checks to review gate`, and `22f8072 Gate C03 adds on day-trade feasibility`.
  - `trading-manager` `4700a02 Register C03 lifecycle cost policy`, `d2363da Register C03 review gate policy`, and `eb140d5 Register C03 day-trade feasibility policy`.
  - Workspace daily-memory/log commits `33dfa5d Record C03 lifecycle cost policy`, `706ed01 Record C03 review gate correction`, and `45cdb86 Record C03 day-trade feasibility decision`.
- Latest correction implemented after compaction: deleted the dedicated C03 PDT/day-trade gate from active code/docs/registry while preserving cautious add/reduce reason evidence and C07 final review.
- Verification passed across the final state: `trading-execution` tests 87 OK; `trading-manager` registry tests 52 OK, registry migration/snapshot/dry-run checks OK, and diff checks OK.

## C07 trade operation review skill and latency tests

- Chentong clarified that live trade operation review is distinct from agent post-trade replay/review; do not mix the two when discussing C07 execution gating.
- C07 final review remains the hard line for live broker submission of open, add, reduce, close/exit, stop, take-profit, option roll, and stock fallback operations.
- Latency tests showed ordinary OpenClaw sub-agent review is too slow for the hot path: cold start around 14-16s and warmed review still around 12s.
- Codex CLI review is materially faster: basic `codex exec` samples were about 5.7-8.8s, and precise dedicated reviewer resume samples were about 5.4s then 2.7s. Avoid `resume --last`; it can attach the wrong context and caused one bad 38s run.
- Model tests for C07 review: `gpt-5.4-mini` usually stayed under 10s, around 3.4-6.8s; `gpt-5.5` was stricter and caught edge cases such as stale/after-hours evidence but often ran about 11.5-14.9s. Current preferred policy: use deterministic hard rules first, default routine Codex CLI C07 review to `gpt-5.4-mini` for speed, and escalate gray-zone/conflicting/high-risk/invalid-output cases to `gpt-5.5`.
- Created and installed the `trade-operation-review` Codex CLI skill. Versioned source: `skills/codex/trade-operation-review/SKILL.md`; installed copy: `$CODEX_HOME/skills/trade-operation-review`.
- The skill requires strict JSON output with `review_status`, `reason_codes`, `missing_fields`, `blocking_conflicts`, `allowed_actions`, `max_valid_until`, `requires_recheck`, and `review_notes`, with safety defaults to `defer` on missing evidence, schema failure, or uncertainty.
- Verification passed: both skill copies passed `quick_validate.py`. Workspace commit pushed: `f679976 Add trade operation review skill`.

## C07 missed-event guard and Codex CLI migration

- Chentong corrected the C07 trade-operation review purpose: it should not repeat deterministic checklist/risk work already handled by scripts and upstream models. Its job is to confirm there is no important target-specific, sector, macro, regulatory, earnings, analyst, filing, halt, litigation, or other market-moving event that the proposal did not already fetch, interpret, and incorporate.
- `trade-operation-review` was refocused first toward scenario awareness, then narrowed to a missed-event guard. Known risks in `known_event_context` must not be double-counted as fresh objections; missing/insufficient live event coverage should return defer/wait-for-event-scan, and newly found unincorporated events should return defer or reject with `missed_events`.
- Chentong decided the latency difference is acceptable and requested always using `gpt-5.5` for this path. This supersedes the earlier mini-default/escalate policy recorded above: C07 missed-event review should default to `gpt-5.5`.
- Workspace commits pushed for this evolution: `eff1d75 Refocus trade review skill on scenarios`, `ea18756 Refocus trade review on missed events`, and `4c316f6 Add Codex CLI review skills`.
- Scripted agent-review paths were migrated to Codex CLI where found. `trading-manager` now routes agent reviews through Codex CLI; `trading-model` uses Codex CLI for promotion reviews; `trading-dashboard` labels the repair runner as Codex.
- Automatic dashboard/server-error repair runner now uses Codex CLI with the `$server-error-repair` skill. Review/repair skill sources are under `skills/codex/...`, with registry paths updated accordingly.
- Pushed repository commits for this migration: `trading-manager` `f43603e Route agent reviews through Codex CLI`; `trading-model` `a6efe8c Use Codex CLI for promotion reviews`; `trading-dashboard` `13a5068 Label Codex repair runner in dashboard`.
- Verification passed after migration: `trading-manager` 99 tests OK; `trading-model` 21 tests OK plus compileall OK; `trading-dashboard` `npm run typecheck` OK; registry snapshot OK; `git diff --check` OK.

## C03 closed and C07 policy refined

- C03 was accepted as complete in the current registry/runtime boundary. It owns only existing-position lifecycle decisions: `hold`, `add`, `reduce`, `stop`, `exit`, and `take_profit`.
- C03 remains underlying-first. Options are expressions of the underlying thesis and are translated downstream by C04; C03 must follow underlying hard stops/model invalidation and must not use fixed option P/L percentage stops.
- C03 does not calculate fees, PDT/day-trade rules, churn formulas, option contract choice, or final order sizing. Add/reduce actions require explicit persuasive reason evidence and must respect C01 portfolio/sector/opportunity mix constraints.
- Any live submission from open/add/reduce/exit/stop/take-profit/roll/stock-fallback paths still requires C07 final review.
- Adjacent C07 registry wording was corrected so C07 is only the missed-event guard and does not re-check fees, spread, or transaction costs.
- Pushed commit: `trading-manager` `ac90d07 Refine C07 missed-event review policy`.
- Verification passed: registry snapshot OK, `tests.test_trading_registry` 52 tests OK, worktree clean after the change.

## C04 option expression review accepted

- Chentong accepted C04 as the option expression / translation layer. It consumes C02/C03 underlying trade intent plus M09 option-expression evidence and chooses whether the intent should be expressed as `long_call`, `long_put`, `underlying_only_expression`, or `no_valid_expression`.
- C04 does not decide whether the underlying should be traded, does not size positions, does not generate broker orders, and does not bypass C06 or C07.
- C04 checks optionability, chain freshness, DTE, delta/moneyness, strike versus target range, liquidity, spread, volume/OI, IV/IV-crush risk, Greeks, target/stop reward-risk, fill quality, event risk, and pending option exposure.
- Accepted V1 scope: single-leg long call, single-leg long put, stock fallback, or no expression. No 0DTE, adjusted contracts, short options, spreads, or complex option strategies.
- Accepted DTE policy: `10min` horizon -> 3-7 DTE, `1h` -> 7-14 DTE, `1D` -> 7-21 DTE, `1W` -> 21-45 DTE. Preferred absolute delta remains `0.35-0.65`, with guardrails against deep OTM lottery contracts and strikes beyond the thesis target range.
- For existing option positions, C04 may compare hold/roll/repair/fallback/skip expression, but roll requires a materially better expression rather than a marginal improvement.
- Pushed commit: `trading-manager` `fd64b01 Register C04 option expression review policy`.

## M09 option-expression training and bypass semantics

- M09 remains the model layer for option expression evidence. It consumes an already formed underlying thesis from M08/C02/C03 and option-chain state, then evaluates whether option expression is reasonable and which contract features fit best. C04 is the execution component that decides whether to adopt that evidence in the live pipeline.
- Chentong corrected M09 training granularity: candidate training rows should be `underlying × minute × option_contract_candidate`, not one row per minute. A separate underlying-minute status/decision row may record the selected contract or expression status.
- Candidate rows should cover good and bad contracts, wide spreads, high IV, stale quotes, illiquid contracts, unsuitable delta/DTE, and other rejected candidates so the model learns both selection and rejection.
- Chentong corrected bypass semantics: if a minute has no usable option chain, or if the underlying is not optionable such as BTC, the live path should not call M09. Those states may be retained as coverage/audit/status rows for historical training/evaluation, but they are not M09 contract-inference calls and do not produce per-contract candidate rows.
- Live C04 calls M09 only when `optionable_chain_available` and usable option-chain evidence exists; otherwise C04 bypasses M09 and routes to underlying-only or no valid option expression according to deterministic context.
- Pushed commits: `trading-model` `d9fdffa Clarify M09 option-surface bypass semantics`; `trading-manager` `2180807 Register M09 option-surface bypass policy`.
- Verification passed: registry migrations applied, registry snapshot matched migrations, `PYTHONPATH=src python3 -m unittest tests.test_trading_registry` passed, and `git diff --check` passed in the touched repos.
