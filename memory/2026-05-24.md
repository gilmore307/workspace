# 2026-05-24

## Pre-compaction flush - Layer 5/6 training granularity

- Chentong rejected a candidate-only interpretation for Layer 6 training because downstream Layer 7 add/reduce/maintain decisions need minute-level risk context even if live components are triggered on demand. Corrected `trading-model` so Layer 6 supports minute-level policy rows and no longer requires every row to have `target_candidate_id`.
- Layer 6 durable decision: training should use every eligible minute of risk-policy context, while live inference may remain demand-triggered. Implemented/recorded `policy_scope` values: `global`, `target_candidate`, and `active_position`. `global` rows cover minute-level portfolio/account/market policy state; target/position rows condition that state for candidates or existing positions.
- Layer 6 verification passed before compaction: `tests.test_dynamic_risk_policy_model`, `tests.test_layer_04_10_script_entrypoints`, full `unittest discover tests` with 235 tests OK, and `git diff --check` clean. Commit pushed in `trading-model`: `1e61658 Support minute-level Layer 6 policy rows`.
- Chentong then rejected pre-screened/candidate-only Layer 5 training because real use will expose the model to many states that candidate filtering would have removed. Corrected Layer 5 to train on dense minute target-state rows rather than only accepted candidates.
- Layer 5 durable decision: training data should include every eligible minute from Layer 3 accepted anonymous target-state rows, including strong signals, weak signals, no-edge states, conflicts, event-risk-suppressed states, poor paths, and weak tradability. Layer 4 `event_failure_risk_vector` is optional conditioning input, not a prerequisite for sample generation.
- Layer 5 route decisions such as `pass_to_layer6`, `watch_only`, and `reject` are post-score calibration/routing thresholds, not pre-training filters. This avoids trigger/selection bias and lets Layer 5 learn the distribution it will see in production.
- Layer 5 verification passed before compaction: `tests.test_alpha_confidence_model`, `tests.test_layer_04_10_script_entrypoints`, full `unittest discover tests` with 236 tests OK, and `git diff --check` clean. Commit pushed in `trading-model`: `0750fc6 Train Layer 5 on dense minute rows`.

## Layer 8 SQL input prefix fix

- Reviewed current `trading-model` Layer 8 route: repository docs define Layer 8 as `UnderlyingActionModel` with physical surface `model_08_underlying_action`; Layer 9 remains optional `TradingGuidanceModel / OptionExpressionModel`.
- Found and fixed a narrow database-generation bug in `scripts/models/model_08_underlying_action/generate_model_08_underlying_action.py`: SQL input assembly read `model_07_position_projection` rows but copied only `6_` prefixed projection fields into `position_projection_vector`, so database runs could lose Layer 7 target exposure/confidence fields and fall back to defaults.
- Added a script-entrypoint test proving Layer 8 database input preserves `7_` projection fields and excludes stray `6_` fields.
- Verification passed in `trading-model`: `PYTHONPATH=src python3 -m unittest tests.test_underlying_action_model tests.test_layer_04_10_script_entrypoints`, full `PYTHONPATH=src python3 -m unittest discover tests` with 237 tests OK, and `git diff --check` clean.
- Commit pushed in `trading-model`: `6ebd2c5 Fix Layer 8 projection SQL input prefix`.

## Layer 9 option-surface bypass settled

- Chentong accepted the Layer 9 route: keep one minute-level Layer 9 status/training row for every eligible minute with a Layer 8 thesis, including non-optionable underlyings such as BTC.
- Implemented Layer 9 option-surface statuses in `trading-model`: `optionable_chain_available`, `optionable_chain_missing`, and `non_optionable_underlying`.
- `non_optionable_underlying` now bypasses option-chain scoring, emits `underlying_only_expression` when the Layer 8 thesis is actionable, clears selected contract fields, and records `non_optionable_underlying` plus `layer_9_bypassed_option_expression_scoring` reason codes.
- Fixed Layer 9 handoff fallbacks to read Layer 8 resolved fields (`8_resolved_action_side`, `8_resolved_dominant_horizon`) instead of stale `9_resolved_*` names.
- Updated Layer 9 docs and package README for dense minute-level status rows and non-optionable bypass semantics; corrected docs from stale `8_resolved_*` expression fields to `9_resolved_*`.
- Added `trading-manager` registry migration `639_register_layer_nine_option_surface_status.sql`, updated `scripts/registry/current.csv`, and registered the option-surface status vocabulary plus the new `9_resolved_option_surface_status` field.
- Verification passed: `trading-model` full `PYTHONPATH=src python3 -m unittest discover tests` with 240 tests OK; `trading-manager` targeted registry/Layer 9 tests 58 tests OK; registry snapshot OK; registry dry-run no pending migrations; `git diff --check` clean for both repos.

## Pre-compaction flush - Layer 9 implementation commits

- Layer 9 option-surface bypass work was pushed as `trading-model` commit `8e49df6 Add Layer 9 option surface bypass status` and `trading-manager` commit `3faf7a4 Register Layer 9 option surface status`.
- Workspace daily-memory/log commit pushed: `021fcdf Record Layer 9 bypass decision`.
- Known unrelated workspace dirt remained after the work and was intentionally left untouched: `DREAMS.md`, `MEMORY.md`, `memory/2026-05-21.md`, `memory/2026-05-22.md`, and previously untracked/dirty daily memory around `memory/2026-05-23.md`.

## Ten-layer model stack rescan

- Rescanned the active `trading-model` ten-layer model stack across docs, source model packages, script model entrypoints, tests, and `trading-manager` registry current snapshot after the Layer 8/9 decisions.
- Confirmed all ten model directories and script entrypoint directories exist from `model_01_market_regime` through `model_10_event_risk_governor`, with docs `10_layer_01_market_regime.md` through `19_layer_10_event_risk_governor.md`.
- Cleaned remaining current-surface drift: Layer 8 docs now use `8_resolved_*` plan fields and `8A` through `8I` internal components; Layer 10 docs now use `10A` through `10C` internal components.
- Added `trading-manager` registry migration `640_clean_layer_eight_nine_registry_writing.sql` and regenerated `scripts/registry/current.csv` so active Layer 8/9 registry rows no longer mention stale `layer_7_full_contract_fit_model`, Layer 7 target-range guardrail payloads, Layer 7 path thesis, or a Layer 6 generator for `model_08_underlying_action`.
- Verification passed: `trading-model` full `PYTHONPATH=src python3 -m unittest discover tests` with 240 tests OK; `trading-manager` targeted registry/Layer 9 tests 58 tests OK; registry snapshot OK; registry dry-run no pending migrations; `git diff --check` clean for both repos.

## Execution component sequence numbering

- Chentong asked for live-trading components to be numbered like model layers so the intraday process order is obvious and names are shorter.
- Implemented concise component sequence fields in `trading-execution` runtime graph output: `C01 Allocation`, `C02 Entry`, `C03 Lifecycle`, `C04 Option Review`, `C05 Failure Review`, `C06 Order Intent`, and `C07 Execution Gate`.
- Kept existing stable `component_id` values such as `opportunity_risk_allocation_engine` as contract-facing interface names to avoid breaking downstream callers; `component_step` and `component_name` are display/order fields.
- Updated `trading-execution` architecture, decision, runtime component docs, component implementation, and runtime component tests.
- Added `trading-manager` registry migration `641_register_execution_component_sequence.sql` and regenerated `scripts/registry/current.csv` to register `EXECUTION_RUNTIME_COMPONENT_SEQUENCE` plus the updated component graph policy.
- Verification passed: `trading-execution` full `PYTHONPATH=src python3 -m unittest discover tests` with 80 tests OK; `python3 -m compileall -q src scripts` OK; `trading-manager` registry tests 52 OK; registry snapshot OK; registry dry-run no pending migrations; `git diff --check` clean for both repos.

## Pre-compaction flush - execution component numbering commits

- Execution component numbering was pushed as `trading-execution` commit `bc6b726 Number execution runtime components` and `trading-manager` commit `336c35e Register execution component sequence`.
- Workspace daily-memory/log commit pushed: `246627c Record execution component numbering`.
- Current component sequence for future discussion: `C01 Allocation`, `C02 Entry`, `C03 Lifecycle`, `C04 Option Review`, `C05 Failure Review`, `C06 Order Intent`, and `C07 Execution Gate`.
- Stable contract-facing component ids remain unchanged beneath those short names, including `opportunity_risk_allocation_engine`, `entry_decision_engine`, `position_lifecycle_controller`, `option_reexpression_review`, `failure_explanation_component`, `order_intent_builder`, and `execution_gate_adapter`.

## Trading model sequence M prefix

- Chentong decided that execution components use `C` prefixes and trading models should use matching `M` prefixes for the intraday model order.
- Implemented `M01` through `M10` as display/order labels in `trading-model`, with `src/models/model_sequence.py` owning the canonical sequence and docs showing concise model names.
- Stable physical and contract-facing surfaces remain unchanged as `model_01_market_regime` through `model_10_event_risk_governor`; the `M` labels do not rename package directories, script directories, SQL fields, or registry contract ids.
- Current model sequence for future discussion: `M01 Market Regime`, `M02 Sector Context`, `M03 Target State`, `M04 Event Failure Risk`, `M05 Alpha Confidence`, `M06 Dynamic Risk Policy`, `M07 Position Projection`, `M08 Underlying Action`, `M09 Option Expression`, and `M10 Event Risk Governor`.
- Added `trading-manager` registry migration `642_register_trading_model_sequence.sql` and regenerated `scripts/registry/current.csv` to register `TRADING_MODEL_SEQUENCE` plus the layer naming policy update.
- Verification passed: `trading-model` full `PYTHONPATH=src python3 -m unittest discover tests` with 242 tests OK; `python3 -m compileall -q src scripts` OK; `trading-manager` registry tests 52 OK; registry snapshot OK; registry dry-run no pending migrations; `git diff --check` clean for both repos.
