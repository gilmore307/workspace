## Pre-compaction flush - Replay source acquisition and TE policy

- Replay source acquisition for `promotion_replay_candidate_policy` completed its external source bootstrap: Trading Economics calendar reached `60/60 complete`, GDELT news `60/60 complete`, and OKX crypto market data `180/180 complete`; missing source windows are now `0`.
- Final TE gaps were `2022-07`, `2024-06`, and `2025-09`; after browser/custom-date/cookie-state recovery and rerunning formal acquisition, they produced 16, 21, and 26 rows respectively.
- Successfully acquired replay artifacts are stored under `trading-storage`, mainly `storage/01_source_data/replay/...` and replay manifests/logs under `storage/05_replay_datasets/promotion_replay_candidate_policy/`; runtime storage artifacts remain uncommitted per storage policy.
- Alpaca replay source requests remain intentionally deferred (`180` items) because they are candidate-symbol dependent and should expand dynamically when replay/fold execution produces point-in-time candidate symbols.
- Chentong clarified TE historical acquisition is a one-time bootstrap; the TE subscription is canceled starting next month, and future TE use should rely on logged-out visible-page access for current/recent/future calendar data rather than authenticated historical backfill.
- Landed and pushed code/docs to reflect logged-out TE routing and completed replay source coverage:
  - `trading-data` commit `2161f2d`
  - `trading-evaluation` commits `d035cd4` and `6862e85`
  - `trading-manager` commit `2c48745`
- Verified after TE route changes: `trading-data` 9 tests plus compileall/diff-check; `trading-evaluation` 2 tests plus compileall/diff-check; `trading-manager` 57 tests plus registry snapshot, compileall, and diff-check.
- Current next step: freeze the completed replay dataset, then proceed into replay execution and fold settlement; do not reopen broad TE historical backfill unless a future replay window has a specific artifact gap.

## Replay freeze correction at 2026-05-22 04:12 EDT

- Continuing the Replay work surfaced a false-positive OKX coverage issue: the prepared acquisition plan used instrument-level OKX output roots, so 60 monthly acquisitions per instrument shared one `completion_receipt.json` path and the saved `crypto_bar.csv` artifact was overwritten by the latest month.
- Fixed and pushed `trading-evaluation` commit `39d64d0` (`Validate replay acquisition receipt paths`):
  - OKX Replay acquisition paths now use `instrument/month` granularity.
  - Replay freeze validation now rejects shared non-deferred acquisition receipt paths.
  - `docs/04_task.md` now marks the first candidate-policy Replay dataset as frozen and shifts the active task to running Replay/fold settlement.
- Rebuilt the candidate-policy Replay dataset manifest, reran public OKX monthly acquisition into corrected paths, and re-froze the dataset:
  - OKX fixed crypto coverage: `180/180 complete`.
  - GDELT: `60/60 complete`.
  - Trading Economics: `60/60 complete`.
  - Accepted deferred Alpaca candidate-dependent rows: `180`.
  - Missing source acquisitions: `0`.
  - Freeze receipt: `/root/projects/trading-storage/storage/05_replay_datasets/promotion_replay_candidate_policy/replay_freeze_receipt.json`.
- Verification passed: `trading-evaluation` 32 tests OK, compileall OK, diff-check OK; Replay runtime dry-run passed with no provider calls, broker calls, broker/account mutation, model training, or active config write.
- Next step: implement/run the real Replay execution that emits decision rows from the frozen dataset and execution-owned component graph; then run fold settlement metrics. Do not treat the dry-run receipt as the 60-month Replay result.

## Pre-compaction flush - workspace memory checkpoint

- Workspace memory changes from the Replay freeze correction were committed and pushed as `1f729cd`.

## Crypto fixed-sleeve Replay execution and settlement

- Added and pushed `trading-evaluation` commit `2bf2f63` (`Add replay execution runner`).
  - New `src/trading_evaluation/replay_execution.py` and `scripts/evaluation/run_replay_execution.py` run the frozen fixed crypto sleeve through `trading-execution` Replay components.
  - The runner reads frozen OKX daily bar artifacts from the Replay dataset manifest and emits settlement-ready `evaluation_replay_decision_row` JSONL.
  - Safety flags remain zero/false for provider calls, broker calls, broker/account mutation, model training, and active model config writes.
- Ran the real crypto fixed-sleeve Replay execution over the frozen `promotion_replay_candidate_policy` dataset:
  - Run id: `crypto_spot_replay_20260522_081953`.
  - Decision rows: `5475`.
  - Market dates: `1826`.
  - Targets: `BTC`, `ETH`, `SOL`.
  - Decision rows path: `/root/projects/trading-storage/storage/05_replay_datasets/promotion_replay_candidate_policy/replay_execution_runs/crypto_spot_replay_20260522_081953/decision_rows.jsonl`.
  - Receipt path: `/root/projects/trading-storage/storage/05_replay_datasets/promotion_replay_candidate_policy/replay_execution_runs/crypto_spot_replay_20260522_081953/replay_execution_receipt.json`.
- Ran fold settlement for that Replay execution:
  - Settlement id: `settlement_ad1cbf4f039eeb76`.
  - Settlement path: `/root/projects/trading-storage/storage/05_replay_datasets/promotion_replay_candidate_policy/fold_settlement_runs/crypto_spot_replay_20260522_081953/fold_settlement_run.json`.
  - Status: `review_required`.
  - Gate failure: `auroc_below_minimum`.
  - Metrics: net/excess return `8.88390602554663`, max drawdown `-0.8204388198497394`, hit rate `0.24127853881278538`, AUROC `0.49929031270838303`, Brier score `0.31549428624684916`.
- Interpretation: this crypto fixed-sleeve evidence is useful Replay/settlement evidence, but it is not promotion-ready. Positive return does not override the failed ranking/classification gate; do not promote from this result.
- Verification after implementation passed in `trading-evaluation`: 34 tests OK, compileall OK, replay contract validation OK, diff-check OK.

## Pre-compaction flush - seven-repo first-principles review/straightening

- Chentong requested a Codex CLI style repo-by-repo review using the prior review skill and first-principles cleanup rule: inspect carefully and straighten detours introduced by route changes.
- Completed and pushed the review/cleanup pass across seven repositories:
  - `trading-evaluation` commit `03397e2`.
  - `trading-execution` commit `38b6c48`.
  - `trading-data` commit `c27e6b5`.
  - `trading-manager` commit `f281db8`.
  - `trading-model` commit `0f74087`.
  - `trading-storage` commit `5f70364`.
  - `trading-dashboard` commit `fb5a3e0`.
- Key dashboard cleanup from the final pass: removed `_vN` read-model alias routing so HTTP/WebSocket surfaces use current direct contract routes only; added `execution_realtime_trading_runtime_status` to the dashboard current read-model allowlist to align with storage's public refresh collection.
- Final verification reported clean across all seven repos:
  - Repo-specific Python compile/tests/build/check gates passed.
  - `trading-dashboard` final gate passed: `compileall`, 12 unittests, `npm run build`, and `git diff --check`.
  - All seven repos had clean `git status --short`.
  - All commits were pushed to GitHub.
- Current engineering state: Replay/evaluation/execution/storage/dashboard code paths are straightened and green after the route-change review; remaining weakness is strategy evidence quality, not baseline engineering plumbing.

## Dashboard Diagnostics and Layer 10 provider coverage repair

- Chentong reported dashboard Diagnostics still showed an unresolved error after the automatic repair system should have acted.
- Investigation found the automatic repair did run for `ERR-000006`: `MANAGER_AGENT_ERROR_AUTOCALL=true`, runner `openclaw_agent`, result `repaired_verified`.
- The repair changed planner behavior so Layer 10 is no longer treated as executable when reviewed event-feed artifacts are missing; it did not call Alpaca/GDELT/SEC because the original error request explicitly prohibited provider calls unless routed through a separate provider-dispatch gate.
- Dashboard Diagnostics had a false-open presentation bug: future-task historical failure receipts were still displayed as open errors. Fixed and pushed `trading-dashboard` commit `be4e630` (`Clarify dashboard task diagnostics`), verified with `npm run build` and `git diff --check`.
- Chentong then asked to fix the underlying coverage gap. Added 24 Layer 10 event-feed backfill task keys and executed 18 reviewed provider dispatches successfully:
  - Alpaca news: `6/6` monthly calls.
  - GDELT news: `6/6` monthly calls.
  - SEC company financials: `6/6` monthly calls.
  - Trading Economics was already present for `6/6` months.
- Layer 10 coverage gate passed after backfill: Alpaca `1282` rows, GDELT `1500` rows, SEC `5412` rows, Trading Economics `48` rows.
- Re-ran AAPL `2016-07..2016-12` fold through safe offline stages: Layer 10 data acquisition, feature generation, model generation, and subsequent ready stages succeeded; fold state became `50/50 succeeded`.
- Dashboard read models were refreshed. Current dashboard state after the repair: `current_system_status_summary=healthy`, `historical_task_progress_summary=complete`, `issue_refs=[]`, and Layer 10 `2016-07..2016-12` no longer failed.
- Safety boundary held: no broker/account/order/position mutation and no model activation. Remaining `realtime_signal_summary=degraded` is a separate realtime signal/execution-side status and was not part of this historical task Diagnostics repair.

## Pre-compaction flush - dashboard diagnostic references and replay fold labeling

- Chentong caught that dashboard Diagnostics `ERR-*` display references were being generated from the current visible-row index, so sorting/filtering/new rows could renumber errors. This was invalid because server error references must be permanent and monotonic from `ERR-000001`.
- Fixed and pushed `trading-dashboard` commit `52eb78d` (`Stabilize diagnostic error references`):
  - Dashboard Diagnostics now uses backend server error catalog `error_ref` values for `ERR-*`.
  - Non-catalog diagnostics no longer masquerade as `ERR-*`; they receive stable non-ERR display references.
  - Diagnostics defaults to unresolved items only.
  - Added filters for diagnostic type and current status; severity cards remain and summarize after those filters.
  - Dashboard docs/decision text now states that `ERR-*` must not be generated or renumbered by UI sorting/filtering.
- Verification for the Diagnostics fix passed: `npm run build`, `git diff --check`, and catalog continuity confirmed as `ERR-000001` through `ERR-000006`.
- Chentong then caught that dashboard model-group replay tasks were displaying `month=2021-01..2026-01`. The correct task period is the training fold such as `2016-01..2016-06`; the `2021-01..2026-01` range is the replay/test window in task details, not the task month.
- Fixed and pushed `trading-manager` commit `2720910` (`Use training fold for model group task period`):
  - `model_group.*` tasks now use `month=2016-01..2016-06`, `dataset_unit_kind=model_group_training_fold`, and `dataset_unit_months=6`.
  - Replay/test horizon is preserved under `detail.replay_window=2021-01..2026-01`.
  - Dashboard read models were refreshed; current `model_group.replay` now shows `month=2016-01..2016-06`, `replay=2021-01..2026-01`, `status=ready`, `state=current`.
- Verification for the model-group task-period fix passed: `tests.test_dashboard_read_models` 23 tests OK, `python3 -m compileall -q src tests` OK, `git diff --check` OK, and read-model refresh completed with no provider calls, broker execution, or model activation.

## Pre-compaction flush - evidence-backed dashboard task progress semantics

- Chentong corrected dashboard task-progress semantics: progress must come from task-specific evidence such as processed rows / required rows, elapsed / expected time, replay months, coverage, or worker-emitted progress checkpoints. It must not invent synthetic fractions like `1/1 task` or `1/10 model steps`.
- Implemented and pushed final task-progress correction:
  - `trading-manager` commit `ce537ff` (`Use evidence-backed task progress`).
  - `trading-dashboard` commit `5aa4a86` (`Document evidence-backed task progress`).
- Final behavior after the correction:
  - Ordinary tasks no longer auto-display fake progress bars.
  - Dashboard progress is shown only when backed by coverage reports, replay progress, active worker progress files, or elapsed/expected timing evidence.
  - Stage executor passes worker progress environment variables: `TRADING_MANAGER_TASK_PROGRESS_ROOT`, `TRADING_MANAGER_TASK_PROGRESS_PATH`, `TRADING_MANAGER_TASK_PROGRESS_WORKER_ID`, `TRADING_MANAGER_TASK_PROGRESS_TASK_UID`, and `TRADING_MANAGER_TASK_PROGRESS_STAGE_ID`.
  - Workers can write active progress files while running; the executor clears that worker's active progress record after task completion to prevent stale progress from leaking into the next task.
- Verification passed after the evidence-backed progress fix:
  - `trading-manager` full test suite: 336 tests OK.
  - Targeted dashboard/manager checks passed.
  - Read models refreshed successfully.
  - Current actual output at verification time: Model Generation progress was `null`; Model Group Replay progress was evidence-backed as `0/60 months`.

## Pre-compaction flush - live historical workflow status at 2026-05-22 07:41 EDT

- `trading-manager-historical-scheduler.service` was `active/running` with no restart indication.
- Dashboard refresh timer was active; the refresh service is oneshot and normally returns to inactive after each run.
- Latest receipts around 07:35-07:38 EDT showed historical workflow stages actively progressing:
  - Layer 3 `model_generation` completed.
  - Layer 3 `model_evaluation` completed.
  - Layer 4 `model_generation` completed.
  - Layer 4 `model_evaluation` completed.
- `runtime/task_progress` had no active progress file at that moment, so dashboard correctly had no line/time-style in-progress bar to display.
- Open issue to continue after compaction: dashboard/read-model summary still falls back to a stale-looking no-active-worker summary, e.g. "Historical workflow for 2026-04 is complete" with `current_tasks=null`, even while scheduler receipts show training-fold work just advanced. Fix the summary read model so that when no worker is active it still reports latest training fold, recently completed stage, and next candidate stage instead of an unrelated complete summary.

## Pre-compaction flush - compact fold labels and replay scheduler bridge

- Chentong asked to express fold task periods as compact labels like `2016-fold1` instead of raw ranges. Implemented and pushed:
  - `trading-manager` commit `686bc8a` (`compact fold labels`).
  - `trading-dashboard` commit `00e4a68` (`dashboard fold label sorting/docs`).
- Current public fold labels now use forms like `2016-fold1`; internal task UIDs still preserve raw ranges such as `2016-01..2016-06:...` for worker progress compatibility.
- Chentong reported `Model Evaluation` for `2016-fold1` was stuck at `0/60 months`. Investigation found dashboard/read-model logic had incorrectly treated idle `model_group.replay` readiness as `Now`; actual scheduler work was still advancing training stages, while replay had no executable dispatch bridge.
- Fixed idle replay presentation and pushed `trading-manager` commit `fb17f59` (`Do not mark idle replay as current`): `model_group.replay` only becomes `Now` after replay progress exists; otherwise it remains ready/future and does not hide current training work.
- Chentong clarified this meant the code framework was incomplete and asked to finish it. Implemented the missing replay execution bridge:
  - `trading-evaluation` commit `6e56f8d` (`Emit replay execution progress`) writes `replay_progress.jsonl` as replay months complete.
  - `trading-manager` commit `d3fe0df` (`Dispatch model group replay from scheduler`) lets scheduler dispatch `model_group.replay` after the training fold is complete, replay dataset is frozen, and replay progress is incomplete.
  - `trading-manager` commit `83af9e3` (`Use review units for model group promotion progress`) reports promotion review as a one-decision review unit instead of month progress.
- Verified by actually executing `2016-fold1` replay over `2021-01..2025-12`: all 60 replay months completed and 5,475 decision rows were generated.
- Dashboard state after bridge completion:
  - `Model Evaluation`: `succeeded`, `60/60 months`.
  - `Promotion Review`: `ready`, `0/1 review-decision`.
  - Main scheduler work continued on `2020-fold2 · Layer 5 · Model Generation`.
- Safety boundary held for replay execution: provider calls `0`, broker/account mutation `false`, model activation `false`.
- Verification passed before compaction:
  - Fold-label work: `trading-manager` 336 tests OK, `trading-dashboard` 12 tests OK, `npm run build` OK.
  - Idle replay presentation: `trading-manager` 337 tests OK, `trading-dashboard` 12 tests OK.
  - Replay bridge: `trading-manager` 339 tests OK, `trading-evaluation` 34 tests OK, `git diff --check` OK.
  - Scheduler and dashboard services were restarted and active after the relevant changes.
- Next correct action: run `Promotion Review` through the `promotion-evaluation-review` skill/evidence path; it must not be auto-treated as accepted just because replay completed.

## Pre-compaction flush - first-model bootstrap promotion and promote/activate boundary

- After replay bridge completion, ran `2016-fold1` model-group promotion review from the latest replay rows:
  - Replay decision rows: `5475`.
  - Settlement metrics: net return `8.88390602554663`, max drawdown `-0.8204388198497394`, AUROC `0.49929031270838303`, Brier `0.31549428624684916`.
  - Initial review artifact was `insufficient_evidence` / `review_required` because it expected anonymous incumbent comparison, shadow-readiness candidate config evidence, first-run/query-count evidence, and AUROC was below the `0.53` gate.
- Chentong corrected the first-model rule: the first model has no incumbent, so it must be directly promotable as the bootstrap baseline for future anonymous incumbent comparisons.
- Implemented and pushed `trading-evaluation` commit `610ced6` (`Allow first model bootstrap promotion`):
  - Added `--first-model-bootstrap` promotion review path.
  - Regenerated `2016-fold1` promotion review artifact with `first_model_bootstrap=true`.
  - Dashboard moved to `Promotion Review: completed / succeeded`; `Maintenance: current / ready`.
  - Artifact preserves risk facts: AUROC still `0.49929`, max drawdown still `-0.82044`, and future candidates must compare against this bootstrap baseline.
  - Safety boundary held: no active model config write, no broker/account/order/position mutation, and no model activation.
- Chentong clarified the core lifecycle distinction:
  - `promote` means the model is legal/valid/established by evaluation and can become a promoted baseline or shadow candidate.
  - `activate` means the promoted model is allowed for live use after execution-side market-hours shadow evidence and lifecycle review.
- Local rule lookup confirmed the intended boundary:
  - Promotion belongs to `trading-evaluation` and should produce promotion eligibility/readiness artifacts, not live activation.
  - Activation belongs to `trading-execution`: promoted models run market-hours shadow cycles, then `runtime_model_lifecycle_review` can select active/shadow roles, and active pointer writes require a separate execution gate.
  - Broker/order/account/position mutation remains beyond both promotion and model-activation review gates.
- Remaining follow-up: align older docs/implementation wording that still says every promotion requires incumbent/blinded comparison. Correct rule is:
  - First model: `first_model_bootstrap=true` can promote as bootstrap baseline but cannot activate without shadow.
  - Later models: must compare against the promoted baseline/incumbent using blinded comparison evidence before promotion.

## Pre-compaction flush - first-model readiness record and maintenance dashboard completion

- Completed the first-model bootstrap promotion/readiness boundary after Chentong clarified that bootstrap promotion is legal but activation is not:
  - `trading-evaluation` now carries `first_model_bootstrap` from `promotion_eligibility_decision` into `promotion_readiness_record`.
  - Docs were updated to state that the first promoted model becomes the frozen baseline for future anonymous incumbent comparisons, but cannot activate without execution-side market-hours shadow evidence.
  - `trading-manager` registry registered the bootstrap promotion/readiness policy while preserving existing stable promotion/readiness tokens.
- Regenerated actual artifacts:
  - `promotion_eligibility_decision.json`: `eligible`, `first_model_bootstrap=true`.
  - `promotion_readiness_record.json`: generated with `execution_shadow_scope=market_hours_shadow`.
  - Explicit safety flags remained false: `model_activation_performed=false`, `active_model_config_written=false`.
- Verification passed:
  - `trading-evaluation`: 37 tests OK, compileall OK, diff-check OK.
  - `trading-manager`: model promotion/registry tests 58 OK, compileall OK, registry snapshot OK.
  - Dashboard read-model refresh triggered successfully; runtime still showed `waiting_for_promoted_model`, which is correct until execution creates a shadow cycle.
- Pushed commits:
  - `trading-evaluation` `c3e222c`.
  - `trading-manager` `a8134a9`.
- Chentong then reported `2016-fold1 · Model Group · Maintenance` stuck at `Ready · 0/1`.
- Root cause: `promotion_readiness_record.json` existed in storage, but the `trading-manager` dashboard read model only read `promotion_review_runs`, not `promotion_readiness_runs`.
- Fixed dashboard read model so Maintenance completed from the readiness record:
  - Maintenance now shows `succeeded / completed`, `100% · 1/1 maintenance-step`.
  - Receipt attached: `/root/projects/trading-storage/storage/05_replay_datasets/promotion_replay_candidate_policy/promotion_readiness_runs/model_group_replay_20260522T123634Z/promotion_readiness_record.json`.
- Verification passed for the maintenance dashboard fix:
  - `tests.test_dashboard_read_models` OK.
  - Manager related 84 tests OK.
  - compileall OK.
  - registry snapshot OK.
  - `latest.json` confirmed refreshed.
- Pushed `trading-manager` commit `e9c5041`.

## Pre-compaction flush - dashboard month order for fold labels

- Chentong asked to adjust Month display ordering so `2016-fold1` appears after `2016-06`, and `2016-fold2` appears after `2016-12`.
- Implemented dashboard/read-model sort semantics:
  - Ordinary months sort by calendar month.
  - Fold labels sort by their training-window end month.
  - `2016-fold1` appears after `2016-06`.
  - `2016-fold2` appears after `2016-12`.
  - Fold-internal task order remains stable by task number/original sequence.
- Verified latest dashboard read model ordering:
  - `2016-06` sequence `51-60`.
  - `2016-fold1` sequence `61-74`.
  - `2016-12` sequence `125-134`.
  - `2016-fold2` sequence `135-144`.
- Verification passed:
  - Dashboard tests 27 OK.
  - Manager related 85 tests OK.
  - compileall OK.
  - registry snapshot OK.
- Pushed `trading-manager` commit `a4f0019`.

## Pre-compaction flush - Layer 9 scheduler stall and node-only progress display

- Chentong reported task `641` (`2020-07 · Layer 9 · Target AAPL · Data Acquisition`) had been stuck for a long time as the only active worker, with blocker text `upstream_layer_08_model_evaluation_complete`.
- Root cause:
  - Layer 9 gate review SQL was doing a broad `source_03_target_state DISTINCT ON` scan.
  - On the `2020-07..2020-12` fold, the query stalled for 70+ minutes.
  - The historical scheduler waited on the subprocess, so no new tasks were dispatched.
- Repair actions:
  - Stopped the stale scheduler process.
  - Cleared stale lock/progress state.
  - Narrowed Layer 9 gate SQL by first limiting to the current fold's Layer 8 rows, then looking up only the relevant target symbols.
  - Added a default 30-minute timeout to the stage executor so one worker subprocess cannot hang forever.
  - Reattached the manually completed `2020-07` Layer 9 gate receipt to workflow state.
- Dashboard/read-model fixes from the same incident:
  - `Ready + stage execution started` now displays as `running`.
  - `blockers` now shows only unsatisfied blockers, not static dependency declarations.
  - Node-only progress without a percentage now passes through instead of disappearing as `0% · Ready`.
- Verification passed:
  - 56 related tests OK.
  - compileall OK.
  - `git diff --check` OK.
- Pushed commits:
  - `trading-manager` `2a86201` (`Fix stalled Layer 9 scheduler task`).
  - `trading-manager` `8ed38a0` (`Expose node-only task progress`).
- Current workflow state after repair:
  - `2020-07..2020-12` Layer 9 data acquisition, feature generation, model generation, and model evaluation completed.
  - Scheduler continued to the next group and was running `2021-01..2021-06` `feature_03_target_state_vector`.
  - `trading-manager-historical-scheduler.service` was active.

## Pre-compaction flush - stage-boundary contract and fold task shape

- Chentong clarified strict definitions for task stages:
  - `data_acquisition` means downloading/snapshotting source evidence from external data sources or already reviewed source surfaces. It may write coverage, source refs, paths, and point-in-time metadata, but must not write model-informed values.
  - `feature_generation` means deterministic derivation from acquired/source data in the same scope. It must not depend on upstream model outputs, scores, decisions, or hidden runtime state.
  - If a table/task requires another model's output, it must be modeled as a separate model-dependent task/table with explicit blockers, not as ordinary source-stage feature generation.
- Implemented and pushed the boundary contract in `trading-manager` commit `9f36c2a` (`Define acquisition and feature stage boundaries`). Verification noted: `git diff --check` OK. Scheduler remained stopped at that point.
- Accepted fold workflow structure for a target such as `2016-fold1 · AAPL`:
  - Phase 1: Monthly Foundation, monthly Layer 1/2 `data_acquisition` and `feature_generation`.
  - Phase 2: Target/Fold Independent Data And Features, once per target/fold, only source-independent acquisition/features.
  - Phase 3: Fold Model Mainline, generate all Layer 1-10 models.
  - Phase 4: Sequential Replay.
  - Phase 5: Promotion Review.
  - Phase 6: Maintenance.
- Phase 2 accepted tasks:
  - `target_fold_setup` freezes target/fold identity, manifest, event-focus registry version, target lane lock, and artifact paths.
  - `layer_03_target_state_vector.data_acquisition` materializes target/source evidence from approved Layer 1/2 source surfaces, with no provider call.
  - `layer_03_target_state_vector.feature_generation` writes source-only target-state features, e.g. `trading_data.feature_03_target_state_vector`, and must not read model outputs.
  - `layer_04_event_failure_risk.data_acquisition` is one dashboard/scheduler task with internal substeps: acquire event index/list metadata, select relevant event refs using target/fold/event-focus rules, and download full source evidence only for selected events.
  - `layer_04_event_failure_risk.feature_generation` interprets selected event evidence and derives source-only event-risk features; it must not perform external downloads or read model outputs.
- Layer 4 event acquisition policy:
  - Do not download all event contents by default.
  - First acquire a bounded event index/list with metadata only: event id, timestamp, source, title/headline, snippet, entities/tickers/tags, family/category, content pointer, and availability time.
  - Filter/select candidate refs from the index using deterministic target/fold/event-focus rules.
  - Download only selected full evidence, then interpret and derive features from that acquired evidence.
  - First-step "all events list" is bounded by source, time window, and coarse domain, not the whole internet.
- Proposed/accepted Layer 4 data surfaces for the detailed task overview:
  - `trading_data.source_04_event_index`
  - `trading_data.source_04_event_candidate`
  - `trading_data.source_04_event_evidence`
  - `trading_data.feature_04_event_failure_risk`
- Chentong corrected model evaluation ordering:
  - Do not run `layer_01.model_evaluation`, then `layer_02.model_evaluation`, etc. between layer generations.
  - Generate all Layer 1-10 models first into one `model_group_candidate`.
  - Then run `model_group.replay`.
  - Then run unified `model_group.evaluation`, including layer-level contribution/problem analysis in the context of the full replay.
  - Reason: single-layer evaluation is incomplete without the full model group and replay path, especially for event, risk, action, and option-expression layers.
