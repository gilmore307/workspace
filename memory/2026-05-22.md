## Pre-compaction flush - Replay source acquisition and TE policy

- Replay source acquisition for `promotion_replay_candidate_policy` completed its external source bootstrap: Trading Economics calendar reached `60/60 complete`, GDELT news `60/60 complete`, and OKX crypto market data `180/180 complete`; missing source windows are now `0`.
- Final TE gaps were `2022-07`, `2024-06`, and `2025-09`; after browser/custom-date/cookie-state recovery and rerunning formal acquisition, they produced 16, 21, and 26 rows respectively.
- Successfully acquired replay artifacts are stored under `trading-storage`, mainly `storage/01_source_data/replay/...` and replay manifests/logs under `storage/05_replay_datasets/promotion_replay_candidate_policy/`; runtime storage artifacts remain uncommitted per storage policy.
- Alpaca replay source requests remain intentionally deferred (`180` items) because they are candidate-symbol dependent and should expand dynamically when replay/fold execution produces point-in-time candidate symbols.
- Chentong clarified TE historical acquisition is a one-time bootstrap; the TE subscription is canceled starting next month, and future TE use should rely on logged-out visible-page access for current/recent/future calendar data rather than authenticated historical backfill.
- Landed and pushed code/docs to reflect logged-out TE routing and completed replay source coverage:
  - `trading-data` commit `2161f2d`
  - `trading-evaluation` commits `d035cd4` and `6862e85`
  - `trading-manager` commit `2c48745`
- Verified after TE route changes: `trading-data` 9 tests plus compileall/diff-check; `trading-evaluation` 2 tests plus compileall/diff-check; `trading-manager` 57 tests plus registry snapshot, compileall, and diff-check.
- Current next step: freeze the completed replay dataset, then proceed into replay execution and fold settlement; do not reopen broad TE historical backfill unless a future replay window has a specific artifact gap.

## Replay freeze correction at 2026-05-22 04:12 EDT

- Continuing the Replay work surfaced a false-positive OKX coverage issue: the prepared acquisition plan used instrument-level OKX output roots, so 60 monthly acquisitions per instrument shared one `completion_receipt.json` path and the saved `crypto_bar.csv` artifact was overwritten by the latest month.
- Fixed and pushed `trading-evaluation` commit `39d64d0` (`Validate replay acquisition receipt paths`):
  - OKX Replay acquisition paths now use `instrument/month` granularity.
  - Replay freeze validation now rejects shared non-deferred acquisition receipt paths.
  - `docs/04_task.md` now marks the first candidate-policy Replay dataset as frozen and shifts the active task to running Replay/fold settlement.
- Rebuilt the candidate-policy Replay dataset manifest, reran public OKX monthly acquisition into corrected paths, and re-froze the dataset:
  - OKX fixed crypto coverage: `180/180 complete`.
  - GDELT: `60/60 complete`.
  - Trading Economics: `60/60 complete`.
  - Accepted deferred Alpaca candidate-dependent rows: `180`.
  - Missing source acquisitions: `0`.
  - Freeze receipt: `/root/projects/trading-storage/storage/05_replay_datasets/promotion_replay_candidate_policy/replay_freeze_receipt.json`.
- Verification passed: `trading-evaluation` 32 tests OK, compileall OK, diff-check OK; Replay runtime dry-run passed with no provider calls, broker calls, broker/account mutation, model training, or active config write.
- Next step: implement/run the real Replay execution that emits decision rows from the frozen dataset and execution-owned component graph; then run fold settlement metrics. Do not treat the dry-run receipt as the 60-month Replay result.

## Pre-compaction flush - workspace memory checkpoint

- Workspace memory changes from the Replay freeze correction were committed and pushed as `1f729cd`.

## Crypto fixed-sleeve Replay execution and settlement

- Added and pushed `trading-evaluation` commit `2bf2f63` (`Add replay execution runner`).
  - New `src/trading_evaluation/replay_execution.py` and `scripts/evaluation/run_replay_execution.py` run the frozen fixed crypto sleeve through `trading-execution` Replay components.
  - The runner reads frozen OKX daily bar artifacts from the Replay dataset manifest and emits settlement-ready `evaluation_replay_decision_row` JSONL.
  - Safety flags remain zero/false for provider calls, broker calls, broker/account mutation, model training, and active model config writes.
- Ran the real crypto fixed-sleeve Replay execution over the frozen `promotion_replay_candidate_policy` dataset:
  - Run id: `crypto_spot_replay_20260522_081953`.
  - Decision rows: `5475`.
  - Market dates: `1826`.
  - Targets: `BTC`, `ETH`, `SOL`.
  - Decision rows path: `/root/projects/trading-storage/storage/05_replay_datasets/promotion_replay_candidate_policy/replay_execution_runs/crypto_spot_replay_20260522_081953/decision_rows.jsonl`.
  - Receipt path: `/root/projects/trading-storage/storage/05_replay_datasets/promotion_replay_candidate_policy/replay_execution_runs/crypto_spot_replay_20260522_081953/replay_execution_receipt.json`.
- Ran fold settlement for that Replay execution:
  - Settlement id: `settlement_ad1cbf4f039eeb76`.
  - Settlement path: `/root/projects/trading-storage/storage/05_replay_datasets/promotion_replay_candidate_policy/fold_settlement_runs/crypto_spot_replay_20260522_081953/fold_settlement_run.json`.
  - Status: `review_required`.
  - Gate failure: `auroc_below_minimum`.
  - Metrics: net/excess return `8.88390602554663`, max drawdown `-0.8204388198497394`, hit rate `0.24127853881278538`, AUROC `0.49929031270838303`, Brier score `0.31549428624684916`.
- Interpretation: this crypto fixed-sleeve evidence is useful Replay/settlement evidence, but it is not promotion-ready. Positive return does not override the failed ranking/classification gate; do not promote from this result.
- Verification after implementation passed in `trading-evaluation`: 34 tests OK, compileall OK, replay contract validation OK, diff-check OK.

## Pre-compaction flush - seven-repo first-principles review/straightening

- Chentong requested a Codex CLI style repo-by-repo review using the prior review skill and first-principles cleanup rule: inspect carefully and straighten detours introduced by route changes.
- Completed and pushed the review/cleanup pass across seven repositories:
  - `trading-evaluation` commit `03397e2`.
  - `trading-execution` commit `38b6c48`.
  - `trading-data` commit `c27e6b5`.
  - `trading-manager` commit `f281db8`.
  - `trading-model` commit `0f74087`.
  - `trading-storage` commit `5f70364`.
  - `trading-dashboard` commit `fb5a3e0`.
- Key dashboard cleanup from the final pass: removed `_vN` read-model alias routing so HTTP/WebSocket surfaces use current direct contract routes only; added `execution_realtime_trading_runtime_status` to the dashboard current read-model allowlist to align with storage's public refresh collection.
- Final verification reported clean across all seven repos:
  - Repo-specific Python compile/tests/build/check gates passed.
  - `trading-dashboard` final gate passed: `compileall`, 12 unittests, `npm run build`, and `git diff --check`.
  - All seven repos had clean `git status --short`.
  - All commits were pushed to GitHub.
- Current engineering state: Replay/evaluation/execution/storage/dashboard code paths are straightened and green after the route-change review; remaining weakness is strategy evidence quality, not baseline engineering plumbing.
