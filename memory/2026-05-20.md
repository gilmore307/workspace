# 2026-05-20

## Benchmark Replay Boundary

- Chentong clarified that many benchmark preparation steps are one-time only: after benchmark data is downloaded/prepared, all benchmark tests must reuse the same frozen data snapshot.
- Chentong also clarified that benchmark evaluation should use the realtime trading/execution replay flow under a historical clock, not the model training flow.
- Updated and pushed `trading-evaluation` commit `376798d` to document the reusable frozen benchmark snapshot and realtime replay boundary.
- Updated and pushed `trading-manager` commit `44a2160` with registry migration `574_register_benchmark_reusable_snapshot_realtime_replay.sql`, current registry export, and registry tests for `BENCHMARK_REUSABLE_DATA_SNAPSHOT_POLICY` and `BENCHMARK_REALTIME_REPLAY_ROUTE_POLICY`.
- Verification passed: `trading-evaluation` 22 tests OK, compileall OK, diff-check OK; `trading-manager` registry migration apply/dry-run OK, 317 tests OK, compileall OK, diff-check OK.

## Layer 2 to Layer 3 Target Selection Boundary

- Chentong clarified that stock selection from Layer 2 to Layer 3 is part of the model, not a benchmark convenience. The model should choose stocks from Layer 2 sectors that are currently easier to trade and then rank/select the highest recent point-in-time dollar-volume/liquidity names.
- Benchmark boundaries should predefine evaluation windows, candidate-pool rules, data standards, N, and metrics, but should not preselect the final target ticker list for promotion benchmarks. Fixed target/window panels should be treated as diagnostics or stress panels, not the source of promotion benchmark targets.
- Current architecture review found a candidate-construction sub-boundary but no complete formal Layer 2 -> Layer 3 stock-selection layer. Layer 2 emits sector/basket handoff and explicitly leaves ETF holdings/downstream stock expansion outside Layer 2. Layer 3 has `anonymous_target_candidate_builder`, but it is input preparation/anonymization, not an independent selection model.
- Current manager materialization creates target candidates from Layer 2 universe/feed artifacts in a coarse one-symbol-one-candidate style; it does not dynamically select top-liquidity stocks from good Layer 2 sectors. Existing target-state features include liquidity/dollar-volume ranks, but those occur after candidate construction and do not replace a pre-state-vector selection boundary.
- Proposed architecture: keep the physical numbering stable and formally split Layer 3 internally into `target_candidate_selection` / `layer_03_target_selection` followed by `target_state_vector`. Layer 3 today is effectively Target State Vector / Target Context State generation, not final stock selection, alpha confidence, trading action, position sizing, or option-contract selection.

## Layer 1 Model Workflow Draft

- Began a layer-by-layer model/workflow review. Layer 1 is `MarketRegimeModel`; it generates `market_context_state` as the global market/cross-asset background for all downstream layers.
- Historical Layer 1 workflow should be: historical window -> broad-market/cross-asset ETF bars -> `source_01_market_regime` -> `feature_01_market_regime` -> `MarketRegimeModel` -> `model_01_market_regime` / `market_context_state` -> evaluation/promotion review.
- Layer 1 should use broad market, risk, rate, credit, liquidity, commodity, dollar, volatility, breadth, correlation, dispersion, and risk-appetite proxies. It should not use sector leadership as a core input, ETF holdings, concrete stock selection, option-contract outcomes, strategy returns, portfolio PnL, or future returns as production inputs.
- Layer 1 output should remain a set of explicit market-state scores, not one vague bullish/bearish regime label. Important families include direction, direction strength, trend quality, stability, stress, transition risk, breadth participation, correlation/crowding, dispersion opportunity, liquidity pressure/support, coverage, and data quality.
- In live trading, Layer 1 should run as a continually updated market-background service using realtime/quasi-realtime versions of the same canonical sources, feeding downstream Layer 2 sector context, Layer 3 target context, Layer 5 alpha confidence, Layer 7/8 action/option expression, and Layer 9 event-risk interpretation.

## Ten-Layer Stack With Dynamic Risk Policy

- Chentong accepted inserting a dynamic risk-policy model as new conceptual Layer 6, shifting downstream layers to a 10-layer stack: MarketRegime, SectorContext, TargetStateVector, EventFailureRisk, AlphaConfidence, DynamicRiskPolicy, PositionProjection, UnderlyingAction, TradingGuidance/OptionExpression, and EventRiskGovernor.
- Layer 6 should learn dynamic premium/risk-budget policy mainly from Layer 1 global market regime, systemic/broad event risk, Layer 5 alpha quality, and portfolio/account replay state. Sector/target-specific evidence can cap or skip the current target, but must not distort the global risk budget.
- Hard order boundaries are outside the model stack and belong to execution/order gates. Layer 6 emits `dynamic_risk_policy_state`, not broker permission or an execution hard-limit override.
- Promotion is now one pinned Layer 1-10 bundle: after all ten layers complete fold evaluation, the bundle is accepted or rejected as a whole. Single-layer results remain diagnostic only.
- Implemented and pushed the decision across repositories: `trading-manager` `ca71c5b`, `trading-model` `00b58b3`, and `trading-evaluation` `15e2570`.

## Ten-Layer Physical Cutover

- Chentong asked to fully cut active implementation to the current 10-layer structure rather than keeping old physical aliases.
- Completed and pushed active physical renumbering across the data/model/manager repos:
  - `trading-data` `a32b99c`: Layer 9 option feature becomes `feature_09_option_expression`; Layer 10 event source/feature become `source_10_event_risk_governor` and `feature_10_event_risk_governor`; docs/catalog cover Layers 1-10.
  - `trading-model` `1516333`: added physical `model_06_dynamic_risk_policy`; moved position/underlying/option/event packages and scripts to `model_07` through `model_10`; updated state-vector/output field prefixes.
  - `trading-manager` `cc6f373`: workflow, gate scripts, realtime handoff, registry rows, and tests now route directly to the current 10-layer physical names; registry migrations `590` and `591` align active rows and score tokens.
- Verification passed after cutover: `trading-model` 222 tests OK; `trading-manager` 320 tests OK plus registry snapshot OK; `trading-data` compileall and all CI test shards OK; diff-check OK for all three touched repos.

## Old Model Output Cleanup

- Chentong clarified that during active development old model outputs and feature artifacts can be cleared, while downloaded/source data must be preserved.
- Stopped `trading-manager-historical-scheduler.service` before cleanup so old runtime output would not regenerate during deletion.
- Cleared database-derived tables by truncating `trading_model.model_%` and `trading_data.feature_%` tables; post-check found all checked `model_*/feature_*` tables empty.
- Permanently deleted old disk-derived runtime artifacts: `trading-model/storage/runtime/model_*` and manager layer/stage/model-training scheduler runtime state. Preserved `trading-data/storage` source/download artifacts and manager provider/source acquisition artifacts.
- Post-cleanup sizes: `trading-model/storage` 8K, `trading-manager/storage` 12M, `trading-data/storage` 1.2G. Scheduler service remained inactive after cleanup.

## Component Storage Boundary Cutover

- Chentong clarified the original project rule: component repos must not own `storage/`; storage files belong in `trading-storage` and should be lifecycle-managed there.
- Moved existing component storage trees into `trading-storage/storage/{data,manager,model,execution}` and permanently removed `/root/projects/trading-data/storage`, `/root/projects/trading-manager/storage`, `/root/projects/trading-model/storage`, and `/root/projects/trading-execution/storage`.
- Updated defaults across data, manager, model, execution, evaluation, and storage dashboard/read-model code so new runtime artifacts resolve to the storage-owned component roots.
- Added manager registry migration `592_route_component_storage_to_trading_storage.sql` and regenerated `scripts/registry/current.csv`; current registry rows no longer point to component-local storage paths.
- Verification passed before commit: component-local storage dirs remained absent; storage-owned sizes were data 1.2G, manager 12M, model 8K, execution 8K; unit tests passed for data, manager, model, execution, evaluation, and storage; compileall and diff-check passed across touched repos.

## Semantic Storage Layout Cutover

- Chentong asked to review storage file/folder names and make the layout more intuitive, systematic, and clean.
- Renamed `trading-storage/storage/{data,manager,model,execution,benchmark,dashboard}` to semantic roots: `source_data`, `control_plane`, `model_artifacts`, `execution_artifacts`, `benchmark_datasets`, and `dashboard_cache`.
- Moved storage-owned lifecycle outputs under `storage/lifecycle/`, including `maintenance`, `runtime`, `tmp`, future `artifact_index`, `protected_set`, `plans`, `execution`, `quarantine`, `cache`, `staging`, `logs`, `runs`, and `outputs`.
- Split downloaded/source evidence inside `source_data`: provider monthly/realtime/runtime outputs remain there; event evidence moved to `source_data/event_evidence`; target-candidate source outputs moved to `source_data/source_outputs/target_candidate_holdings`.
- Added manager registry migrations `593`-`595` to update current storage path terms, dashboard cache terms, benchmark dataset paths, and lifecycle archive/cache references.
- Verification passed: stale active path scan found no old storage roots; data 67 tests, manager 320 tests, model 222 tests, execution 53 tests, evaluation 15 tests, and storage 90 tests all passed; storage dashboard refresh smoke succeeded and `trading-storage-dashboard-read-model-refresh.timer` was restarted.

## Ordered Storage Layout Cutover

- Chentong asked to prefix `trading-storage/storage` root folders by workflow order.
- Renamed active storage roots to: `01_source_data`, `02_control_plane`, `03_model_artifacts`, `04_execution_artifacts`, `05_benchmark_datasets`, `06_dashboard_cache`, and `90_lifecycle`; routed generic storage artifact payloads to `02_control_plane/artifacts` so no future helper recreates an unnumbered `storage/artifacts` root.
- Updated data, manager, model, execution, evaluation, and storage code/docs/tests to use the numbered storage roots; added manager registry migrations `596_add_ordered_storage_root_prefixes.sql`, `597_finish_ordered_storage_root_prefixes.sql`, and `598_route_storage_artifacts_to_ordered_control_plane.sql`.
- Verification passed: active stale-path scan found no unnumbered storage root references outside immutable migration history; registry dry-run had no pending migrations and snapshot OK; data 67 tests, manager 320 tests, model 222 tests, execution 53 tests, evaluation 15 tests, and storage 90 tests all passed; storage dashboard refresh wrote to `06_dashboard_cache`; `trading-storage-dashboard-read-model-refresh.timer` was active/enabled after restart.
- Pushed commits: `trading-data` `f883c4d`, `trading-manager` `0923d72`, `trading-model` `a415075`, `trading-execution` `f47edc1`, `trading-evaluation` `d8cf36b`, and `trading-storage` `6cd4a1e`.

## Storage Lifecycle Inventory Connection

- Chentong asked to formally connect the file lifecycle management system to the numbered storage layout.
- Updated `trading-storage` so scheduled maintenance emits a `storage_root_inventory_summary` and per-root inventory for all numbered roots: `01_source_data`, `02_control_plane`, `03_model_artifacts`, `04_execution_artifacts`, `05_benchmark_datasets`, `06_dashboard_cache`, and `90_lifecycle`.
- Expanded artifact-index default roots to include `02_control_plane` and `04_execution_artifacts`; local retention now reports durable numbered roots instead of only `02_control_plane/artifacts`.
- Fixed scheduled fold-completion detection to require the current ten-layer model-worker shape rather than the old nine-layer shape.
- Added manager registry migration `599_register_ordered_storage_lifecycle_inventory.sql` to register `storage_root_inventory_summary` and update scheduled-maintenance/artifact-index registry text.

## Storage Lifecycle Policy: Source, Model, Execution, Benchmark Roots

- Chentong finalized `01_source_data` lifecycle policy: reusable foundation data such as Layer 1/2, monthly/source foundation, PIT/provider-window-limited/paid-window/expensive-to-reproduce source data is not a deletion candidate; one-time fold/target source data must live under `storage/01_source_data/fold_scoped/<fold_id>/` and can become a cleanup candidate only after that fold's Layer 1-10 work is complete.
- Implemented and pushed the source-data lifecycle boundary: `trading-storage` `a944526`, `trading-manager` `a8fe475`. Added concepts including `storage_fold_source_cleanup_candidate`, `fold_complete_delete_allowed`, artifact metadata fields `storage_retention_class` and `storage_reproducibility_class`, and maintenance logic that reports fold-scoped source cleanup candidates only from completed manager fold state.
- Chentong clarified that Layer 1/2 intermediate artifacts, runtime, scratch, staging, cache, stdout/stderr, and logs can be rolling-cleaned after run/fold close, while Layer 1/2 final foundation data remains protected and should only be compressed or archived. Implemented in `trading-storage` artifact indexing/retention behavior and pushed commit `a65e86c`.
- Chentong accepted `03_model_artifacts` policy: promoted model bodies, old promoted model bodies, promotion decisions, activation/deactivation records, evaluation summaries, feature/schema refs, dataset/split manifests, lineage refs, and rollback evidence are permanent; unpromoted model-run intermediates, scratch feature files, failed-run temp files, duplicate dry-run payloads, logs, and non-unique diagnostics can be rolling-cleaned once run/fold closure, compact summary, receipt/manifest, lineage preservation, protected-set check, quarantine/recheck, and deletion receipt are satisfied.
- Chentong accepted `04_execution_artifacts` policy: active/shadow lifecycle evidence, live/shadow performance evidence, broker/order/account mutation receipts, order construction receipts, safety invariant receipts, activation/deactivation evidence, active model config write evidence, and evidence justifying active/shadow/eliminate decisions are protected; high-frequency observations, repeated provider snapshots, temporary capture rows, stale runtime logs, failed-smoke scratch, duplicate debug outputs, and similar runtime/debug/cache can be compressed or rolling-cleaned after summaries/safety evidence are preserved and no open incident, unresolved alert, lifecycle-review reference, or protected-set hit exists.
- Chentong refined `05_benchmark_datasets` policy: reusable benchmark inputs such as Layer 1, Layer 2, event-layer news, and reusable event inputs should be downloaded once and continuously reused; every model pipeline benchmark result summary/scorecard must be permanently preserved; model-specific one-off downloaded files such as point-in-time option snapshots, temporary raw snapshots, and other non-reusable benchmark downloads should be cleaned after benchmark completion to prevent storage growth.
- Implemented and pushed the benchmark dataset lifecycle refinement: `trading-storage` `9e76618`, `trading-manager` `0097ee1`. Validation passed for `trading-storage` tests/compileall/diff-check and `trading-manager` registry dry-run/snapshot/diff-check.
- Across these lifecycle policies, deletion remains gated by artifact index, protected-set clearance, lifecycle plan, quarantine/recheck, deletion receipt, and reviewed destructive executor; no direct raw deletion path was accepted.

## Storage Lifecycle Policy: Dashboard Cache and Lifecycle Roots

- Chentong accepted `06_dashboard_cache` as a read-model cache boundary, not an audit or evidence source. `latest`, schemas, and indexes are protected/current cache structure; dashboard snapshots are rolling cache and should retain only the most recent few per read-model contract.
- Implemented `06_dashboard_cache/read_models/*/snapshots/*` retention as "keep latest 10 snapshots per contract"; older snapshots become delete candidates, while `latest.json`, schemas, and indexes are not pruned by snapshot cleanup. True deletion still requires `--apply` and reviewed approval. Pushed `trading-storage` `612f5e9` and `trading-manager` `8fb8d5c`; validation passed.
- Chentong accepted `90_lifecycle` as lifecycle-system workspace with a strict split: evidence/ledger artifacts are permanent or long-term, while runtime context can roll off. Protected evidence includes receipts, tombstones, executed/reviewed plans, protected sets, artifact indexes, and quarantine/recheck evidence.
- Clarified `90_lifecycle/runs` and `outputs`: they are lifecycle runner work directories and debug/output context, not business data or the canonical ledger. They may be rolling-cleaned after roughly 30 days, but if they contain formal lifecycle evidence such as receipts, manifests, tombstones, protected sets, plans, artifact indexes, or quarantine rechecks, those files must be retained and extracted to canonical evidence directories before cleanup.
- Implemented the `90_lifecycle/runs`, `outputs`, and `staging` policy so formal lifecycle evidence is marked `retain` and ordinary run/debug/temp context can be cleanup candidate after the retention window. Pushed `trading-storage` `3470aa2` and `trading-manager` `607e20f`; validation passed.
- All seven numbered storage roots now have accepted lifecycle rules connected through policy/docs/tests/maintenance helpers and manager registry: `01_source_data`, `02_control_plane`, `03_model_artifacts`, `04_execution_artifacts`, `05_benchmark_datasets`, `06_dashboard_cache`, and `90_lifecycle`.

## Model Stack and Benchmark Dataset Continuity

- Model architecture review has reached and closed through Layer 10. Layer 9 is `TradingGuidanceModel / OptionExpressionModel`, producing offline trading/option-expression guidance without broker/account mutation. Layer 10 is `EventRiskGovernor / EventIntelligenceOverlay`, governing residual event risk, blocks, caps, review posture, and risk explanation without standalone event alpha or buy/sell output.
- Recent event-layer conclusion remains: CPI can be macro risk/control context, scheduled earnings/guidance shells can be direction-neutral path-risk/volatility context, and the current option-derivatives-abnormality definition is saturated and not acceptable as a clean alpha/control signal. The event layer remains Layer 10 governance, not an independent event-alpha model.
- Next model work should return to the promote-first route at Layer 1 `MarketRegimeModel`: fix promotion gate/evidence, produce Layer 1 substrate diagnostics, baseline/stability/leakage/calibration evidence, then build toward the pinned Layer 1-10 bundle.
- Benchmark dataset status is not complete. Current candidate is `/root/projects/trading-storage/storage/05_benchmark_datasets/primary_benchmark_candidate_20260519`; manifest status is `prepared_one_shot_acquisition_bundle`, `freeze_status: not_frozen`, `benchmark_freeze_performed: false`.
- Benchmark candidate coverage at review time: 1843 required acquisitions, 636 available, 1207 missing; 196 coverage rows with 68 complete and 128 incomplete. Complete feeds include `alpaca_bars`, `alpaca_news`, and `okx_crypto_market_data`; missing items remain for `alpaca_liquidity` (312), `gdelt_news` (312), `trading_economics_calendar_web` (312), and `sec_company_financials` (271).
- Correct posture: benchmark dataset design and preparation bundle exist, but the reusable frozen benchmark dataset is not done. Next steps are to fill or explicitly accept stress exceptions for missing feeds, regenerate coverage summary, produce frozen snapshot manifest, permanently preserve benchmark summaries/scorecards, and mark model-specific one-off benchmark downloads for cleanup after benchmark completion.

## Promotion ML Diagnostics Policy

- Chentong asked to add machine-learning validity diagnostics such as PCA, PCoA, AUROC, AUPRC, calibration, IC/RankIC, confidence-bucket EV, fold stability, and related metrics into the promotion process.
- Accepted rule: these diagnostics are required promotion evidence, not automatic promotion gates.
- Final promotion disposition must be made by an agent review over benchmark results, ML diagnostics, stability, leakage risk, market/regime coverage, data scarcity, and trading-risk evidence.
- Allowed dispositions include `promote`, `shadow`, `hold`, `reject`, and `needs_more_evidence`.
- If ML diagnostics and trading performance conflict, the review agent must explain the conflict instead of mechanically passing or failing the model.

## Manager Model-Training Target Rotation

- Chentong asked manager to autonomously advance model training to the next target once the current target has reached the latest completed data month, so a new target can start Layer 3+ training from `2016-01`.
- Implemented and pushed `trading-manager` commit `9aa66c3` (`Add model worker target rotation`).
- Layer 3+ model worker fold state is now target-scoped as `model_training_fold_state_<target>_<start_month>_<end_month>.json`.
- If `--target-symbol` is supplied, the worker continues the fixed target. If omitted, the daemon reads `/root/projects/trading-storage/storage/02_control_plane/runtime/model_training_target_queue.json`.
- When the current target is complete through the latest completed month such as `2026-04`, manager skips it, selects the next queued target, and begins the Layer 3+ fold chain from `2016-01`.
- Boundary recorded: target queue routing is a training scheduler route only; it is not promotion benchmark evidence and does not replace Layer 3 candidate-policy replay.
- Verification passed before push: `trading-manager` 322 tests OK, registry snapshot OK, compileall OK, and diff-check OK.

## Execution Runtime and TE Refresh Readiness

- Chentong asked to complete execution-side realtime trading readiness before any promoted model exists, keep Trading Economics data refresh running, and finish related obvious setup.
- Implemented and pushed `trading-execution` commit `a82a266`: realtime trading runtime readiness now reports `waiting_for_promoted_model` when no active promoted model pointer exists; once a pointer exists, runtime can proceed into model activation and order-intent gating. Broker submit and account mutation remain closed.
- Enabled systemd timer `trading-execution-realtime-runtime-check.timer` to refresh execution runtime status every minute.
- Implemented and pushed `trading-data` commit `b5ec034`: Trading Economics recent calendar refresh writes to `storage/01_source_data/realtime/trading_economics_calendar_web`; TE historical data already existed in storage through `2026-05` with 125 completion receipts; live refresh succeeded with 281 rows.
- Enabled systemd timer `trading-data-te-calendar-refresh.timer` to refresh TE recent calendar hourly.
- Implemented and pushed `trading-manager` commit `904c28a` to register execution runtime and TE refresh registry surfaces.
- Verification passed: `trading-execution` 56 tests OK, compileall OK, diff-check OK; `trading-data` 69 tests OK, compileall OK, diff-check OK; `trading-manager` 322 tests OK, registry snapshot OK, no pending migrations.
- Remaining explicit execution TODO: true broker submit adapter should stay closed until a promoted model exists and paper/live execution gate plus full broker receipt contract are ready.

## Execution Runtime WS Route

- After compaction, Chentong clarified that the execution realtime runtime status should use the WebSocket/read-model route rather than a polling timer.
- Reworked the execution runtime status flow so `trading-execution-realtime-runtime-check.timer` is disabled/inactive, and a storage path trigger is enabled/active to refresh the dashboard read model when the status file changes.
- Dashboard consumers should read the latest execution runtime state through the existing WS route: `/ws/read-models/execution_realtime_trading_runtime_status/latest`.
- Current runtime status after the change was `waiting_for_promoted_model`.
- Safety boundary remained unchanged: zero broker calls, zero account mutation, and zero model activation while no promoted active model pointer exists.
- Verification passed: `trading-execution` 56 tests, `trading-storage` 109 tests, `trading-manager` 322 tests plus registry snapshot OK; all three worktrees were clean after push.
- Pushed commits: `trading-execution` `89ebcb1`, `trading-storage` `d4d9034`, and `trading-manager` `89cfc3b`.

## Remaining To-Do Cleanup Push

- Chentong asked to clear the remaining major to-do lines in one pass after the scheduler/benchmark/promotion/model/execution/storage lifecycle status review.
- Implemented and pushed `trading-manager` commit `ea79704`: scheduler no longer defaults to pinned `AAPL`; target queue is `AAPL, BTC, ETH, SOL, AAOI`; Layer 3+ model worker selects the next trainable target/fold from the queue; registry now includes settlement metric script and execution monitor service surfaces.
- Manager service was active after the push with status `observe_service_status_and_decision_log`; no open operational items were reported at closeout.
- Implemented and pushed `trading-evaluation` commit `63f1f5d`: fold settlement metric generation now includes AUROC, Brier, PCA top-2 variance, and PCoA-style pairwise distance; outputs mark `agent_review_required=true`, so metrics feed promotion review but do not mechanically promote.
- Implemented and pushed `trading-execution` commit `e7c27af`: added realtime monitor loop systemd service, enabled it on a 60-second loop, kept default mode plan-only, and corrected plan-only status to `planned`; no provider call, model activation, order construction, broker submit, or account mutation is allowed by default.
- In `trading-storage`, dashboard snapshot rolling cleanup was executed: 14,456 old snapshots deleted, about 13.2GB freed, latest 10 snapshots per contract retained, and `latest/schema/index`, Layer 1/2, and SQL data left untouched.
- Verification passed: `trading-manager` 324 tests plus registry snapshot/no pending migrations; `trading-evaluation` 18 tests; `trading-execution` 57 tests plus systemd verify; `trading-storage` 109 tests; all four worktrees were clean at closeout.
- Remaining runtime truth: benchmark dataset acquisition is prepared but not frozen. The bundle exists, but the 360 feed acquisitions still need to run or receive explicit accepted exceptions before the benchmark can be marked frozen. Manager/execution services should continue under safety gates rather than pretending model training or frozen benchmark completion already happened.

## Post-Compaction Status And Codex Review

- After compaction, Chentong asked what remained unresolved. Current state was clarified as runtime/data-state work rather than uncommitted code: benchmark candidate had an acquisition bundle but was still `not_frozen` with 360 missing feed acquisitions; no model had been promoted; manager still needed the 2016-01 through 2016-06 Layer 1/2 foundation folds before Layer 3+ could truly train; execution realtime monitor remained safe plan-only with `execute_live_observe=false`; Trading Economics refresh timer was active.
- Chentong asked to use Codex CLI as an independent judge after the stack moved from 9 layers to 10 layers. Initial `codex login status` showed `Not logged in`, and `codex exec` returned 401 missing authentication.
- Re-authenticated Codex CLI successfully through ChatGPT login; smoke test reported `CODEX_CLI_READY`.
- Ran Codex CLI review using the project-development review posture for a broad multi-repo inspection, not only the 10-layer migration. Report path: `/tmp/codex_full_project_review.txt`.
- Review found P1 issues to fix next: `trading-execution` realtime input coverage still uses the old layer numbering and misses `layer_06_dynamic_risk_policy`; `trading-dashboard` SQL/data table allowlist still exposes old model table names and misses current Layer 6/10 names; `trading-manager/scripts/registry/current.csv` still contains stale option-expression/event-risk-governor numbering/paths and needs a migration plus snapshot regeneration; `trading-evaluation/src/trading_evaluation/promotion.py` promotion readiness is too weak because it accepts string/eligible evidence without enforcing frozen benchmark, full Layer 1-10 fold evidence, guardrail pass, incumbent comparison, and agent review evidence.
- Review also found P2 cleanup: active `trading-model` docs still mention the nine-layer stack and old layer numbers; `trading-execution` tests protect the old 9-layer matrix; `trading-dashboard` docs mention nine-layer subtabs; `trading-storage` tests still fixture old `model_08_option_expression` and `model_09_event_risk_governor` paths.
- Recommended next action recorded for continuation: fix P1 first across execution, dashboard, manager registry, and evaluation promotion gate; then sweep P2 docs/tests and rerun full tests plus registry snapshots.
