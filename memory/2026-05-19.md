# 2026-05-19

## Model output table quality audit completed
- Chentong paused Dashboard/trading-execution work and asked to finish the remaining non-dashboard items in one pass.
- Implemented a read-only all-layer model-output table audit in `trading-model`: `scripts/models/audit_model_output_tables.py` over all nine primary `trading_model.model_XX_*` surfaces plus `_explainability` and `_diagnostics`.
- The audit classifies all-null/sparse columns as optional missing evidence, upstream data gaps, support/generator defects, or reviewed cleanup candidates; it emits review-only cleanup SQL and performs no DB mutation.
- Hardened `model_output_support.write_model_output_with_support` so empty support payloads no longer create empty diagnostics/explainability rows.
- Registered the callable audit in `trading-manager` migration `534_register_model_output_table_quality_audit.sql`.
- Verification passed: `trading-model` 217 tests OK; `trading-manager` 314 tests OK; compileall/diff-check OK; registry dry-run no pending migrations. Pushed `trading-model` `5776a85` and `trading-manager` `a56c616`.

## Model output quality gate completed
Continued the non-dashboard/non-execution cleanup by adding a post-generation `model_output_quality_gate_v1` in `trading-model` and registering it in `trading-manager`. The gate runs the read-only all-layer output audit, exits non-zero on primary output defects, and keeps known long-history/data-accumulation gaps plus optional selection/evidence gaps as classified warnings/info instead of unknown errors.

Regenerated Layer 1 and Layer 2 model SQL outputs from existing feature tables to confirm behavior. Current gate result is `passed` with 0 blockers; remaining gaps are classified as data accumulation, optional selection/evidence, sparse coverage, or support explainability/diagnostic warnings. Verification passed: `trading-model` 222 tests OK; `trading-manager` 314 tests OK; registry dry-run no pending migrations; compileall/diff-check OK. Pushed `trading-model` `2a21625` and `trading-manager` `f00e418`.

## Server error catalog moved to SQL control plane
- Chentong clarified that `failure_register` and `server_error_catalog.jsonl` should not be split; owner-facing server error catalog facts belong in SQL.
- Implemented `trading_manager.server_error_catalog` in `trading-manager` migrations `538_create_sql_server_error_catalog.sql` and `539_add_server_error_catalog_entry_number_guard.sql` with entry/occurrence rows, ERR numbering checks, dedup metadata, request/diagnosis storage refs, indexes by error ref/fingerprint/component, and a unique SQL guard for non-duplicate entry error numbers.
- Updated `agent_error_handler` so SQL is the default catalog storage; JSONL remains only as a legacy/test compatibility path for old runtime files. `list_agent_errors.py` now reads SQL by default, and dashboard read models can consume SQL-backed agent error rows when a database URL is provided.
- Regenerated `scripts/registry/current.csv` and registered `MANAGER_AGENT_ERROR_SQL_CATALOG_POLICY`.
- Verification passed: `trading-manager` 316 tests OK, compileall OK, registry dry-run no pending migrations, registry snapshot check OK, diff-check OK.

## Legacy server error catalog file migrated and removed
- Chentong required old `server_error_catalog.jsonl` files to be migrated into SQL and deleted afterward.
- Found one runtime catalog file at `trading-manager/storage/runtime/agent_error_handling/server_error_catalog.jsonl` with 4 rows: `ERR-000001` through `ERR-000004`.
- Removed 4 accidental `/tmp` test rows from `trading_manager.server_error_catalog`, imported the 4 runtime rows into SQL, verified no imported request ids were missing, then initially mishandled deletion by moving the old JSONL file to trash.
- Patched `stage_executor` tests to force `MANAGER_AGENT_ERROR_CATALOG_STORAGE=jsonl`, preventing future test runs from writing temp catalog rows to the real SQL database while production default remains SQL.
- Verification passed: targeted stage/error tests 21 OK; full `trading-manager` 316 tests OK; compileall OK; registry dry-run no pending migrations; registry snapshot check OK; diff-check OK.

## Pre-compaction continuity: server error SQL migration commits
- The SQL server error catalog change was committed and pushed in `trading-manager` as `1a339f6` (`Move server error catalog to SQL`).
- The legacy runtime JSONL migration/removal and test-isolation fix was committed and pushed in `trading-manager` as `37a9e07` (`Isolate agent error catalog tests`).
- Durable rule accepted from Chentong for this area: owner-facing error catalog/register facts should not be split between SQL and JSONL-style files; old `server_error_catalog.jsonl`-class catalog files should be migrated into the SQL control plane and removed after successful import, while referenced evidence payloads may remain as storage artifacts.
- Correction after Chentong objected that trash is not deletion: permanently deleted `/root/.local/share/Trash/files/server_error_catalog.jsonl` and matching `.trashinfo`; verified no `server_error_catalog.jsonl` remains under `/root/projects` or `/root/.local/share/Trash`; recorded the direct-delete preference in `USER.md`, `AGENTS.md`, and `memory/codex-oversight.md`.

## Fold cleanup logical backup gate implemented
- Chentong chose logical PostgreSQL backup over git-tracking the PostgreSQL data directory and clarified cleanup granularity: backup and cleanup happen once after a whole fold has all models and all tasks completed, not once per model.
- Implemented `trading-manager` fold cleanup plan helper `src/trading_manager_tasks/fold_cleanup.py` and CLI `scripts/tasks/plan_fold_cleanup.py`.
- The plan emits `manager_fold_cleanup_plan` with `cleanup_granularity=fold_all_models_all_tasks_once`, blocks until all 9 layers and all model-worker stage types are complete, and emits a required `manager_fold_sql_logical_backup_plan` using `pg_dump -Fc` plus `pg_dumpall --globals-only`.
- The helper is plan-only: no provider calls, no storage lifecycle mutation, no database mutation, no source deletion, no model activation, and no broker/account mutation.
- Registered migration `540_register_fold_cleanup_logical_backup_gate.sql`, regenerated `scripts/registry/current.csv`, updated scheduler runtime docs and decision D016, and added tests.

## Storage maintenance service boundary implemented
- Chentong clarified backup and deletion actions should live in one storage-owned system service and include scheduled log cleanup.
- Confirmed `trading-storage` previously had a deployed dashboard read-model refresh service/timer and older local-retention templates under `main/templates/maintenance/`, but no formal `deploy/systemd` storage maintenance service.
- Implemented `trading-storage` scheduled maintenance runner `src/trading_storage/storage_maintenance.py` and CLI `scripts/lifecycle/run_storage_maintenance.py`, plus `deploy/systemd/trading-storage-maintenance.service`, `.timer`, and env example.
- Current maintenance phase wraps local retention: tmp/cache delete after TTL, logs archive-then-delete, runs/outputs/staging archive-then-delete, local archives prune. The runner writes `storage_scheduled_maintenance_summary` and explicitly marks fold SQL backup as `not_configured` until a reviewed storage executor consumes manager fold-backup plans.
- Registered the service and backup/delete boundary in `trading-manager` migration `541_register_storage_maintenance_service.sql`.
- Verification passed: `trading-storage` 89 tests OK, compileall OK, systemd-analyze verify OK, diff-check OK; `trading-manager` 320 tests OK, compileall OK, registry snapshot/dry-run OK, diff-check OK.
- Pushed commits: `trading-storage` `15e6ee5` and `trading-manager` `579575f`.

## Fold backup planning moved fully to storage monitoring
- Chentong corrected the boundary again: storage does not need manager to propose backup/cleanup plans; storage only needs to monitor fold completion state and start once a fold is complete.
- Updated `trading-storage` maintenance runner to scan `trading-manager/storage/runtime/model_training_fold_state_*.json` when `--manager-root` is provided. Completed folds produce `storage_fold_sql_backup_candidate` entries in `storage_scheduled_maintenance_summary` with logical `pg_dump -Fc` command templates and checksum/restore-smoke requirements.
- Updated `trading-storage-maintenance.service` to pass `TRADING_MANAGER_ROOT=/root/projects/trading-manager`; CLI smoke against the live manager runtime detected `fold_2016-01_2016-06` as `ready_for_storage_backup`.
- Removed the active `trading-manager` fold cleanup/backup plan helper, CLI, tests, docs command, and active registry rows. Manager now owns fold progress state only; storage owns backup/archive/delete planning and execution.
- Registered manager migration `542_route_fold_cleanup_planning_to_storage.sql`.
- Verification passed: `trading-storage` 90 tests OK, compileall OK, systemd-analyze verify OK, diff-check OK; `trading-manager` 317 tests OK, compileall OK, registry snapshot/dry-run OK, diff-check OK.
- Pushed commits: `trading-storage` `ecb9916`; `trading-manager` `124dee8`.

## Fold backup/delete boundary clarified: storage reads, manager does not signal
- Chentong corrected the wording: manager should not actively emit backup/cleanup signals, requests, or plans; storage should actively read manager fold runtime state.
- Updated `trading-manager` decision D016 and registry migration `543_clarify_storage_reads_fold_state.sql`: `STORAGE_MAINTENANCE_BACKUP_DELETE_BOUNDARY_POLICY` now uses `storage_reads_manager_fold_state` and `no_manager_signal_or_request`.
- Updated `trading-storage` README/task/decision docs and CLI help to say storage reads manager fold-state files directly.
- Smoke verified `trading-storage` detects `fold_2016-01_2016-06` from `/root/projects/trading-manager/storage/runtime/model_training_fold_state_2016-01_2016-06.json`, emits a `storage_fold_sql_backup_candidate`, keeps `source_delete_allowed_before_backup=false`, and performs no provider/model/broker/account side effects.
- Verification passed: `trading-storage` 90 tests OK, compileall OK, systemd-analyze verify OK, diff-check OK; `trading-manager` 317 tests OK, compileall OK, registry snapshot/dry-run OK, diff-check OK.
- Pushed commits: `trading-storage` `7370ff4`; `trading-manager` `6191b01`.

## Trading evaluation repository created
- Chentong accepted splitting frozen benchmark, fold settlement, and promotion eligibility into a new independent repository rather than leaving model-quality judgment in manager/model.
- Created `/root/projects/trading-evaluation` and GitHub repo `https://github.com/gilmore307/trading-evaluation.git`.
- Initial repo owns docs spine, benchmark/fold-settlement/promotion-eligibility boundaries, `src/trading_evaluation/benchmark.py`, CLI `scripts/evaluation/validate_benchmark_contract.py`, fixture, and tests.
- First validator enforces a non-empty benchmark target, chronological and sufficiently long window, at least four market-condition tags, data snapshot/cost model/baselines, and no benchmark target overlap with training-used symbols. The actual primary benchmark symbol/window remains intentionally unselected pending review.
- Registered `TRADING_EVALUATION_REPO`, `EVALUATION_BENCHMARK_CONTRACT`, `EVALUATION_BENCHMARK_CONTRACT_VALIDATION`, `FOLD_SETTLEMENT_RUN`, `FOLD_SETTLEMENT_METRIC`, `PROMOTION_ELIGIBILITY_DECISION`, `EVALUATION_PRIMARY_BENCHMARK_POLICY`, and `TRADING_EVALUATION_VALIDATE_BENCHMARK_CONTRACT` in `trading-manager` migration `544_register_trading_evaluation_repo.sql`.
- Verification passed: `trading-evaluation` 4 tests OK, compileall OK, benchmark CLI smoke OK, diff-check OK; `trading-manager` 318 tests OK, compileall OK, registry snapshot/dry-run OK, diff-check OK.
- Pushed commits: `trading-evaluation` `cdd7e3e`; `trading-manager` `c0a5f6f`.

## Promotion activation boundary moved to evaluation
- Chentong clarified that model activation belongs in `trading-evaluation`; `trading-manager` should remain a scheduler rather than a model-quality judge or activation owner.
- Updated `trading-evaluation` to own `promotion_eligibility_decision`, `model_activation_record`, and `active_model_config`. Added activation builder/validator, CLI `scripts/evaluation/build_model_activation_record.py`, docs `docs/50_activation.md`, fixtures, and tests.
- Updated `trading-manager` to remove its activation-record builder/export/tests, route promotion review requests to `trading-evaluation`, and report model activation as evaluation-owned in scheduler status.
- Registered manager migrations `545_move_activation_boundary_to_evaluation.sql` and `546_cleanup_evaluation_activation_registry_text.sql`; regenerated `scripts/registry/current.csv`.
- Safety boundary: activation is a config-release record after an eligible evaluation decision; it does not execute brokers, construct orders, mutate accounts, call providers, mutate SQL, or perform storage lifecycle work.

## Fixed promotion evaluation reviewer skill added
- Chentong raised benchmark-overfit risk: even if benchmark data is not used in training, repeated benchmark-driven candidate selection can overfit the benchmark regime.
- Accepted policy: the primary benchmark is sealed, used only for evaluation, and formally run once after training for a candidate lineage.
- Created workspace skill `skills/openclaw/promotion-evaluation-review` so reviewer agents judge promotion candidates with a constant rubric: benchmark integrity, hard guardrails, incumbent vector comparison, uncertainty, shadow eligibility, and activation readiness.
- The skill requires structured advisory output only. It cannot activate models, switch active config pointers, call providers, mutate SQL/storage, submit orders, or mutate accounts.
- Registered `EVALUATION_PROMOTION_REVIEW_SKILL` and `EVALUATION_PROMOTION_VECTOR_RUBRIC_POLICY` in `trading-manager` migration `547_register_promotion_evaluation_review_skill.sql`; updated `trading-evaluation` docs to require the fixed skill for reviewer-agent passes.

## Runtime activation moved to execution shadow-cycle lifecycle
- Chentong refined the promotion/activation boundary: benchmark promotion should not directly activate a model. The current active model should trade during market hours while promoted-but-not-active models run as shadow candidates.
- Accepted runtime lifecycle: after a live/shadow cycle, normally around one month, execution chooses the best overall candidate as active, keeps ranks 2-4 as realtime candidates, keeps others shadow-only unless there is sufficient elimination evidence, and retires models only after repeated eliminate-candidate cycles or another accepted policy.
- Updated `trading-evaluation`: it no longer owns final model activation or active config pointers. It owns `promotion_eligibility_decision` and `promotion_readiness_record`, with source renamed from `activation.py` to `promotion.py` and CLI `build_promotion_readiness_record.py`.
- Updated `trading-execution`: added `execution_shadow_cycle_selection` in `src/trading_execution/model_lifecycle.py` and CLI `scripts/execution/build_shadow_cycle_selection.py`. The record recommends active/realtime/shadow/eliminate roles but does not write active pointers, construct orders, call brokers, or mutate accounts.
- Updated `trading-manager` registry migrations `548_move_runtime_activation_to_execution.sql` through `551_rename_evaluation_promotion_source_path.sql` so manager schedules only, evaluation owns offline readiness, and execution owns runtime activation selection.
- Chentong accepted a separate execution gate for actually writing the active model config pointer after `execution_shadow_cycle_selection`.
- Added `execution_active_model_config_write` to `trading-execution`: it requires a valid shadow-cycle selection, expected previous active model ref, new active config ref, rollback ref, and write window ref. It records the pointer write and rollback availability but still performs no order construction, broker calls, or account mutation.
- Registered `EXECUTION_ACTIVE_MODEL_CONFIG_WRITE`, `EXECUTION_ACTIVE_MODEL_CONFIG_WRITE_POLICY`, and `TRADING_EXECUTION_BUILD_ACTIVE_MODEL_CONFIG_WRITE` in `trading-manager` migration `552_register_execution_active_model_config_write.sql`.

## Stock-first benchmark candidate and option-expression fallback completed
- Chentong tightened the benchmark panel shape: avoid broad overlapping event windows because shared market background reduces diversity; keep ETFs minor; focus on optionable single-name equities because the system mainly trades stock options; include a small crypto sleeve and controlled missing-data stress cases.
- Updated `trading-evaluation` primary benchmark candidate `benchmarks/primary_benchmark_candidate_20260519.json` to 20 components: 67% equity single-name, 21% ETF background anchors, 12% crypto spot, with stress components totaling 11%. The candidate includes hot/thematic optionable equities such as TSLA, MRNA, GME, MP, CCJ, META, FSLR, NVDA, VRT, SMCI, LLY, COIN, AAOI, AMD plus limited ETF and BTC-USDT components.
- Added validation rules requiring equity single-name weight >=55%, ETF weight <=30%, crypto weight <=15%, and updated docs/tests. The benchmark remains a candidate pending materialization/review, and same-target overlapping training folds remain blocked.
- Updated `trading-model` Layer 8 OptionExpressionModel to support `underlying_only_expression`: when the Layer 7 thesis is usable but option policy/contract/liquidity/IV/Greek/DTE/quote conditions make options unsuitable, the model can recommend a direct-underlying expression while keeping selected option contract null and option-fit scores zero. This is still not an order or execution instruction.
- Registered these policy names in `trading-manager` migration `559_register_stock_first_benchmark_and_underlying_expression.sql`.
- Verification passed: `trading-evaluation` 16 tests OK plus benchmark validation/compileall/diff-check; `trading-model` 222 tests OK plus compileall/diff-check; `trading-manager` 50 registry tests OK plus registry snapshot/compileall/diff-check.
- Pushed commits: `trading-evaluation` `b574895`, `trading-model` `d31024d`, `trading-manager` `e2775fa`.
- Chentong caught that the first stock-first benchmark candidate lacked recent 2025/2026 windows. Corrected `trading-evaluation` by adding sealed completed recent components: WDC 2025-04-01 to 2026-04-30, MU 2025-06-02 to 2026-04-30, CEG 2025-01-02 to 2025-12-31, and LITE 2025-08-01 to 2026-04-30. New candidate composition is 24 components: 73% single-name optionable equities, 17% ETF anchors, 10% crypto, 10% controlled stress. Verification passed and pushed `trading-evaluation` commit `62151a8`.
- Chentong then asked to make time allocation more even. Rebalanced the candidate into four 25% time buckets: 2020/2021, 2022, 2023/2024, and 2025/2026. Added OXY and ENPH to strengthen the 2022 single-name sleeve. New candidate composition is 26 components: 80% single-name optionable equities, 13% ETF anchors, 7% crypto, 7% controlled stress. Registered `balanced_time_bucket_panel` and `recent_completed_windows_required` in manager migration `560_register_benchmark_time_balance_policy.sql`. Verification passed; pushed `trading-evaluation` `45912d8` and `trading-manager` `609ba4b`.
- Chentong clarified benchmark must also explicitly cover earnings-crossing and event-driven situations. Added `event_coverage_tags` to benchmark components, event allocation buckets, validator requirements for at least 10% earnings-crossing weight and 25% event-driven weight, and manager migration `561_register_benchmark_event_coverage_policy.sql`. Current candidate event coverage includes earnings crossing 54%, policy/macro shock 41%, liquidity/squeeze/data stress 19%, product-cycle repricing 60%, and crypto cycle 9% (overlapping buckets by design). Verification passed; pushed `trading-evaluation` `a18f623` and `trading-manager` `a725f33`.
- Chentong asked for the final benchmark combination to cover more sectors such as consumption and entertainment. Finalized the candidate to 35 components across four equal 25% time buckets, with 88% single-name optionable equities, 9% ETF anchors, 3% crypto spot, and 5% controlled stress. Added sector coverage metadata and validator requirements: at least 10 distinct sector tags plus required consumer discretionary, consumer staples, and entertainment/media coverage. Final sector buckets include consumer/retail 22%, entertainment/media/gaming/travel 17%, AI compute/storage/optical 25%, energy/nuclear/clean-energy/rare-earth 21%, healthcare 6%, financials/crypto 7%, macro anchors 5%, and data-center infrastructure 10% (overlap allowed). Registered manager migration `562_register_benchmark_sector_coverage_policy.sql`; verification passed; pushed `trading-evaluation` `626c39d` and `trading-manager` `3b1150d`.
- Chentong accepted the final benchmark candidate and asked to organize it into a CSV under the storage repo shared folder. Added `trading-storage/main/shared/evaluation_primary_benchmark_candidate.csv` as the storage-owned shared static view of `trading-evaluation/benchmarks/primary_benchmark_candidate_20260519.json`, with 35 rows covering component windows, fixed weights, time buckets, sector/event/market/data tags, target-context refs, stress exceptions, and same-target training-exclusion reasons. Added storage shared-contract tests and README entry. Registered the shared CSV and key fields in `trading-manager` migration `563_register_benchmark_candidate_shared_csv.sql` and regenerated `scripts/registry/current.csv`. Verification passed: storage shared tests/full test suite/compileall/diff-check, manager registry tests/snapshot/compileall/diff-check. Pushed `trading-storage` `463437e` and `trading-manager` `73d8d31`.
- Chentong then asked to prepare the benchmark dataset. Implemented `trading-evaluation` benchmark dataset preparation: `src/trading_evaluation/benchmark_dataset.py`, CLI `scripts/evaluation/prepare_benchmark_dataset.py`, docs `22_benchmark_dataset_preparation.md`, tests, and exports. The script validates the benchmark contract, writes storage-owned runtime artifacts under `trading-storage/storage/benchmark/<contract_id>/`, generates component/feed/coverage CSVs and fail-closed task keys, scans local `trading-data/storage` coverage, and performs no provider calls, SQL mutation, benchmark freeze, model training, activation, broker execution, or account mutation. Generated the first runtime bundle for `primary_benchmark_candidate_20260519`: 35 components, 947 feed task requirements, 40 locally available, 907 missing; task keys are under `trading-storage/storage/benchmark/primary_benchmark_candidate_20260519/task_keys`. Registered manager migration `564_register_benchmark_dataset_preparation.sql`; verification passed for evaluation and manager. Pushed `trading-evaluation` `4a630ed` and `trading-manager` `38d664f`.
- Chentong asked to prepare the benchmark data in one pass. Fixed benchmark task keys so source feed outputs land in `trading-data/storage/monthly_backfill/<source>/<symbol>/<month>` while benchmark manifests remain under `trading-storage/storage/benchmark`. Added OKX historical benchmark candle support using `/api/v5/market/history-candles` with `benchmark_window_start/end`, switched crypto benchmark tasks to daily candles, and filled the 11 crypto month receipts. Filled all available equity `alpaca_bars` and `alpaca_news` benchmark requirements. Full-month `alpaca_liquidity` was explicitly marked `deferred` because the current route pulls raw trades/quotes transiently and timed out for a representative full-month window; it needs narrow event windows or a dedicated aggregate route. Final runtime bundle: 35 components, 947 feed tasks, 635 available, 312 deferred, 0 missing. Registered deferred coverage and OKX historical route in manager migration `565_register_benchmark_dataset_deferred_coverage.sql`. Verification passed across evaluation/data/manager; pushed `trading-evaluation` `b849601`, `trading-data` `ab6c185`, and `trading-manager` `a73e370`.
- Chentong corrected that benchmark data acquisition is a one-time sealed action and should not use the manager task route. Updated `trading-evaluation` benchmark dataset preparation to emit `feed_acquisition_plan.csv` with one-shot acquisition rows and no `task_keys/` or `feed_task_plan.csv`; runtime bundle was regenerated and stale task artifacts were deleted. Updated safety fields to record `manager_request_route_used=false`. Registered manager migration `566_route_benchmark_dataset_to_one_shot_acquisition.sql` to replace `BENCHMARK_FEED_TASK_PLAN` with `BENCHMARK_FEED_ACQUISITION_PLAN` and replace task-key policy with `BENCHMARK_ONE_SHOT_ACQUISITION_GATE_POLICY`. Verification passed; pushed `trading-evaluation` `a5bfb33` and `trading-manager` `2c49bf2`.
- Chentong clarified he only wants the benchmark data fully prepared and left the route design to OpenClaw. Implemented sampled Alpaca liquidity acquisition: each equity component month uses three deterministic five-minute regular-session windows (open, midday, close) via `feed=iex`, with raw trades/quotes transient and persisted `equity_liquidity_bar` receipts. Updated `trading-data` liquidity feed to support `sample_windows` and provider `null` trade/quote arrays; updated `trading-evaluation` acquisition params and registry migration `567_register_benchmark_liquidity_sampled_acquisition.sql`. Ran all remaining liquidity acquisitions. Final runtime bundle has 947/947 available, 0 deferred, 0 missing: Alpaca bars 312, Alpaca news 312, Alpaca liquidity 312, OKX crypto 11. Three liquidity receipts are successful zero-row samples where provider returned no IEX rows (`DIS` 2020-03, `RCL` 2020-05, `RCL` 2020-06). Verification passed; pushed `trading-data` `1d56512`, `trading-evaluation` `9f626f7`, and `trading-manager` `3ee80e5`.
