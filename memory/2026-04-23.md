# 2026-04-23

- Development-skill design direction clarified:
  - keep the future `universal-dictionary` as a first-class dependency rather than stripping it out
  - split the large development-governance skill into lighter specialized skills over time
  - treat workflow and acceptance as tightly coupled; each workflow should carry its own acceptance standard even if acceptance still has a dedicated artifact or summary surface
  - allow aggressive use of diagrams and tables in project docs when they improve clarity
- Codex delegation rule: task sizing should stay in the middle band — not fragmented into tiny handoffs, not so broad that control and review break down
- Repository structure rule: each meaningful maintained directory should have a `README.md` explaining boundary and contents; generated/vendor/disposable directories are excluded by default
- Post-acceptance stewardship rule: when a Codex task passes acceptance, OpenClaw is responsible for commit and push to GitHub unless explicitly told otherwise
- Near-term skill direction: finish the initial skill set first, keep `universal-dictionary` as a separate future project, and let naming/dictionary concerns stay split so they can be updated centrally later
- Development governance skill was split into lighter initial units: `project-docs-spine`, `project-shape-governance`, `codex-task-dispatch`, `completion-review-acceptance`, `project-maintenance-loop`, and `dictionary-interface`
- Further split preference: Codex-facing work should stay distinct from non-Codex project governance because dispatch/review/acceptance is a different operating mode from general project structure work
- Final preferred initial split: `development-governance-core`, `project-docs-spine`, `codex-orchestration`, and `dictionary-interface`
- Created matching executor-facing Codex skill set instead of governance mirrors: `codex-executor-core` and `codex-dictionary-guard` as standing executor skills; docs work stays with OpenClaw by default
- Governance correction: `README.md` and routine post-code project docs updates belong to OpenClaw after acceptance, not to Codex during normal implementation flow
- Code principles established:
  - structure before implementation
  - boundaries before wiring
  - small coherent change before broad refactor
  - clarity before cleverness
  - root cause before patch
  - explicit contracts before hidden behavior
  - tests follow behavior
  - remove stale implementation
  - acceptance over aesthetic polish
- GitHub configured via local secret store and implicit git credential helper:
  - secret root: `/root/secrets/`
  - registry: `/root/secrets/registry.json`
  - GitHub PAT alias: `github/pat`
  - GitHub SSH placeholder removed; HTTPS is the intended path on this machine
- Formal repositories on this host should live under `/root/projects/`
- Public GitHub repos created and pushed:
  - `https://github.com/gilmore307/universal-dictionary`
  - `https://github.com/gilmore307/workspace`
- Codex custom skills installed under `/root/.codex/skills/`
- GitHub PAT file corrected at `/root/secrets/github/pat`; helper-backed git operations now authenticate correctly
