
## trading-data docs expanded with optional domain/source guides
- User directed that project docs include optional guides, not only the required spine, and defined three `trading-data` data categories by purpose: market board data / 盘面数据, instrument data / 标的数据, and option data / 期权数据. These correspond to later model lanes, while `trading-data` only fetches, cleans, validates, and outputs data.
- Implemented `trading-data` commit `96e84e0`:
  - added `docs/07_data_domains.md` documenting the three domains, their purpose, model-lane relationship, composition rule, and output boundary;
  - added `docs/08_data_sources.md` documenting the provider/source connector boundary, planned source-connection layer, credential/secret alias handling under `/root/secrets/`, provider inventory template, and connection acceptance checklist;
  - updated README and docs spine/context/workflow/acceptance/task/decision/memory to state input is manager task instructions and output is cleaned data artifacts plus manifests/ready signals once contracts are accepted;
  - recorded D007 optional component docs, D008 three data domains, and D009 source connectors before domain pipelines.
- Updated workspace project-development docs guidance in commit `4ac4432`: optional project-specific docs may follow the required spine when they own a clear local planning/operating surface and are listed in docs/root README; reusable templates and registry source-of-truth files still stay out of docs.
- Verification passed for trading-data: markdown link check, `git diff --check`, and docs file inspection.
