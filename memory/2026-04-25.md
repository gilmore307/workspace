# 2026-04-25 Memory

## network-framework packaged client: Windows OpenClaw shim fix
- User tested build/commit `be9a63a` and reported `openclaw.node.install` failed with: `openclaw.exe is not installed or is not available in PATH.`
- Attached app output showed OpenClaw server status/model status successfully loaded over SSH (`ssh-openclaw-server`), but local Windows node control was blocked by the app’s local CLI detection: `Local openclaw.exe is required for Windows node service control.`
- Diagnosis: the app was too narrow on Windows. npm/pnpm global installs usually expose `openclaw.cmd` rather than `openclaw.exe`, and Electron launched from desktop may not inherit `%APPDATA%\npm` in PATH.
- Fix completed in `network-framework` commit `379174a Support Windows OpenClaw command shims`.
- Fix behavior:
  - stop hard-requiring `openclaw.exe`
  - resolve/check `openclaw` command shims instead
  - augment app child-process PATH with `%APPDATA%\npm`
  - run app-owned OpenClaw commands through `cmd.exe /d /s /c openclaw ...`
  - preserve app command gateway override via configured `OPENCLAW_GATEWAY_URL`, currently expected as `ws://127.0.0.1:18789` for the tunnel flow
- Verification reported after fix:
  - `npm run test:runtime`
  - `npm run test:desktop`
  - `npm run test:installer`
  - `npm test` with 54 passing tests
  - installer build
  - `npm audit --omit=optional` with 0 vulnerabilities
- New installer produced:
  - `/root/projects/network-framework/dist/network-framework-setup-33e7ce0.exe`
  - SHA-256 `33e7ce001aa170cfad0ff9852fa145485cadeb5895fdfd9d7737b67ec24a5263`
  - suggested download command: `scp root@openclaw:/root/projects/network-framework/dist/network-framework-setup-33e7ce0.exe "$env:USERPROFILE\Desktop\network-framework-setup-33e7ce0.exe"`
- Next user-facing test: install `network-framework-setup-33e7ce0.exe` and confirm Node page no longer says Windows OpenClaw CLI is missing. If it still does, ask for Windows `where openclaw` output to confirm whether the shim exists.

## network-framework prior accepted state still relevant
- Latest stable project boundary remains `/root/projects/network-framework`, with formal docs spine under `docs/00_scope.md` through `docs/05_decision.md`.
- OpenClaw Gateway stays loopback-bound on server and dashboard access is via Tailscale Serve or Windows SSH tunnel.
- Windows app default dashboard/tunnel gateway target remains local: `127.0.0.1:18789` / `http://127.0.0.1:18789/`.
- Windows OpenClaw Node controls must use OpenClaw CLI scheduled-task abstraction (`openclaw node status/restart/stop --json` and install repair with `openclaw node install --force ...`), not `sc.exe OpenClawNode`.
- App-owned OpenClaw commands must continue injecting configured `OPENCLAW_GATEWAY_URL` to avoid stale global/user Windows env values.
- Tray scope remains lightweight: VPN switch, live speed, session totals, Quit only.
- SMB remains outside packaged app ownership.
- Internet Proxy VPN remains Tailscale exit-node based, with default exit node `openclaw`; speed may be poor from China because only current exit node is Tampa, FL.

## network-framework packaged client: existing OpenClaw Scheduled Task handling
- User tested post-shim build and showed Windows app now resolves local OpenClaw CLI successfully (`source: local-openclaw-cli`) with app gateway override `ws://127.0.0.1:18789`.
- New blocker: `openclaw.node.install` failed because `openclaw node install --force ...` tried to recreate/repair an already-registered Windows Scheduled Task and `schtasks create` returned access denied. The attached output also exposed OpenClaw status JSON with a node environment token, reinforcing that GUI command payloads need redaction before display.
- Existing task in the user output was already registered with the expected local tunnel host/port (`127.0.0.1:18789`) and display name flow, so forcing repair was unnecessary unless the task is stale or mismatched.
- Implemented `network-framework` commit `dfc6bb6 Handle existing Windows OpenClaw node task`:
  - Register Node now runs `openclaw node status --json` first.
  - If the registered Scheduled Task command already matches configured host/port/display name, the app skips `openclaw node install --force` and treats registration as complete.
  - Forced install/repair remains for missing or mismatched tasks, with clearer guidance if an existing task cannot be repaired due permissions.
  - Added runtime redaction for secret-like OpenClaw command output before exposing parsed status/model/node JSON to the GUI.
- Verification passed: `git diff --check`, `npm run test:desktop`, `npm run test:runtime`, `npm run test:installer`, `npm test` (55 pass), bootstrap build, `npm run build:desktop`, `npm run build:installer`, `npm audit --omit=optional`.
- Latest installer artifact: `/root/projects/network-framework/dist/network-framework-setup-c1e1a86.exe`, SHA-256 `c1e1a860efd7c4e862ef82fab063bf4c71f9a8740a5c730cad0ce413d25d6147`.
- Next test: install `network-framework-setup-c1e1a86.exe`; expected Register Node no longer fails with access denied when the existing task matches. Then start SSH Tunnel before Node so local gateway `127.0.0.1:18789` is reachable, and use Node start/restart.

## OpenClaw pairing reset for Windows node retest
- User asked to clear all paired devices and nodes to reduce interference before retrying Windows node pairing.
- Server-side OpenClaw pairing state was cleared on 2026-04-25 around 03:22 EDT.
- Commands/actions performed:
  - `openclaw devices clear --yes --pending --json`
  - rejected node pending requests via `openclaw nodes pending --json` + `openclaw nodes reject <requestId> --json`
  - directly reset storage files to `{}` because local server CLI auto-self-paired immediately during verification:
    - `/root/.openclaw/devices/paired.json`
    - `/root/.openclaw/devices/pending.json`
    - `/root/.openclaw/nodes/paired.json`
    - `/root/.openclaw/nodes/pending.json`
- Backup of pre-clear pairing files created under `/root/.openclaw-repair/pairing-clear-20260425T072231Z`.
- Direct file verification showed all four pairing stores at count 0 after reset.
- Caveat: running local `openclaw devices list` on the server can silently recreate a local Linux CLI operator pairing. That server-local CLI pairing is not the Windows node, but avoid using devices-list as a zero-state verifier right before retrying Windows node pairing.
