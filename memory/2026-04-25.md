

## trading-main helper packaging boundary clarified
- User identified a real ambiguity: `helpers/registry/` contains working JavaScript helper code (`getKeyById`, `getPayloadById`, `getPathById`, `loadSecretTextByConfigId`) and tests pass, but there is no `package.json`, version policy, Node engine requirement, install/import contract, or formal cross-repository consumption path. This conflicts with the Python-centered shared `.venv` environment anchor.
- Decision recorded in `trading-main` D026: current JavaScript registry helpers are internal `trading-main` maintenance/test helpers, not component-repository runtime dependencies.
- Components must not directly import `helpers/registry/` at runtime until a formal distribution strategy is accepted: Node package, Python package aligned with `.venv`, or explicitly internal-only tool.
- Updated `trading-main/docs/04_task.md`: helper package/distribution strategy is queued before any component runtime imports shared helper code.
- Updated `docs/07_helpers.md`, `helpers/README.md`, `helpers/registry/README.md`, `docs/03_acceptance.md`, and `docs/08_registry.md` to distinguish registered helper method surfaces from package installation/runtime dependency contracts.
- Updated workspace project-development guidance: helper code existence is not package readiness; shared helper consumption requires package metadata/version/runtime/install/import contract or explicit internal-only status.
- Pushed commits: `trading-main` `c035d67` and workspace `42f05b2`.

## trading platform fixable planning gaps cleared
- User asked to clear all currently fixable `trading-main` planning gaps and leave items that require other repositories for later.
- `trading-main` commit `84c5170` cleared fixable platform gaps:
  - added `requirements.txt` as the reviewed shared Python dependency ledger;
  - documented shared environment baseline as Python 3.12 at `/root/projects/trading-main/.venv`, installed with `pip` from `requirements.txt`;
  - formalized helper distribution as internal-only for now: component repositories must not import loose files from `trading-main/helpers/`; future runtime helpers need an accepted package strategy, preferably Python aligned with `.venv` unless Node is justified;
  - formalized repository visibility policy: trading repos remain private unless the owner explicitly approves visibility change after pre-change review;
  - updated `docs/04_task.md` so queued tasks now only contain cross-repository coordination blockers: artifact/reference, manifest, ready-signal, request schemas, and shared storage root.
- Created initial docs spines and `.gitignore` files for remaining component repositories:
  - `trading-manager` commit `487cff8`
  - `trading-storage` commit `21f91e3`
  - `trading-strategy` commit `2b62846`
  - `trading-model` commit `54b3ab1`
  - `trading-execution` commit `7524d58`
  - `trading-dashboard` commit `4639711`
- `trading-data` docs spine already existed at commit `bacce39`.
- Final status after this pass: all eight trading repos clean; `trading-main/docs/04_task.md` has no active tasks and no safe standalone queued tasks, only cross-repository coordination waits.

## trading-main registry helper official runtime switched to Python
- User pointed out future component repositories will need registry helpers from Python, so JS helper runtime dependency would be awkward.
- Implemented `trading-main` commit `4759216`:
  - added root `pyproject.toml` for Python package `trading-main-helpers`;
  - official import package is `trading_registry` under `helpers/python/trading_registry/`;
  - added Python package tests under `helpers/python/tests/` using stdlib `unittest`;
  - added `setuptools>=69,<76` to `requirements.txt` as packaging backend dependency;
  - verified editable install into `/root/projects/trading-main/.venv` and import from the shared environment;
  - kept JavaScript helpers under `helpers/registry/` as internal maintenance/test code only.
- Official registered helper surface is now Python:
  - `RegistryReader.get_key_by_id`
  - `RegistryReader.get_payload_by_id`
  - `RegistryReader.get_path_by_id`
  - `SecretResolver.load_secret_text_by_config_id`
- Registry migration `004_register_python_registry_helper_surface.sql` updated script rows to point to Python source files and regenerated `registry/current.csv`.
- Verification passed: markdown links, registry migration dry-run, Python `unittest` via installed package and `PYTHONPATH`, and legacy JS registry tests.
- Workspace guidance updated in commit `886e902`: `trading-main` accepted helper runtime strategy is Python; JS helpers are internal unless a future Node package decision changes that.

## trading-main registry helpers are Python-only
- User requested deleting the remaining JS registry helper implementation and using Python only.
- Implemented `trading-main` commit `05ca1af`:
  - deleted `helpers/registry/` entirely;
  - kept `helpers/python/trading_registry/` as the only registry helper implementation;
  - removed docs references to the old JS/internal helper path;
  - updated `docs/05_decision.md` with D031: registry helpers are Python-only;
  - updated `registry/sql/schema_migrations/002_bootstrap_trading_registry.sql` script rows to point directly to Python helper methods and removed the now-redundant Python-retarget migration file;
  - adjusted local registry migration ledger so dry-run remains clean after the pre-release migration cleanup.
- Verification passed after deletion: markdown links, registry migration dry-run, Python package editable install, Python `unittest` via installed package and `PYTHONPATH`, and registry script rows all point to Python helpers.
- Workspace guidance updated in commit `7f89d8e`: `trading-main` helper strategy is Python-only.

## trading-main helper package layout flattened
- User said the `helpers/python/` layer was meaningless after choosing Python-only helpers.
- Implemented `trading-main` commit `0ed9e12`:
  - moved package source from `helpers/python/trading_registry/` to `helpers/trading_registry/`;
  - moved tests from `helpers/python/tests/` to `helpers/tests/`;
  - removed `helpers/python/README.md` and the now-empty `helpers/python/` layer;
  - updated `pyproject.toml` package-dir/find paths to `helpers`;
  - updated docs, registry rows, and SQL bootstrap paths to `helpers/trading_registry` and `helpers/tests`.
- Verification passed: markdown links, registry dry-run no pending, editable install imports from `/root/projects/trading-main/helpers/trading_registry/__init__.py`, Python unittest via installed package and `PYTHONPATH`, and no old `helpers/python`, JS, Node, or old camelCase helper refs in docs/source/registry surfaces.
- Workspace guidance updated in commit `ea007ac` to point to flattened `helpers/trading_registry/`.

## trading-main registry payload formats expanded
- User observed that all registry `payload_format` values being `text` was too coarse and suggested supporting values like time.
- Implemented `trading-main` commit `7d5359a`:
  - added migration `004_expand_payload_formats.sql` with accepted formats: `text`, `file`, `json`, `integer`, `decimal`, `boolean`, `iso_date`, `iso_time`, `iso_datetime`, `iso_duration`, `timezone`, `secret_alias`, `repo_name`, `field_name`, `status_value`, `command`, `python_symbol`;
  - backfilled current rows to narrower formats where clear: fields, statuses, repos, timezone config, secret alias config, registry export command, and Python helper symbols;
  - added migration `005_register_payload_format_helper_exports.sql` registering `PAYLOAD_FORMATS`, `is_payload_format`, and `assert_payload_format` as Python helper exports;
  - added `helpers/trading_registry/payload_formats.py` and validation through `map_registry_item_row`;
  - documented payload-format semantics in registry docs and helper docs.
- Verification passed: markdown links, migration dry-run no pending, editable install, Python unittest, and registry assertion showing 104 rows with multiple payload formats instead of text-only.

## trading-main helper packaging boundary clarified
- User identified a real ambiguity: `helpers/registry/` contains working JavaScript helper code (`getKeyById`, `getPayloadById`, `getPathById`, `loadSecretTextByConfigId`) and tests pass, but there is no `package.json`, version policy, Node engine requirement, install/import contract, or formal cross-repository consumption path. This conflicts with the Python-centered shared `.venv` environment anchor.
- Decision recorded in `trading-main` D026: current JavaScript registry helpers are internal `trading-main` maintenance/test helpers, not component-repository runtime dependencies.
- Components must not directly import `helpers/registry/` at runtime until a formal distribution strategy is accepted: Node package, Python package aligned with `.venv`, or explicitly internal-only tool.
- Updated `trading-main/docs/04_task.md`: helper package/distribution strategy is queued before any component runtime imports shared helper code.
- Updated `docs/07_helpers.md`, `helpers/README.md`, `helpers/registry/README.md`, `docs/03_acceptance.md`, and `docs/08_registry.md` to distinguish registered helper method surfaces from package installation/runtime dependency contracts.
- Updated workspace project-development guidance: helper code existence is not package readiness; shared helper consumption requires package metadata/version/runtime/install/import contract or explicit internal-only status.
- Pushed commits: `trading-main` `c035d67` and workspace `42f05b2`.

## trading platform fixable planning gaps cleared
- User asked to clear all currently fixable `trading-main` planning gaps and leave items that require other repositories for later.
- `trading-main` commit `84c5170` cleared fixable platform gaps:
  - added `requirements.txt` as the reviewed shared Python dependency ledger;
  - documented shared environment baseline as Python 3.12 at `/root/projects/trading-main/.venv`, installed with `pip` from `requirements.txt`;
  - formalized helper distribution as internal-only for now: component repositories must not import loose files from `trading-main/helpers/`; future runtime helpers need an accepted package strategy, preferably Python aligned with `.venv` unless Node is justified;
  - formalized repository visibility policy: trading repos remain private unless the owner explicitly approves visibility change after pre-change review;
  - updated `docs/04_task.md` so queued tasks now only contain cross-repository coordination blockers: artifact/reference, manifest, ready-signal, request schemas, and shared storage root.
- Created initial docs spines and `.gitignore` files for remaining component repositories:
  - `trading-manager` commit `487cff8`
  - `trading-storage` commit `21f91e3`
  - `trading-strategy` commit `2b62846`
  - `trading-model` commit `54b3ab1`
  - `trading-execution` commit `7524d58`
  - `trading-dashboard` commit `4639711`
- `trading-data` docs spine already existed at commit `bacce39`.
- Final status after this pass: all eight trading repos clean; `trading-main/docs/04_task.md` has no active tasks and no safe standalone queued tasks, only cross-repository coordination waits.

## trading-main registry helper official runtime switched to Python
- User pointed out future component repositories will need registry helpers from Python, so JS helper runtime dependency would be awkward.
- Implemented `trading-main` commit `4759216`:
  - added root `pyproject.toml` for Python package `trading-main-helpers`;
  - official import package is `trading_registry` under `helpers/python/trading_registry/`;
  - added Python package tests under `helpers/python/tests/` using stdlib `unittest`;
  - added `setuptools>=69,<76` to `requirements.txt` as packaging backend dependency;
  - verified editable install into `/root/projects/trading-main/.venv` and import from the shared environment;
  - kept JavaScript helpers under `helpers/registry/` as internal maintenance/test code only.
- Official registered helper surface is now Python:
  - `RegistryReader.get_key_by_id`
  - `RegistryReader.get_payload_by_id`
  - `RegistryReader.get_path_by_id`
  - `SecretResolver.load_secret_text_by_config_id`
- Registry migration `004_register_python_registry_helper_surface.sql` updated script rows to point to Python source files and regenerated `registry/current.csv`.
- Verification passed: markdown links, registry migration dry-run, Python `unittest` via installed package and `PYTHONPATH`, and legacy JS registry tests.
- Workspace guidance updated in commit `886e902`: `trading-main` accepted helper runtime strategy is Python; JS helpers are internal unless a future Node package decision changes that.

## trading-main registry helpers are Python-only
- User requested deleting the remaining JS registry helper implementation and using Python only.
- Implemented `trading-main` commit `05ca1af`:
  - deleted `helpers/registry/` entirely;
  - kept `helpers/python/trading_registry/` as the only registry helper implementation;
  - removed docs references to the old JS/internal helper path;
  - updated `docs/05_decision.md` with D031: registry helpers are Python-only;
  - updated `registry/sql/schema_migrations/002_bootstrap_trading_registry.sql` script rows to point directly to Python helper methods and removed the now-redundant Python-retarget migration file;
  - adjusted local registry migration ledger so dry-run remains clean after the pre-release migration cleanup.
- Verification passed after deletion: markdown links, registry migration dry-run, Python package editable install, Python `unittest` via installed package and `PYTHONPATH`, and registry script rows all point to Python helpers.
- Workspace guidance updated in commit `7f89d8e`: `trading-main` helper strategy is Python-only.

## trading-main helper package layout flattened
- User said the `helpers/python/` layer was meaningless after choosing Python-only helpers.
- Implemented `trading-main` commit `0ed9e12`:
  - moved package source from `helpers/python/trading_registry/` to `helpers/trading_registry/`;
  - moved tests from `helpers/python/tests/` to `helpers/tests/`;
  - removed `helpers/python/README.md` and the now-empty `helpers/python/` layer;
  - updated `pyproject.toml` package-dir/find paths to `helpers`;
  - updated docs, registry rows, and SQL bootstrap paths to `helpers/trading_registry` and `helpers/tests`.
- Verification passed: markdown links, registry dry-run no pending, editable install imports from `/root/projects/trading-main/helpers/trading_registry/__init__.py`, Python unittest via installed package and `PYTHONPATH`, and no old `helpers/python`, JS, Node, or old camelCase helper refs in docs/source/registry surfaces.
- Workspace guidance updated in commit `ea007ac` to point to flattened `helpers/trading_registry/`.

## trading-main registry payload formats expanded
- User observed that all registry `payload_format` values being `text` was too coarse and suggested supporting values like time.
- Implemented `trading-main` commit `7d5359a`:
  - added migration `004_expand_payload_formats.sql` with accepted formats: `text`, `file`, `json`, `integer`, `decimal`, `boolean`, `iso_date`, `iso_time`, `iso_datetime`, `iso_duration`, `timezone`, `secret_alias`, `repo_name`, `field_name`, `status_value`, `command`, `python_symbol`;
  - backfilled current rows to narrower formats where clear: fields, statuses, repos, timezone config, secret alias config, registry export command, and Python helper symbols;
  - added migration `005_register_payload_format_helper_exports.sql` registering `PAYLOAD_FORMATS`, `is_payload_format`, and `assert_payload_format` as Python helper exports;
  - added `helpers/trading_registry/payload_formats.py` and validation through `map_registry_item_row`;
  - documented payload-format semantics in registry docs and helper docs.
- Verification passed: markdown links, migration dry-run no pending, editable install, Python unittest, and registry assertion showing 104 rows with multiple payload formats instead of text-only.

## 2026-04-25 pre-compaction checkpoint
- Latest durable trading-main state: helper package layout is flattened under `helpers/trading_registry/`, registry helpers are Python-only, and registry `payload_format` is now a real constrained vocabulary rather than defaulting everything to `text`.
- Latest pushed commits from this segment: `trading-main` `0ed9e12` for helper layout flattening and `7d5359a` for payload-format expansion; workspace memory/guidance commits mentioned in-session include `b07352e`/`52b7b30` around the final summaries.
- Current registry payload-format distribution after expansion was reported as: `field_name` 51, `status_value` 28, `repo_name` 8, `python_symbol` 7, `text` 7, `timezone` 1, `secret_alias` 1, `command` 1.
- Current accepted payload formats: `text`, `file`, `json`, `integer`, `decimal`, `boolean`, `iso_date`, `iso_time`, `iso_datetime`, `iso_duration`, `timezone`, `secret_alias`, `repo_name`, `field_name`, `status_value`, `command`, `python_symbol`.
- Next real project work remains the first functional slice, likely starting in `trading-data`, after coordinating cross-repository contract/storage decisions for artifact references, manifests, ready signals, requests, and shared storage root.

## trading-main payload format validators unregistered from script rows
- User questioned why `is_payload_format(value)` used a different naming pattern and what `REGISTRY_PAYLOAD_FORMATS_CONSTANT` / `PAYLOAD_FORMATS` were for.
- Clarified the boundary: `is_payload_format` and `assert_payload_format` are Python package validation utilities, analogous to `is_registry_kind` / `assert_registry_kind`, not registry item lookup helpers. `PAYLOAD_FORMATS` is the Python package tuple mirroring the SQL constraint so tests and code avoid hardcoded duplicate vocabularies.
- Identified and fixed over-registration: package constants/passive validators should not be registered as registry `script` rows. Script rows should represent stable callable helper/automation exports, not package constants.
- Implemented `trading-main` commit `514fe32`: added migration `006_unpublish_payload_format_validator_exports.sql`, deleted the three payload-format validator/constant rows from `registry/current.csv`, and updated docs/kind boundaries accordingly.
- After cleanup, registry is back to 101 rows and 5 script rows; payload format distribution is `field_name` 51, `status_value` 28, `repo_name` 8, `text` 7, `python_symbol` 4, `timezone` 1, `secret_alias` 1, `command` 1.
- Verification passed: markdown links, migration dry-run no pending, Python unittest.

## trading-main payload_format vocabulary registered
- User said `is_payload_format(value)` is a test/internal utility and should not live in the runtime helper package; also said legal `payload_format` values should be registered.
- Implemented `trading-main` commit `c681421`:
  - added `payload_format` as a registry kind and created `registry/kinds/payload_format.md`;
  - added migration `007_register_payload_format_values.sql` registering 17 legal payload-format values as `kind=payload_format` rows: `text`, `file`, `json`, `integer`, `decimal`, `boolean`, `iso_date`, `iso_time`, `iso_datetime`, `iso_duration`, `timezone`, `secret_alias`, `repo_name`, `field_name`, `status_value`, `command`, `python_symbol`;
  - removed `helpers/trading_registry/payload_formats.py` and removed `PAYLOAD_FORMATS`, `is_payload_format`, and `assert_payload_format` from the public Python package exports;
  - removed runtime `map_registry_item_row` payload_format validation; DB constraint + registered rows + tests now own the vocabulary boundary;
  - updated tests so they compare SQL payload_format constraint values with `kind=payload_format` rows in `registry/current.csv`;
  - updated docs/decisions to say payload-format vocabulary belongs in registry rows, not package-level validators.
- Verification passed: markdown links, registry migration dry-run no pending, editable install, Python unittest. Registry now has 118 rows, including 17 `payload_format` rows and 5 `script` rows.

## trading-main registry kind validators removed from runtime helpers
- User observed that `registry_types.py` itself belongs in tests rather than the runtime helper package, matching the earlier payload-format validator boundary.
- Implemented `trading-main` commit `23fc204`:
  - deleted `helpers/trading_registry/registry_types.py`;
  - removed `REGISTRY_KINDS`, `is_registry_kind`, and `assert_registry_kind` from `trading_registry` public exports;
  - changed `RegistryReader.list_items_by_kind` to validate only non-empty input and leave legal-kind authority to schema/registry tests;
  - updated tests to compare the latest SQL `kind` check constraint with `registry/kinds/*.md` and ensure current CSV rows use constrained kinds;
  - updated docs/decision D034 to record that registry kind vocabulary is not a runtime helper export.
- Verification passed: markdown links, registry migration dry-run no pending, editable install, Python unittest, and runtime export assertions confirming both `registry_types` and `payload_formats` modules are no longer importable.

## trading-main test script inventory boundary documented
- User clarified that test scripts should not enter the registry, but each test folder should explain what every test script verifies.
- Implemented `trading-main` commit `4ecfcaa`:
  - expanded `helpers/tests/README.md` with the rule that test scripts are repository-local verification assets, not registry entries;
  - inventoried `test_trading_registry.py` and listed its coverage: registry reader behavior, secret resolver behavior, SQL kind constraint vs `registry/kinds/*.md`, payload_format constraint vs registered rows, and test-script governance;
  - added a unit test that every first-party `helpers/tests/test_*.py` file is documented in `helpers/tests/README.md` and that registry `script` rows do not point at test files;
  - updated docs/acceptance/registry/script-kind guidance with D035: test scripts are documented locally, not registered.
- Verification passed: markdown links, registry migration dry-run no pending, Python unittest (12 tests), and explicit check that 5 registry `script` rows point only to helper/export files, not `/tests/` paths.
