

## Layer 3 family_01 MA profiles expanded for trading-use coverage

- User clarified moving-average combinations must cover different trading rhythms while keeping unified 1Min bars: fast crypto / near-zero-slippage high-volume options, intraday stock day-trading, and slower day/swing holds for instruments with meaningful slippage.
- In `/root/projects/trading-model`, replaced `family_01_moving_average_crossover` axis `ma_window_minutes` with `ma_window_profile` tuples `(profile_id, fast_window_1min_bars, slow_window_1min_bars)`.
- Current profiles: `micro_3_10`, `scalp_5_20`, `fast_10_30`, `intraday_15_60`, `intraday_30_120`, `intraday_60_240`, `intraday_120_480`, `intraday_240_960`, `equity_day_390_1950`, `equity_swing_780_3900`, `continuous_day_1440_7200`, `continuous_swing_4320_20160`.
- Moving-average variant count is now 288: 12 profiles × 2 MA types × 3 confirmation bars × 2 min_slope × 2 trend_filter_enabled.
- Kept `alpaca_data_support = equity_bar` and fixed `signal_bar_interval = 1Min`; did not introduce a new data-kind name.
- Verification passed: `git diff --check`; `PYTHONPATH=src python3 -m compileall -q src tests`; `PYTHONPATH=src python3 -m unittest discover -s tests -p 'test_*.py'` (66 tests OK).
- Pushed `trading-model` commit `34d5631` (`Expand moving average window profiles`).
