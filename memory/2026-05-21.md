# 2026-05-21

## Comprehensive 10-layer remediation completed

- After compaction, Chentong asked to "fix everything in one pass" following a Codex CLI full-project review guided by the project development review skill.
- Completed the P1 and P2 remediation across the active trading repositories, not just the original 10-layer model numbering concern.
- `trading-manager`: removed active registry/current 9-layer residue, added formal migrations `610/611`, regenerated snapshot, and removed the old one-off current-table script. Commit: `86f2613`.
- `trading-model`: aligned active docs with the 10-layer architecture and corrected stale `Layer 07 PositionProjectionModel` prose that still referred to Layer 6 / old prefixes. Commit: `0748cda`.
- `trading-execution`: updated realtime coverage to the full 10-layer model stack, adding `layer_06_dynamic_risk_policy` and correcting shifted downstream layers. Commit: `0812bfc`.
- `trading-dashboard`: aligned table allowlist/read-model docs with the 10-layer model stack. Commit: `37b6801`.
- `trading-storage`: corrected lifecycle/index tests and templates that still referenced old layer/model names. Commit: `e9ae05e`.
- `trading-evaluation`: hardened the promotion gate so `eligible`/readiness now requires benchmark-freeze evidence, complete fold-stack evidence, guardrail pass, incumbent comparison, and agent-review evidence instead of accepting a thin eligibility string. Commit: `f8fd29f`.
- Verification passed before closeout:
  - `trading-manager`: 324 tests, registry snapshot, registry dry-run, compileall, diff-check.
  - `trading-model`: 222 tests, compileall, diff-check.
  - `trading-execution`: 57 tests, compileall, diff-check.
  - `trading-dashboard`: 10 tests, compileall, diff-check.
  - `trading-storage`: 109 tests, compileall, diff-check.
  - `trading-evaluation`: 19 tests, compileall, diff-check.
- Cross-repository active-surface scan for old 9-layer naming was clean; historical migrations intentionally retain old names as audit records. All six repositories ended with clean git status after commit/push.

## Historical scheduler formally restarted

- Chentong approved startup repair after asking whether the system program could formally run.
- Stopped the stale/backoff `trading-manager-historical-scheduler.service` before repair; it had been active but stuck around `selected_target_symbol_required`.
- Migrated live database event source surfaces to current Layer 10 names:
  - `trading_data.source_10_event_risk_governor` is now the base table with 53,196 rows across `equity_abnormal_activity`, `macro_data`, `symbol_news`, `sector_news`, `earnings_guidance`, and `sec_filing`.
  - `trading_data.feature_10_event_risk_governor` is now the base feature table.
  - `source_09_event_risk_governor` and `feature_09_event_risk_governor` remain as read-only compatibility views over the Layer 10 tables.
- Ran bounded scheduler smoke windows:
  - Completed remaining Layer 1/2 foundation feature stages for `2016-04`, `2016-05`, and `2016-06`.
  - Confirmed `model_worker_1` selected `AAPL` from `runtime/model_training_target_queue.json` and began fold `2016-01_2016-06`.
- Restarted `trading-manager-historical-scheduler.service`; post-start status was active/running and continued executing with `last_work_selection_reason=selected_target_has_open_model_worker_fold` instead of backoff.

## Workspace memory and startup closeout

- Chentong asked to finish all remaining workspace closeout work after discovering uncommitted workspace edits and untracked memory/skill/media/tmp artifacts.
- Closed and pushed workspace memory/skill/rule updates in commit `1312ac0` (`Close workspace memory and skill updates`); `main` was aligned with `origin/main` and `git status` was clean afterward.
- The closeout submitted workspace rules, user preference/tool notes, long-term/daily memory, Codex oversight notes, and the `event-interpretation` skill update. `memory/2026-05-14-1255.md` was merged into canonical `memory/2026-05-14.md` and removed as a timestamped variant.
- Added `.gitignore` coverage so `media/` screenshots and `tmp/` database backups are not accidentally tracked; intentionally kept the local 2GB DB backup in place because Chentong had not explicitly requested irreversible deletion.
- After scheduler repair/startup, recorded and pushed the restart state in commit `d6f4d89` (`Record historical scheduler restart`).

## Dashboard read-model path repair

- Chentong reported the dashboard error `Dashboard data unavailable` with ENOENT for `/root/projects/trading-storage/storage/dashboard/read_models/current_system_status_summary/latest.json`.
- Root cause: `trading-storage` correctly materialized dashboard read models under the accepted `storage/06_dashboard_cache/read_models/` layout, while `trading-dashboard` Vite/Python adapters still read the old `storage/dashboard/read_models/` path.
- Fixed `trading-dashboard` to read `storage/06_dashboard_cache/read_models/<contract_type>/latest.json`, updated tests and active docs, rebuilt/restarted `trading-dashboard-web.service`, and corrected the installed unit's stale `Documentation=` path.
- Verification passed: dashboard Python tests, `npm run build`, `curl` checks for `current_system_status_summary` and `historical_task_progress_summary`, service active status, and repo clean after commit/push.
- Pushed `trading-dashboard` commit `85fb36b` (`Read dashboard models from storage cache`).

## Dashboard background-service inventory completed

- Chentong noticed the dashboard Current Status / Background Services panel was visibly incomplete.
- Root cause: `current_system_status_summary` hard-coded only three units instead of enumerating all installed `trading-*` unit files.
- Fixed `trading-storage` so the system-status producer enumerates `systemctl list-unit-files 'trading-*'`, merges systemd properties for service/timer/path units, records unit kind/type/load state, and treats inactive successful oneshot/disabled units as non-failing while still flagging actual failures.
- Fixed `trading-dashboard` labels/status rendering for the full service/timer/path inventory.
- Refreshed the live current-status read model and restarted `trading-dashboard-web.service`; the API now returns 10 background units. The only unhealthy unit is `trading-execution-realtime-monitor-loop.service`, which remains in auto-restart and should be diagnosed separately.
- Verification passed: `trading-storage` 112 tests, `trading-dashboard` 10 tests, `npm run build`, curl service-count check, service active checks, and clean repo states.
- Pushed `trading-storage` commit `f6b141f` (`Include all trading systemd units in dashboard status`) and `trading-dashboard` commit `9d42acf` (`Display full background service inventory`).

## Dashboard source data and source-connection coverage expanded

- Chentong asked whether Current Status / Dashboard Data had complete file coverage and suggested moving Trading Economics-related status into a renamed API/source-connection panel.
- Root cause: Dashboard Data covered only five manager runtime artifacts, so execution runtime, realtime monitor, Trading Economics, and storage-hosted dashboard read-model files were not visible in the freshness panel.
- Fixed `trading-storage` current-system-status producer so `source_outputs` now covers 16 cross-component files: manager scheduler/workflow/stage artifacts, execution runtime status, realtime monitor receipt/cycle files, Trading Economics recent/historical calendar artifacts, the dashboard read-model index, and all active dashboard `latest.json` read models.
- Added `source_connections` alongside backward-compatible `apis`, with Trading Economics Calendar Web included next to Alpaca, OKX, and ThetaData.
- Fixed `trading-dashboard` to render `Source Connections` instead of `API Connections` and use `source_connections` when available.
- Refreshed the live current-status read model and restarted `trading-dashboard-web.service`; the API now returns 4 source connections and 16 dashboard data source outputs.
- Verification passed: `trading-storage` 112 tests, `trading-dashboard` 10 tests, `npm run build`, `python3 -m compileall src`, curl source-count check, service active checks, and clean repo states.
- Pushed `trading-storage` commit `b870b8c` (`Expand dashboard source evidence coverage`) and `trading-dashboard` commit `a909838` (`Rename API panel to source connections`).

## TE refresh units moved into Source Connections

- Chentong clarified that Trading Economics Calendar Worker/Schedule should also live under Source Connections rather than Background Services.
- Updated `current_system_status_summary` source connections to include `Trading Economics Calendar Schedule` and `Trading Economics Calendar Worker` as first-class source-connection rows with their linked systemd units.
- Updated dashboard rendering to filter source-connection-linked units out of Background Services while still keeping their systemd health evidence in the payload.
- Refreshed the live current-status read model and restarted `trading-dashboard-web.service`; the API now returns 6 source connections and Background Services no longer includes TE worker/schedule.
- Verification passed: `trading-storage` dashboard-system-status test and full 112-test suite, `trading-dashboard` 10 tests, `npm run build`, curl source-connection check, and clean repo states.
- Pushed `trading-storage` commit `f18845c` (`Classify TE refresh units as source connections`) and `trading-dashboard` commit `dba06ac` (`Move source refresh units out of background services`).

## Current Status panel ordering

- Chentong asked to sort Current Status / Background Services and Dashboard Data rows.
- Updated `trading-dashboard` display ordering only: Background Services now sorts by dashboard refresh, historical automation, then execution runtime responsibilities; Dashboard Data now sorts dashboard read-model artifacts first, then manager, execution, and Trading Economics/source artifacts.
- Restarted `trading-dashboard-web.service`; verification passed with dashboard Python tests, `npm run build`, curl read-model check, service active check, and clean repo state.
- Pushed `trading-dashboard` commit `17c4ba5` (`Sort current status service and data panels`).

## Pre-compaction checkpoint at 2026-05-21 05:04 EDT

- Recent dashboard repairs are complete and pushed across repos:
  - `trading-dashboard` `85fb36b` fixed dashboard read-model path from old `storage/dashboard/read_models/` to `storage/06_dashboard_cache/read_models/`.
  - `trading-storage` `f6b141f` and `trading-dashboard` `9d42acf` expanded Background Services to all installed `trading-*` `.service`, `.timer`, and `.path` units.
  - `trading-storage` `b870b8c` and `trading-dashboard` `a909838` expanded Dashboard Data to 16 cross-component source outputs and renamed API Connections to Source Connections with Trading Economics included.
  - Workspace tracking commits were recorded for these repairs, including `8b46e61`, `8195e43`, and `1a6cc63`.
- Live dashboard state after refresh/restart: dashboard web active, historical scheduler active, dashboard read-model refresh timer active, API returning 4 source connections and 16 available dashboard data outputs.
- Remaining operational issue: `trading-execution-realtime-monitor-loop.service` is still the real unhealthy unit, in auto-restart, and should be diagnosed next rather than hidden by dashboard presentation fixes.

## Pre-compaction checkpoint at 2026-05-21 05:21 EDT

- Dashboard Current Status classification continued after the 05:04 checkpoint:
  - Chentong clarified that `Trading Economics Calendar Schedule` and `Trading Economics Calendar Worker` belong in Source Connections.
  - Implemented and pushed `trading-storage` commit `f18845c` (`Classify TE refresh units as source connections`).
  - Implemented and pushed `trading-dashboard` commit `dba06ac` (`Move source refresh units out of background services`).
  - Workspace tracking commit for this classification: `0fbefe2` (`Record TE source connection classification`).
- Source Connections now has 6 rows:
  - Alpaca Market Data API
  - OKX Market Data API
  - ThetaData Options API
  - Trading Economics Calendar Web
  - Trading Economics Calendar Schedule
  - Trading Economics Calendar Worker
- Dashboard Background Services and Dashboard Data display order was adjusted in `trading-dashboard`:
  - Background Services now groups dashboard refresh, historical automation, realtime monitor, and realtime runtime check responsibilities.
  - Dashboard Data now orders dashboard read-model files first, then manager runtime artifacts, execution/realtime artifacts, then Trading Economics source artifacts.
  - Pushed `trading-dashboard` commit `17c4ba5` (`Sort current status service and data panels`) and workspace commit `c962320` (`Record dashboard current status ordering`).
- Verification for the TE classification/order work passed:
  - `trading-storage` 112 tests OK.
  - `trading-dashboard` 10 tests OK.
  - `npm run build` OK.
  - Live dashboard API returned `source_connections = 6`; dashboard web was restarted and active.
- Benchmark location clarification:
  - Benchmark is not part of `trading-manager` monthly Layer workflow or the historical scheduler task route.
  - Benchmark belongs to `trading-evaluation` evaluation / promotion-readiness work.
  - Current benchmark line is the first candidate-policy replay benchmark with canonical replay window `2021-01-01` to `2026-01-01` end-exclusive.
  - Dataset preparation artifacts live under `trading-storage/storage/05_benchmark_datasets/<contract_id>/`.
  - `prepare_benchmark_dataset` is dataset preparation, not benchmark freeze, and is not currently shown in dashboard Historical Tasks.
  - Chentong corrected the dashboard grouping: benchmark should be visible inside the Model Evaluation task lane, not as a separate Background Service or a disconnected Dashboard Data item.
  - Implemented and pushed `trading-manager` commit `f68f15b` (`Show promotion benchmark in model evaluation tasks`): `historical_task_progress_summary.chart_payload.task_timeline` now appends evaluation-owned benchmark rows with `stage_type=model_evaluation`.
  - Live dashboard read model now includes 3 benchmark task rows:
    - `Promotion Benchmark Dataset Preparation`: completed.
    - `Promotion Benchmark Acquisition Coverage`: current/blocked, 360/360 feed acquisitions missing, provider acquisition requires a separate gate.
    - `Promotion Benchmark Freeze`: future/blocked on acquisition coverage and benchmark contract review.
  - Verification passed: `PYTHONPATH=src python3 -m unittest tests.test_dashboard_read_models`, `PYTHONPATH=src python3 -m compileall src tests`, `git diff --check`, storage refresh, and dashboard API returned 3 `evaluation_benchmark.*` tasks.
- Chentong clarified the final phase boundaries:
  - `Model Evaluation` should not absorb all benchmark setup work.
  - Benchmark dataset preparation/acquisition coverage belongs to `Data Acquisition`.
  - Frozen benchmark validation/replay evaluation belongs to `Model Evaluation`.
  - AUROC, Brier, return/drawdown/cost/hit-rate/payoff/turnover, PCA/PCoA fold-settlement metrics plus advisory `promotion-evaluation-review` belong to `Promotion Review`.
  - `Maintenance` is the post-run/fold cleanup/compress/archive/delete-with-receipt lifecycle phase.
- Reworked the dashboard task split and pushed `trading-manager` commit `3c16a64` (`Split benchmark setup from model evaluation tasks`).
- Live dashboard now returns 6 evaluation-related task rows:
  - `evaluation_benchmark.dataset_preparation`: `data_acquisition`, completed.
  - `evaluation_benchmark.acquisition_coverage`: `data_acquisition`, current/blocked, 360/360 feed acquisitions missing.
  - `evaluation_benchmark.freeze`: `model_evaluation`, future/blocked.
  - `evaluation_benchmark.replay_evaluation`: `model_evaluation`, future/blocked.
  - `evaluation_promotion.fold_settlement`: `promotion_review`, future/blocked, owns AUROC/PCA-style metrics.
  - `evaluation_promotion.readiness_review`: `promotion_review`, future/blocked, waits for settlement plus advisory agent review.
- Chentong asked to rerun everything after Model Generation after the task boundary correction.
  - Stopped `trading-manager-historical-scheduler.service`.
  - Backed up all 100 target-scoped fold checkpoint files to `/root/projects/trading-storage/storage/02_control_plane/runtime/state_backups/20260521T094303Z_post_model_generation_rerun`.
  - Reset 3000 post-model-generation fold stages (`model_evaluation`, `promotion_review`, `maintenance`) to pending without deleting artifacts: 200 previously succeeded and 2800 blocked stages were invalidated for rerun.
  - Refreshed all 100 fold checkpoints through `advance_workflow_state`; this yielded 200 ready model-evaluation stages, 600 preserved succeeded pre/equal model-generation stages, and 4200 blocked dependent stages.
  - Restarted `trading-manager-historical-scheduler.service`; it began executing rerun model-evaluation stages and dashboard refresh completed successfully.
  - Follow-up status shortly after restart: scheduler active, dashboard refresh successful, fold post-model statuses at 9 succeeded / 191 ready / 2800 blocked.
  - Observed existing scheduler limitation: once a fold's Layer 1/2 model evaluations rerun, Layer 3 target-chain data-acquisition may be ready but the model-worker selector currently only selects ready model-worker stages, not ready target-chain data/feature stages.
- Fixed the model-worker selector limitation and pushed `trading-manager` commit `0328ac2` (`Route fold prep stages through model worker`).
  - Scheduler fold selection now treats Layer 3+ fold-scoped data-acquisition and feature-generation stages as model-worker-routable prep stages.
  - Dashboard task timeline now exposes fold-scoped target-chain prep rows rather than hiding them behind model-only rows.
  - Verification passed: `PYTHONPATH=src python3 -m unittest tests.test_scheduler_daemon tests.test_dashboard_read_models`, `PYTHONPATH=src python3 -m compileall src tests`, and `git diff --check`.
  - Restarted `trading-manager-historical-scheduler.service`; it loaded the fix and executed `layer_03_target_state_vector.data_acquisition`, confirming the previous selector stall is cleared.
  - Follow-up post-model status after restart: 55 succeeded / 145 ready / 2800 blocked, with the scheduler still active.
- Remaining operational issue is unchanged: `trading-execution-realtime-monitor-loop.service` is still the real unhealthy unit and should be diagnosed separately.

## Dashboard task progress completion at 2026-05-21 05:58 EDT

- Chentong asked that every task's expanded `Current progress` section show realtime progress instead of being blank for non-current/future tasks.
- Implemented and pushed `trading-manager` commit `775da78` (`Show progress for every dashboard task`):
  - `historical_task_progress_summary.chart_payload.task_timeline[*].detail.progress` is now always populated.
  - Existing manager stage-coverage progress still overrides the generic status progress when coverage evidence exists.
  - Generic progress now maps succeeded/not_applicable to complete, failed/latest-failed to failed, running to partial, and ready/blocked/pending to pending.
  - Evaluation benchmark/promotion-review synthetic tasks also receive progress even when they do not have acquisition coverage rows.
- Implemented and pushed `trading-dashboard` commit `74b2957` (`Render task progress fallback consistently`):
  - The task detail panel now renders the fallback `Current progress` card for any task missing producer progress, not just current tasks.
- Verification passed:
  - `trading-manager`: `PYTHONPATH=src python3 -m unittest tests.test_dashboard_read_models tests.test_scheduler_daemon`, `PYTHONPATH=src python3 -m compileall src tests`, `git diff --check`.
  - `trading-dashboard`: `npm test`, `npm run build`, `git diff --check`.
  - Refreshed public dashboard read models successfully.
  - Restarted `trading-dashboard-web.service`; dashboard web, historical scheduler, and read-model refresh timer were active.
  - Live dashboard API check: `historical_task_progress_summary` had 1422 timeline tasks and 0 missing `detail.progress` entries.

## Dashboard work-unit progress and Diagnostics repair status at 2026-05-21 06:15 EDT

- Chentong clarified that `Current progress` should use task-specific work units, not just status fallback: e.g. 2 years of a 5-year benchmark should show 40%, and 10 days of a 1-month download should show roughly 30%.
- Implemented and pushed `trading-manager` commit `784d7af` (`Use work-unit dashboard progress`):
  - Added `unit_label` to progress payloads.
  - Benchmark acquisition coverage now reports `source-months` units.
  - Benchmark freeze, replay evaluation, and fold settlement now report 60-month work-unit progress for the 2021-01..2026-01 benchmark window, so future replay logs with 24 completed months would show 40%.
  - Existing manager stage coverage remains request-granular; day-level download progress will require component receipts/coverage to emit expected/covered day counts before the dashboard can show 10-days-of-1-month semantics without guessing.
  - Fixed Diagnostics parsing for OpenClaw agent runner stdout by reading `result.meta.finalAssistantRawText` / `finalAssistantVisibleText`, recognizing `repaired_verified`, resolving SQL catalog `storage/runtime/...` diagnosis refs to control-plane runtime paths, and reading SQL-backed server error catalog rows by default for the live storage root.
- Implemented and pushed `trading-dashboard` commit `384602f` (`Label task progress units`), so task detail progress reads like `0/60 months` or `0/360 source-months` instead of always saying `ready`.
- Verification passed:
  - `trading-manager`: 44 dashboard/scheduler tests OK, compileall OK, diff-check OK.
  - `trading-dashboard`: 10 tests OK, build OK, diff-check OK.
  - Refreshed dashboard read models and restarted `trading-dashboard-web.service`.
  - Live API showed 1422 task rows, 0 missing progress entries; benchmark tasks expose month/source-month units.
  - Live Diagnostics status after parser fix: `ERR-000002` repaired/closed, `ERR-000003` repaired/closed, `ERR-000004` repaired/awaiting_retry, `ERR-000001` not_supported/open.

## Diagnostics ref namespace and realtime monitor health at 2026-05-21 06:24 EDT

- Chentong reported Diagnostics rows mixing formal `ERR-000004` refs with generated hash-like `ERR-B48AEE2F` rows and `[object Object]` root-cause text.
- Fixed `trading-dashboard` so formal `ERR-*` refs are reserved for server-error catalog entries; generated non-agent diagnostics now use category prefixes such as `SVC-*`, `SRC-*`, `READMODEL-*`, `DATA-*`, `TASK-*`, and `ISSUE-*`, and the Diagnostics table heading is `Ref`.
- Fixed dashboard diagnostic text extraction so structured agent output displays bounded summary/message/reason/detail text instead of `[object Object]`; issue-ref titles now use `issue_type` when present.
- Fixed `trading-manager` agent-error summary producer to sanitize structured `root_cause` and `retry_recommendation` values into text before writing dashboard read models.
- Fixed `trading-storage` current-system-status health logic: a service in `activating/auto-restart` with `Result=success` is treated as healthy between planned cycles. This matches `trading-execution-realtime-monitor-loop.service`, which runs one cycle, exits 0, writes realtime monitor receipts, then waits for systemd `RestartSec=60`.
- Refreshed public dashboard read models and restarted `trading-dashboard-web.service`. Live `current_system_status_summary` is now `healthy/info` with empty `issue_refs`; realtime monitor remains `activating/auto-restart` but `healthy: true`; `ERR-000004` root cause is now text and remains `awaiting_retry`.

## Pre-compaction continuity at 2026-05-21 06:06 EDT

- Active work state:
  - Chentong approved the corrected dashboard phase split and requested a rerun of all fold stages after `Model Generation`; this was done by checkpoint-state reset only, without deleting model artifacts.
  - Post-model rerun is ongoing under `trading-manager-historical-scheduler.service`; the service was active after the scheduler selector fix.
  - The fold-prep routing bug has been fixed and verified: Layer 3+ fold-scoped `data_acquisition` / `feature_generation` prep stages are now selected by the model worker when ready.
  - Current dashboard task timeline behavior is complete: all tasks, including ready/blocked/future/synthetic benchmark rows, should expose `detail.progress` and the UI should render `Current progress`.
- Important commits from this work:
  - `trading-manager` `3c16a64`: `Split benchmark setup from model evaluation tasks`.
  - `trading-manager` `0328ac2`: `Route fold prep stages through model worker`.
  - `trading-manager` `775da78`: `Show progress for every dashboard task`.
  - `trading-dashboard` `74b2957`: `Render task progress fallback consistently`.
- Dashboard/evaluation boundary now accepted:
  - Benchmark dataset preparation and acquisition coverage are `Data Acquisition`.
  - Benchmark freeze and replay evaluation are `Model Evaluation`.
  - AUROC/Brier/return/drawdown/cost/hit-rate/payoff/turnover plus PCA/PCoA diagnostics and advisory promotion agent review are `Promotion Review`.
  - Maintenance is storage lifecycle cleanup/compress/archive/delete-with-receipt after run/fold/benchmark close.
- Current known blockers / next checks:
  - Evaluation benchmark acquisition coverage remains blocked at 360/360 missing feed acquisitions and requires a separate acquisition/provider gate before freeze/replay can proceed.
  - `trading-execution-realtime-monitor-loop.service` remains the real unhealthy unit and has not been diagnosed in this thread.
  - After compaction, first operational check should be scheduler/dashboard status and post-model rerun counts before making further code changes.

## Pre-compaction diagnostics closeout at 2026-05-21 06:27 EDT

- Chentong asked why Diagnostics mixed formal names such as `ERR-000004` with UID/hash-like rows such as `ERR-B48AEE2F`; accepted boundary is now: `ERR-*` is reserved for formal server-error catalog records, while generated non-agent diagnostics use category refs (`SVC-*`, `SRC-*`, `READMODEL-*`, `DATA-*`, `TASK-*`, `ISSUE-*`).
- Final pushed commits from the diagnostics namespace / monitor-health closeout:
  - `trading-dashboard` `855f052` (`Clarify diagnostics references`)
  - `trading-storage` `68712c7` (`Treat successful monitor cycles as healthy`)
  - `trading-manager` `f3734b6` (`Sanitize agent error root cause text`)
  - workspace `5c4019c` recorded the memory closeout.
- Verification passed before compaction:
  - `trading-dashboard`: tests and build OK; dashboard web restarted.
  - `trading-storage`: relevant tests and compileall OK.
  - `trading-manager`: dashboard/scheduler tests, compileall, and diff-check OK.
- Current live Diagnostics interpretation at closeout:
  - `ERR-000001`: still real open `not_supported/error` for `layer_09_event_risk_governor.data_acquisition`; needs manual review or future deterministic support.
  - `ERR-000004`: `repaired/awaiting_retry/warning`; code type-mismatch fix verified, but original write stage still needs scheduler retry before closure.
  - `ERR-000002` and `ERR-000003`: repaired and closed notices.
  - `trading-execution-realtime-monitor-loop.service`: now considered healthy when in `activating/auto-restart` with `Result=success`, because the service intentionally exits 0 after each monitor cycle and waits for systemd restart.
- If this resumes after compaction, do not reopen the diagnostics ref-prefix issue; only recheck live dashboard/API state if Chentong asks for current status or if more diagnostics rows appear.
