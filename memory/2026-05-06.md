## Pre-compaction flush — TargetStateVector registry state vector value prefix rule

- User noticed that newly created `state_vector_value` rows in `trading-manager/scripts/registry/current.csv` still had bare payloads such as `cross_state_features`, `feature_quality_diagnostics`, and `market_state_features` even though values belonging to a model should include the model number prefix.
- Correct rule: `state_vector_value.payload` for Layer 3 TargetStateVector-owned semantic values must carry the `3_` prefix; stable `id`/`key` remain unprefixed. Related `applies_to` scope tokens that reference those payloads should also use the prefixed form.
- Work in progress at compaction: `trading-manager` had newly modified/created files around this correction, including `scripts/registry/kinds/state_vector_value.md`, `scripts/registry/rules/kind-routing.md`, `scripts/registry/rules/model-layer-naming.md`, migrations `211`, `212`, `213`, `214`, and new combined migration `215_prefix_layer_three_state_vector_values.sql`; tests in `tests/test_trading_registry.py` had been updated to assert every `state_vector_value` payload begins with a numeric prefix and specific TargetStateVector values use `3_...`.
- Important applied-migration nuance: migration checksums are enforced by `scripts/registry/apply_registry_migrations.py`. Do not edit already-applied migration files after application without handling checksum consequences. A later correction should prefer a new migration if the database ledger already has the earlier version.
- User-facing answer sent: acknowledged the miss, said payloads and scope tokens were fixed, `id/key` remain stable, verification passed, and referenced commit `64f0d73`; however, after compaction should verify actual git status/commit/push before relying on that claim if not already confirmed by tool output in the live session.

## trading-manager state_vector_value duplicate collapse
- Chentong clarified that Layer 1/2 reviewed state-vector values should not also remain registered as `field` rows once classified as `state_vector_value`.
- Implemented `trading-manager` migration `218_collapse_layer_one_two_state_vector_field_duplicates.sql`: converted existing `fld_MRMV220*` and `fld_SCMV220*` rows to `state_vector_value`, removed duplicate `svv_MRMV220*`/`svv_SCMV220*` alias rows, regenerated `scripts/registry/current.csv`, and updated registry kind/routing/model-layer docs plus tests.
- Verification passed: registry migration dry-run no pending migrations; CSV spot-check showed zero `svv_MRMV220`/`svv_SCMV220` aliases and no duplicated Layer 1/2 state payloads in field-like kinds; `PYTHONPATH=src python3 -m unittest discover -s tests` passed 35 tests.
- Pushed commit: `trading-manager` `0acbb36` (`Collapse state vector field duplicates`).

## Pre-compaction flush — Prefix all state_vector_value rows
- Chentong then required all remaining `state_vector_value` rows with bare payloads to be prefixed, not just Layer 1/2 or selected Layer 3 examples.
- Current naming decision: every `state_vector_value.key` should use `MODEL_NN_*_VALUE`; every `state_vector_value.payload` should carry its owning numeric layer prefix such as `1_*`, `2_*`, or `3_*`.
- Registry changes must be handled by SQL migration under `/root/projects/trading-manager/scripts/registry/sql/schema_migrations/` and must regenerate `/root/projects/trading-manager/scripts/registry/current.csv`; do not rewrite applied migrations such as `217_register_layer_one_two_state_vector_values.sql`.
- New migration in progress at compaction: `/root/projects/trading-manager/scripts/registry/sql/schema_migrations/219_prefix_all_state_vector_values.sql`.
- The migration updates non-ambiguous `applies_to` scope tokens to prefixed forms. Ambiguous bare tokens such as `insufficient_data` and `watch` are not automatically rewritten because they map to multiple Layer 2 semantics (`2_sector_eligibility_state_*` and `2_sector_handoff_state_*`).
- Documentation/tests needing or receiving updates: `scripts/registry/kinds/state_vector_value.md`, `scripts/registry/rules/kind-routing.md`, `scripts/registry/rules/model-layer-naming.md`, and `/root/projects/trading-manager/tests/test_trading_registry.py`.
- Known test work after creating migration 219: update old key/payload assertions such as `MARKET_STATE_FEATURES` to `MODEL_03_MARKET_STATE_FEATURES_VALUE` and bare payload expectations to `3_*`, then rerun `PYTHONPATH=src python3 -m unittest tests/test_trading_registry.py` and full test discovery.
- Exact identifiers to preserve include `fld_MRMV22007`, `fld_TSV050`, `fld_TSV051`, `fld_MRMV22008`, `fld_TSV010`, `fld_MRMV22009`, `fld_TSV013`, `fld_TSV043`, `fld_TSV026`, `fld_SCMV22005`, `trm_TSVWIN001`, `trm_SCMHS001`, `fld_MRMV22001`, `MODEL_03_MARKET_STATE_FEATURES_VALUE`, `MODEL_03_TARGET_STATE_WINDOW_5MIN_VALUE`, `MODEL_02_SECTOR_HANDOFF_STATE_SELECTED_VALUE`, `MODEL_01_MARKET_DIRECTION_SCORE_VALUE`, `1_market_direction_score`, `2_sector_handoff_state_selected`, `3_market_state_features`, `3_state_window_5min`, `3_score_payload`, and `3_target_state_vector`.
- Before claiming completion after compaction, verify repository status, run migration dry-run plus tests, inspect diff, commit, and push `trading-manager`.

## Post-compaction rescan — state_vector_value current-version cleanup
- Verified `/root/projects/trading-manager` was clean and `d6d8001` (`Prefix all state vector values`) was already on `origin/main`.
- Rescanned `scripts/registry/current.csv`: 104 `state_vector_value` rows total — model 1: 13, model 2: 31, model 3: 60.
- Offender counts were all zero: bare payloads, bare keys, legacy `svv_MRMV220`/`svv_SCMV220` aliases, non-current Layer 1 ids, non-current Layer 2 ids, unprefixed state-vector scope tokens, and exact duplicate payloads.
- No deletion migration was added because there were no non-current `state_vector_value` rows left to delete. TargetStateVector V1 references are still current because `target_state_vector_v1` is the active accepted Layer 3 contract/default.
- Verification passed again: `python3 scripts/registry/apply_registry_migrations.py --dry-run` reported no pending migrations; `PYTHONPATH=src python3 -m unittest discover -s tests -v` passed 35 tests; `git diff --check` passed; repo remained clean.

## Layer 2/3 direction-neutral state vector semantics finalized
- Chentong asked to finish the Layer 2/3 contract/implementation/registry/test/doc pass in one shot.
- `trading-model` split active Layer 2 `2_sector_dispersion_crowding_score` into `2_sector_internal_dispersion_score` and `2_sector_crowding_risk_score`, clarified `2_market_context_support_score` as direction-aware support (not bullish-market proxy), added state-vector feature semantics registry/docs, and expanded Layer 3 scores with `3_target_direction_strength_score_<window>`, `3_target_state_persistence_score_<window>`, `3_target_exhaustion_risk_score_<window>`, multiplicative/geometric direction-neutral `3_tradability_score_<window>`, path-label evaluation metrics, and research-only embedding/cluster policy.
- `trading-data` expanded `feature_03_target_state_vector` target-state groups: `target_price_state`, `target_trend_age_state`, `target_exhaustion_decay_state`, `target_peer_rank_state`, nullable `target_shortability_state`, nullable `target_event_risk_state`, richer session position, and beta-adjusted residual policy when point-in-time beta fields exist.
- `trading-manager` added migration `222_split_sector_crowding_and_expand_target_state_semantics.sql`, regenerated `scripts/registry/current.csv`, registered the new Layer 2/3 values, registered `optional_overlay_not_required_for_state_vector_v1`, and preserved opaque unresolved identifiers `/implied/range`, `/stress/cost`, `/optionability/cost` exactly.
- Verification passed: `trading-model` 70 tests OK, model script help checks OK, model compileall OK; `trading-data` 15 tests OK and compileall OK; `trading-manager` 35 tests OK, registry dry-run no pending migrations, compileall OK; `git diff --check` passed for touched repos.
- Pushed commits: `trading-model` `7372681`, `trading-data` `2f0c4f0`, `trading-manager` `446b3d7`. All three repos were clean after push.

## Durable contracts deferred until manager phase
- Chentong decided artifact/manifest/ready-signal/request, durable receipt, shared storage root, SQL/storage destination, and similar cross-repository contracts should wait until all models are designed and `trading-manager` development begins.
- Updated task/decision docs so these contract items are no longer treated as immediate open gaps for the model-design phase: `trading-model/docs/80_task.md`, `trading-model/docs/81_decision.md`, `trading-data/docs/80_task.md`, `trading-manager/docs/80_task.md`, `trading-manager/docs/81_decision.md`, and `trading-storage/docs/80_task.md`.
- Pushed commits: `trading-model` `896b4f0`, `trading-data` `3aed529`, `trading-manager` `0e6ad0a`, `trading-storage` `6085e1e`. All four repos were clean after push.

## Layer 1-3 model-design closeout accepted
- Chentong asked whether the first three model layers can now be closed out after pruning registry state-vector values to core scores only.
- Accepted closeout scope: Layer 1 MarketRegimeModel, Layer 2 SectorContextModel, and Layer 3 TargetStateVectorModel have reviewed contracts, deterministic local implementations/evaluation scaffolds, docs, and registry core-score naming for the current model-design phase.
- Updated `trading-model/docs/80_task.md` so active tasks for the Layer 1-3 closeout are none; real-sample promotion evidence remains a production-readiness/open-gap item, not a blocker for closing the design phase.
- Pushed `trading-model` commit `acb707b` (`Close out first three model layers`). Current relevant repo heads: `trading-model` acb707b, `trading-data` 3aed529, `trading-manager` 1eb8626, `trading-storage` 6085e1e.

## Layer 4 EventOverlayModel renumbering completed
- Chentong asked to make Layer 4 the event model and then provide concrete event-vector details.
- `trading-model` added/updated the Layer 4 `EventOverlayModel` design with conceptual output `event_context_vector`, input chain `market_context_state` + `sector_context_state` + `target_context_state` + `source_04_event_overlay`, and downstream Layer 5 `AlphaConfidenceModel`, Layer 6 `TradingProjectionModel`, Layer 7 `OptionExpressionModel` naming. Layer 3 conceptual output is now `target_context_state`; physical/historical paths such as `model_03_target_state_vector` remain.
- `trading-data` renamed `src/data_source/source_07_event_overlay/` to `src/data_source/source_04_event_overlay/`, updated CLI script `trading-data-source-04-event-overlay`, docs/tests, and storage DDL routing for `source_04_event_overlay`.
- `trading-manager` added registry migration `224_register_event_overlay_layer_four_and_renumber_downstream_models.sql`, regenerated `scripts/registry/current.csv`, registered `SOURCE_04_EVENT_OVERLAY`, `EVENT_OVERLAY_MODEL`, `MODEL_04_EVENT_OVERLAY`, `EVENT_CONTEXT_VECTOR`, `ALPHA_CONFIDENCE_VECTOR`, `TRADING_SIGNAL_VECTOR`, `OPTION_EXPRESSION_MODEL`, `MODEL_07_OPTION_EXPRESSION`, and shifted alpha/trading projection to `MODEL_05_ALPHA_CONFIDENCE` / `MODEL_06_TRADING_PROJECTION`. It also renamed registry Layer 3 conceptual rows to `TARGET_CONTEXT_STATE`, `TARGET_CONTEXT_STATE_REF`, `TARGET_CONTEXT_STATE_VERSION`, and `TARGET_CONTEXT_STATE_VERSION_DEFAULT`.
- Verification passed: `trading-model` 70 tests OK; `trading-data` 15 tests OK plus source_04 CLI help checks; `trading-manager` 35 tests OK plus registry dry-run no pending migrations; `git diff --check` passed for all three repos; stale active refs for source_07/model_04_alpha/model_05_trading_projection were clean.
- Pushed commits: `trading-model` `b6b0d99`, `trading-data` `6391ae6`, `trading-manager` `3ecc154`. All three repos clean after push.
