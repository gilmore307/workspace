
## Manager SQL control-plane rows written for QQQ/SPY/TLT receipts

- Chentong said `gp`, interpreted as go/proceed with the recommended next step.
- Persisted the Layer 1 2016-01 manager request/input-binding rows for the 22 prepared Alpaca bar tasks using `prepare_layer_one_historical_training.py --write`; this wrote `manager_request=22` and `input_binding=22` with `provider_calls=0`.
- Persisted normalized completion receipt rows for the three real small-batch provider runs QQQ/SPY/TLT using `record_completion_receipt.py --write`.
- DB row counts afterward: `trading_manager.manager_request=22`, `input_binding=22`, `run_manifest=3`, `artifact_ref=6`, `ready_signal=3`.
- `task_summary` now shows QQQ/SPY/TLT as `task_status=ready`, `latest_run_status=succeeded`, `latest_ready_signal_status=ready`, and `artifact_count=2` each.
- Artifact refs in SQL are canonical: receipt refs are `storage://trading-data/.../completion_receipt.json`; output refs are `storage://trading-data/.../saved/equity_bar.csv`; output artifact kind is `equity_bar`, media type `text/csv`, row counts QQQ 1000 / SPY 1000 / TLT 445.
- No model activation, broker/execution, storage lifecycle mutation, or additional provider calls were performed during this SQL persistence step.

## Manager stage coverage gate implemented

- Chentong approved continuing after SQL task-summary showed QQQ/SPY/TLT ready individually.
- Implemented `manager_stage_coverage_v1` in `trading-manager` to distinguish task-level readiness from workflow-stage completion.
- Added `src/trading_manager_tasks/stage_coverage.py` and CLI `scripts/tasks/check_stage_coverage.py`.
- Added workflow ingestion for `--stage-coverage-report`; partial coverage attaches evidence/reason only, while a stage can become `succeeded` only when the coverage report has `status=ready` and `can_unlock_downstream=true`.
- Registered migration `286_register_stage_coverage_gate.sql` and regenerated `scripts/registry/current.csv`.
- Current real Layer 1 January 2016 coverage report: `partial_ready`, `ready_count=3`, `expected_count=22`, `can_unlock_downstream=false`.
- Wrote coverage evidence to `trading-manager/storage/runtime/stage_coverage/layer_01_market_regime_data_acquisition_2016-01.json` and refreshed workflow state so `layer_01_market_regime.data_acquisition` remains `ready` with reason `stage coverage partial 3/22; downstream remains blocked`, while `layer_01_market_regime.feature_generation` remains `blocked`.
- Verification passed: `trading-manager` 122 tests OK, registry dry-run no pending migrations, compileall OK, diff-check OK.
- Pushed commit `9af42a8` (`Add stage coverage gate`).

## Stage coverage gate added after SQL receipt persistence

- Chentong approved continuing mechanism completion after SQL persistence of the three real QQQ/SPY/TLT receipt rows.
- Implemented `manager_stage_coverage_v1` in `trading-manager` so task-level `ready_signal_v1` rows do not imply full workflow-stage completion.
- Added `src/trading_manager_tasks/stage_coverage.py` and wrapper `scripts/tasks/check_stage_coverage.py`.
- Added workflow-state ingestion of stage coverage reports through `advance_model_training_workflow.py --stage-coverage-report PATH`.
- Registered the contract/script/unlock policy through migration `286_register_stage_coverage_gate.sql` and regenerated `scripts/registry/current.csv`.
- Current real Layer 1 January 2016 coverage from manager SQL is `partial_ready`: 3 ready of 22 expected, `can_unlock_downstream=false`.
- Workflow state remains correct: `layer_01_market_regime.data_acquisition` stays `ready`, `layer_01_market_regime.feature_generation` stays `blocked`, with reason `stage coverage partial 3/22; downstream remains blocked`.
- Verification passed: `trading-manager` 122 tests OK, registry dry-run no pending migrations, compileall OK, diff-check OK.
- Pushed `trading-manager` commit `9af42a8` (`Add stage coverage gate`).
- Recommended next mechanism step before expanding the remaining 19 provider requests: enrich component-specific artifact discovery so ready signals carry stronger output metadata.

## Component-specific artifact discovery implemented

- Continued after Chentong approved the recommended mechanism-hardening step.
- Enhanced `trading-manager` completion receipt normalization so final `outputs` remain downstream output artifacts while `steps.*.references` become supporting component artifacts.
- Duplicate artifact references are collapsed, so the saved CSV is not counted twice when it appears in both `outputs` and the `save` step.
- The real QQQ/SPY/TLT receipts now normalize to 21 rows total: 3 `run_manifest`, 15 `artifact_ref`, and 3 `ready_signal` rows.
- Per real small-batch run, manager SQL now has 5 artifact refs: `component_completion_receipt`, `equity_bar`, `clean_equity_bar`, `clean_schema`, and `request_manifest`; each ready signal now references all 5 artifacts.
- `task_summary` now shows artifact_count=5 for QQQ/SPY/TLT while stage coverage remains correctly `partial_ready` 3/22 with `can_unlock_downstream=false`.
- Added registry migration `287_register_component_artifact_discovery_policy.sql` and regenerated `scripts/registry/current.csv`.
- Verification passed: `trading-manager` 123 tests OK, registry dry-run no pending migrations, compileall OK, diff-check OK.
- Pushed `trading-manager` commit `ae859ca` (`Discover component receipt artifacts`).
- No additional provider calls, model activation, broker execution, or storage lifecycle mutation were performed.

## Remaining approved Layer 1 provider requests executed and no-data failure path hardened

- Chentong approved continuing with the remaining Layer 1 provider requests.
- Identified 19 not-ready January 2016 Alpaca bar requests from manager SQL; plan-only approval validation passed for all 19 under `lcav1_layer_01_market_regime_2016_01_alpaca_bars_20260510` with zero provider calls.
- Executed the approved remaining requests. The first BITW dispatch failed and exposed that manager provider dispatch aborted the batch on one component failure. Hardened `trading-manager` provider dispatch with `--continue-on-error` so per-request failures produce failed receipts/control-plane evidence instead of losing the rest of an approved batch.
- Continued the remaining 18 with `--continue-on-error`: 15 succeeded, 3 failed. Together with BITW, the 4 failed January 2016 symbols were BITW, ETHA, FSOL, and IBIT. The error was `AlpacaBarsError: Alpaca field 'bars' was not a list`, consistent with current crypto ETF symbols lacking 2016 bars / Alpaca returning a null no-data shape.
- Persisted all 19 new completion receipts into manager SQL. Task summary now has 18 ready succeeded requests and 4 failed requests for the 22 Layer 1 January 2016 tasks.
- Stage coverage is now `failed`: ready 18, failed 4, expected 22, `can_unlock_downstream=false`. Workflow state keeps `layer_01_market_regime.data_acquisition` ready with reason `4/22 requests failed; downstream remains blocked`; feature generation remains blocked.
- Fixed `trading-data` Alpaca bars feed to treat provider `bars: null` no-data responses as empty successful acquisitions with manifest/schema/CSV headers and row count 0, instead of failed receipts. This supports historical months where current-universe symbols did not yet have bars.
- Verification passed: `trading-manager` 124 tests OK, registry dry-run no pending, compileall OK, diff-check OK; `trading-data` 34 tests OK, compileall src OK, diff-check OK.
- Pushed commits: `trading-manager` `7d0d886` (`Continue approved provider dispatch failures`) and `trading-data` `6a144b0` (`Treat Alpaca null bars as empty data`).
- No model activation, broker execution, or storage lifecycle mutation occurred. Existing live-call approval budget has effectively been consumed by the 22 provider attempts; rerunning the 4 fixed no-data symbols should require a fresh explicit approval artifact.

## Manager SQL control-plane rows written for QQQ/SPY/TLT receipts

- Chentong said `gp`, interpreted as go/proceed with the recommended next step.
- Persisted the Layer 1 2016-01 manager request/input-binding rows for the 22 prepared Alpaca bar tasks using `prepare_layer_one_historical_training.py --write`; this wrote `manager_request=22` and `input_binding=22` with `provider_calls=0`.
- Persisted normalized completion receipt rows for the three real small-batch provider runs QQQ/SPY/TLT using `record_completion_receipt.py --write`.
- DB row counts afterward: `trading_manager.manager_request=22`, `input_binding=22`, `run_manifest=3`, `artifact_ref=6`, `ready_signal=3`.
- `task_summary` now shows QQQ/SPY/TLT as `task_status=ready`, `latest_run_status=succeeded`, `latest_ready_signal_status=ready`, and `artifact_count=2` each.
- Artifact refs in SQL are canonical: receipt refs are `storage://trading-data/.../completion_receipt.json`; output refs are `storage://trading-data/.../saved/equity_bar.csv`; output artifact kind is `equity_bar`, media type `text/csv`, row counts QQQ 1000 / SPY 1000 / TLT 445.
- No model activation, broker/execution, storage lifecycle mutation, or additional provider calls were performed during this SQL persistence step.

## Manager stage coverage gate implemented

- Chentong approved continuing after SQL task-summary showed QQQ/SPY/TLT ready individually.
- Implemented `manager_stage_coverage_v1` in `trading-manager` to distinguish task-level readiness from workflow-stage completion.
- Added `src/trading_manager_tasks/stage_coverage.py` and CLI `scripts/tasks/check_stage_coverage.py`.
- Added workflow ingestion for `--stage-coverage-report`; partial coverage attaches evidence/reason only, while a stage can become `succeeded` only when the coverage report has `status=ready` and `can_unlock_downstream=true`.
- Registered migration `286_register_stage_coverage_gate.sql` and regenerated `scripts/registry/current.csv`.
- Current real Layer 1 January 2016 coverage report: `partial_ready`, `ready_count=3`, `expected_count=22`, `can_unlock_downstream=false`.
- Wrote coverage evidence to `trading-manager/storage/runtime/stage_coverage/layer_01_market_regime_data_acquisition_2016-01.json` and refreshed workflow state so `layer_01_market_regime.data_acquisition` remains `ready` with reason `stage coverage partial 3/22; downstream remains blocked`, while `layer_01_market_regime.feature_generation` remains `blocked`.
- Verification passed: `trading-manager` 122 tests OK, registry dry-run no pending migrations, compileall OK, diff-check OK.
- Pushed commit `9af42a8` (`Add stage coverage gate`).

## Stage coverage gate added after SQL receipt persistence

- Chentong approved continuing mechanism completion after SQL persistence of the three real QQQ/SPY/TLT receipt rows.
- Implemented `manager_stage_coverage_v1` in `trading-manager` so task-level `ready_signal_v1` rows do not imply full workflow-stage completion.
- Added `src/trading_manager_tasks/stage_coverage.py` and wrapper `scripts/tasks/check_stage_coverage.py`.
- Added workflow-state ingestion of stage coverage reports through `advance_model_training_workflow.py --stage-coverage-report PATH`.
- Registered the contract/script/unlock policy through migration `286_register_stage_coverage_gate.sql` and regenerated `scripts/registry/current.csv`.
- Current real Layer 1 January 2016 coverage from manager SQL is `partial_ready`: 3 ready of 22 expected, `can_unlock_downstream=false`.
- Workflow state remains correct: `layer_01_market_regime.data_acquisition` stays `ready`, `layer_01_market_regime.feature_generation` stays `blocked`, with reason `stage coverage partial 3/22; downstream remains blocked`.
- Verification passed: `trading-manager` 122 tests OK, registry dry-run no pending migrations, compileall OK, diff-check OK.
- Pushed `trading-manager` commit `9af42a8` (`Add stage coverage gate`).
- Recommended next mechanism step before expanding the remaining 19 provider requests: enrich component-specific artifact discovery so ready signals carry stronger output metadata.

## Component-specific artifact discovery implemented

- Continued after Chentong approved the recommended mechanism-hardening step.
- Enhanced `trading-manager` completion receipt normalization so final `outputs` remain downstream output artifacts while `steps.*.references` become supporting component artifacts.
- Duplicate artifact references are collapsed, so the saved CSV is not counted twice when it appears in both `outputs` and the `save` step.
- The real QQQ/SPY/TLT receipts now normalize to 21 rows total: 3 `run_manifest`, 15 `artifact_ref`, and 3 `ready_signal` rows.
- Per real small-batch run, manager SQL now has 5 artifact refs: `component_completion_receipt`, `equity_bar`, `clean_equity_bar`, `clean_schema`, and `request_manifest`; each ready signal now references all 5 artifacts.
- `task_summary` now shows artifact_count=5 for QQQ/SPY/TLT while stage coverage remains correctly `partial_ready` 3/22 with `can_unlock_downstream=false`.
- Added registry migration `287_register_component_artifact_discovery_policy.sql` and regenerated `scripts/registry/current.csv`.
- Verification passed: `trading-manager` 123 tests OK, registry dry-run no pending migrations, compileall OK, diff-check OK.
- Pushed `trading-manager` commit `ae859ca` (`Discover component receipt artifacts`).
- No additional provider calls, model activation, broker execution, or storage lifecycle mutation were performed.

## Remaining approved Layer 1 provider requests executed and no-data failure path hardened

- Chentong approved continuing with the remaining Layer 1 provider requests.
- Identified 19 not-ready January 2016 Alpaca bar requests from manager SQL; plan-only approval validation passed for all 19 under `lcav1_layer_01_market_regime_2016_01_alpaca_bars_20260510` with zero provider calls.
- Executed the approved remaining requests. The first BITW dispatch failed and exposed that manager provider dispatch aborted the batch on one component failure. Hardened `trading-manager` provider dispatch with `--continue-on-error` so per-request failures produce failed receipts/control-plane evidence instead of losing the rest of an approved batch.
- Continued the remaining 18 with `--continue-on-error`: 15 succeeded, 3 failed. Together with BITW, the 4 failed January 2016 symbols were BITW, ETHA, FSOL, and IBIT. The error was `AlpacaBarsError: Alpaca field 'bars' was not a list`, consistent with current crypto ETF symbols lacking 2016 bars / Alpaca returning a null no-data shape.
- Persisted all 19 new completion receipts into manager SQL. Task summary now has 18 ready succeeded requests and 4 failed requests for the 22 Layer 1 January 2016 tasks.
- Stage coverage is now `failed`: ready 18, failed 4, expected 22, `can_unlock_downstream=false`. Workflow state keeps `layer_01_market_regime.data_acquisition` ready with reason `4/22 requests failed; downstream remains blocked`; feature generation remains blocked.
- Fixed `trading-data` Alpaca bars feed to treat provider `bars: null` no-data responses as empty successful acquisitions with manifest/schema/CSV headers and row count 0, instead of failed receipts. This supports historical months where current-universe symbols did not yet have bars.
- Verification passed: `trading-manager` 124 tests OK, registry dry-run no pending, compileall OK, diff-check OK; `trading-data` 34 tests OK, compileall src OK, diff-check OK.
- Pushed commits: `trading-manager` `7d0d886` (`Continue approved provider dispatch failures`) and `trading-data` `6a144b0` (`Treat Alpaca null bars as empty data`).
- No model activation, broker execution, or storage lifecycle mutation occurred. Existing live-call approval budget has effectively been consumed by the 22 provider attempts; rerunning the 4 fixed no-data symbols should require a fresh explicit approval artifact.

## Failed Layer 1 January 2016 symbols explained as not-yet-listed instruments

- Chentong asked to check whether the 4 failed 2016-01 Alpaca symbols were simply not launched yet.
- Conclusion: yes, the four failures are historical eligibility/listing-date issues, not missing pipeline output.
- `BITW`: Bitwise 10 Crypto Index Fund inception reported as 2017-11-22 and public OTCQX quotation/trading began around 2020-12-09; no 2016-01 trade bars should exist.
- `ETHA`: iShares Ethereum Trust ETF inception date reported as 2024-06-24; no 2016-01 bars should exist.
- `FSOL`: Fidelity Solana Fund appears to be a 2025 product; no 2016-01 bars should exist.
- `IBIT`: iShares Bitcoin Trust ETF / spot bitcoin ETF trading began in January 2024; no 2016-01 bars should exist.
- Recommended mechanism correction: treat these as `no_data_not_yet_listed` / historical eligibility evidence in Layer 1 backfill planning or stage coverage, rather than failed requests requiring real bars.

## Missing-data tolerance policy accepted and registered

- Chentong clarified that models should have fault tolerance and must handle missing data.
- Implemented this as a durable project policy rather than treating it as a chat-only note.
- `trading-model` now documents Layer 1 missing-data tolerance: not-yet-listed symbols, reviewed provider no-data responses, and insufficient minimum history should reduce coverage/data-quality diagnostics rather than crash model construction or fabricate bars. Added a unit test confirming missing model values reduce coverage without crashing evaluation.
- `trading-manager` registered migration `289_register_missing_data_tolerance_policy.sql` with `MODEL_MISSING_DATA_TOLERANCE_POLICY`; docs now clarify that terminal stage coverage means every expected request has reviewed terminal evidence, not that every symbol produced positive rows. Zero-row no-data receipts may count as ready coverage; provider/schema failures remain failures until corrected or reclassified.
- `trading-data` Layer 1 docs now state that valid absent historical bars should be preserved as explicit receipt/manifest evidence with schema headers and no fabricated bars.
- Verification passed: `trading-model` 101 tests OK; `trading-manager` 124 tests OK, registry dry-run no pending migrations; `trading-data` 34 tests OK; compileall and diff-check passed for all touched repos.
- Pushed commits: `trading-model` `eed7ccd`, `trading-manager` `e87838d`, `trading-data` `220364a`.

## Stage coverage accepted-failure policy implemented

- Chentong corrected the missing-data handling interpretation: the January 2016 Layer 1 state should remain `18 ready / 4 failed`, not be rewritten to `22 ready / 0 failed`; after agent verification that the four failures are normal not-yet-listed historical absences, the stage is allowed to pass.
- Implemented `trading-manager` accepted-failure coverage support in `src/trading_manager_tasks/stage_coverage.py`: `failed_count` is preserved, `accepted_failed_count` is separate, and `ready_count + accepted_failed_count >= expected_count` can unlock downstream only when all failed request ids are explicitly accepted by reviewed evidence.
- Added CLI flags to `check_stage_coverage.py` via stage_coverage main: repeated `--accepted-failure-request-id` and `--accepted-failure-ref`.
- Added migration `290_register_stage_coverage_accepted_failure_policy.sql`, registry key `MANAGER_STAGE_COVERAGE_ACCEPTED_FAILURE_POLICY`, and regenerated `scripts/registry/current.csv`.
- Added/updated tests and docs so failures are not rewritten as ready rows.
- Wrote local accepted-failure review artifact: `trading-manager/storage/runtime/stage_coverage/layer_01_market_regime_accepted_failures_2016-01.json`.
- Recomputed real Layer 1 January 2016 stage coverage: `status=ready`, `ready_count=18`, `failed_count=4`, `accepted_failed_count=4`, `pending_count=0`, `can_unlock_downstream=true`.
- Advanced workflow state: `layer_01_market_regime.data_acquisition=succeeded`; `layer_01_market_regime.feature_generation=ready`; next stage is `layer_01_market_regime.feature_generation`.
- Verification passed: `trading-manager` 126 tests OK, registry dry-run no pending migrations, compileall OK, diff-check OK.
- Pushed `trading-manager` commit `5d9924f` (`Accept reviewed stage coverage failures`).

## Agent failure-review requirement added

- Chentong clarified that every failure must receive agent evaluation/analysis before being accepted.
- Tightened `trading-manager` accepted-failure coverage: `accepted_failure_request_ids` now require at least one agent failure-review evidence reference; otherwise stage coverage raises `TaskSystemError` and cannot unlock downstream.
- Added migration `291_require_agent_failure_review.sql`, registry key `MANAGER_FAILED_REQUEST_AGENT_REVIEW_REQUIRED_POLICY`, and regenerated `scripts/registry/current.csv`.
- Updated docs to say a failed component request can use an accepted-failure exception only after agent evaluation of every failed request; failed task state remains visible.
- Updated local Layer 1 January 2016 accepted-failure review artifact to include per-symbol agent analysis for BITW, ETHA, FSOL, and IBIT under `manager_stage_coverage_accepted_failure_review_v1`.
- Recomputed coverage with the review ref: still `status=ready`, `ready_count=18`, `failed_count=4`, `accepted_failed_count=4`, `can_unlock_downstream=true`; workflow next stage remains `layer_01_market_regime.feature_generation`.
- Verification passed: `trading-manager` 127 tests OK, registry dry-run no pending migrations, compileall OK, diff-check OK.
- Pushed `trading-manager` commit `a85a2d9` (`Require agent review for accepted failures`).

## Manager failure register implemented

- Chentong clarified that all failures must be durably registered with current status; fixable failures should move to fixed/corrected after remediation, and normal historical absences should be recorded so future planning/dispatch can skip them directly.
- Implemented `trading_manager.failure_register` through migration `292_create_failure_register.sql` with statuses `observed`, `agent_review_required`, `retry_required`, `corrected`, `accepted_skip`, and `unresolved`; `accepted_skip`/`corrected` require `agent_review_ref`, and `skip_future_matching` is allowed only for `accepted_skip`.
- Added helper module `src/trading_manager_tasks/failure_register.py` plus CLIs `scripts/tasks/register_failure.py` and `scripts/tasks/list_failure_register.py`.
- Seeded the four known Layer 1 January 2016 accepted failures in SQL as `accepted_skip` / `no_data_not_yet_listed`: BITW, ETHA, FSOL, and IBIT. They preserve failed task history while enabling future matching dispatch to skip repeated provider calls.
- Wired stage coverage to read accepted-skip evidence from the failure register automatically; `check_stage_coverage.py` now reports Layer 1 January 2016 as `ready`, `18 ready`, `4 failed`, `4 accepted_failed`, `can_unlock_downstream=true` without manual accepted-failure flags.
- Wired provider dispatch with `--skip-registered-failures`; a BITW/ETHA/FSOL/IBIT plan returns `request_count=4`, `validation_count=0`, `dispatch_count=0`, `provider_calls=0`, all items `skipped_registered_accepted_failure`.
- Verification passed: `trading-manager` 131 tests OK, registry dry-run no pending migrations, compileall OK, diff-check OK.
- Pushed `trading-manager` commit `d3a4cd6` (`Add manager failure register`).

## Layer 1 January 2016 offline workflow stages completed

- Continued the formal `layer_01_market_regime` workflow after data acquisition reached failure-register-backed ready coverage: 18 successful Alpaca bar tasks plus 4 registered accepted-skip not-yet-listed failures.
- Added `trading-data/src/data_feature/feature_01_market_regime/from_feed_artifacts.py`, an offline bridge that reads successful saved `01_feed_alpaca_bars` `equity_bar.csv` artifacts, upserts them into `trading_data.source_01_market_regime`, and then generates `trading_data.feature_01_market_regime` rows with `provider_calls=0`.
- Updated `trading-manager` workflow commands so Layer 1 feature generation uses the artifact bridge with `${START_MONTH}`, Layer 1 evaluation uses `--from-database --output-json`, promotion review preparation consumes that summary with a conservative local fallback review, and safe stage execution resolves embedded month placeholders and runs manager task scripts from the manager repo.
- Registered migration `293_register_layer_one_feature_artifact_materialization.sql` and regenerated `scripts/registry/current.csv`.
- Executed safe offline stages for 2016-01: feature generation wrote 13,791 source rows and 13 feature rows; model generation wrote 13 model rows, 169 explainability rows, and 13 diagnostics rows; model evaluation read database rows and wrote `trading-model/storage/runtime/model_01_market_regime/evaluation_summary_2016-01.json`; promotion review preparation deferred promotion because evidence/thresholds are insufficient; maintenance planned a manager-side promotion-review request only.
- Final workflow state: all Layer 1 stages succeeded; next ready stage is `layer_02_sector_context.data_acquisition`. No provider calls, model activation, broker execution, or storage lifecycle mutation occurred during these offline stages.
- Verification passed before push: `trading-manager` 136 tests OK plus registry dry-run no pending migrations, compileall, diff-check; `trading-data` 36 tests OK plus compileall src and diff-check; `trading-model` 101 tests OK plus compileall and diff-check.
- Pushed commits: `trading-data` `e5203ce` (`Materialize Layer 1 feed artifacts`), `trading-manager` `8d906dc` (`Execute Layer 1 offline workflow stages`), and `trading-model` `1a76102` (`Persist Layer 1 evaluation summaries`).

## Layer 2 no-provider historical-training preparation implemented
- Continued Layer 2 `layer_02_sector_context` workflow investigation after Layer 1 offline closeout.
- Confirmed Layer 2 feature generation needs the reviewed sector/industry ETF bars, but prior monthly/historical preparation only expanded Layer 1 symbols.
- Generalized manager monthly backfill and historical-training preparation to support `layer_02_sector_context` ETF universe rows from `trading-storage/main/shared/market_regime_etf_universe.csv`.
- Added `scripts/tasks/prepare_layer_two_historical_training.py`; it prepares 25 Layer 2 Alpaca-bar requests/task keys/input bindings and validates handoff with `provider_calls=0`, no dispatch, no model activation, and no broker execution.
- Updated provider dispatch to select Layer 1/2 by `--model-layer`; actual live calls still require `live_call_approval_v1` and `--execute-approved-provider-calls`.
- Ran Layer 2 preparation for `2016-01` with `--write`: 25 requests, 25 payloads, 25 handoff validations, 0 provider calls. Workflow state now has `layer_02_sector_context.data_acquisition` blocked only on `live_call_approval_v1`.
- Added registry migration `294_register_layer_two_historical_training_preparation.sql` and exported `scripts/registry/current.csv`.

## Layer 2 historical-training preparation completed and pushed

- Completed the Layer 2 `layer_02_sector_context` safe planning/control-plane slice after adding no-provider historical-training preparation.
- Final Layer 2 state: 25 sector/industry ETF Alpaca-bar task keys/input bindings prepared for `2016-01`; handoff validations passed; `provider_calls=0`; no dispatch, model activation, broker execution, or storage lifecycle mutation.
- Workflow state now has `layer_02_sector_context.data_acquisition` blocked only on `live_call_approval_v1`, so actual Layer 2 provider calls require a fresh reviewed approval artifact and explicit execution flag.
- Verification passed in `trading-manager`: 140 tests OK, registry dry-run reported no pending migrations, compileall OK, and `git diff --check` OK.
- Pushed `trading-manager` commit `d4598b3` (`Prepare Layer 2 historical training batch`).
- Recommended next action after this point: if continuing, start with a small Layer 2 live-call smoke batch only after reviewing/approving `live_call_approval_v1`; otherwise continue hardening failure-register/coverage behavior before increasing provider volume.

## Layer 2 stage coverage/preflight skip hardening completed

- Continued from the Layer 2 approval-gated state without making provider calls.
- Found a mechanism gap: after Layer 2 manager requests were persisted, `manager_stage_coverage_v1` still matched Layer 1 by same-month Alpaca bars only, which could mix Layer 1 and Layer 2 rows.
- Patched `trading-manager/src/trading_manager_tasks/stage_coverage.py` so Layer 1 and Layer 2 coverage match by reviewed model-layer universe/request ids. Layer 2 coverage now supports `layer_02_sector_context.data_acquisition` with expected count 25.
- Added preflight accepted-skip semantics: reviewed not-yet-listed requests can be registered as `accepted_skip` before a known-useless provider call; coverage counts them as reviewed terminal skips, not ready output, and downstream remains blocked until ready + reviewed skips cover the expected count.
- Registered five Layer 2 January 2016 preflight accepted skips in `trading_manager.failure_register`: AIQ, ARKF, ARKX, BKCH, and XLC. Evidence artifact: `trading-manager/storage/runtime/stage_coverage/layer_02_sector_context_preflight_not_yet_listed_2016-01.json`; JSONL seed: `trading-manager/storage/runtime/stage_coverage/layer_02_sector_context_failure_register_preflight_2016-01.jsonl`.
- Layer 2 coverage artifact now reports `partial_ready`: 0 ready, 0 failed, 5 reviewed failed/skip, 20 pending, downstream blocked. Path: `trading-manager/storage/runtime/stage_coverage/layer_02_sector_context_data_acquisition_2016-01.json`.
- Added registry migration `295_register_layer_two_stage_coverage_and_preflight_skip.sql` and exported `scripts/registry/current.csv`.
- Verification passed: `trading-manager` 144 tests OK, registry dry-run no pending migrations, compileall OK, diff-check OK.
- Pushed `trading-manager` commit `c350e1e` (`Support Layer 2 stage coverage skips`).

## Layer 2 live-call approval planning hardening completed

- Continued improving Layer 2 safely without live provider calls.
- Added skip-aware live-call approval proposal tooling in `trading-manager`: `src/trading_manager_tasks/live_call_planning.py` and CLI `scripts/tasks/plan_live_call_approval.py`.
- The proposal emits `manager_live_call_approval_proposal_v1`, selects exact request ids for a future `live_call_approval_v1`, excludes `accepted_skip` rows from `trading_manager.failure_register`, and includes deliberately invalid review placeholders so it cannot be mistaken for approval.
- Generated a local Layer 2 smoke proposal for XLB/XLK/XLE at `trading-manager/storage/runtime/approvals/layer_02_sector_context/live_call_approval_proposal_layer_02_smoke_xlb_xlk_xle_2016-01.json`; it has 3 request ids, 0 skipped, `provider_calls=0`, and `dispatch_performed=false`.
- Registered proposal tooling in migration `296_register_live_call_approval_planning.sql`; registry export now has 642 rows.
- Updated workflow dispatch command templates to include `--skip-registered-failures` and still omit `--execute-approved-provider-calls`, so workflow plans are skip-aware and plan-only by default.
- Verification passed: `trading-manager` 146 tests OK, compileall OK, diff-check OK, registry dry-run no pending migrations.
- Pushed commits: `trading-manager` `4db48cc` (`Add live call approval proposal planning`) and `32e6f53` (`Use registered failure skips in workflow dispatch`).

## Layer 2 feature-artifact materialization path completed

- Continued safe mechanism hardening without provider calls.
- Added `trading-data/src/data_feature/feature_02_sector_context/from_feed_artifacts.py`, mirroring the Layer 1 artifact bridge for Layer 2.
- The Layer 2 bridge reads successful local `01_feed_alpaca_bars` completion receipts, materializes saved `equity_bar.csv` rows into the shared `trading_data.source_01_market_regime` table, then generates `trading_data.feature_02_sector_context`; it reports `provider_calls=0`, no model activation, and no broker execution.
- Updated `trading-manager` workflow so `layer_02_sector_context.feature_generation` uses `data_feature.feature_02_sector_context.from_feed_artifacts --month ${START_MONTH}` instead of assuming source rows were already materialized.
- Added registry migration `297_register_layer_two_feature_artifact_materialization.sql`; registry export now has 644 rows.
- Verification passed: `trading-data` 37 tests OK, compileall OK, diff-check OK; `trading-manager` 147 tests OK, compileall OK, registry dry-run no pending migrations, diff-check OK.
- Pushed commits: `trading-data` `3b07010` (`Materialize Layer 2 feed artifacts`) and `trading-manager` `65284d9` (`Use Layer 2 feed artifact materialization`).

## Provider-stage reconciliation automation completed

- Continued system hardening without live provider calls.
- Added `trading-manager/src/trading_manager_tasks/stage_reconcile.py` and CLI `scripts/tasks/reconcile_provider_stage.py`.
- New contract: `manager_provider_stage_reconcile_v1`.
- The reconcile tool discovers existing Layer 1/2 Alpaca completion receipts by reviewed universe/request id, normalizes them into `run_manifest_v1`, `artifact_ref_v1`, and `ready_signal_v1` rows, optionally persists manager control-plane rows, refreshes `manager_stage_coverage_v1`, writes a coverage report, and can ingest that written coverage report into workflow state.
- Safety boundary: it never dispatches components, calls providers, activates models, mutates broker state, or executes storage lifecycle actions. A dry-run Layer 2 reconcile found 0 receipts and reported `provider_calls=0`, `dispatch_performed=false`.
- Added registry migration `298_register_provider_stage_reconcile.sql`; registry export now has 646 rows.
- Updated task-system/runtime/script/test docs.
- Verification passed: `trading-manager` 150 tests OK, compileall OK, diff-check OK, registry dry-run no pending migrations.
- Pushed `trading-manager` commit `a344f00` (`Add provider stage reconciliation`).

## Failure proposal generation during provider-stage reconcile completed

- Continued hardening the provider-stage post-dispatch path without live provider calls.
- Extended `trading-manager/src/trading_manager_tasks/stage_reconcile.py` so failed component receipts produce `manager_failure_register_v1` proposal rows with `failure_status=agent_review_required`.
- The proposal path preserves failure facts but does not accept, skip, correct, or retry them; `skip_future_matching=false` and `agent_review_ref` remains empty until a later reviewed disposition.
- CLI additions to `scripts/tasks/reconcile_provider_stage.py`: `--write-failure-proposal`, `--failure-proposal-path`, and `--write-failure-register` for optional persistence of observed failures as review-required register rows.
- Added registry migration `299_register_stage_reconcile_failure_proposals.sql`; registry export now has 647 rows.
- Layer 2 dry probe wrote an empty proposal file at `trading-manager/storage/runtime/stage_coverage/layer_02_sector_context_failure_register_observed_proposals_2016-01.jsonl` because Layer 2 still has no receipts; probe reported `provider_calls=0`, `dispatch_performed=false`.
- Verification passed: `trading-manager` 152 tests OK, compileall OK, diff-check OK, registry dry-run no pending migrations.
- Pushed `trading-manager` commit `5779cde` (`Propose failure register rows during reconcile`).

## Live-call approval proposal-bound validation completed

- Continued mechanism hardening without live provider calls.
- Added proposal-bound approval validation in `trading-manager/src/trading_manager_tasks/live_call_planning.py` plus CLI `scripts/tasks/validate_live_call_approval_proposal.py`.
- New validation contract: `manager_live_call_approval_proposal_validation_v1`.
- The validator requires a reviewed `live_call_approval_v1` to exactly match `manager_live_call_approval_proposal_v1` request ids, reject any registered skip ids listed in the proposal, keep `max_requests` equal to proposal request count, keep providers/scope inside the proposal template, and then run normal `live_call_approval_v1` gate checks on regenerated non-dry-run manager requests.
- Validation remains plan-only: `provider_calls=0`, `dispatch_performed=false`, no model activation, broker mutation, or storage lifecycle mutation.
- Added registry migration `300_register_live_call_proposal_validation.sql`; registry export now has 649 rows.
- Smoke validated the local Layer 2 XLB/XLE/XLK approval proposal against a temporary reviewed test-only approval artifact: request_count=3, gate_validation_count=3, provider_calls=0, dispatch_performed=false.
- Verification passed: `trading-manager` 156 tests OK, compileall OK, diff-check OK, registry dry-run no pending migrations.
- Pushed `trading-manager` commit `1b8155e` (`Validate live call approvals against proposals`).

## Provider execution now requires proposal-bound validation evidence

- Continued hardening the live-provider safety path without making provider calls.
- Updated `trading-manager/src/trading_manager_tasks/provider_dispatch.py` and CLI `scripts/tasks/dispatch_approved_provider_acquisition.py` so actual `--execute-approved-provider-calls` requires `--approval-validation` pointing to a `manager_live_call_approval_proposal_validation_v1` artifact.
- The dispatch adapter now checks the validation artifact approval id, stage id, exact executable request ids, skip exclusion, gate validation count, `provider_calls=0`, and `dispatch_performed=false` before it will execute provider commands.
- Plan-only dispatch may still run with only approval validation; if `--approval-validation` is supplied in plan-only mode it is checked and reported, but no provider calls occur.
- Added registry migration `301_require_proposal_validation_before_provider_execute.sql`; registry export now has 650 rows.
- Smoke ran Layer 2 XLB/XLE/XLK dispatch in plan-only mode using the temporary proposal-validation artifact: request_count=3, validation_count=3, dispatch_count=0, provider_calls=0, dispatch_performed=false, approval_validation_ref set.
- Verification passed: `trading-manager` 158 tests OK, compileall OK, diff-check OK, registry dry-run no pending migrations.
- Pushed `trading-manager` commit `e0a4fcd` (`Require proposal validation before provider execute`).

## Live-call approval packet bundle generation completed

- Continued safety/operability hardening without live provider calls.
- Added `trading-manager/src/trading_manager_tasks/live_call_packet.py` and CLI `scripts/tasks/create_live_call_approval_packet.py`.
- New contract: `manager_live_call_approval_packet_v1`.
- The packet generator writes a local runtime bundle containing `proposal.json`, `reviewed_approval_TEMPLATE.json`, `packet.json`, `README.md`, validation output target, dispatch plan/execute command templates, and reconcile command template for one exact request set.
- Packet generation is non-dispatching: it does not approve, does not validate as reviewed, does not dispatch providers, and reports `provider_calls=0`, `dispatch_performed=false`.
- Added `--write --output-path` support to `validate_live_call_approval_proposal.py` so packet validation commands can materialize a proposal-validation artifact path.
- Added registry migration `302_register_live_call_approval_packet.sql`; registry export now has 652 rows.
- Smoke generated a Layer 2 XLB/XLK/XLE packet at `trading-manager/storage/runtime/approvals/layer_02_sector_context/layer_02_sector_context_2016_01_xlb_xle_xlk/`; proposal and reviewed-approval template exist, validation artifact is intentionally absent until reviewed approval is filled and validated. Smoke reported request_count=3, provider_calls=0, dispatch_performed=false.
- Verification passed: `trading-manager` 160 tests OK, compileall OK, diff-check OK, registry dry-run no pending migrations.
- Pushed `trading-manager` commit `ba17b28` (`Create live call approval packet bundles`).

## Live-call approval packet lifecycle closed

- Chentong asked to finish this section in one pass.
- Extended `manager_live_call_approval_packet_v1` so a packet now owns full local lifecycle targets: proposal, reviewed approval template, proposal validation artifact, dispatch plan summary, dispatch execute summary, reconcile summary, failure proposal JSONL, stage coverage output, and packet status output.
- Added read-only packet status contract `manager_live_call_approval_packet_status_v1` and CLI `scripts/tasks/inspect_live_call_approval_packet.py`.
- Statuses: `missing_packet`, `packet_incomplete`, `template_pending_review`, `approval_ready_pending_validation`, `approval_validated_pending_dispatch_plan`, `dispatch_plan_ready_pending_execute`, `executed_pending_reconcile`, `reconciled`, and `packet_inconsistent`.
- Status inspection reports next safe action and booleans for `can_validate_approval`, `can_plan_dispatch`, `can_execute_dispatch`, and `can_reconcile` without approving, dispatching, or calling providers.
- Added `--write --output-path` to provider dispatch summaries and `--write-summary --summary-output-path` to provider-stage reconcile so packet commands can materialize durable local evidence files.
- Re-generated the Layer 2 XLB/XLK/XLE packet and wrote `packet_status.json`; current status is `template_pending_review`, next action is to fill/review the approval template, and `provider_calls=0`.
- Added registry migration `303_register_live_call_packet_status.sql`; registry export now has 654 rows.
- Verification passed: registry dry-run no pending migrations, `trading-manager` 162 tests OK, compileall OK, diff-check OK.
- Pushed `trading-manager` commit `dbf6072` (`Close live call approval packet lifecycle`).

## Live-call approval packet rehearsal added

- Continued packet workflow hardening after access was restored, still without live provider calls.
- Added `manager_live_call_approval_packet_rehearsal_v1` and CLI `scripts/tasks/rehearse_live_call_approval_packet.py`.
- Rehearsal uses ephemeral temporary approval/validation files to run proposal-bound validation and plan-only dispatch, then discards them; it does not write a persistent approval, persistent validation artifact, persistent dispatch plan, or execute artifact into the packet.
- Packet status remains unchanged by rehearsal; for the Layer 2 XLB/XLK/XLE packet, status stayed `template_pending_review` before and after rehearsal.
- Extended packets with `manager_storage_root` so rehearsal/plan tooling can resolve task keys from the same manager storage root used during packet creation.
- Registered migration `304_register_live_call_packet_rehearsal.sql`; registry export now has 656 rows.
- Smoke result for Layer 2 XLB/XLK/XLE rehearsal: contract `manager_live_call_approval_packet_rehearsal_v1`, request_count=3, validation_count=3, dispatch_count=0, provider_calls=0, dispatch_performed=false, status_before/status_after both `template_pending_review`.
- Verification passed: registry migration apply + dry-run no pending, `trading-manager` 163 tests OK, compileall OK, diff-check OK.
- Pushed `trading-manager` commit `e16cdf4` (`Add live call packet rehearsal`).

## Layer 2 packet advanced to dispatch-plan-ready without provider calls

- Chentong approved continuing the runtime event after packet rehearsal.
- Filled the narrow Layer 2 XLB/XLE/XLK packet approval artifact at `trading-manager/storage/runtime/approvals/layer_02_sector_context/layer_02_sector_context_2016_01_xlb_xle_xlk/reviewed_approval_TEMPLATE.json`.
- Approval id: `lcav1_layer_02_sector_context_2016_01_alpaca_bars_xlb_xle_xlk_20260510`.
- Scope: provider data acquisition only for request ids `mgrreq_backfill_alpaca_bars_xlb_2016_01`, `mgrreq_backfill_alpaca_bars_xle_2016_01`, and `mgrreq_backfill_alpaca_bars_xlk_2016_01`; broker execution, model activation, and storage lifecycle mutation remain false.
- Wrote proposal validation artifact: `proposal_validation.json`; validation status `approved_against_live_call_proposal`, request_count=3, gate_validation_count=3, provider_calls=0, dispatch_performed=false.
- Wrote plan-only dispatch summary: `dispatch_plan.json`; request_count=3, validation_count=3, dispatch_count=0, provider_calls=0, dispatch_performed=false. All three items are `validated_not_dispatched`.
- Refreshed packet status: `packet_status.json`; status is now `dispatch_plan_ready_pending_execute`, `can_execute_dispatch=true`, `provider_calls=0`, `dispatch_performed=false`.
- No provider calls were executed and no git-tracked files changed because these are ignored runtime artifacts.

## Layer 2 XLB/XLE/XLK provider execution completed and reconciled

- Chentong explicitly said `执行`, approving execution of the already-reviewed narrow Layer 2 packet.
- Executed only the approved request ids: `mgrreq_backfill_alpaca_bars_xlb_2016_01`, `mgrreq_backfill_alpaca_bars_xle_2016_01`, and `mgrreq_backfill_alpaca_bars_xlk_2016_01`.
- Approval id used: `lcav1_layer_02_sector_context_2016_01_alpaca_bars_xlb_xle_xlk_20260510`; proposal validation artifact matched the exact request set.
- Dispatch summary `dispatch_execute.json`: request_count=3, validation_count=3, dispatch_count=3, provider_calls=3, dispatch_performed=true, all three `dispatched_succeeded`.
- Completion receipts:
  - XLB: succeeded, `equity_bar` rows=299.
  - XLE: succeeded, `equity_bar` rows=539.
  - XLK: succeeded, `equity_bar` rows=356.
- Reconciled provider stage with `reconcile_provider_stage.py`: discovered_receipt_count=3, normalized_run_manifest_count=3, normalized_artifact_ref_count=15, normalized_ready_signal_count=3, failure_proposal_count=0, persisted control-plane rows, wrote coverage, advanced workflow state.
- Packet status refreshed to `reconciled`: provider_calls=3, dispatch_performed=true, reconcile_ready=true, no missing files, no inconsistencies.
- Layer 2 coverage remains partial and downstream-blocking: ready_count=3, accepted_failed_count=5, failed_count=0, pending_count=17, status=`partial_ready`, can_unlock_downstream=false.
- No model activation, broker execution, or storage lifecycle mutation occurred. `trading-manager` and `trading-data` git statuses remained clean because artifacts are runtime/storage outputs.

## Pending-only live-call packet planning implemented

- Chentong emphasized the current goal is perfecting the process, not simply running more data.
- Identified a flow-safety gap: new live-call packets could be manually created for requests that were already ready or reviewed terminal, risking duplicate provider calls.
- Added pending-only planning to `trading-manager` live-call proposal/packet creation:
  - `plan_live_call_approval.py --pending-only`
  - `create_live_call_approval_packet.py --pending-only`
  - planning now reads `manager_stage_coverage_v1`, excludes ready request ids, excludes reviewed terminal accepted skips/failures, and blocks if unreviewed failed stage requests exist.
  - `--limit` now applies after registered-skip/terminal-coverage filtering, so batch size means actual pending calls, not first-N universe rows before exclusions.
- Provider execute command templates now include `--reject-terminal-coverage`; provider dispatch refuses execution for request ids already ready/reviewed-terminal when that guard is enabled, and refuses to continue while unreviewed failed requests exist.
- Added registry migration `305_register_pending_only_live_call_planning.sql`; registry export now has 658 rows.
- No-provider smoke created next Layer 2 pending-only packet with `--limit 5`: `layer_02_sector_context_2016_01_arkg_arkw_cibr_igv_iyt`.
  - Selected pending request ids: ARKG, ARKW, CIBR, IGV, IYT.
  - Skipped registered accepted ids: AIQ, ARKF, ARKX, BKCH, XLC.
  - Skipped terminal ready ids: XLB, XLE, XLK.
  - provider_calls=0, dispatch_performed=false, execute template contains `--reject-terminal-coverage`.
  - Status is `template_pending_review`; rehearsal passed with request_count=5, validation_count=5, dispatch_count=0, provider_calls=0, status unchanged.
- Verification passed: registry dry-run no pending migrations, `trading-manager` 167 tests OK, compileall OK, diff-check OK.
- Pushed `trading-manager` commit `a19c5eb` (`Plan live call packets from pending coverage`).

## Stage-run dashboard/receipt implemented

- Chentong wanted the flow simplified so humans do not need to inspect many scattered artifacts manually.
- Added `manager_stage_run_dashboard_v1` in `trading-manager/src/trading_manager_tasks/stage_run_dashboard.py` plus CLI `scripts/tasks/summarize_stage_run.py`.
- Dashboard is now the intended single human-facing entry point for a provider-stage/month. It summarizes:
  - `manager_stage_coverage_v1` counts/status and request ids;
  - discovered packet statuses under `storage/runtime/approvals/<model_layer>/...`;
  - observed provider calls/dispatch state from packet statuses;
  - next pending-only packet preview and create command;
  - next safe action and blocking reason;
  - evidence refs to underlying artifacts.
- Lower-level artifacts are preserved as audit/replay evidence, but operators should normally inspect dashboard first instead of manually opening proposal/approval/validation/dispatch/reconcile/coverage files.
- Added registry migration `306_register_stage_run_dashboard.sql`; registry export now has 660 rows.
- Smoke wrote Layer 2 dashboard to `trading-manager/storage/runtime/stage_run_dashboard/layer_02_sector_context_data_acquisition_2016-01.json`.
  - Dashboard status: coverage partial_ready with ready_count=3, accepted_failed_count=5, pending_count=17, can_unlock_downstream=false.
  - It found two packets: `layer_02_sector_context_2016_01_arkg_arkw_cibr_igv_iyt` (`template_pending_review`, provider_calls=0) and `layer_02_sector_context_2016_01_xlb_xle_xlk` (`reconciled`, provider_calls=3).
  - Next action is `review_existing_pending_packet` because the ARKG/ARKW/CIBR/IGV/IYT packet already exists and awaits review, rather than creating a duplicate packet.
  - Next pending preview still shows ARKG, ARKW, CIBR, IGV, IYT.
- Verification passed: registry dry-run no pending migrations, `trading-manager` 171 tests OK, compileall OK, diff-check OK.
- Pushed `trading-manager` commit `007f24d` (`Add stage run dashboard receipt`).

## Layer 2 ARKG/ARKW/CIBR/IGV/IYT packet advanced to dispatch-plan-ready

- Continued from the single `manager_stage_run_dashboard_v1` entry point.
- Dashboard next action was `review_existing_pending_packet` for `layer_02_sector_context_2016_01_arkg_arkw_cibr_igv_iyt`.
- Filled reviewed approval artifact for that packet with approval id `lcav1_layer_02_sector_context_2016_01_alpaca_bars_arkg_arkw_cibr_igv_iyt_20260510`; scope remains provider_data_acquisition_only and explicitly disables broker execution, model activation, and storage lifecycle mutation.
- Wrote proposal validation artifact: request_count=5, gate_validation_count=5, provider_calls=0, dispatch_performed=false.
- Wrote plan-only dispatch summary: request_count=5, validation_count=5, dispatch_count=0, provider_calls=0, dispatch_performed=false; all five items are `validated_not_dispatched`.
- Refreshed packet status to `dispatch_plan_ready_pending_execute`; `can_execute_dispatch=true`, provider_calls=0.
- Refreshed dashboard; next action is now `review_execute_or_defer_ready_packet`, blocking reason names the ARKG/ARKW/CIBR/IGV/IYT packet as ready for explicit execution review.
- No provider calls were executed; `trading-manager` and `trading-data` git statuses remained clean.

## Conservative stage-run controller implemented

- Chentong asked whether the whole system can be completed in one pass. Clarified the safe boundary: we cannot honestly declare the whole trading system complete in one pass because model validity, real-data evidence, promotions, provider calls, and execution require staged review; but we can complete the current manager-controlled stage-run operating loop.
- Implemented `manager_stage_run_controller_receipt_v1` in `trading-manager/src/trading_manager_tasks/stage_run_controller.py` and CLI `scripts/tasks/run_stage_controller.py`.
- The controller runs one conservative no-provider stage-control step from the dashboard:
  - creates the next pending-only packet when the dashboard says `create_or_review_next_pending_only_packet` and no packet already needs review;
  - refreshes the dashboard;
  - emits a controller receipt;
  - stops at human approval review, provider execution review, failure review, downstream workflow unlock, packet repair, model activation, broker execution, and storage lifecycle mutation gates.
- Registered migration `307_register_stage_run_controller.sql`; registry export now has 662 rows.
- Smoke on current Layer 2 Jan 2016 did not execute provider calls and correctly stopped at `external_call_gate` because ARKG/ARKW/CIBR/IGV/IYT is already `dispatch_plan_ready_pending_execute`.
- Verification passed: targeted controller/dashboard/packet/planning tests 21 OK; full `trading-manager` tests 174 OK; compileall OK; diff-check OK; registry dry-run no pending migrations.
- Pushed `trading-manager` commit `e5e59a6` (`Add conservative stage run controller`).

## Layer 2 ARKG/ARKW/CIBR/IGV/IYT provider execution completed

- Chentong explicitly said `执行`; interpreted as approval to execute the current dispatch-plan-ready Layer 2 packet only: `layer_02_sector_context_2016_01_arkg_arkw_cibr_igv_iyt`.
- Executed 5 approved Alpaca bar provider calls with proposal validation and terminal coverage guard:
  - ARKG, ARKW, CIBR, IGV, IYT all `dispatched_succeeded`.
  - Dispatch summary wrote `dispatch_execute.json` with request_count=5, validation_count=5, dispatch_count=5, provider_calls=5, dispatch_performed=true.
  - No model activation, broker execution, or storage lifecycle mutation occurred.
- New trading-data completion receipts each succeeded with `equity_bar` row_count=20.
- Reconciled the provider stage:
  - discovered_receipt_count=8 total Layer 2 receipts;
  - normalized_run_manifest_count=8;
  - normalized_artifact_ref_count=40;
  - normalized_ready_signal_count=8;
  - failure_proposal_count=0;
  - persisted_control_plane=true;
  - coverage remains partial_ready with ready_count=8, accepted_failed_count=5, failed_count=0, pending_count=12, can_unlock_downstream=false.
- Packet status refreshed to `reconciled` with provider_calls=5.
- Stage controller then safely created the next pending-only no-provider packet `layer_02_sector_context_2016_01_smh_xbi_xlf_xli_xlp`; rehearsal passed with request_count=5, validation_count=5, dispatch_count=0, provider_calls=0, status unchanged `template_pending_review`.
- Current dashboard next action is `review_existing_pending_packet` for the SMH/XBI/XLF/XLI/XLP packet.
- Targeted tests passed: controller/dashboard/packet/planning tests 21 OK. `trading-manager` and `trading-data` git statuses remained clean.

## Layer 2 SMH/XBI/XLF/XLI/XLP packet advanced to dispatch-plan-ready

- Chentong said `jixu1`; treated as continue the safe no-provider flow, not execute provider calls.
- Dashboard next action was `review_existing_pending_packet` for `layer_02_sector_context_2016_01_smh_xbi_xlf_xli_xlp`.
- Filled reviewed approval artifact with approval id `lcav1_layer_02_sector_context_2016_01_alpaca_bars_smh_xbi_xlf_xli_xlp_20260510`; scope remains provider_data_acquisition_only and explicitly disables broker execution, model activation, and storage lifecycle mutation.
- Wrote proposal validation artifact: request_count=5, gate_validation_count=5, provider_calls=0, dispatch_performed=false.
- Wrote plan-only dispatch summary: request_count=5, validation_count=5, dispatch_count=0, provider_calls=0, dispatch_performed=false; all five items are `validated_not_dispatched`.
- Refreshed packet status to `dispatch_plan_ready_pending_execute`; `can_execute_dispatch=true`, provider_calls=0.
- Refreshed dashboard; next action is now `review_execute_or_defer_ready_packet`, blocking reason names the SMH/XBI/XLF/XLI/XLP packet as ready for explicit execution review.
- No provider calls were executed; targeted controller/dashboard/packet/planning tests 21 OK; `trading-manager` and `trading-data` git statuses clean.

## Layer 2 SMH/XBI/XLF/XLI/XLP provider execution completed

- Chentong explicitly said `执行`; interpreted as approval to execute the current dispatch-plan-ready Layer 2 packet only: `layer_02_sector_context_2016_01_smh_xbi_xlf_xli_xlp`.
- Executed 5 approved Alpaca bar provider calls with proposal validation and terminal coverage guard:
  - SMH, XBI, XLF, XLI, XLP all `dispatched_succeeded`.
  - Dispatch summary wrote `dispatch_execute.json` with request_count=5, validation_count=5, dispatch_count=5, provider_calls=5, dispatch_performed=true.
  - No model activation, broker execution, or storage lifecycle mutation occurred.
- New trading-data completion receipts succeeded with equity_bar row counts: SMH=20, XBI=20, XLF=451, XLI=306, XLP=314.
- Reconciled the provider stage:
  - discovered_receipt_count=13 total Layer 2 receipts;
  - normalized_run_manifest_count=13;
  - normalized_artifact_ref_count=65;
  - normalized_ready_signal_count=13;
  - failure_proposal_count=0;
  - persisted_control_plane=true;
  - coverage remains partial_ready with ready_count=13, accepted_failed_count=5, failed_count=0, pending_count=7, can_unlock_downstream=false.
- Packet status refreshed to `reconciled` with provider_calls=5.
- Stage controller then safely created the next pending-only no-provider packet `layer_02_sector_context_2016_01_xlre_xlu_xlv_xly_xme`; rehearsal passed with request_count=5, validation_count=5, dispatch_count=0, provider_calls=0, status unchanged `template_pending_review`.
- Current dashboard next action is `review_existing_pending_packet` for the XLRE/XLU/XLV/XLY/XME packet.
- Targeted tests passed: controller/dashboard/packet/planning tests 21 OK. `trading-manager` and `trading-data` git statuses remained clean.

## Layer 2 XLRE/XLU/XLV/XLY/XME packet advanced to dispatch-plan-ready

- Chentong said `继续`; treated as continue the safe no-provider flow, not execute provider calls.
- Dashboard next action was `review_existing_pending_packet` for `layer_02_sector_context_2016_01_xlre_xlu_xlv_xly_xme`.
- Filled reviewed approval artifact with approval id `lcav1_layer_02_sector_context_2016_01_alpaca_bars_xlre_xlu_xlv_xly_xme_20260510`; scope remains provider_data_acquisition_only and explicitly disables broker execution, model activation, and storage lifecycle mutation.
- Wrote proposal validation artifact: request_count=5, gate_validation_count=5, provider_calls=0, dispatch_performed=false.
- Wrote plan-only dispatch summary: request_count=5, validation_count=5, dispatch_count=0, provider_calls=0, dispatch_performed=false; all five items are `validated_not_dispatched`.
- Refreshed packet status to `dispatch_plan_ready_pending_execute`; `can_execute_dispatch=true`, provider_calls=0.
- Refreshed dashboard; next action is now `review_execute_or_defer_ready_packet`, blocking reason names the XLRE/XLU/XLV/XLY/XME packet as ready for explicit execution review.
- No provider calls were executed; targeted controller/dashboard/packet/planning tests 21 OK; `trading-manager` and `trading-data` git statuses clean.

## Layer 2 XLRE/XLU/XLV/XLY/XME provider execution completed

- Chentong explicitly said `执行`; interpreted as approval to execute the current dispatch-plan-ready Layer 2 packet only: `layer_02_sector_context_2016_01_xlre_xlu_xlv_xly_xme`.
- Executed 5 approved Alpaca bar provider calls with proposal validation and terminal coverage guard:
  - XLRE, XLU, XLV, XLY, XME all `dispatched_succeeded`.
  - Dispatch summary wrote `dispatch_execute.json` with request_count=5, validation_count=5, dispatch_count=5, provider_calls=5, dispatch_performed=true.
  - No model activation, broker execution, or storage lifecycle mutation occurred.
- New trading-data completion receipts succeeded with equity_bar row counts: XLRE=67, XLU=365, XLV=354, XLY=333, XME=20.
- Reconciled the provider stage:
  - discovered_receipt_count=18 total Layer 2 receipts;
  - normalized_run_manifest_count=18;
  - normalized_artifact_ref_count=90;
  - normalized_ready_signal_count=18;
  - failure_proposal_count=0;
  - persisted_control_plane=true;
  - coverage remains partial_ready with ready_count=18, accepted_failed_count=5, failed_count=0, pending_count=2, can_unlock_downstream=false.
- Packet status refreshed to `reconciled` with provider_calls=5.
- Stage controller then safely created the final pending-only no-provider packet `layer_02_sector_context_2016_01_xop_xrt`; rehearsal passed with request_count=2, validation_count=2, dispatch_count=0, provider_calls=0, status unchanged `template_pending_review`.
- Current dashboard next action is `review_existing_pending_packet` for the XOP/XRT packet.
- Targeted tests passed: controller/dashboard/packet/planning tests 21 OK. `trading-manager` and `trading-data` git statuses remained clean.

## Layer 2 XOP/XRT packet advanced to dispatch-plan-ready

- Chentong said `jixu1`; treated as continue the safe no-provider flow, not execute provider calls.
- Dashboard next action was `review_existing_pending_packet` for `layer_02_sector_context_2016_01_xop_xrt`.
- Filled reviewed approval artifact with approval id `lcav1_layer_02_sector_context_2016_01_alpaca_bars_xop_xrt_20260510`; scope remains provider_data_acquisition_only and explicitly disables broker execution, model activation, and storage lifecycle mutation.
- Wrote proposal validation artifact: request_count=2, gate_validation_count=2, provider_calls=0, dispatch_performed=false.
- Wrote plan-only dispatch summary: request_count=2, validation_count=2, dispatch_count=0, provider_calls=0, dispatch_performed=false; both items are `validated_not_dispatched`.
- Refreshed packet status to `dispatch_plan_ready_pending_execute`; `can_execute_dispatch=true`, provider_calls=0.
- Refreshed dashboard; next action is now `review_execute_or_defer_ready_packet`, blocking reason names the XOP/XRT packet as ready for explicit execution review.
- No provider calls were executed; targeted controller/dashboard/packet/planning tests 21 OK; `trading-manager` and `trading-data` git statuses clean.

## Layer 2 XOP/XRT provider execution completed; Layer 2 data acquisition ready

- Chentong explicitly said `执行`; interpreted as approval to execute the current dispatch-plan-ready final Layer 2 packet only: `layer_02_sector_context_2016_01_xop_xrt`.
- Executed 2 approved Alpaca bar provider calls with proposal validation and terminal coverage guard:
  - XOP and XRT both `dispatched_succeeded`.
  - Dispatch summary wrote `dispatch_execute.json` with request_count=2, validation_count=2, dispatch_count=2, provider_calls=2, dispatch_performed=true.
  - No model activation, broker execution, or storage lifecycle mutation occurred.
- New trading-data completion receipts succeeded with equity_bar row counts: XOP=20, XRT=20.
- Reconciled the provider stage:
  - discovered_receipt_count=20 total Layer 2 receipts;
  - normalized_run_manifest_count=20;
  - normalized_artifact_ref_count=100;
  - normalized_ready_signal_count=20;
  - failure_proposal_count=0;
  - persisted_control_plane=true;
  - coverage is now ready with ready_count=20, accepted_failed_count=5, failed_count=0, pending_count=0, can_unlock_downstream=true.
- Packet status refreshed to `reconciled` with provider_calls=2.
- Dashboard next action is now `advance_downstream_workflow`; next pending packet is unavailable because selected requests are already terminal/not pending.
- Controller stopped at `downstream_unlock_available` with `requires_explicit_workflow_advance`.
- Workflow state now has `layer_02_sector_context.data_acquisition` succeeded and `layer_02_sector_context.feature_generation` ready; model generation/evaluation/promotion remain blocked.
- Targeted tests passed: controller/dashboard/packet/planning tests 21 OK. `trading-manager` and `trading-data` git statuses remained clean.

## Layer 2 offline workflow completed after data acquisition ready

- Chentong said `继续` after Layer 2 data acquisition reached ready.
- Ran Layer 2 feature generation from existing feed artifacts only; no provider calls.
  - Dry-run found 38 feed artifacts and 17,375 source rows.
  - Executed `layer_02_sector_context.feature_generation` through manager safe offline stage executor.
  - Feature materialization stdout: receipt_count=38, source_rows_found=17,375, source_rows_written=17,375, feature_rows_written=416, provider_calls=0.
- Continued safe offline Layer 2 stages:
  - `model_generation` succeeded, generating 325 rows into `trading_model.model_02_sector_context` plus explainability/diagnostics support artifacts.
  - Initial `promotion_review_preparation` exposed a mechanism bug: Layer 2 workflow review command lacked `--evaluation-summary-json`.
  - Fixed `trading-manager/src/trading_manager_tasks/model_training_workflow.py` so Layers 1 and 2 evaluation commands both use `--from-database --output-json /root/projects/trading-model/storage/runtime/model_XX_slug/evaluation_summary_${START_MONTH}.json`, and review commands both pass that summary plus `--local-fallback-review`.
  - Added/updated tests in `trading-manager/tests/test_model_training_workflow.py`.
  - Fixed `trading-model/scripts/models/model_02_sector_context/evaluate_model_02_sector_context.py` to create the parent directory before writing `--output-json`.
  - Wrote real DB-read Layer 2 evaluation summary to `/root/projects/trading-model/storage/runtime/model_02_sector_context/evaluation_summary_2016-01.json`.
  - Evaluation summary evidence_source is `postgresql:trading_data.feature_02_sector_context+trading_model.model_02_sector_context`; promotion_evidence_ready=false; feature_rows=403, model_rows=325, eval_labels=320.
  - Re-ran `promotion_review_preparation`; local fallback review deferred promotion with blocker `thresholds_pass`.
  - Ran `maintenance`; emitted a dry-run `manager_request_v1` for model promotion review and did not persist or activate anything.
- Workflow state now has all Layer 2 stages succeeded and Layer 3 `layer_03_target_state_vector.data_acquisition` ready. Layer 3 feature/model/eval/promotion/maintenance are still blocked behind their stage dependencies.
- Safety preserved: provider_calls=0 for all offline stages, no model activation, no broker execution, no storage lifecycle mutation.
- Verification passed:
  - `trading-manager`: targeted model workflow/stage executor tests 13 OK, full tests 174 OK, compileall OK, diff-check OK.
  - `trading-model`: targeted sector/market evaluation/model tests 21 OK, compileall OK, diff-check OK.
- Pushed commits: `trading-manager` `9a92e71` (`Wire Layer 2 promotion review summary path`) and `trading-model` `02c480d` (`Create Layer 2 evaluation output directories`). `trading-manager`, `trading-model`, and `trading-data` git statuses clean afterward.

## Layer 3 target-state workflow completed

- Continued from Chentong's `继续` after Layer 2 offline workflow completion.
- Confirmed `layer_03_target_state_vector.data_acquisition` was safe local reuse, not provider dispatch: it materializes `source_03_target_state` from already reviewed Layer 2 Alpaca bar artifacts.
- Added `trading-manager` local Layer 3 materializer: `src/trading_manager_tasks/layer_three_target_state.py` and `scripts/tasks/materialize_layer_three_target_state_inputs.py`.
- Registered migration `308_register_layer_three_target_state_materialization.sql` with `manager_layer_three_target_state_input_materialization_v1`; registry dry-run has no pending migrations.
- Updated manager workflow/stage executor so the approved safe data-acquisition command is executable offline, with explicit zero provider calls/model activation/broker execution.
- Ran Layer 3 2016-01 workflow through the manager safe offline executor:
  - data acquisition succeeded from 20 Layer 2 feed artifacts with `provider_calls=0`;
  - `trading_data.source_03_target_state` rows: `3419`;
  - `trading_data.feature_03_target_state_vector` rows: `3419`;
  - `trading_model.model_03_target_state_vector` rows: `3409`;
  - evaluation summary: `/root/projects/trading-model/storage/runtime/model_03_target_state_vector/evaluation_summary_2016-01.json`;
  - threshold summary passed all 11 thresholds, but promotion review remained `deferred` because explicit approval is still required before activation.
- Hardened Layer 3 model scripts to support database-backed generation/evaluation/review summary flow:
  - `generate_model_03_target_state_vector.py --from-database` writes SQL model rows and optional JSONL;
  - `evaluate_model_03_target_state_vector.py --from-database --output-json` writes evaluation summary;
  - `review_target_state_vector_promotion.py --evaluation-summary-json` supports conservative review prep.
- Final workflow state: all Layer 3 stages succeeded; next ready stage is `layer_04_event_overlay.data_acquisition`, which is not yet executed and needs a boundary review because it is `source_04_event_overlay` event-input work.
- Verification passed: `trading-manager` 182 tests OK, compileall OK, diff-check OK, registry dry-run no pending; `trading-model` 101 tests OK, compileall OK, diff-check OK.
- Pushed commits: `trading-manager` `6112345` (`Execute Layer 3 local workflow`) and `trading-model` `109c0c6` (`Persist Layer 3 database workflow`).

## Pre-compaction flush — Layer 3 target-state workflow completed

- Chentong asked to continue after Layer 2 offline workflow completed and `layer_03_target_state_vector.data_acquisition` became ready.
- Confirmed Layer 3 could proceed as a safe local/internal stage: it reused already-approved Layer 2 Alpaca bar artifacts and did not require new provider calls.
- Added manager-owned local Layer 3 target-state input materialization:
  - `trading-manager/src/trading_manager_tasks/layer_three_target_state.py`
  - `trading-manager/scripts/tasks/materialize_layer_three_target_state_inputs.py`
  - registry migration `308_register_layer_three_target_state_materialization.sql`
  - tests in `trading-manager/tests/test_layer_three_target_state.py`
- Added DB-backed Layer 3 model workflow support in `trading-model`:
  - `scripts/models/model_03_target_state_vector/generate_model_03_target_state_vector.py` now supports `--from-database` and writes `trading_model.model_03_target_state_vector`.
  - `scripts/models/model_03_target_state_vector/evaluate_model_03_target_state_vector.py` now supports `--from-database`, `--output-json`, and real database evidence.
  - `scripts/models/model_03_target_state_vector/review_target_state_vector_promotion.py` now supports `--evaluation-summary-json` and local fallback review semantics.
- Ran the full Layer 3 2016-01 workflow through the safe offline executor:
  - `source_03_target_state`: 3,419 rows.
  - `feature_03_target_state_vector`: 3,419 rows.
  - `model_03_target_state_vector`: 3,409 rows.
  - Provider calls: 0.
  - Model activation: false / 0.
  - Broker or account execution: false / 0.
- Layer 3 evaluation summary was written under `trading-model/storage/runtime/model_03_target_state_vector/evaluation_summary_2016-01.json`; all 11 thresholds passed, but promotion review still returned `deferred` because explicit reviewed approval is required before activation.
- Workflow state now has all Layer 3 stages succeeded through maintenance and points to `layer_04_event_overlay.data_acquisition` as the next gate.
- Did not execute Layer 4; it needs event-input/source boundary review first because `source_04_event_overlay` requires event rows or detector policy, not the same simple local materialization path as Layer 3.
- Verification passed after the Layer 3 work:
  - `trading-manager`: 182 tests OK, compileall OK, diff-check OK, registry dry-run no pending migrations.
  - `trading-model`: 101 tests OK, compileall OK, diff-check OK.
- Pushed commits:
  - `trading-manager` `6112345` (`Execute Layer 3 local workflow`).
  - `trading-model` `109c0c6` (`Persist Layer 3 database workflow`).
- Recommended next action after compaction: review/shape the Layer 4 EventOverlay input-materialization path before running `layer_04_event_overlay.data_acquisition`; preserve no-provider/no-activation/no-broker/no-storage-lifecycle-mutation boundaries unless explicitly approved.

## Layers 5-7 safe offline database workflow completed
- Continued formal 2016-01 training workflow after Layer 4; did not make provider calls, activate models, perform broker/order execution, or mutate storage lifecycle.
- Implemented SQL-backed `--from-database` generation/evaluation support in `trading-model` for `model_05_alpha_confidence`, `model_06_position_projection`, and `model_07_underlying_action`; Layer 7 joins completed Layer 5/6 rows and uses local point-in-time default quote/liquidity/risk context for offline planning evidence only.
- Updated `trading-manager` workflow wiring so Layers 5-7 generation/evaluation/review use database-backed rows and conservative fixture-outcome promotion review, registered migration `310_register_layers_five_to_seven_database_workflow.sql`, and documented the safe offline boundary.
- Executed manager safe offline stages for Layers 5, 6, and 7 through maintenance. Each stage receipt reported `provider_calls=0`, `broker_execution_performed=false`, and `model_activation_performed=false`.
- Runtime row counts for Jan 2016: `model_05_alpha_confidence` 279, `model_06_position_projection` 279, `model_07_underlying_action` 279. Promotion reviews for Layers 5-7 all remained `deferred` with activation disallowed because evidence is local/fixture-style.
- Workflow is now blocked at `layer_08_option_expression.data_acquisition`, waiting for `live_call_approval_v1`; safe `继续` must not cross that provider/active-target-chain gate without explicit approval.
- Verification passed before commit: `trading-manager` 186 tests OK, registry dry-run no pending migrations, compileall OK, diff-check OK; `trading-model` 101 tests OK, compileall OK, diff-check OK.

## Pre-compaction flush — Layers 5-7 finalized and Layer 8 approval gate reached

- Continued after Layer 4 completion by reviewing Layer 5/6/7 boundaries and adding database-backed safe offline support instead of blindly running local fixture scripts.
- Implemented and executed SQL-backed workflows for Layers 5-7 for `2016-01`:
  - `model_05_alpha_confidence`: generated/evaluated/reviewed 279 rows.
  - `model_06_position_projection`: generated/evaluated/reviewed 279 rows.
  - `model_07_underlying_action`: generated/evaluated/reviewed 279 rows.
- Layer 7 database generation joins completed Layer 5/6 rows and uses conservative local default current/pending position, quote/liquidity/borrow, risk-budget, and policy-gate states; it remains an offline `underlying_action_plan` / `underlying_action_vector` planning surface and does not emit broker/order/fill/account mutation or option-contract selection.
- Updated `trading-manager` workflow wiring for database-backed Layers 5-7 generation/evaluation/review and registered `310_register_layers_five_to_seven_database_workflow.sql`.
- Executed all Layer 5, 6, and 7 manager stages through maintenance with safety receipts showing `provider_calls=0`, `broker_execution_performed=false`, and `model_activation_performed=false`.
- Promotion reviews for Layers 5-7 remained `deferred`; activation stayed disallowed because evidence is local/fixture-style and requires explicit reviewed approval before any model activation.
- Workflow state now has Layers 1-7 succeeded for the current safe offline run and is blocked at `layer_08_option_expression.data_acquisition`, waiting for `live_call_approval_v1`; safe `继续` / `jixu1` must not cross this provider/active-target-chain gate without explicit approval such as `执行`.
- Verification passed before final report:
  - `trading-manager`: 186 tests OK, registry dry-run no pending migrations, compileall OK, diff-check OK.
  - `trading-model`: 101 tests OK, compileall OK, diff-check OK.
- Final pushed commits:
  - `trading-manager` `10df093` (`Complete Layers 4-7 offline workflow`).
  - `trading-model` `bf71922` (`Persist Layers 4-7 database workflows`).
- Recommended next action after compaction: inspect/prepare the Layer 8 approval packet/dashboard only; do not execute provider calls unless Chentong explicitly says `执行` and proposal-bound approval validation plus terminal-coverage guard are present.

## Layer 8 no-provider/no-option path completed
- Reviewed Jan 2016 Layer 8 option-expression gate in `trading-manager`: Layer 7 had 279 rows, 0 active target chains, and 0 option snapshot request previews, so the gate review returned `no_provider_skip_accepted` / `accepted_skip_no_active_target_chain` with `provider_calls=0`, no dispatch, no broker execution, no model activation, and no storage lifecycle mutation.
- Added manager gate review implementation: `src/trading_manager_tasks/layer_eight_option_expression.py`, CLI `scripts/tasks/review_layer_eight_option_expression_gate.py`, tests, docs, and registry migration `311_register_layer_eight_option_gate_review.sql`.
- Added SQL-backed Layer 8 no-option generation/evaluation in `trading-model` scripts. The generator consumes completed `trading_model.model_07_underlying_action` rows and emits deterministic `no_option_expression` rows when Layer 7 is no-trade/maintain/neutral.
- Ran Layer 8 safe workflow through completion: data acquisition accepted no-provider skip, feature generation marked complete from reviewed operator evidence, model generation/evaluation/review/maintenance completed through manager safe offline executor.
- Runtime results: `trading_model.model_08_option_expression` has 279 Jan 2016 rows, all `8_resolved_expression_type = no_option_expression`; promotion review remains `deferred` with activation disallowed.
- Verification passed: `trading-manager` 189 tests OK, registry dry-run no pending migrations, compileall OK, diff-check OK; `trading-model` 101 tests OK, compileall OK, diff-check OK.
