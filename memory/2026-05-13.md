
## Trading-dashboard Status summary card simplified
- Chentong said the Current Status summary card had little value and should only keep refresh time.
- Updated `trading-dashboard` Status view so the summary card renders only `Last Refreshed` plus the formatted refresh timestamp; detailed source/freshness/live metadata remains hidden from the public Status card.
- Verification passed: dashboard npm/Python tests, Vite build, compileall, diff-check, and browser snapshot confirmed the card now only shows `Last Refreshed` with a timestamp.
- Pushed `trading-dashboard` commit `22d591d` (`Reduce status summary card to refresh time`). Repo clean on `main...origin/main` afterward.

## Trading-dashboard shared top status bar added
- Chentong clarified the refresh-time/status area should be shared across all tabs and live in a fixed/top page bar with `Healthy` and `Refresh`, rather than as a Status-only card.
- Updated `trading-dashboard` so the main content starts with a sticky `Dashboard status bar` showing health, read-only posture, last refreshed time, live update status, and a global Refresh button that refreshes both Current Status and Tasks summaries.
- Removed the Status-only refresh-time summary card; Tasks still keeps its task summary card below the shared bar.
- Verification passed: dashboard npm/Python tests, Vite build, compileall, diff-check; browser snapshot confirmed the top bar appears on Current Status and remains when switching to Tasks.
- Pushed `trading-dashboard` commit `af431a0` (`Move refresh status into shared top bar`). Repo clean on `main...origin/main` afterward.

## Trading-dashboard explanatory copy removed
- Chentong asked to remove annotation-like UI prose such as the Status hero paragraph about dashboard summaries/navigation/live updates.
- Removed visible explanatory paragraphs from the dashboard shell: hero description, sidebar read-only explainer, brand subtitle, placeholder explanatory text, and extra error help copy; kept compact labels, metrics, status, refresh, and page headings.
- Verification passed: dashboard npm/Python tests, Vite build, compileall, diff-check, and browser snapshot confirmed the Status page is now label/metric-focused.
- Pushed `trading-dashboard` commit `102a86e` (`Remove explanatory dashboard copy`). Repo clean on `main...origin/main` afterward.

## Trading-dashboard live-update wording removed
- Chentong questioned the meaning of `Live updates Live` and asked to remove it.
- Removed visible `Live updates`, `Live Updates`, and `Live Feed` labels from the shared top bar, Status metric cards, Status server resources, and non-Status summary metadata. The WebSocket/fallback mechanism remains internal.
- Verification passed: dashboard npm/Python tests, Vite build, compileall, diff-check, rg found no visible `Live updates`/`Live Updates`/`Live Feed` strings, and browser snapshot confirmed top bar now shows only health, last refreshed, and Refresh.
- Pushed `trading-dashboard` commit `8a50f36` (`Remove live update labels from status page`). Repo clean on `main...origin/main` afterward.

## Trading-dashboard Status server resources moved first
- Chentong asked to move `Server Resources` to the top of Current Status as the first card and include current CPU, memory, network upload, and network download.
- Updated `trading-storage` current system status producer to sample CPU usage from `/proc/stat`, memory usage from `/proc/meminfo`, and non-loopback upload/download rates from `/proc/net/dev`; payload now includes `cpu_usage_percent`, `memory_usage_percent`, `network_download_kbps`, and `network_upload_kbps`.
- Updated `trading-dashboard` Status page so the first visible card is `Server Resources` with CPU, Memory, Download, and Upload; replaced Linux load-average copy with `Server Online / Running normally` and moved old uptime/memory details under `Server Details`.
- Updated dashboard/storage docs for the accepted Status resource-card direction.
- Verification passed: `trading-storage` 24 tests, compileall, diff-check, live producer smoke; `trading-dashboard` npm/Python tests, Vite build, compileall, diff-check; browser snapshot confirmed Server Resources appears first with CPU/memory/download/upload values.
- Refreshed public dashboard read models and pushed commits: `trading-storage` `f1c9e8a` (`Add live server resource metrics`) and `trading-dashboard` `09e1493` (`Lead status page with server resources`). Both repos clean on `main...origin/main` afterward.

## Trading-dashboard Server Resources placed under top bar
- Chentong clarified `Server Resources` should sit directly below the fixed/top status bar, and storage space plus other server-related metrics can be included there.
- Updated `trading-dashboard` so `Server Resources` renders immediately under the shared top status bar and before the page title on Current Status.
- The resource card now includes CPU, Memory, Storage, Download, Upload, and Uptime. Removed the duplicate lower `Server Details` card; `Server` and `Auto Refresh` remain as separate status cards below the page title.
- Verification passed: dashboard npm/Python tests, Vite build, compileall, diff-check, and browser snapshot confirmed the new order and included resource metrics.
- Pushed `trading-dashboard` commit `d5fc94d` (`Place server resources below status bar`). Repo clean on `main...origin/main` afterward.

## Trading-dashboard duplicate Status cards trimmed
- Chentong clarified Server Resources should appear below the page title, not directly under the fixed status bar.
- Removed duplicate Status metric cards: `Server / Online / Running normally` duplicated the top-bar `Healthy`, and `Auto Refresh / 30s / Refresh schedule active` was unnecessary because the top bar already has refresh posture/action.
- Current Status order is now: fixed status bar, page title, Server Resources, then Background Services and Dashboard Data.
- Verification passed: dashboard npm/Python tests, Vite build, compileall, diff-check, and browser snapshot confirmed the revised order and duplicate cards were gone.
- Pushed `trading-dashboard` commit `4cebbdf` (`Trim duplicate status cards`). Repo clean on `main...origin/main` afterward.

## Trading-dashboard API Connections card added
- Chentong asked for an API card showing all server APIs and connection status.
- Updated `trading-storage` current system status payload with a public-facing API list: `Dashboard Data API` and `Live Status API`, each with kind/status/healthy fields. Internal route paths remain implementation details.
- Updated `trading-dashboard` Current Status to render an `API Connections` card after Server Resources, showing public API names and statuses such as `Connected`; stream fallback would display as `Automatic refresh`.
- Added dashboard decision D013 documenting public API labels and hidden internal routes.
- Verification passed: `trading-storage` 24 tests, compileall, diff-check, API payload smoke; `trading-dashboard` npm/Python tests, Vite build, compileall, diff-check; refreshed public read models; browser snapshot confirmed API Connections card with Dashboard Data API and Live Status API connected.
- Pushed commits: `trading-storage` `de017a4` (`Add dashboard API connection summary`) and `trading-dashboard` `b29d827` (`Add API connections status card`). Both repos clean on `main...origin/main` afterward.

## Dashboard API card corrected to provider APIs
- Chentong clarified that the API card should show provider APIs such as Alpaca/OKX/ThetaData, not dashboard HTTP/WebSocket APIs.
- Corrected `trading-storage` current system status summary so `chart_payload.apis` lists `Alpaca Market Data API`, `OKX Market Data API`, and `ThetaData Options API` with local configuration/runtime status only. It does not perform provider calls or expose secret paths/values. Current observed statuses: Alpaca configured, OKX configured, ThetaData local service offline.
- Corrected `trading-dashboard` API Connections rendering to show provider statuses: `Configured`, `Not configured`, `Local service online`, or `Local service offline`.
- Updated D013 to define provider API status labels and note that live provider connectivity checks require a separate bounded read-only approval path.
- Verification passed: `trading-storage` 24 tests, compileall, diff-check, provider API payload smoke; `trading-dashboard` npm/Python tests, Vite build, compileall, diff-check; refreshed public read models; browser snapshot confirmed provider API rows without internal dashboard route paths.
- Pushed commits: `trading-storage` `4b19a3e` (`Show provider API status in dashboard summary`) and `trading-dashboard` `80d7259` (`Render provider API status labels`). Both repos clean on `main...origin/main` afterward.

## Dashboard Current Status layout and data-file timestamps updated
- Chentong asked to place API Connections and Background Services on the same row, and make Dashboard Data its own row listing every dashboard-read file with the file's last update time.
- Updated `trading-storage` current system status read-model freshness rows with `file_label` and `latest_updated_at_utc` from the latest read-model file mtime.
- Updated `trading-dashboard` Current Status layout: Server Resources first, then API Connections + Background Services side by side, then full-width Dashboard Data. Dashboard Data now lists `System Health Summary file` and `Task Progress Summary file` with formatted last-updated timestamps and relative age.
- Added dashboard decision D014 documenting the grouping and data-file freshness requirement.
- Verification passed: `trading-storage` 24 tests, compileall, diff-check, payload smoke; `trading-dashboard` npm/Python tests, Vite build, compileall, diff-check; refreshed public read models; browser snapshot confirmed the row layout and Dashboard Data file timestamps.
- Pushed commits: `trading-storage` `634dd6c` (`Expose dashboard data file update times`) and `trading-dashboard` `dce21db` (`Reflow status panels and data file times`). Both repos clean on `main...origin/main` afterward.

## Dashboard data source-output status corrected
- Chentong clarified that aggregated dashboard JSON should be a sanitized/cache adapter, not a new source of truth. Dashboard Data should report the original script/model/task outputs that feed the dashboard, even if scattered and aggregated for display.
- Updated `trading-storage` current system status payload to emit `chart_payload.source_outputs` instead of dashboard read-model freshness as the visible Dashboard Data source. It now reports original manager outputs: Historical Scheduler State, Scheduler Decision Log, Active Workflow State, Latest Stage Coverage Output, and Latest Stage Run Output, each with availability and last-updated timestamp derived from the source output itself when possible.
- Updated `trading-dashboard` Current Status Dashboard Data panel to render those source outputs, not `current_system_status_summary/latest.json` and `historical_task_progress_summary/latest.json` as source files.
- Updated docs/decision D014 to say aggregation/sanitization is an adapter/cache step and original source outputs remain canonical for status display.
- Verification passed: `trading-storage` 24 tests, compileall, diff-check, source-output payload smoke; `trading-dashboard` npm/Python tests, Vite build, compileall, diff-check; refreshed public read models; browser snapshot confirmed Dashboard Data lists original source outputs with timestamps.
- Pushed commits: `trading-storage` `145ad5d` (`Report dashboard source output status`) and `trading-dashboard` `5de7084` (`Show dashboard source output freshness`). Both repos clean on `main...origin/main` afterward.

## Dashboard source-output inventory rule recorded
- Chentong noted that as website development adds more original/source outputs, the dashboard source-output inventory must be updated to include them.
- Recorded the rule in `trading-dashboard` docs/tasks/decision/read-model docs and `trading-storage` dashboard read-model docs, and added a code comment near `dashboard_system_status._dashboard_source_outputs` so future source-output consumers update the freshness inventory in the same slice.
- Verification passed: `trading-dashboard` npm/Python tests OK; `trading-storage` dashboard system status targeted tests OK, compileall OK, and diff-check OK.
- Pushed commits: `trading-dashboard` `fbed696` (`Document dashboard source output inventory rule`) and `trading-storage` `023886a` (`Document dashboard source output sync rule`).

## Historical Task Progress content completed further
- Chentong said Historical Task Progress is now reachable but the current content is not finished, and asked to finish the current content first.
- Treated the current slice as the Historical Task Progress page rather than opening additional dashboard sections.
- Updated `trading-dashboard` Tasks/Historical Modeling page: removed duplicate Stage Counts panel, added workflow/progress cards, system gates (service runtime, lock, provider posture, terminal complete), next step/blocker panel, and renamed coverage to Latest Stage Coverage.
- Updated `trading-storage` dashboard refresh so `historical_task_progress_summary` automatically attaches the newest manager stage-coverage artifact when no explicit path is supplied; public read models were refreshed and the live page now shows latest coverage instead of an empty placeholder.
- Verification passed: `trading-dashboard` tests, Vite build, compileall, diff-check, and browser snapshot; `trading-storage` full 25 tests, compileall, diff-check, and public read-model refresh.
- Pushed commits: `trading-dashboard` `1464815` (`Complete historical task progress content`) and `trading-storage` `85b6778` (`Attach latest coverage to dashboard refresh`).

## Stable ID correction after Layer 3 dashboard/blocker fix
- Chentong correctly caught that the Layer 3 artifact-discovery patch reintroduced `monthly_backfill_v1` in active code, violating the prior stable semantic naming decision.
- Corrected `trading-manager` so active Layer 3 code/tests use stable `monthly_backfill`; legacy local storage is bridged by an ignored symlink `/root/projects/trading-data/storage/monthly_backfill -> monthly_backfill_v1` rather than business code naming.
- Pushed `trading-manager` commit `66e7f85` (`Use stable monthly backfill storage id`). Verification passed: manager full tests, registry dry-run, compileall, diff-check.
- Follow-up after Chentong's stronger correction: added `trading-manager/tests/test_stable_semantic_ids.py` as a regression guard so active src/tests/docs cannot reintroduce `monthly_backfill_v1`; documented it in `tests/README.md`. Pushed commit `ae5be63` (`Guard stable semantic storage ids`). Verification passed: stable-id test, full manager tests, registry dry-run, compileall, diff-check.

## Registry payload direct-use audit and ThetaData cleanup
- Chentong clarified the stable-id audit should check direct use of registry `payload`, not just `_v1` tokens.
- Audit found active direct registry payload field-name inference only in three `trading-data` ThetaData feed pipelines: option selection snapshot, option primary tracking, and option event timeline.
- Updated those feeds so registry rows validate retained ids/kinds when present, but emitted field names come from stable code-local id maps rather than `row["payload"]`; renamed resolver method from `payload` to `field_name`.
- Added `trading-data/tests/test_registry_payload_boundary.py` to prevent these ThetaData feeds from reintroducing direct registry payload field-name inference.
- Verification passed: targeted boundary test, full `trading-data` 38 tests, compileall, diff-check. Pushed `trading-data` commit `89af5fc` (`Stop inferring ThetaData fields from registry payload`).

## Server-wide agent error handoff implemented
- Chentong clarified that automated agent diagnosis/repair should be server-wide, not limited to model training.
- Implemented `trading-manager/src/trading_manager_tasks/agent_error_handler.py` and CLI `scripts/tasks/call_agent_for_error.py` as the unified error-agent entrypoint for any component.
- New artifacts: `server_error_agent_request`, `agent_error_diagnosis`, and `agent_error_handling_result`; `schema_version` carries versioning while business ids remain stable.
- The entrypoint records component/repo/scope, command, exit code, stdout/stderr refs and bounded excerpts, evidence refs, allowed actions, forbidden actions, and a standard agent prompt. Default behavior writes/queues artifacts; actual agent runner invocation requires explicit reviewed runner config via CLI/env.
- Central safety boundary bans provider calls, broker/order/account mutation, secret exfiltration, destructive storage/service/package changes unless a separate approval path exists.
- Wired `stage_executor` so failed safe-offline model-training stage commands automatically create agent error request/diagnosis artifact refs and expose them through dashboard diagnostics.
- Registered migration `355_register_server_wide_agent_error_handoff.sql` and regenerated `scripts/registry/current.csv`.
- Verification passed in `trading-manager`: CLI smoke queued artifact, targeted tests, full 213 tests OK, registry dry-run no pending migrations, compileall OK, diff-check OK. Pushed commit `67b2e2e` (`Add server-wide agent error handoff`).

## Trading-manager Discord error notifications enabled
- Implemented Discord notification in the unified `trading-manager` server-wide agent error handoff path. Reviewed Discord destination: server `1480186849241731084`, channel `1504100135200620665`, target `channel:1504100135200620665`.
- `agent_error_handler.py` now supports best-effort Discord alerting via OpenClaw message CLI and records `discord_notification` in the handling result; notification failure does not block durable request/diagnosis artifacts.
- `call_agent_for_error.py` supports notification flags/env; scheduler env template and `/etc/default/trading-manager-historical-scheduler` set `MANAGER_AGENT_ERROR_NOTIFY_DISCORD=true` and the reviewed target.
- Historical scheduler daemon fatal startup/runtime exceptions now route through the same handoff before exiting non-zero; failed safe-offline stage commands already create error request/diagnosis refs.
- Verification passed in `trading-manager`: full 214 tests OK, registry dry-run no pending migrations, compileall OK, diff-check OK; OpenClaw message CLI dry-run resolved the Discord channel target.
- Pushed commits: `trading-manager` `ad48b30` (`Notify Discord for server error handoffs`) and `2feef8c` (`Route scheduler daemon fatals through error handoff`).
- Restarted `trading-manager-historical-scheduler.service`; service active with Discord env loaded. Scheduler advanced past the stale Layer 3 failure and was at `layer_04_event_overlay.model_generation` on `2019-05` with `last_error=None`.

## Pre-compaction flush — Discord error notification and scheduler handoff

- Continued the server-wide `trading-manager` error handoff work after compaction.
- Implemented Discord notification in the unified agent error handoff path for server/guild `1480186849241731084` and channel `1504100135200620665`, using OpenClaw message target `channel:1504100135200620665`.
- Notification behavior is best-effort and non-blocking: failed Discord delivery must not prevent server-error request/diagnosis/result artifacts from being written.
- Added runtime/env controls including `MANAGER_AGENT_ERROR_NOTIFY_DISCORD=true` and `MANAGER_AGENT_ERROR_DISCORD_TARGET=channel:1504100135200620665`; configured `/etc/default/trading-manager-historical-scheduler` so the service loads the Discord notification setting.
- Added scheduler-daemon fatal-error wrapping so errors outside individual stage execution, such as stale locks or daemon startup failures, route through `handle_server_error()` and can notify Discord.
- Verified `trading-manager`: full tests passed with 214 OK, registry dry-run had no pending migrations, compileall and diff-check passed, and Discord command dry-run resolved the target without sending a real alert.
- Pushed commits: `trading-manager` `ad48b30` (`Notify Discord for server error handoffs`) and `2feef8c` (`Route scheduler daemon fatals through error handoff`).
- Cleared a stale scheduler lock created during restart testing and restarted/verified the historical scheduler service. The service was active with Discord env loaded, had moved past the prior Layer 3 failure, and was at `2019-05` `layer_04_event_overlay.model_generation` with `last_error=None`.
- No fake Discord error notification was sent; only dry-run verification was used to avoid polluting the alert channel.

## Numbered server error catalog implemented
- Chentong asked for a real Discord test message and for all server errors to be organized together with a short number so he can cite the number later.
- Sent a Discord test alert to channel `1504100135200620665` with test reference `TEST-000000`; no fake error artifact/catalog row was created.
- Implemented `trading-manager` human-facing error numbering for every error routed through `handle_server_error()`: append-only `storage/runtime/agent_error_handling/server_error_catalog.jsonl` assigns monotonic refs like `ERR-000001` while `request_id` remains the machine-stable id.
- Discord error alerts now include `Error No: ERR-......`; request artifacts, handling results, and stage execution summaries carry `error_number` / `error_ref` or `agent_error_number` / `agent_error_ref`.
- Added `scripts/tasks/list_agent_errors.py` so an error ref can be resolved with `PYTHONPATH=src python3 scripts/tasks/list_agent_errors.py --error-ref ERR-000001`.
- Registered migration `357_register_agent_error_catalog.sql` and regenerated `scripts/registry/current.csv`.
- Verification passed in `trading-manager`: 216 tests OK, registry dry-run no pending migrations, compileall OK, diff-check OK, list-agent-errors smoke OK.
- Pushed `trading-manager` commit `70ef033` (`Add numbered server error catalog`) and restarted `trading-manager-historical-scheduler.service`; service active with Discord env loaded, current stage `2019-05 layer_04_event_overlay.model_evaluation`, last_error `None`.

## Scheduler stale-lock auto-recovery fixed
- Chentong asked why the `Workflow Blocked / Lock Stale` state did not auto-resolve after the alert system was added.
- Root cause: dashboard status classified a lock with a non-running PID as `stale` immediately, but `scheduler_daemon.acquire_daemon_lock()` only auto-replaced stale locks after `DEFAULT_STALE_LOCK_SECONDS = 6h`; until then systemd restart attempts failed and generated repeated numbered errors (`ERR-000014`..`ERR-000016`).
- Additional reason: the unified agent handoff defaults to queued diagnosis/notification only; automatic repair runner is not configured, and service/lock deletion is intentionally outside generic repair without explicit policy.
- Fixed `acquire_daemon_lock()` so a lock with a recorded dead PID is immediately replaceable; the age threshold remains only for malformed locks without a dead owner PID. Added regression tests for dead-PID replacement and recent malformed-lock protection.
- Verification passed in `trading-manager`: targeted scheduler/agent tests 19 OK, full 218 tests OK, registry dry-run no pending migrations, compileall OK, diff-check OK.
- Pushed `trading-manager` commit `b0381c1` (`Recover scheduler dead-pid locks immediately`). Restarted the scheduler after the patch; service active, `service_runtime_ready=True`, lock active with live PID, no open operational items, `last_error=None`, current stage `2019-05 layer_04_event_overlay.maintenance`.

## Manual error mechanism test succeeded
- Chentong asked to intentionally create a safe synthetic error to verify the server error notification/catalog mechanism.
- Ran `scripts/tasks/call_agent_for_error.py` with `source_component=manual.mechanism_test`, `error_kind=manual_test_error`, severity `warning`, exit code `42`, and explicit Discord notify target `channel:1504100135200620665`.
- Mechanism worked: catalog assigned `ERR-000018`, Discord send returned success with message id `1504108358687522907`, and request/diagnosis artifacts exist under `storage/runtime/agent_error_handling/erragent_82cac4cee0a92e8e/`.
- Diagnosis status is `queued`, which is expected because the reviewed auto-repair runner is still intentionally not configured.

## Trading-manager server error auto-repair/dedup completed
- Chentong said automatic repair is the core requirement and error alerts need time plus deduplication.
- Implemented `trading-manager` server-error deduplication: repeated errors with the same fingerprint inside the 3600-second window reuse the existing `ERR-......` number, append `server_error_catalog_occurrence`, and suppress duplicate Discord notifications by default.
- Discord-facing error messages now include occurred and recorded timestamps in both UTC and America/New_York, plus deduplication status.
- Added reviewed deterministic safe auto-repair runner `scripts/tasks/run_safe_error_repair.py` / `src/trading_manager_tasks/safe_error_repair.py`; current allowed repair is scheduler dead-PID lock removal only. Unknown errors remain diagnosis-only. No provider calls, broker/account mutation, arbitrary shell execution, package/service destructive changes, or secret exposure are allowed.
- Configured scheduler env with `MANAGER_AGENT_ERROR_AUTOCALL=true`, runner command `/usr/bin/python3 /root/projects/trading-manager/scripts/tasks/run_safe_error_repair.py`, and `MANAGER_AGENT_ERROR_DEDUP_SECONDS=3600`; restarted `trading-manager-historical-scheduler.service` and verified it is active with `service_runtime_ready=true`, `last_error=null`, and no open operational items.
- Verification passed: synthetic dead-PID lock repair removed the lock, duplicate synthetic error reused `ERR-000001` and suppressed Discord; `trading-manager` 222 tests OK, registry dry-run no pending migrations, compileall OK, and diff-check OK.
- Registered migration `358_register_agent_error_repair_and_dedup.sql` and pushed `trading-manager` commit `cbcbea1` (`Add deduplicated safe error repair`). Repo clean on `main...origin/main` afterward.

## Dashboard Tasks list-first view completed
- Chentong clarified that Tasks should show operational work history/current/future tasks at a finer level such as data acquisition and feature generation, while model-specific historical-modeling widgets belong under Models.
- `trading-manager` now adds sanitized `historical_task_progress_summary.chart_payload.task_timeline` rows from workflow state or the current workflow plan. Rows include sequence, task id/label/state, status, stage type, layer, timestamp, reason, receipt count, and blocker count; no raw dashboard internals are required by the UI.
- `trading-dashboard` now keeps the left sidebar fixed on desktop, renders Tasks as a list-first task timeline plus Last Execution/Next Step, moves Current month, Active stage, Historical Modeling Progress, Latest Stage Coverage, and gates to Models, and removes the low-value `Task Progress Summary` page card.
- Public read model refreshed successfully; current timeline contains 48 stages and the live Tasks page shows the first current item as Layer 1 Data Acquisition with future feature/model/evaluation stages visible.
- Verification passed: `trading-manager` 222 tests, registry dry-run no pending migrations, compileall, diff-check; `trading-dashboard` Python tests, Vite build/typecheck, compileall, diff-check; browser/CDP smoke confirmed Tasks and Models render as intended.
- Pushed commits: `trading-manager` `3f0f0a9` (`Expose dashboard task timeline`) and `trading-dashboard` `20b34aa` (`Make tasks page list-first`).

## Dashboard Tasks/Models split completed
- Completed Chentong's dashboard request to finish current task content before broader dashboard expansion.
- `trading-manager` now emits a sanitized `task_timeline` inside `historical_task_progress_summary.chart_payload`, sourced from manager workflow state and suitable for dashboard consumption without exposing raw internals. Timeline rows cover past/current/future workflow stages with stable ids, layer metadata, stage/work type, state/status, updated time, reason, receipt counts, and blocker counts.
- Registered dashboard task timeline vocabulary via migration `359_register_dashboard_task_timeline.sql` and regenerated `scripts/registry/current.csv`.
- `trading-dashboard` left navigation is fixed/sticky on desktop. `Tasks` is now list-first (`Task list`) and shows operational task history/current/future work plus Last Execution/Next Step; model-specific cards (`Current month`, `Active stage`, `Historical Modeling Progress`, `Latest Stage Coverage`) moved to `Models` (`Historical modeling`). The generic/low-value `Task Progress Summary` page content was removed.
- Verification passed: `trading-manager` 222 tests OK, registry dry-run no pending migrations, compileall OK, diff-check OK; `trading-dashboard` tests/typecheck/build OK, compileall OK, diff-check OK, browser/CDP smoke confirmed Tasks and Models render.
- Pushed commits: `trading-manager` `3f0f0a9` (`Expose dashboard task timeline`) and `trading-dashboard` `20b34aa` (`Make tasks page list-first`).

## Dashboard Task List filters added
- Chentong asked for filters at the top of Task List by layer, status, and task/work type, with the default showing only current `Now` work instead of the full task timeline.
- Updated `trading-dashboard` Task List with layer/status/task select filters, a `Showing X of Y tasks` summary, and `Reset to Now`. Default filter state is status `Now`, all layers, all task types.
- Updated dashboard docs with accepted decision D016: filtering is read-only presentation over the complete storage-hosted task timeline; the underlying timeline remains complete.
- Verification passed: dashboard Python tests OK, Vite build/typecheck OK, compileall OK, diff-check OK, and browser/CDP smoke confirmed default Tasks view shows 1 row with Status `Now` and the expected filters.
- Pushed `trading-dashboard` commit `1619344` (`Add task list filters`).

## Dashboard task child-row/detail refinement completed
- Completed Chentong's requested Task List refinement: months are grouping/work-window dimensions, while visible rows are finest child tasks at month + layer + stage/work-type granularity.
- `trading-manager` read model now emits `task_timeline[].month` and sanitized expandable `detail` with blockers, receipt refs/counts, safety posture, latest execution summary, and matching stage-coverage progress when available.
- `trading-dashboard` Task List now groups by month, defaults to current `Now` tasks, keeps layer/status/task filters, and each row can expand to show detail/progress; current tasks show progress detail or an explicit no-progress-artifact message.
- Verification passed: `trading-manager` 222 tests, registry dry-run, compileall, diff-check; `trading-dashboard` 5 tests, Vite build/typecheck, compileall, diff-check; browser/CDP smoke confirmed month grouping and expanded detail rendering.
- Pushed commits: `trading-manager` fd2000c (`Add dashboard task detail read model`) and `trading-dashboard` 894f303 (`Group task rows by month with details`).

## Pre-compaction flush — dashboard task list/details completion

- Completed the dashboard `Tasks` refinement requested by Chentong: months are grouping/batch dimensions while visible task rows are fine-grained `month + layer + stage/work_type` child tasks.
- `trading-manager` read model now exposes sanitized `task_timeline[].month` and expandable `task_timeline[].detail` fields with blockers, receipt counts/refs, safety posture, latest execution summary, and matching stage-coverage progress when available.
- Added registry migration `360_register_dashboard_task_detail_fields.sql` and updated manager task/decision docs with D136.
- `trading-dashboard` now groups task rows by month, preserves Layer/Status/Task filters with default `Now`, and adds row-level expandable `Details` panels. Expanded details show task identity, reason, progress/current-stage evidence, latest execution, safety posture, receipts, and blockers.
- Verification passed before final report: `trading-manager` 222 tests, registry dry-run no pending migrations, compileall, diff-check; `trading-dashboard` tests, Vite build/typecheck, compileall, diff-check; browser smoke confirmed `Tasks` renders `2019-06` grouping and expandable current-task details.
- Pushed commits: `trading-manager` `fd2000c` (`Add dashboard task detail read model`) and `trading-dashboard` `894f303` (`Group task rows by month with details`).
- Recommended next action for the project: deepen the expanded current-task progress panel with richer stage coverage/execution evidence if the available sanitized read model data is still too status-level.

## Dashboard task history month visibility fixed
- Chentong asked why the Task List only showed 2019-06 and where previous months were.
- Root cause: the manager dashboard read model emitted only the active scheduler month's workflow plan/checkpoint, so completed month workflow-state files were not included in `task_timeline`.
- Fixed `trading-manager` so `task_timeline` includes completed months from daemon `last_completed_months` when durable `model_training_workflow_state_<month>.json` files exist, then appends the active/current month. Only the active month can have `current`, latest execution, or active stage-coverage progress.
- Added a dashboard Month filter in `trading-dashboard`; default `Now` still shows only active work, but changing Status to Past/completed exposes prior months.
- Refreshed storage read model: latest timeline now has 1,968 child tasks across 41 months, from 2016-02 through 2019-06; states are 1,680 completed, 246 skipped, 41 future, 1 current.
- Verification passed: `trading-manager` 223 tests, registry dry-run, compileall, diff-check; `trading-dashboard` 5 tests, Vite build/typecheck, compileall, diff-check; browser smoke confirmed default Now shows 1/1968 and completed status shows prior month groups starting 2016-02.
- Pushed commits: `trading-manager` `7f924ff` (`Show completed months in dashboard tasks`) and `trading-dashboard` `0223a84` (`Add month filter to task list`).

## Pre-compaction flush — dashboard task view and scheduler freshness recovery

- Completed the current dashboard/task experience before broader page expansion:
  - `trading-dashboard` left nav is fixed/sticky on desktop.
  - `Tasks` is now a list-first task timeline, not a generic progress-summary card.
  - Model-specific historical-modeling widgets moved from `Tasks` to `Models`.
  - `Tasks` supports Month, Layer, Status, and Task/work-type filters; default status is `Now`.
  - Timeline rows are fine-grained subtasks grouped by month; month is a grouping/filter dimension, not the task identity.
  - Task rows have expandable sanitized detail panels; current rows show progress/detail evidence when attached.
- Manager dashboard read model now exposes sanitized `task_timeline` with stable task ids/labels, status, month, layer, stage/work type, receipt/blocker counts, and expandable `detail`.
  - Completed historical workflow-state months are included before the active month when durable state files exist.
  - Latest observed public read model: 1968 child tasks across 41 months (`2016-02` through `2019-06`), with 1680 completed, 246 skipped, 41 future, and 1 current.
- Added/maintained dashboard/task decisions:
  - Dashboard remains read-only and consumes storage-hosted summaries.
  - Active code/docs/tests use stable semantic ids; versioning belongs in `schema_version`/metadata or immutable migration history.
  - Direct registry `payload` usage must not drive business field/path/contract names.
- Implemented manager-owned server-wide error handling path:
  - `trading_manager_tasks.agent_error_handler`, `scripts/tasks/call_agent_for_error.py`, `list_agent_errors.py`, and `run_safe_error_repair.py`.
  - Server errors receive numbered refs like `ERR-000001`, deduplicate by fingerprint within the configured window, include UTC and America/New_York timestamps, and can notify Discord target `channel:1504100135200620665` / guild `1480186849241731084`.
  - Configured scheduler env for Discord notification, autocall, safe repair runner, and 3600s dedup window.
  - Current reviewed automatic repair class is scheduler dead-PID lock cleanup only; unknown errors remain diagnosis-only.
- Scheduler stale-data investigation result:
  - Dashboard read model refresh was fresh, but underlying source artifacts looked stale because the historical scheduler had stopped progressing after repeated DB check-constraint failures.
  - Root cause: active code emitted stable `contract_type='manager_request'`, but SQL check constraints/defaults still accepted old `_v1` names after stable semantic id normalization.
  - Added migration `362_normalize_control_plane_contract_type_constraints.sql` in `trading-manager` to normalize durable control-plane `contract_type` values/defaults/checks for `manager_request`, `input_binding`, `run_manifest`, `run_step`, `artifact_ref`, `ready_signal`, and `manager_failure_register`.
  - After applying, the next scheduler tick recovered: `last_decision_status=executed`, `last_error=None`, `consecutive_errors=0`, `model_training_workflow_state_2019-06.json` regenerated, and Layer 1 / 2019-06 data acquisition showed `5/22 ready, 17 pending`.
  - Verification passed in `trading-manager`: 223 tests OK, registry dry-run no pending migrations, compileall OK, diff-check OK.
  - Pushed `trading-manager` commit `1adf62c`.
- Pushed related dashboard/manager commits during this work included:
  - `trading-manager`: `ad48b30`, `2feef8c`, `70ef033`, `b0381c1`, `cbcbea1`, `3f0f0a9`, `fd2000c`, `7f924ff`, `1adf62c`.
  - `trading-dashboard`: `20b34aa`, `1619344`, `894f303`, `0223a84`.
- Recommended next improvement: adjust Dashboard Data copy/freshness semantics so users can distinguish “dashboard read model refresh time” from event-driven “source artifact last write time”; stale event-driven artifacts should not automatically look like dashboard refresh failures.

## Dashboard per-task lifecycle timestamps completed

- Chentong asked that every visible dashboard task show generation/creation time, start time, end time, and status update time so stuck tasks are easy to detect.
- `trading-manager` dashboard read model now enriches `historical_task_progress_summary.chart_payload.task_timeline` rows with sanitized lifecycle timestamps: `created_at_utc`, `started_at_utc`, `ended_at_utc`, and `status_updated_at_utc`, derived from workflow stage metadata and attached manager-owned receipt timing metadata when available. Existing `updated_at_utc` remains for compatibility.
- Added migration `364_register_dashboard_task_lifecycle_timestamps.sql`, regenerated `scripts/registry/current.csv`, and added decision D138.
- `trading-dashboard` task detail panels now show a Task timing card with Generated, Started, Ended, and Status updated values; task row meta now says Status updated instead of generic Updated.
- Refreshed the storage read model; latest `historical_task_progress_summary` still has 1968 task rows, and the current 2019-06 task exposes created/status-updated timestamps while start/end remain `Not recorded` until execution evidence appears.
- Verification passed: `trading-manager` full tests, registry dry-run, compileall, diff-check; `trading-dashboard` tests, Vite build/typecheck, diff-check.
- Pushed commits: `trading-manager` `4be1121` (`Expose dashboard task lifecycle timestamps`) and `trading-dashboard` `ba32288` (`Show task lifecycle timestamps`).

## Historical scheduler dynamic provider multithreading activated
- Chentong asked to raise throughput because server load was very low, using dynamic thread selection based on load and memory, and to show multitask parameters on the Status page.
- Implemented bounded concurrent provider acquisition only for historical provider dispatch; scheduler still selects one stage per tick and broker/account/model activation boundaries remain unchanged.
- `trading-manager` provider dispatch now selects worker count dynamically from CPU load and memory, with defaults: interval 60 seconds, provider batch limit 12, max workers 4, load target 0.70 per CPU, worker memory budget 512 MB, reserved memory 2048 MB.
- `trading-storage` current system status read model now emits `chart_payload.parallelism`; refreshed latest status showed selected/max workers 4/4, batch limit 12, interval 60s, 16 CPUs, ~26 GiB available memory, status active.
- `trading-dashboard` Current Status now renders a Multitask Threads card with worker/thread, batch, interval, load, and memory parameters.
- Registered migration `365_register_scheduler_parallelism_status.sql`; verification passed across manager/storage/dashboard; pushed commits: `trading-manager` `018ae50`, `trading-storage` `06c9107`, `trading-dashboard` `24cc430`.
- Installed the updated systemd unit/env and restarted `trading-manager-historical-scheduler.service`; service active with `--interval-seconds 60 --provider-stage-next-limit 12 --provider-stage-max-workers 4`.
- First post-restart scheduler tick dispatched the remaining 5 Layer 2 provider requests for 2019-06 and advanced `layer_02_sector_context.data_acquisition` to succeeded; next stage became Layer 2 feature generation.

## Dashboard terminal task timing fallbacks fixed
- Chentong noticed many dashboard task detail rows showed `Started: Not recorded` and `Ended: Not recorded`.
- Root cause: older terminal workflow-state rows often only retained `updated_utc` plus stage-coverage refs, not manager stage-execution receipts with explicit start/end timing metadata.
- Updated `trading-manager` dashboard read-model timestamp enrichment so terminal rows (`succeeded`, `failed`, `not_applicable`) use the workflow status-update timestamp as best-available started/ended timing when no finer receipt timing exists. Open/current/future rows still remain unfilled until real execution timing exists.
- Refreshed historical task progress read model: 1968 tasks; completed rows now have 1708/1708 started and ended, skipped rows 246/246 started and ended; only current/future open rows remain without start/end.
- Verification passed: `trading-manager` 226 tests OK, registry dry-run no pending migrations, compileall OK, diff-check OK. Pushed `trading-manager` commit `77d792b` (`Fill terminal task timing fallbacks`).
