## Trading dashboard stale service restart
- Chentong reported the trading dashboard seemed broken at 2026-06-20 03:14 EDT.
- Loaded project development baseline and checked memory: a prior 2026-05-21 dashboard outage had been caused by stale storage read-model path consumption.
- Current diagnosis: `trading-dashboard-web.service` was active and port 5173 served HTML, but it had last built on 2026-06-17 while dashboard code changed on 2026-06-19. The running Vite preview still rejected `/api/read-models/model_readiness_summary/latest` with `unregistered dashboard read-model contract_type: model_readiness_summary`.
- Restarted only `trading-dashboard-web.service`; `npm run build` succeeded and the endpoint returned 200 afterward. Dashboard read models were fresh.
- Remaining system posture: `current_system_status_summary` still reports `degraded` because `trading-manager-historical-scheduler.service` is enabled but inactive/dead since 2026-06-18 10:38:51 EDT. Evidence shows it was stopped by SIGTERM with `result=success`, not a crash. Did not restart the historical scheduler because that can resume provider/storage/modeling work and needs explicit approval.

## Model-group replay repair
- Chentong asked to clear replay todos and inspect stock selection layer by layer. Continued the replay repair after gateway interruption.
- Fixed and pushed `trading-evaluation` commit `beb101b Preserve monthly replay progress`: replay-month fallback equity bar loading now stays within the requested month, and global replay progress preserves prior completed months instead of overwriting them.
- Runtime evidence now has completed full fixed-universe replay months 2021-01 and 2021-02. Latest successful February run: `model_group_replay_20260620T093530Z`; manager plan-only now reports `ready_replay_months=2` and next `replay_month=2021-03`.
- M05 replay option feature repair used ThetaData provider calls for February point-in-time requirements only; replay safety flags remained broker/account/order/model-activation false. Early stock-selection evidence is no longer AAPL-only, but execution/fill remains blocked by missing selected option contract paths, so replay rows are still `simulated_rejected`.

## Fold-scoped replay contract fixes
- Before the monthly replay-progress repair, `trading-manager` was fixed and pushed at commit `02d34b1 Fix replay model scope for stock selection`: replay `candidate_model_ref` now uses fold-scoped refs such as `storage://trading-manager/model_group/2016-01_2016-06`, after-cost alpha artifacts use fold-scoped names, training no longer passes `--target-symbol`, and missing alpha training scripts produce explicit backoff instead of unclear subprocess errors.
- `trading-evaluation` was fixed and pushed at commit `7734738 Reject target scoped refs in full universe replay`: full-universe replay now rejects target-scoped refs such as `model_group/aapl/...` so AAPL-trained target artifacts cannot masquerade as stock-selection replay evidence.
- Verification at that point: `trading-manager` 17 tests passed, `trading-evaluation` 40 tests passed, targeted `compileall` passed, and `git diff --check` passed. The old `model_group_replay_20260617T180922Z` remains failure-diagnosis evidence only, not valid stock-selection evidence.

## Replay review first two months
- After Chentong approved fixing the replay issues, pushed `trading-manager` commit `691b8d0 Validate replay month progress`: replay progress readiness now requires progress month, receipt `replay_month`, and decision-row timestamp months to match, so polluted rows such as `model_group_replay_20260620T092205Z` cannot unlock completed months.
- Removed the polluted 2021-01 progress row from `promotion_replay_candidate_policy/replay_progress.jsonl`. Clean January was regenerated as `model_group_replay_20260620T120317Z`; then selected option contract paths were acquired through the replay-owned path route for clean January and February.
- Path-verified reruns are now the formal progress rows: `model_group_replay_20260620T120729Z_2021_01_path_verify` and `model_group_replay_20260620T120745Z_2021_02_path_verify`. Manager plan-only reports `ready_replay_months=2` and next `replay_month=2021-03`.
- Layer review finding: first two months do not show catastrophic stock selection failure. Selected underlying next-day returns beat the same-timestamp candidate universe: January selected average `+3.91%` with average cross-sectional percentile `0.692`; February selected average `+1.03%` with percentile `0.588`.
- The main defect is expression/execution mechanism, not raw stock selection: January selected 31 targets but only 7 long-call rows filled; February selected 29 targets but only 9 long-call rows filled. The rest were mostly `underlying_only_expression` rows that consumed planned notional in portfolio selection but were `simulated_rejected` and realized `0`, leaving cash exhausted early in the month.
- Option expression itself is mixed: January long calls averaged `+16.5%` realized, but February long calls averaged `-7.7%`; February failures were dominated by ZS, MS, SNOW, and UBER despite several strong underlying picks.

## Replay executable-capital fix
- Chentong approved the next fix after the first two-month replay review. Pushed `trading-evaluation` commit `3c6f034 Skip unexecutable option plans in replay capital`.
- Fix: `_select_candidate_policy_portfolio_replay_keys` now skips M05 plans that exist but have no selected option contract before consuming replay capital. `plan is None` still means feature data is missing and should trigger replay option-feature requirements rather than silently skipping.
- Added regression test `test_portfolio_selection_skips_unexecutable_m05_plan_before_capital`: a higher-ranked AAPL candidate with `underlying_only_expression` no longer blocks a lower-ranked executable MSFT option candidate from using capital.
- Verification passed: `trading-evaluation` full unittest discover ran 87 tests OK, `compileall src` OK, and `git diff --check` OK.
- Real 2021-01 executable-capital validation now advances beyond the previous early rejected rows, but it exposes a large tail of point-in-time M05 option-feature requirements. Acquired several bounded batches through the replay-owned ThetaData/provider route, but the latest check still reports 6 requirements at `2021-01-14T16:00:00-05:00` for `F`, `INTC`, `KLAC`, `MSTR`, `NCLH`, `ONDS`; direct feature-table reads for those keys returned 0 rows despite the drain route reporting ready. Formal `replay_progress.jsonl` was not moved to the incomplete executable-capital validation runs.

## Replay option-feature repair readiness
- Fixed and pushed `trading-manager` commit `b3e1c92 Verify replay option feature repair`: `run_model_group_replay_option_features_for_replay_backoff` now performs a post-repair `_feature_missing_requirements` check over the same batch before returning ready/executed. If replay still cannot read feature rows, the drain returns `backoff` with `model_group_replay_option_feature_repair_incomplete` instead of falsely declaring readiness.
- Verification passed: `tests.test_model_group_replay_option_features` plus `tests.test_model_group_replay` ran 31 tests OK, `compileall src` had passed earlier, and `git diff --check` passed.
- Real validation used the latest six old requirements (`F`, `INTC`, `KLAC`, `MSTR`, `NCLH`, `ONDS`) from `model_group_replay_20260620T134833Z_2021_01_exec_cap_check`; the bounded drain generated rows or recorded source unavailability and passed the new post-repair check.
- The next 2021-01 executable-capital rerun advanced past those old six and exposed the next batch at the same timestamp: `AAL`, `AAOI`, `FCX`, `FITB`, `TWLO`, `XOM`. This is progress evidence, not completed replay evidence; formal replay progress should stay on the path-verified January/February rows until executable-capital validation completes.
