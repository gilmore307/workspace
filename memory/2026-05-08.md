# 2026-05-08

## Trading-data design closeout completed

- Closed `trading-data` repository at the data-source/model-input design phase without claiming production approval.
- Added `docs/95_data_stack_closeout.md` and updated README/docs spine/task/decision/model-input docs to state that active data-design tasks are closed and remaining items are production hardening / manager-storage orchestration.
- Resolved prior active hardening items conservatively:
  - `source_02_target_candidate_holdings` default `available_time` is now the next regular US session open after `as_of_date` when no explicit source/task availability evidence is supplied.
  - `equity_abnormal_activity_event` default `model_standard` is now `equity_abnormal_activity_conservative_v1` with `calibration_status=conservative_fixture_default_not_production_calibrated`; production labels/gates still require reviewed historical calibration evidence.
- Added `tests/test_data_closeout_readiness.py` covering holdings availability default/override and equity-abnormal-activity standard propagation.
- Registered shared closeout policies in `trading-manager` migration `240_register_data_stack_closeout_contracts.sql` and regenerated `scripts/registry/current.csv`.
- Verification passed:
  - `trading-data`: `PYTHONPATH=src python3 -m unittest discover tests` => 19 tests OK; `git diff --check` clean.
  - `trading-manager`: `PYTHONPATH=src python3 -m unittest discover tests` => 35 tests OK; registry dry-run no pending migrations; `git diff --check` clean.
- Pushed commits:
  - `trading-data` `69357e8` — `Close out data stack design phase`
  - `trading-manager` `544cfc4` — `Register data stack closeout contracts`

## Promotion closeout decisions actually persisted

- Corrected earlier overstatement: repo/docs closeout was not the same as actual promotion closeout.
- Ran real read-only PostgreSQL evaluations where current production evidence tables exist:
  - Layer 1 `model_01_market_regime`: `trading_data.feature_01_market_regime` + `trading_model.model_01_market_regime` -> eval run `mdevrun_1d00f2757982bd63`, snapshot `mdsnap_dc61e0e823ca4850`.
  - Layer 2 `model_02_sector_context`: `trading_data.feature_02_sector_context` + `trading_model.model_02_sector_context` -> eval run `mdevrun_00c81e53569941df`, snapshot `mdsnap_fa3982c8d482017f`.
- Persisted deferred promotion decisions to PostgreSQL:
  - Layer 1 candidate `mpcand_b79411e80a774787`, decision `mpdec_d743cb5dbc8159f2`; blockers: stability evidence and failed thresholds (baseline, leakage/alignment, row count, stability).
  - Layer 2 candidate `mpcand_a6044e72162553f9`, decision `mpdec_3ab83ea1f423326d`; blocker: failed thresholds (baseline improvement and split stability).
- Verified no activation rows for Layers 1-2 (`model_promotion_activation` count 0); no production config was activated.
- Added `trading-model/docs/96_promotion_closeout.md`, updated readiness docs/status, and updated readiness matrix code.
- Registered manager shared status in migration `241_register_promotion_closeout_decisions.sql`; status now records Layers 1-2 as deferred-after-real-evaluation and Layers 3-8 as blocked-no-production-eval-run.
- Verification passed: `trading-model` 98 tests OK; `trading-manager` 35 tests OK; registry dry-run no pending migrations; `git diff --check` clean.
- Pushed commits: `trading-model` `983c60a`; `trading-manager` `12e9303`.

## Full Layer 1-8 promotion closeout receipts completed

- Chentong pushed back that all promotion items should be done, not just Layers 1-2 plus informal blockers.
- Persisted formal blocked/deferred promotion closeout rows for Layers 3-8, including blocked eval runs, `production_eval_run_available = 0` metrics, promotion candidates, and deferred decisions:
  - Layer 3 `model_03_target_state_vector`: eval `mdevrun_closeout_l03_no_eval_substrate_20260508`, candidate `mpcand_fffc92ba53b09199`, decision `mpdec_31899733788d324d`.
  - Layer 4 `model_04_event_overlay`: eval `mdevrun_closeout_l04_no_eval_substrate_20260508`, candidate `mpcand_6ab73401f22ab057`, decision `mpdec_c118afa20c4e9bf2`.
  - Layer 5 `model_05_alpha_confidence`: eval `mdevrun_closeout_l05_no_eval_substrate_20260508`, candidate `mpcand_72289e5cc95ae2d5`, decision `mpdec_dc408c9914a4723a`.
  - Layer 6 `model_06_position_projection`: eval `mdevrun_closeout_l06_no_eval_substrate_20260508`, candidate `mpcand_622c6ffa9ffca030`, decision `mpdec_7b9d7279fecfdf6a`.
  - Layer 7 `model_07_underlying_action`: eval `mdevrun_closeout_l07_no_eval_substrate_20260508`, candidate `mpcand_d4911cef39a14b97`, decision `mpdec_5e6e83b02ccda12e`.
  - Layer 8 `model_08_option_expression`: eval `mdevrun_closeout_l08_no_eval_substrate_20260508`, candidate `mpcand_9de333239d5c3f12`, decision `mpdec_90721592be6591c8`.
- Updated `trading-model/docs/96_promotion_closeout.md` so every Layer 1-8 row now has a decision receipt; updated readiness matrix and tests.
- Added `trading-manager` migration `242_register_full_promotion_closeout_decisions.sql`; registry now records Layers 1-2 as `deferred_after_real_evaluation` and Layers 3-8 as `deferred_no_production_eval_substrate`.
- Verification passed: `trading-model` 98 tests OK; `trading-manager` 35 tests OK; registry dry-run no pending migrations; `git diff --check` clean.
- DB verification: 8/8 closeout decisions are deferred and `model_promotion_activation` count for `model_0%` is 0.
- Pushed commits: `trading-model` `99b285d`; `trading-manager` `bf5cd90`.

## Layer 3-8 promotion closeout rerouted through reviewer agent

- Chentong clarified that Layers 3-8 should follow the same principle as Layers 1-2: the script must call the reviewer agent for final audit before persisting the closeout decision.
- Added `trading-model/scripts/models/review_layers_03_08_promotion_closeout.py`.
  - The script builds blocked evaluation artifacts when production eval substrate is missing.
  - It creates promotion candidates, calls `openclaw agent`, validates the reviewer JSON, persists the reviewed decision, and never activates deferred/rejected candidates.
- Ran the script for Layers 3-8 with `--all --write-decision`.
- New reviewer-agent deferred decisions:
  - Layer 3 `model_03_target_state_vector`: `mpdec_d8e027dd9b5aa939`.
  - Layer 4 `model_04_event_overlay`: `mpdec_76b07ea01a3f525b`.
  - Layer 5 `model_05_alpha_confidence`: `mpdec_9c3e19d6559ef55b`.
  - Layer 6 `model_06_position_projection`: `mpdec_b118232e76fae092`.
  - Layer 7 `model_07_underlying_action`: `mpdec_fabc9c709149a698`.
  - Layer 8 `model_08_option_expression`: `mpdec_e7448aaab1334345`.
- DB verification after the rerun: current expected 8/8 Layer 1-8 closeout decisions are deferred; model-layer activation count remains 0.
- Updated `trading-model` docs/readiness/tests and pushed commit `2c6686f` (`Add agent-reviewed Layer 3-8 closeout`). Verification: 100 tests OK.
- Added `trading-manager` migration `243_register_agent_reviewed_layer_3_8_closeout.sql`, registered the callable script and updated closeout receipts/status matrix/current registry CSV, then pushed commit `33fecf6` (`Register agent-reviewed Layer 3-8 closeout`). Verification: 35 tests OK and registry dry-run no pending migrations.

## Layer 3 production eval substrate follow-up
- Chentong asked whether the Layers 3-8 production-eval substrate section could be closed out honestly.
- Built real Layer 3 substrate from Alpaca-backed ETF target bars via `trading-data` source/feature path into PostgreSQL: 576 `trading_data.feature_03_target_state_vector` rows and 576 generated `trading_model.model_03_target_state_vector` rows.
- Persisted Layer 3 evaluation artifacts: snapshot `mdsnap_9b7c3bd598114c7c`, eval run `mdevrun_327616bb447ceb5b`, 1604 labels, 19 metrics, candidate `mpcand_1b077bca49a18dbf`, reviewer decision `mpdec_70fef0f31847cc1c`.
- Layer 3 measured thresholds passed, but reviewer deferred promotion because Layer 1/2 are not production-approved/active and Layer 3 calibration evidence is missing. No activation row was created.
- Added reproducible script `trading-model/scripts/models/model_03_target_state_vector/review_target_state_vector_production_substrate.py`, updated docs/decisions/tests, and registered the new Layer 3 substrate receipt in `trading-manager` migration `244_register_layer_3_production_eval_substrate.sql`.
- Layers 4-8 remain honestly blocked on missing real production eval surfaces/labels/metrics.
# 2026-05-08

## Trading-data design closeout completed

- Closed `trading-data` repository at the data-source/model-input design phase without claiming production approval.
- Added `docs/95_data_stack_closeout.md` and updated README/docs spine/task/decision/model-input docs to state that active data-design tasks are closed and remaining items are production hardening / manager-storage orchestration.
- Resolved prior active hardening items conservatively:
  - `source_02_target_candidate_holdings` default `available_time` is now the next regular US session open after `as_of_date` when no explicit source/task availability evidence is supplied.
  - `equity_abnormal_activity_event` default `model_standard` is now `equity_abnormal_activity_conservative_v1` with `calibration_status=conservative_fixture_default_not_production_calibrated`; production labels/gates still require reviewed historical calibration evidence.
- Added `tests/test_data_closeout_readiness.py` covering holdings availability default/override and equity-abnormal-activity standard propagation.
- Registered shared closeout policies in `trading-manager` migration `240_register_data_stack_closeout_contracts.sql` and regenerated `scripts/registry/current.csv`.
- Verification passed:
  - `trading-data`: `PYTHONPATH=src python3 -m unittest discover tests` => 19 tests OK; `git diff --check` clean.
  - `trading-manager`: `PYTHONPATH=src python3 -m unittest discover tests` => 35 tests OK; registry dry-run no pending migrations; `git diff --check` clean.
- Pushed commits:
  - `trading-data` `69357e8` — `Close out data stack design phase`
  - `trading-manager` `544cfc4` — `Register data stack closeout contracts`

## Promotion closeout decisions actually persisted

- Corrected earlier overstatement: repo/docs closeout was not the same as actual promotion closeout.
- Ran real read-only PostgreSQL evaluations where current production evidence tables exist:
  - Layer 1 `model_01_market_regime`: `trading_data.feature_01_market_regime` + `trading_model.model_01_market_regime` -> eval run `mdevrun_1d00f2757982bd63`, snapshot `mdsnap_dc61e0e823ca4850`.
  - Layer 2 `model_02_sector_context`: `trading_data.feature_02_sector_context` + `trading_model.model_02_sector_context` -> eval run `mdevrun_00c81e53569941df`, snapshot `mdsnap_fa3982c8d482017f`.
- Persisted deferred promotion decisions to PostgreSQL:
  - Layer 1 candidate `mpcand_b79411e80a774787`, decision `mpdec_d743cb5dbc8159f2`; blockers: stability evidence and failed thresholds (baseline, leakage/alignment, row count, stability).
  - Layer 2 candidate `mpcand_a6044e72162553f9`, decision `mpdec_3ab83ea1f423326d`; blocker: failed thresholds (baseline improvement and split stability).
- Verified no activation rows for Layers 1-2 (`model_promotion_activation` count 0); no production config was activated.
- Added `trading-model/docs/96_promotion_closeout.md`, updated readiness docs/status, and updated readiness matrix code.
- Registered manager shared status in migration `241_register_promotion_closeout_decisions.sql`; status now records Layers 1-2 as deferred-after-real-evaluation and Layers 3-8 as blocked-no-production-eval-run.
- Verification passed: `trading-model` 98 tests OK; `trading-manager` 35 tests OK; registry dry-run no pending migrations; `git diff --check` clean.
- Pushed commits: `trading-model` `983c60a`; `trading-manager` `12e9303`.

## Full Layer 1-8 promotion closeout receipts completed

- Chentong pushed back that all promotion items should be done, not just Layers 1-2 plus informal blockers.
- Persisted formal blocked/deferred promotion closeout rows for Layers 3-8, including blocked eval runs, `production_eval_run_available = 0` metrics, promotion candidates, and deferred decisions:
  - Layer 3 `model_03_target_state_vector`: eval `mdevrun_closeout_l03_no_eval_substrate_20260508`, candidate `mpcand_fffc92ba53b09199`, decision `mpdec_31899733788d324d`.
  - Layer 4 `model_04_event_overlay`: eval `mdevrun_closeout_l04_no_eval_substrate_20260508`, candidate `mpcand_6ab73401f22ab057`, decision `mpdec_c118afa20c4e9bf2`.
  - Layer 5 `model_05_alpha_confidence`: eval `mdevrun_closeout_l05_no_eval_substrate_20260508`, candidate `mpcand_72289e5cc95ae2d5`, decision `mpdec_dc408c9914a4723a`.
  - Layer 6 `model_06_position_projection`: eval `mdevrun_closeout_l06_no_eval_substrate_20260508`, candidate `mpcand_622c6ffa9ffca030`, decision `mpdec_7b9d7279fecfdf6a`.
  - Layer 7 `model_07_underlying_action`: eval `mdevrun_closeout_l07_no_eval_substrate_20260508`, candidate `mpcand_d4911cef39a14b97`, decision `mpdec_5e6e83b02ccda12e`.
  - Layer 8 `model_08_option_expression`: eval `mdevrun_closeout_l08_no_eval_substrate_20260508`, candidate `mpcand_9de333239d5c3f12`, decision `mpdec_90721592be6591c8`.
- Updated `trading-model/docs/96_promotion_closeout.md` so every Layer 1-8 row now has a decision receipt; updated readiness matrix and tests.
- Added `trading-manager` migration `242_register_full_promotion_closeout_decisions.sql`; registry now records Layers 1-2 as `deferred_after_real_evaluation` and Layers 3-8 as `deferred_no_production_eval_substrate`.
- Verification passed: `trading-model` 98 tests OK; `trading-manager` 35 tests OK; registry dry-run no pending migrations; `git diff --check` clean.
- DB verification: 8/8 closeout decisions are deferred and `model_promotion_activation` count for `model_0%` is 0.
- Pushed commits: `trading-model` `99b285d`; `trading-manager` `bf5cd90`.

## Layer 3-8 promotion closeout rerouted through reviewer agent

- Chentong clarified that Layers 3-8 should follow the same principle as Layers 1-2: the script must call the reviewer agent for final audit before persisting the closeout decision.
- Added `trading-model/scripts/models/review_layers_03_08_promotion_closeout.py`.
  - The script builds blocked evaluation artifacts when production eval substrate is missing.
  - It creates promotion candidates, calls `openclaw agent`, validates the reviewer JSON, persists the reviewed decision, and never activates deferred/rejected candidates.
- Ran the script for Layers 3-8 with `--all --write-decision`.
- New reviewer-agent deferred decisions:
  - Layer 3 `model_03_target_state_vector`: `mpdec_d8e027dd9b5aa939`.
  - Layer 4 `model_04_event_overlay`: `mpdec_76b07ea01a3f525b`.
  - Layer 5 `model_05_alpha_confidence`: `mpdec_9c3e19d6559ef55b`.
  - Layer 6 `model_06_position_projection`: `mpdec_b118232e76fae092`.
  - Layer 7 `model_07_underlying_action`: `mpdec_fabc9c709149a698`.
  - Layer 8 `model_08_option_expression`: `mpdec_e7448aaab1334345`.
- DB verification after the rerun: current expected 8/8 Layer 1-8 closeout decisions are deferred; model-layer activation count remains 0.
- Updated `trading-model` docs/readiness/tests and pushed commit `2c6686f` (`Add agent-reviewed Layer 3-8 closeout`). Verification: 100 tests OK.
- Added `trading-manager` migration `243_register_agent_reviewed_layer_3_8_closeout.sql`, registered the callable script and updated closeout receipts/status matrix/current registry CSV, then pushed commit `33fecf6` (`Register agent-reviewed Layer 3-8 closeout`). Verification: 35 tests OK and registry dry-run no pending migrations.

## Layer 3 production eval substrate follow-up
- Chentong asked whether the Layers 3-8 production-eval substrate section could be closed out honestly.
- Built real Layer 3 substrate from Alpaca-backed ETF target bars via `trading-data` source/feature path into PostgreSQL: 576 `trading_data.feature_03_target_state_vector` rows and 576 generated `trading_model.model_03_target_state_vector` rows.
- Persisted Layer 3 evaluation artifacts: snapshot `mdsnap_9b7c3bd598114c7c`, eval run `mdevrun_327616bb447ceb5b`, 1604 labels, 19 metrics, candidate `mpcand_1b077bca49a18dbf`, reviewer decision `mpdec_70fef0f31847cc1c`.
- Layer 3 measured thresholds passed, but reviewer deferred promotion because Layer 1/2 are not production-approved/active and Layer 3 calibration evidence is missing. No activation row was created.
- Added reproducible script `trading-model/scripts/models/model_03_target_state_vector/review_target_state_vector_production_substrate.py`, updated docs/decisions/tests, and registered the new Layer 3 substrate receipt in `trading-manager` migration `244_register_layer_3_production_eval_substrate.sql`.
- Layers 4-8 remain honestly blocked on missing real production eval surfaces/labels/metrics.

## Layer 3 production eval substrate committed and pushed

- Finalized the honest Layers 3-8 production-eval closeout boundary: Layer 3 now has real production evaluation substrate but remains deferred; Layers 4-8 remain blocked on missing real production evaluation surfaces, labels, and metrics.
- `trading-model` additions included:
  - `scripts/models/model_03_target_state_vector/review_target_state_vector_production_substrate.py` as the reproducible Layer 3 substrate/reviewer entrypoint.
  - `docs/96_promotion_closeout.md` updated to cite the Layer 3 real substrate receipt and distinguish it from Layers 4-8 blocked closeout receipts.
  - `docs/81_decision.md` decision D033: Layer 3 production-evaluation substrate is present but not promotable.
  - tests updated to cover the production substrate script behavior.
- `trading-manager` additions included migration `244_register_layer_3_production_eval_substrate.sql`, updated `scripts/registry/current.csv`, and tests asserting the Layer 3 receipt/status matrix/blockers/script registration.
- Final pushed commits:
  - `trading-model` `98d5aee` — `Record Layer 3 production eval substrate`.
  - `trading-manager` `3689a6a` — `Register Layer 3 production eval substrate`.
- Final verification:
  - `trading-model`: `PYTHONPATH=src python3 -m unittest discover tests` => 101 tests OK.
  - `trading-manager`: `PYTHONPATH=src python3 -m unittest discover tests` => 35 tests OK.
  - `trading-manager`: `python3 scripts/registry/apply_registry_migrations.py --dry-run` => no pending migrations.
  - `git diff --check` clean in both repos.
  - DB evidence check: Layer 3 deferred decision `mpdec_70fef0f31847cc1c` exists; L3 activation count is 0; L3 eval labels count is 1604; L3 metric count is 19.


## L1/L2 production-evaluation repair pass completed

- Chentong asked to continue diagnosing and repairing L1/L2 promotion/evaluation issues without repeating completed work or lowering thresholds to force a pass.
- Root cause found: earlier `feature_01_market_regime` / `feature_02_sector_context` generation was stale/incomplete and too slow over the full source window. `trading_data.source_01_market_regime` contained ~1.9M rows for 2025-05-01 through 2026-05-01, but daily-derived L1 features were only populated near the final ~130 snapshots before repair.
- `trading-data` repaired feature generation by downsampling 1-minute source bars to the regular-session 30-minute decision surface, adding prepared-bar/bisect lookup caching and point-in-time daily/intraday fallback optimization for L1, and reusing faster helper logic for L2.
- Full regeneration succeeded after repair:
  - L1 `trading_data.feature_01_market_regime`: 3275 rows; daily-style SPY evidence improved materially (`spy_return_1d=3262`, `spy_return_5d=3210`, `spy_distance_to_ma200=689`).
  - L2 `trading_data.feature_02_sector_context`: 104800 rows.
- `trading-model` optimized L1/L2 model generators and regenerated:
  - L1 `trading_model.model_01_market_regime`: 3275 rows.
  - L2 `trading_model.model_02_sector_context`: 81875 rows.
- Repaired L1/L2 evidence was persisted through reviewer/promotion flow but remained honestly deferred:
  - L1 eval `mdevrun_1f36fd090ec5dc03`, decision `mpdec_fb175b8c8a6b7bbf`; still failed baseline improvement, coverage, and stability gates.
  - L2 eval `mdevrun_696127b7faef4cac`, decision `mpdec_03cd8113817e7cd9`; still failed baseline improvement, selected-vs-blocked lift, and stability gates.
- No thresholds were lowered, no production config was activated, and model-layer activation rows remained `0`.
- Final pushed commits:
  - `trading-data` `2062b53` — fixed L1/L2 feature regeneration completeness/performance and added 1-minute downsampling tests plus data-side decision docs.
  - `trading-model` `1bc3fa1` — persisted repaired L1/L2 deferred promotion evidence and updated closeout docs.
- Verification passed:
  - `trading-data`: 21 tests OK; `git diff --check` clean.
  - `trading-model`: 101 tests OK; `git diff --check` clean.
  - Both repos were pushed and clean after completion.

## Trading-data first-principles route cleanup completed
- Re-audited active `trading-data` Markdown and lightweight script comments after repeated route changes.
- Rewrote docs around the direct route: `provider/API/web/file -> data_feed -> data_source -> data_feature -> SQL/artifact handoff`.
- Cleaned stale route-drift wording around legacy/bundle/deferred/scaffold/retired semantics; fixed feed availability docs to use actual `--feed` CLI flags.
- Tightened README/docs/source/feed/storage/script READMEs, ThetaData runtime wording, macro route language, and source/feature boundaries.
- Verification passed: targeted stale-term scans clean except intentional threshold/current-pending uses, `git diff --check`, `python3 -m compileall -q src scripts`, and `PYTHONPATH=src python3 -m unittest discover tests` (21 OK).
- Pushed `trading-data` commit `2e21e34` (`Clarify trading-data route documentation`); repo clean afterward.

## Layer 4-8 script coverage and production-hardening handoff work

- Added local/fixture-safe Layer 4-8 model script coverage in `trading-model` without activating production configs:
  - helper: `src/model_governance/local_layer_scripts.py`
  - wrappers under `scripts/models/model_04_event_overlay/` through `model_08_option_expression/`
  - smoke tests: `tests/test_layer_04_08_script_entrypoints.py`
  - docs: `scripts/README.md`
- Registered Layer 4-8 script entrypoints in `trading-manager` migration `245_register_layer_4_8_script_entrypoints.sql`, regenerated `scripts/registry/current.csv`, and expanded registry tests.
- Cleaned stale `source_06_position_execution` semantics in `trading-data`: it remains selected-contract option market-data tracking for `OptionExpressionModel` replay/evaluation, not a separate `PositionExecutionModel` or execution layer.
- Added storage-owned V1 manager/storage handoff contract templates in `trading-storage/main/templates/contracts/` for `manager_request_v1`, `run_manifest_v1`, `artifact_ref_v1`, and `ready_signal_v1`.
- Registered manager/storage handoff vocabulary in `trading-manager` migration `246_register_manager_storage_handoff_contracts.sql`, including contract required-field names plus production hardening, checkpoint/resume, and live-call guardrail policy rows.
- Added `trading-data/docs/96_production_hardening.md` and updated data docs for live-call guardrails, retry/rate-limit behavior, checkpoint/resume evidence, manifests, artifact refs, ready signals, and ThetaData runbook language.
- Started ThetaData Terminal runtime from `/root/tools/thetadata-terminal/ThetaTerminalv3.jar`; it listened on `127.0.0.1:25503`. This is runtime state only, not systemd/autostart.
- Controlled ThetaData live smoke succeeded through `trading-data` primary tracking for AAPL 2026-05-15 270 CALL on 2026-04-24 at `1Min`: 443 active transient OHLC rows aggregated into 242 saved `option_bar` rows under `/tmp/thetadata-live-smoke-20260508044010`.
- Verification across this batch passed: `trading-model` 103 tests OK, `trading-data` 21 tests OK, `trading-manager` 35 tests OK, registry dry-run no pending migrations, and `trading-storage` diff-check clean.
- Pushed commits included:
  - `trading-model` `ee3f036` (`Add layer 4-8 local script entrypoints`) and `68b324a` (Layer 3 blocker wording correction)
  - `trading-data` `06b272e` (`Clarify selected-contract tracking source semantics`), `5bd6ff5`, and `548f817` (production hardening / ThetaData smoke readiness)
  - `trading-manager` `9329785` (`Register layer 4-8 model script entrypoints`) and `c9414d6` (manager/storage handoff registry)
  - `trading-storage` `d8a87d4` (V1 handoff contract templates)

## Trading-data missing layer-doc spine fixed
- Chentong noticed `trading-data/docs/` had standalone layer files only for Layers 1-3 even though closeout claimed Layers 1-8 coverage.
- Root cause: Layer 4/8 boundaries were documented in `docs/94_model_inputs.md` and source READMEs, while Layers 5-7 were documented only as no-new-source rows. That was correct semantically but poor docs shape.
- Added top-level layer docs:
  - `docs/05_layer_04_event_overlay.md`
  - `docs/06_layer_05_alpha_confidence.md`
  - `docs/07_layer_06_position_projection.md`
  - `docs/08_layer_07_underlying_action.md`
  - `docs/09_layer_08_option_expression.md`
- Updated README, docs index, and closeout table so Layers 5-7 explicitly say no dedicated `trading-data` source and name the owning boundary.
- Verification passed: layer-file listing includes Layers 1-8, stale-term scan clean, `git diff --check`, and `PYTHONPATH=src python3 -m unittest discover tests` (21 OK).
- Pushed `trading-data` commit `510ae5b` (`Add missing trading-data layer docs`); repo clean afterward.

## Trading-data layer structure catalog added
- Chentong challenged that `trading-data` had docs for Layers 4-8 but lacked visible maintained structure across `src/`, `scripts/`, and `tests`.
- Audited current structure: Layer 4 source exists as `source_04_event_overlay`; Layer 8 exists as `source_05_option_expression` and `source_06_position_execution`; Layers 5-7 intentionally have no dedicated `trading-data` source because their inputs/outputs belong to `trading-model`, position/risk/control-plane state, or action/execution boundaries.
- Removed stale empty local directories `src/data_feature/feature_03_strategy_selection` and `src/data_source/source_03_strategy_selection`.
- Added `src/data_layers/catalog.py` as the maintained Layer 1-8 ownership map for docs/src/CLI/tests, plus `src/data_layers/README.md`.
- Added `tests/test_layer_structure_catalog.py` to enforce Layer 1-8 coverage, top-level docs, owned package README/importability, pyproject CLI entrypoints, explicit no-source reasons for Layers 5-7, absence of symmetry-only source/feature packages, and cataloged test paths.
- Updated README, `docs/90_data_organization.md`, `scripts/README.md`, `src/data_source/README.md`, and normalized the Layer 3 doc header.
- Verification passed: stale strategy/legacy scan clean, `PYTHONPATH=src python3 -m unittest discover tests` (27 OK), `python3 -m compileall -q src scripts`, and `git diff --check`.
- Pushed `trading-data` commit `8f534c0` (`Add trading-data layer structure catalog`); repo clean afterward.

## Layer 4/8 trading-data feature surfaces added
- Chentong challenged whether later model layers had no `data_feature` packages.
- Implemented real deterministic `trading-data` feature packages only where justified:
  - `feature_04_event_overlay` derives source-only event category/scope/dedup/source-priority/quality JSONB payloads from `source_04_event_overlay`; `trading-model` still owns `event_context_vector`.
  - `feature_08_option_expression` derives source-only option candidate moneyness/spread/liquidity/IV/Greeks/quality JSONB payloads from `source_05_option_expression`; `trading-model` still owns contract ranking and option-expression plans.
- Kept Layers 5-7 explicitly no-source/no-feature in docs and catalog; no fake symmetry packages were created.
- Registered feature rows and CLI script rows through `trading-manager` migration `247_register_layer_4_8_data_features.sql` and regenerated `scripts/registry/current.csv`.
- Verification passed: `trading-data` 31 tests, compileall, diff-check, new feature CLI help; `trading-manager` 35 tests, registry dry-run no pending, diff-check.
- Pushed commits: `trading-data` `8877b68`; `trading-manager` `dccf73a`.

## Target candidate source layer ownership aligned
- Reviewed Chentong's attached structural critique and accepted the core correction: `source_02_target_candidate_holdings` is a historical source-contract id for Layer 3 input preparation, not a Layer 2 `SectorContextModel` source surface.
- Updated `trading-data/src/data_layers/catalog.py`: Layer 2 now owns only `feature_02_sector_context`; Layer 3 owns `source_02_target_candidate_holdings`, `source_03_target_state`, and `feature_03_target_state_vector`.
- Updated `trading-data` docs/script posture: source-contract numbers are not model-layer numbers; Layer 3 docs/closeout include target candidate preparation; Layer 5-7 remain no-source/no-feature; Layer 8/source_06 wording clarifies selected-option-contract market-path tracking, not broker execution or Layer 6.
- Cleaned compatibility wrappers `scripts/generate_feature_01_market_regime.py` and `scripts/generate_feature_02_sector_context.py` to import only `main`; tests now import SQL implementation modules directly.
- Added `trading-manager` migration `248_align_target_candidate_source_with_layer_three.sql`, regenerated `scripts/registry/current.csv`, and tested that `SOURCE_02_TARGET_CANDIDATE_HOLDINGS` no longer applies to `sector_context_model`.
- Verification passed: `trading-data` 32 tests, compileall, diff-check; `trading-manager` 35 tests, registry dry-run no pending, diff-check.
- Pushed commits: `trading-data` `9a0e3d9`; `trading-manager` `8d8d9b3`.

## Trading-model current-route cleanup completed
- Reorganized active `trading-model` docs around the accepted eight-layer route: MarketRegime -> SectorContext -> TargetStateVector -> EventOverlay -> AlphaConfidence -> PositionProjection -> UnderlyingAction -> OptionExpression.
- Rewrote root README, scope, docs index, and scripts README to be direct/current-route-first; removed stale seven-layer, TradingProjectionModel, PortfolioRiskModel, risk-work, legacy strategy-selection, and source-evidence-bundle wording from active docs/scripts.
- Updated task/promotion docs to current closeout receipts for Layer 1/2 decisions and clarified that production gaps do not reopen the accepted Layer 1-8 contracts.
- Tidied script docstrings for Layer 4-8 local entrypoints and removed local cache directories plus stale empty `src/models/model_03_strategy_selection` directory.
- Verification passed: stale-route scan clean except intentional excluded `portfolio_risk_budget` input field; `PYTHONPATH=src python3 -m unittest discover tests` (103 OK); `python3 -m compileall -q src scripts`; `git diff --check`.
- Pushed `trading-model` commit `f9aa35a` (`Clarify trading-model route documentation`); repo clean afterward.

## Trading-model route documentation cleanup completed

- Continued after compaction from the trading-stack first-principles cleanup and focused on `trading-model` active docs/scripts/package wording.
- Accepted current route is eight layers: `MarketRegimeModel -> SectorContextModel -> TargetStateVectorModel -> EventOverlayModel -> AlphaConfidenceModel -> PositionProjectionModel -> UnderlyingActionModel -> OptionExpressionModel`.
- Rewrote/cleaned `trading-model` docs and script wording so active Markdown describes the current route directly instead of historical detours:
  - `README.md`
  - `docs/00_scope.md`
  - `docs/README.md`
  - `scripts/README.md`
  - `docs/80_task.md`
  - `docs/81_decision.md`
  - `docs/90_system_model_architecture_rfc.md`
  - `docs/91_model_decomposition.md`
  - `docs/95_promotion_readiness.md`
  - Layer 4-8 local script docstrings.
- Removed stale route artifacts and names including `seven-layer`, old `TradingProjectionModel`, old `PortfolioRiskModel`, stale `strategy_selection`, stale risk/execution wording, stale source-evidence wording, local cache directories, `.pytest_cache`, and empty old `src/models/model_03_strategy_selection` structure.
- Verified `trading-model`: `PYTHONPATH=src python3 -m unittest discover tests` => 103 tests OK; `python3 -m compileall -q src scripts` OK; `git diff --check` clean; targeted stale-term scans clean except intentional Layer 5 boundary reference to `portfolio_risk_budget` as an excluded input.
- Pushed `trading-model` commit `f9aa35a` (`Clarify trading-model route documentation`); repo was clean and `main...origin/main` synchronized afterward.

## Trading-model state-vector feature registry coverage fixed
- Reviewed external critique against repo state instead of applying the provided replacement blindly.
- Confirmed `src/models/state_vector_feature_registry.py` had Layer 1/2/3 and Layer 7/8 semantics but was missing `1_breadth_participation_score` plus Layer 4/5/6 score-family coverage.
- Added explicit semantics for Layer 4 `EventOverlayModel` core and impact score families, Layer 5 `AlphaConfidenceModel` score families, Layer 6 `PositionProjectionModel` score families, and Layer 1 `1_breadth_participation_score`.
- Expanded `tests/test_state_vector_feature_registry.py` so Layer 4-8 contract score-family constants must all be present in the semantics registry.
- Updated `docs/92_vector_taxonomy.md` Clean V2.2 flow so `underlying_action_plan / underlying_action_vector` feeds `OptionExpressionModel`, producing `option_expression_plan / expression_vector`, then crossing into downstream execution-owned broker/order lifecycle.
- Verification passed: targeted state-vector registry tests (3 OK), full `trading-model` suite (104 OK), `python3 -m compileall -q src scripts`, and `git diff --check`.
- Pushed `trading-model` commit `fa9441d` (`Complete state vector feature registry coverage`); repo clean and synchronized afterward.

## Trading-manager first-principles contract design started
- Chentong approved starting the manager phase by designing contracts from first principles instead of relying on previous drafts.
- Added `/root/projects/trading-manager/docs/93_contracts.md` as the manager/control-plane contract design doc.
- Defined core MVP contracts: `component_ref_v1`, `manager_request_v1`, `input_binding_v1`, `run_manifest_v1`, `run_step_v1`, `artifact_ref_v1`, and `ready_signal_v1`.
- Defined second-layer evaluation/promotion contracts: `dataset_snapshot_v1`, `model_output_envelope_v1`, `evaluation_run_v1`, `metric_result_v1`, `promotion_candidate_v1`, `review_decision_v1`, and `activation_record_v1`.
- Defined downstream handoff contracts: `downstream_handoff_v1` and reference-only `execution_intent_ref_v1`; broker/order/fill/account lifecycle remains owned by `trading-execution`.
- Updated `trading-manager` README docs spine, `docs/92_templates.md`, and `docs/80_task.md` to point to `93_contracts.md` and queue registry/type-field freezing before implementation.
- Verification passed: `trading-manager` 35 tests OK and `git diff --check` clean.
- Pushed `trading-manager` commit `0d12cae` (`Design manager control-plane contracts`); repo clean and synchronized afterward.

## Trading-manager contract persistence policy accepted
- Chentong accepted the principle that SQL stores durable control-plane facts/audit state, storage stores bulky/transient retention-managed payloads, and pure temp scratch stores run-local material that never becomes contract evidence.
- Updated `trading-manager/docs/93_contracts.md` with a Persistence Policy section: SQL durable control plane, storage retention-managed payloads, pure temp scratch, and practical table rule.
- Added `trading-manager/docs/81_decision.md` D106: `Split durable SQL facts from retention-managed payloads`.
- Updated `docs/80_task.md` so the MVP SQL-backed contracts are `manager_request_v1`, `input_binding_v1`, `run_manifest_v1`, `run_step_v1`, `artifact_ref_v1`, and `ready_signal_v1`; `component_ref_v1` starts as registry-backed fields rather than a separate component catalog table.
- Verification passed: `trading-manager` 35 tests OK and `git diff --check` clean.
- Pushed `trading-manager` commit `bf9502a` (`Define manager contract persistence policy`); repo clean and synchronized afterward.

## Trading-manager MVP contract persistence implemented
- Implemented concise SQL-backed manager/control-plane contracts in `trading-manager` migration `249_create_manager_control_plane_contract_tables.sql`.
- New durable tables under schema `trading_manager`: `manager_request`, `input_binding`, `run_manifest`, `run_step`, `artifact_ref`, and `ready_signal`.
- Kept `component_ref_v1` registry-backed on those rows; no separate component catalog table was added.
- Registered concise contract type/field/status vocabulary in `scripts/registry/current.csv`, including `MANAGER_CONTRACT_SQL_TABLES`, `INPUT_BINDING_V1_REQUIRED_FIELDS`, `RUN_STEP_V1_REQUIRED_FIELDS`, and manager lifecycle statuses.
- Updated `docs/93_contracts.md` and `docs/80_task.md` to mark the MVP implementation slice complete and defer helper validation until a real consumer appears.
- Verification passed: database table introspection confirmed all six tables exist; `PYTHONPATH=src python3 -m unittest discover -s tests` (36 OK); registry dry-run no pending migrations; `git diff --check` clean.
- Pushed `trading-manager` commit `5fa7aa2` (`Implement manager control plane contracts`).

## Initial data-source earliest availability probe
- Chentong asked to use the manager task system to move month-by-month from the common start across data sources, starting by determining the earliest obtainable date for each source.
- Ran bounded read-only probes without printing secrets:
  - Alpaca SPY bars first row: `2016-01-04T05:00:00Z`.
  - Alpaca SPY trades first row: `2015-12-01T14:30:00.1618Z`.
  - Alpaca SPY quotes first row: `2016-01-04T09:00:00.02Z`.
  - Alpaca SPY news first row: `2015-01-02T10:01:01Z`.
  - ThetaData local Standard entitlement: SPY option OHLC/trade_quote worked on `2016-01-04`; 2015 and 2012 probes returned 403. Published Pro history says earliest can be 2012-06-01, but current local entitlement evidence supports 2016.
  - ThetaData option snapshot endpoint requested for 2016 returned current 2026 contracts/timestamps, so the current selection-snapshot feed is not historical point-in-time backfillable as implemented.
  - OKX BTC-USDT history-candles evidence indicates earliest daily candle around `2017-12-31T16:00:00Z`.
  - SEC AAPL companyfacts evidence had earliest fact period end `2006-09-30` and earliest filed date `2009-07-22`.
  - GDELT 2.0 docs/search evidence indicates data files begin late morning `2015-02-19`; direct MIN scan would require a high BigQuery byte budget and was not run beyond bounded failed caps.
  - ETF issuer holdings and accepted Trading Economics visible-calendar route appear current/visible-page oriented, not honest historical PIT backfill sources without another archive/source.
- Derived preliminary common monthly start: if limited to historical/backfillable equity/news/SEC/GDELT/ThetaData Standard history, use `2016-01`; if OKX crypto is included, use `2018-01`; if current-only ETF holdings, Trading Economics visible page, or ThetaData snapshot are mandatory PIT inputs, no honest historical common start exists before the first collection month without changing source route or accepting leakage.

## Trading-manager monthly backfill planner added
- Chentong accepted `2016-01` as the common monthly historical backfill start, with crypto allowed to join later.
- Implemented `trading-manager` monthly backfill planning without triggering provider calls or inserting live tasks.
- Added `docs/94_monthly_backfill.md`: common start `2016-01`, OKX crypto start `2018-01`, included historical feeds, and current-only/PIT-unsafe excluded feeds (`06_feed_etf_holdings`, `07_feed_trading_economics_calendar_web`, `09_feed_thetadata_option_selection_snapshot`).
- Added `src/trading_manager_tasks/monthly_backfill.py` and wrapper `scripts/tasks/plan_monthly_backfill.py`; it emits deterministic dry-run `manager_request_v1` rows with monthly windows, policy refs, expected output refs, and parameter refs.
- Registered migration `250_register_monthly_backfill_planning.sql`, including `DATA_BACKFILL_MONTH_V1`, common/crypto start config rows, included/excluded feed configs, and `MANAGER_MONTHLY_BACKFILL_PLAN` script row.
- Verification passed: planner CLI sample shows 2016-01 requests and OKX only at 2018-01; `PYTHONPATH=src python3 -m unittest discover -s tests` (41 OK); registry dry-run no pending; compileall OK; diff-check clean.
- Pushed `trading-manager` commit `b03b115` (`Add monthly backfill planner`).

## Trading-manager unified task system completed
- Continued Chentong's manager-control-plane request that the task system should uniformly manage every component's requests and completion receipts, not only monthly backfill planning.
- Implemented component-neutral task-system helpers in `trading-manager/src/trading_manager_tasks/control_plane.py`.
  - `manager_request_v1` validation/normalization now covers component requests across data/model/storage/execution/dashboard boundaries.
  - Component completion receipts normalize into `run_manifest_v1`, `artifact_ref_v1`, and `ready_signal_v1` rows.
  - Receipt/output payloads stay by reference; manager SQL stores concise durable control-plane facts only.
- Added thin scripts:
  - `scripts/tasks/submit_manager_requests.py`
  - `scripts/tasks/record_completion_receipt.py`
- Added `docs/95_task_system.md` and updated manager README/task/contract/script/test docs to define the unified lifecycle: manager request -> component execution -> completion receipt -> run/artifact/ready rows.
- Registered vocabulary through migrations:
  - `251_register_unified_task_system.sql`
  - `252_register_component_output_refs.sql`
- Added `tests/test_task_control_plane.py` covering request validation, success/failure receipt normalization, output artifact refs, and JSONL loading.
- Verification passed: `PYTHONPATH=src python3 -m unittest discover -s tests` => 46 OK; registry dry-run no pending migrations; `python3 -m compileall -q src scripts` OK; `git diff --check` clean; request and receipt CLI smoke tests printed normalized rows correctly.
- Pushed `trading-manager` commit `1c99a7e` (`Add unified manager task system`); repo clean afterward.

## Trading-manager global task summary completed
- Implemented the global manager task summary/prioritization surface requested by Chentong.
- Added migration `253_create_global_task_summary.sql`, creating `trading_manager.task_summary` as a derived read model over `manager_request`, latest `run_manifest`, latest `ready_signal`, and artifact counts. It does not own task state.
- Added accepted priority values `critical`, `high`, `normal`, `low`, `backlog`; default request priority is `normal`, and summary order is priority rank, deadline, created time, then request id.
- Surfaced priority/deadline in `manager_request_v1` validation and monthly backfill planning.
- Added `fetch_task_summary` plus thin CLI `scripts/tasks/list_task_summary.py` for priority-ordered JSONL summary output.
- Registered `MANAGER_GLOBAL_TASK_SUMMARY_VIEW`, `MANAGER_TASK_PRIORITY_VALUES`, `MANAGER_TASK_PRIORITY_SORT_ORDER`, and `MANAGER_TASK_SUMMARY_LIST` in `scripts/registry/current.csv`.
- Updated task-system/contracts/task/script/src/test docs and tests.
- Verification passed: `PYTHONPATH=src python3 -m unittest discover -s tests` => 48 OK; registry dry-run no pending migrations; `python3 -m compileall -q src scripts` OK; `git diff --check` clean; `list_task_summary.py --limit 5` and `fetch_task_summary(limit=1)` returned cleanly with no rows.
- Pushed `trading-manager` commit `6ebbf60` (`Add global manager task summary`); repo clean afterward.
