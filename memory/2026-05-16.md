
## Earnings/guidance official artifact coverage gate completed
- Continued event-layer earnings/guidance scouting after readiness route showed missing guidance/expectation evidence.
- Added `sec_filing_document` support to `trading-data` feed `08_feed_sec_company_financials`: fetches a specific SEC filing document by CIK/accession/document name, saves compact metadata CSV plus `saved/sec_filing_document_text.txt`, and documents the exception to the no-raw-SEC-persistence rule for reviewed official document text.
- Added `trading-model` official-artifact coverage scout:
  - code: `src/models/model_04_event_overlay/earnings_guidance_artifact_coverage.py`
  - script: `scripts/models/model_04_event_overlay/run_earnings_guidance_artifact_coverage_scout.py`
  - artifact: `storage/earnings_guidance_artifact_coverage_scout_q4_2025_20260515/`
- Coverage result: 12/12 Q4 2025 events had SEC result filing references, but 0 local official filing/release/transcript text artifacts, 0 accepted guidance interpretations, 0 expectation baselines, and 0 signed-direction-ready rows. Status: `blocked_missing_local_official_guidance_artifacts`.
- Registered manager decision D183 and migrations `429_register_earnings_guidance_artifact_coverage.sql` and `430_keep_sec_filing_document_as_feed_capability.sql`; `SEC_FILING_DOCUMENT` remains `feed_capability`, not active `data_kind`, to preserve current registry boundary tests.
- Verification passed: `trading-data` 38 tests; `trading-model` 122 tests plus targeted artifact-coverage/readiness tests; `trading-manager` 271 tests, registry apply/dry-run/export, compileall, diff-check.
- Pushed commits: `trading-data` `8b637da`, `trading-model` `602d431`, `trading-manager` `f35ad21`. Scheduler/dashboard were not started.
- Current conclusion/blocker: SEC metadata and normalized facts are still insufficient for guidance surprise or signed alpha. Next acceptable route is bounded official document text acquisition plus reviewed guidance interpretation and point-in-time expectation baselines.

## Earnings/guidance exhibit-level prior guidance extraction closed

- Continued the event-layer earnings/guidance baseline work without starting scheduler/dashboard services or making new provider calls.
- `trading-model` added exhibit-level prior-company-guidance extraction:
  - Code: `src/models/model_04_event_overlay/earnings_guidance_prior_guidance_exhibit_extraction.py`
  - CLI: `scripts/models/model_04_event_overlay/run_earnings_guidance_prior_guidance_exhibit_extraction.py`
  - Artifact: `storage/earnings_guidance_prior_guidance_exhibit_extraction_q4_2025_20260515/`
- Final artifact status: 12 events, 21 candidate official exhibit documents, 7 accepted prior-company-guidance baseline-context events, 8 accepted documents, 42 accepted spans, 5 no-context events, 0 signed-direction-ready rows, 0 provider calls.
- Fixed the artifact CSV writer to use LF line endings after `git diff --check` caught Python CSV CRLF as trailing whitespace.
- Formalized revenue-consensus route decision: existing Nasdaq route remains EPS-only; Trading Economics is only a future revenue-consensus route candidate until pre-event persisted artifacts with PIT clocks/provenance exist. No signed historical revenue/guidance claims are unlocked.
- `trading-manager` added registry migration `441_register_prior_guidance_exhibit_extraction.sql`, updated `scripts/registry/current.csv`, and recorded decisions D195/D196.
- Verification passed:
  - `trading-model`: 140 tests OK, compileall OK, cached/working diff-check OK.
  - `trading-manager`: 273 tests OK, registry dry-run no pending migrations, compileall OK, cached/working diff-check OK.
- Pushed commits: `trading-model` `ce17e96` (`Extract prior guidance from earnings exhibits`) and `trading-manager` `8a32e8f` (`Register prior guidance exhibit extraction`). `trading-data` remained clean at `8b637da`; `trading-execution` remained clean at `2141703`.
- Current conclusion: prior-company-guidance baseline context is partially unblocked by official earnings exhibits, but signed earnings/guidance alpha, beat/miss, raise/cut, model activation, broker/account mutation, and stronger EventRiskGovernor escalation remain blocked until accepted current comparisons and revenue/other PIT expectation baselines exist.

## EventRiskGovernor surface rename pushed
- Continued after compaction by verifying and pushing the already-implemented conceptual EventRiskGovernor rename/boundary slice.
- Pushed commits: `trading-model` `0c26112` (`Rename event risk governor surfaces`), `trading-manager` `e039a89` (`Register event risk governor naming`), `trading-data` `3776e7f` (`Clarify event risk data boundary`), and `trading-execution` `84a7ccc` (`Route realtime event risk layer`).
- Verification passed before push: `trading-model` 140 tests OK plus compileall/diff-check; `trading-manager` 273 tests OK plus registry dry-run no pending migrations, compileall, diff-check; `trading-data` 38 tests OK plus compileall/diff-check; `trading-execution` 48 tests OK plus compileall/diff-check.
- Scheduler and dashboard services remained inactive; no provider calls, model activation, broker/account mutation, or dashboard runtime start were performed.

## Earnings/guidance current-vs-prior readiness scout completed
- Continued the no-provider earnings/guidance comparison route after EventRiskGovernor rename push.
- `trading-model` added current-vs-prior guidance comparison readiness:
  - Code: `src/models/model_08_event_risk_governor/earnings_guidance_current_prior_comparison_readiness.py`
  - CLI: `scripts/models/model_08_event_risk_governor/run_earnings_guidance_current_prior_comparison_readiness.py`
  - Artifact: `storage/earnings_guidance_current_prior_comparison_readiness_q4_2025_20260516/`
- Artifact result: 12 events, 7 accepted prior-guidance baseline events, 9 current partial guidance-context events, 0 current comparable guidance events, 0 accepted guidance raise/cut rows, 0 signed-direction-ready rows, 0 provider calls. Status: `blocked_missing_current_comparable_guidance_context`.
- Updated `trading-model` docs (`docs/80_task.md`, `docs/101_earnings_guidance_event_family_packet.md`) to state that partial current primary-document context is not comparable company guidance and cannot unlock raise/cut, guidance surprise, signed alpha, model activation, broker/account mutation, or stronger EventRiskGovernor intervention.
- `trading-manager` registered migration `444_register_current_prior_guidance_comparison_readiness.sql`, current registry rows for the scout script/artifact/non-claim policy, and regenerated `scripts/registry/current.csv`.
- Verification passed: `trading-model` 142 tests OK, compileall OK, diff-check OK; `trading-manager` 273 tests OK, registry dry-run no pending migrations, compileall OK, diff-check OK.
- Pushed commits: `trading-model` `2710d12` (`Add current-prior guidance readiness scout`) and `trading-manager` `bedda79` (`Register current-prior guidance readiness scout`). All trading repos were clean/synced afterward. Scheduler and dashboard services remained inactive; no provider calls, model activation, broker/account mutation, or dashboard runtime start were performed.
- Current conclusion: prior context is partially available, but the comparison route is still blocked until current earnings-release/exhibit/transcript guidance text and PIT expectation baselines are accepted.

## Stale model naming recheck completed
- Rechecked active trading repos for stale conceptual layer/model names after the EventRiskGovernor reorder.
- Patched active `trading-model` docs/source headers so conceptual ordering is Layer 4 `AlphaConfidenceModel`, Layer 5 `PositionProjectionModel`, Layer 6 `UnderlyingActionModel`, Layer 7 `TradingGuidanceModel / OptionExpressionModel`, and Layer 8 `EventRiskGovernor / EventIntelligenceOverlay`; legacy physical package/table/score tokens are explicitly marked legacy until dedicated migrations.
- Renamed the stale local `model_04_rows` variable in `trading-model/scripts/models/model_05_alpha_confidence/generate_model_05_alpha_confidence.py` to `event_risk_rows` while preserving the legacy physical table `model_08_event_risk_governor`.
- Refreshed `trading-manager` registry notes with migrations `445_refresh_layer_reorder_registry_notes.sql` and `446_refresh_option_expression_reorder_notes.sql`, regenerated `scripts/registry/current.csv`, and clarified legacy `layer_08_option_expression` as conceptual Layer 7 option-expression/trading-guidance work.
- Clarified `trading-data` option-expression/position-execution docs so `source_05_option_expression` / `source_06_position_execution` are source ids, not conceptual model layer numbers.
- Final scan found no unclassified active stale names; remaining hits are explicit active architecture notices, superseded-decision notes, or accepted legacy physical ids.
- Verification passed: `trading-model` 142 tests OK, compileall OK, diff-check OK; `trading-manager` 273 tests OK, registry dry-run no pending migrations, compileall OK, diff-check OK; `trading-data` 38 tests OK, compileall src OK, diff-check OK. Scheduler/dashboard remained inactive.
- Pushed commits: `trading-model` `809cc10`, `trading-manager` `a007444`, `trading-data` `01f8995`; all trading repos clean/synced afterward.

## Numbering physical audit and active runtime target cleanup
- Completed expanded numbering audit after Chentong asked to include all numbering, data table names, and table contents.
- Confirmed local `openclaw` PostgreSQL still has legacy physical/data surfaces and row content: `trading_model.model_04_event_overlay`, `model_05_alpha_confidence`, `model_06_position_projection`, `model_07_underlying_action`, `model_08_option_expression`, plus `trading_data.source_04_event_overlay` / `feature_04_event_overlay` and stored `model_layer` / run refs using old numbering.
- Added `trading-manager/docs/103_numbering_physical_audit.md` and registry migrations `447_record_numbering_physical_audit_and_fix_registry_notes.sql` / `448_align_model_promotion_targets_to_conceptual_numbering.sql` to record that physical SQL/package/data numbering is not clean until a dedicated migration.
- Patched safe active runtime surfaces: `trading-execution` realtime coverage, `trading-model` realtime decision handoff, and `trading-manager` realtime shadow handoff/model-promotion request planning now expose conceptual Layer 4 alpha, Layer 5 position, Layer 6 underlying action, Layer 7 option expression/trading guidance, and Layer 8 event risk governor keys while preserving legacy evidence/package refs as aliases.
- Verification passed: `trading-model` 142 tests OK + compileall + diff-check; `trading-manager` 274 tests OK + compileall + registry dry-run no pending + diff-check; `trading-execution` 48 tests OK + compileall + diff-check. Scheduler and dashboard services remained inactive.
- Pushed commits: `trading-model` `46ca88d`, `trading-manager` `9d7c779`, `trading-execution` `3c38023`; all trading repos clean/synced afterward.

## Current layer physical numbering migration completed
- Finished and pushed the current physical numbering alignment slice across `trading-manager`, `trading-model`, `trading-data`, and `trading-execution`.
- `trading-manager` commit `b640e27`: added current table migration `scripts/current_table_migrations/001_align_current_layer_numbering.sql`, registry migrations `449`-`456`, refreshed `scripts/registry/current.csv`, fixed registry tests, and cleaned post-migration stale notes/keys such as `LAYER_08_OPTION_BUCKET_*` -> `LAYER_07_OPTION_BUCKET_*`.
- `trading-model` commit `64a5ee7`: renamed physical packages/scripts for Layers 4-7 to current numbering (`model_04_alpha_confidence`, `model_05_position_projection`, `model_06_underlying_action`, `model_07_option_expression`) and kept Layer 8 as `model_08_event_risk_governor`.
- `trading-data` commit `5de5e28`: renamed event-risk governor source/feature surfaces to `source_08_event_risk_governor` / `feature_08_event_risk_governor`, option-expression feature to `feature_07_option_expression`, and updated storage SQL dispatch.
- `trading-execution` commit `efd142c`: aligned execution feature numbering references.
- Verification passed: manager 274 tests, model 142 tests, data 38 tests, execution 48 tests; compileall/diff-check passed; registry dry-run no pending; current table migration re-run completed idempotently after backup context `/root/.openclaw/workspace/tmp/db_backups/current_numbered_tables_before_20260516_0217.sql`.

## Storage artifact index V0.1 implemented
- Continued from the post-numbering clean state and selected the first safe storage lifecycle slice: a conservative artifact index, before protected-set/deletion/archive execution.
- `trading-storage` added `src/trading_storage/artifact_index.py`, CLI wrapper `scripts/lifecycle/build_artifact_index.py`, tests, and docs updates.
- The builder scans bounded storage-owned filesystem roots, defaults to `storage/artifacts`, emits JSONL records plus a summary, hashes payloads, derives producer/schema/run metadata where available, and classifies ambiguous artifacts as `retention_class=manual_review_required` with `protected_reason_codes=[unknown_metadata]`.
- Deliberately avoided default scanning of the unbounded `storage/dashboard/read_models` tree after confirming it is snapshot-heavy (~139G); explicit `--include-root` can target specific read-model latest files or bounded roots.
- `trading-manager` registered script `STORAGE_ARTIFACT_INDEX_BUILD` in migration `460_register_storage_artifact_index_builder.sql` and regenerated `scripts/registry/current.csv`.
- Verification passed: `trading-storage` 32 tests OK, compileall OK, artifact-index CLI smoke OK, diff-check OK; `trading-manager` 275 tests OK, registry dry-run no pending migrations, compileall OK, diff-check OK.
- Pushed commits: `trading-storage` `5ea58db` (`Add storage artifact index builder`) and `trading-manager` `5970963` (`Register storage artifact index builder`). Other repos remained clean at `trading-model` `357b3ea`, `trading-data` `d516664`, `trading-execution` `efd142c`, `trading-dashboard` `bee7ba1`.
- Next safe storage lifecycle slice: protected-set builder; lifecycle mutation remains unauthorized until protected-set checks, quarantine/recheck, and receipts are implemented/reviewed.

## Storage protected-set and lifecycle planner V0.1 implemented
- Continued storage lifecycle work in safe/report-only slices after artifact index V0.1.
- `trading-storage` added `src/trading_storage/protected_set.py`, CLI wrapper `scripts/lifecycle/build_protected_set.py`, tests, and docs updates. It builds `storage_protected_set_v1` from artifact-index records plus optional reason-code refs/manual pins/candidate refs, conservatively blocks unknown/ambiguous artifacts with `unknown_metadata`, and emits candidate clear/protected summary evidence without mutating payloads.
- `trading-manager` registered `STORAGE_PROTECTED_SET_BUILD` in migration `461_register_storage_protected_set_builder.sql`, regenerated `scripts/registry/current.csv`, and pushed commit `8c5bb21`.
- `trading-storage` pushed protected-set commit `697bcd7` (`Add storage protected set builder`).
- Continued one more safe slice: `trading-storage` added dry-run lifecycle planner `src/trading_storage/lifecycle_planner.py`, wrapper `scripts/lifecycle/plan_storage_lifecycle.py`, tests, and docs updates. The planner combines artifact-index metadata, protected-set evidence, and reviewed policy rules into `storage_lifecycle_plan_v1` / summary output; all records are dry-run only and protected artifacts become `retain_protected`.
- Default planner rules retain receipt/evidence artifacts, retain manual-review/unknown artifacts, mark unprotected uncompressed source-like artifacts as `compress_candidate`, and mark unprotected TTL-delete artifacts as `quarantine_candidate`; no compression, archive, quarantine, deletion, SQL detach/drop, model activation, broker execution, or account mutation is performed.
- `trading-manager` registered `STORAGE_LIFECYCLE_PLAN_DRY_RUN` in migration `462_register_storage_lifecycle_planner.sql`, regenerated `scripts/registry/current.csv`, and pushed commit `d83b146`.
- Verification passed: `trading-storage` 43 tests OK, compileall OK, lifecycle-plan CLI smoke OK, diff-check OK; `trading-manager` 275 tests OK, registry dry-run no pending migrations, compileall OK, diff-check OK.
- Pushed latest commits: `trading-storage` `18670b2` (`Add dry-run storage lifecycle planner`) and `trading-manager` `d83b146` (`Register storage lifecycle planner`). Lifecycle mutation remains unauthorized until quarantine/recheck, mutation executors, and receipts are implemented/reviewed.

## Storage quarantine/recheck evidence V0.1 implemented
- Continued storage lifecycle work with the next safe report-only slice after protected-set and dry-run lifecycle planner.
- `trading-storage` added `src/trading_storage/quarantine_recheck.py`, CLI wrapper `scripts/lifecycle/build_quarantine_recheck_evidence.py`, tests, and docs updates.
- The builder consumes a `storage_lifecycle_plan_v1` plus optional final protected-set JSON and emits `storage_quarantine_recheck_evidence_v1` / `storage_quarantine_recheck_summary_v1` with initial protection status, quarantine candidate state, final recheck status, and explicit `deletion_allowed=false` / `mutation_performed=false` on every row.
- Current states include `blocked_initial_protection`, `not_quarantine_candidate`, `dry_run_candidate_pending_recheck`, `blocked_final_recheck`, and `dry_run_recheck_clear`. A clear final recheck is only evidence for a future reviewed executor; it does not authorize deletion.
- `trading-manager` registered `STORAGE_QUARANTINE_RECHECK_EVIDENCE_BUILD` in migration `463_register_storage_quarantine_recheck_evidence.sql`, regenerated `scripts/registry/current.csv`, and pushed commit `1fb39d8`.
- Verification passed: `trading-storage` 49 tests OK, compileall OK, quarantine/recheck CLI smoke OK, diff-check OK; `trading-manager` 275 tests OK, registry dry-run no pending migrations, compileall OK, diff-check OK.
- Pushed latest commits: `trading-storage` `413763d` (`Add quarantine recheck evidence builder`) and `trading-manager` `1fb39d8` (`Register storage quarantine recheck evidence`). Lifecycle mutation remains unauthorized until reviewed compression/archive/delete executors and receipts exist.

## Storage lifecycle execution scaffold V0.1 implemented
- Chentong asked to finish compression/archive/etc. in one go. Safety boundary preserved: no real compression, archive export, restore materialization, deletion, SQL detach/drop, artifact-index mutation, model activation, broker execution, or account mutation was performed.
- `trading-storage` added `src/trading_storage/lifecycle_execution_scaffold.py`, CLI wrapper `scripts/lifecycle/build_lifecycle_execution_scaffold.py`, tests, and docs updates.
- The scaffold consumes `storage_lifecycle_plan_v1` rows and emits `storage_lifecycle_execution_scaffold_v1` with draft evidence families: `compression_manifest_draft_v1`, `compression_receipt_draft_v1`, `sql_archive_manifest_draft_v1`, `archive_receipt_draft_v1`, and `restore_receipt_draft_v1`.
- `compress_candidate` rows generate compression manifest/receipt drafts plus restore-verification receipt drafts; `archive_candidate` rows generate SQL archive manifest/receipt drafts plus restore-verification receipt drafts; protected rows are skipped; `quarantine_candidate` rows do not produce deletion receipts.
- Every draft carries `dry_run=true`, `mutation_performed=false`, and `status=planned_not_executed` where status applies. Lifecycle planner rows now carry artifact size/checksum/content metadata so receipt drafts can include source evidence from the artifact index.
- `trading-manager` registered `STORAGE_LIFECYCLE_EXECUTION_SCAFFOLD_BUILD` in migration `464_register_storage_lifecycle_execution_scaffold.sql`, regenerated `scripts/registry/current.csv`, and pushed commit `3604e39`.
- Verification passed: `trading-storage` 54 tests OK, compileall OK, lifecycle execution scaffold CLI smoke OK, diff-check OK; `trading-manager` 275 tests OK, registry dry-run no pending migrations, compileall OK, diff-check OK.
- Pushed latest commits: `trading-storage` `8eadf14` (`Add lifecycle execution scaffold`) and `trading-manager` `3604e39` (`Register storage lifecycle execution scaffold`). Next truly mutating step must be a reviewed explicit executor slice, likely starting with single-file compression that writes compressed copy only and preserves the original until restore smoke and receipts are reviewed.

## Storage single-file compression executor V0.1 implemented
- Continued storage lifecycle work with the first reviewed real executor slice after the non-mutating compression/archive/restore scaffold.
- `trading-storage` added `src/trading_storage/single_file_compression.py`, CLI wrapper `scripts/lifecycle/compress_single_file_candidates.py`, tests, and docs updates.
- Scope is intentionally narrow: default dry-run; `--apply` only writes zstd compressed copies for unprotected `compress_candidate` regular files under `storage/archive/compressed/<artifact_id>/<original-name>.zst`; originals are always preserved.
- Safety guards: skips protected rows and non-`compress_candidate` rows; refuses source symlinks/directories/missing files; refuses existing compressed outputs unless `--overwrite`; verifies source checksum from the lifecycle plan; verifies zstd decompression checksum smoke before successful receipt.
- Emitted contracts: `storage_single_file_compression_result_v1`, `storage_single_file_compression_summary_v1`, `compression_manifest_v1`, `compression_receipt_v1`, and `restore_receipt_v1`.
- Explicitly still disabled/false: original deletion, artifact-index mutation, quarantine moves, SQL archive/export, SQL detach/drop, model activation, broker execution, and account mutation.
- `trading-manager` registered `STORAGE_SINGLE_FILE_COMPRESSION_EXECUTE` in migration `465_register_storage_single_file_compression_executor.sql` and regenerated `scripts/registry/current.csv`.
- Verification passed: `trading-storage` 60 tests OK, compileall OK, single-file compression CLI dry-run smoke OK, diff-check OK; `trading-manager` 275 tests OK, registry dry-run no pending migrations, compileall OK, diff-check OK.
- Pushed commits: `trading-storage` `0ac4c25` (`Add single-file compression executor`) and `trading-manager` `3b6f81b` (`Register storage single-file compression executor`). Both repos clean on `main...origin/main` afterward.
- Next storage lifecycle slice should remain conservative: reviewed restore verifier or SQL archive exporter; original deletion/quarantine moves remain unauthorized.

## Storage dashboard snapshot pruning and Layer 1/2 retention policy implemented
- Chentong noted that existing stored data already consumed about half the storage and clarified the intended retention policy: Layer 1/2 data need persistent retention, while later metadata can be deleted after the model run cycle ends.
- Verified local `trading-storage/storage` usage: about 141G total, almost entirely `storage/dashboard/read_models`; dry-run dashboard snapshot pruning found ~74,989 snapshot rows and ~86.3GB of delete candidates with defaults `max_age_hours=24` and `keep_latest_per_contract=24`.
- `trading-storage` updated lifecycle docs to make Layer 1/2 data persistent by default and later-layer model-run/dashboard metadata TTL-delete eligible after run-cycle close, while preserving latest summaries, receipts, manifests, lineage refs, unresolved-alert evidence, SQL, and promoted model bodies.
- `trading-storage` updated artifact-index classification: explicit Layer 1/2 hints classify as `compress_and_retain`; dashboard `latest.json` classifies as protected `dashboard_latest_retained`; explicitly indexed dashboard snapshots classify as `ttl_delete_allowed`; ambiguous artifacts remain `manual_review_required` with `unknown_metadata`.
- Added `src/trading_storage/dashboard_snapshot_lifecycle.py` and `scripts/dashboard/prune_dashboard_snapshots.py`. The helper is dry-run by default; `--apply` deletes only old `storage/dashboard/read_models/*/snapshots/**/*.json` metadata snapshots and preserves `latest.json`, schemas, index rows, Layer 1/2 data, SQL, model bodies, receipts, and manifests.
- `trading-manager` registered `STORAGE_DASHBOARD_SNAPSHOT_PRUNE` in migration `466_register_storage_dashboard_snapshot_pruner.sql` and regenerated `scripts/registry/current.csv`.
- Verification passed: `trading-storage` 65 tests OK, compileall OK, dashboard prune dry-run smoke OK, diff-check OK; `trading-manager` 275 tests OK, registry dry-run no pending, compileall OK, diff-check OK.
- Pushed commits: `trading-storage` `ce14a56` (`Add dashboard snapshot lifecycle pruning`) and `trading-manager` `30d7ba6` (`Register dashboard snapshot pruning`). Both repos clean on `main...origin/main` afterward.
- No deletion was applied in this slice; applying the current default prune plan would reclaim roughly 86GB from dashboard snapshot metadata only.

## Storage dashboard snapshot lifecycle pruning added
- Chentong observed that already-stored data is consuming about half of available storage and clarified the retention policy: Layer 1 and Layer 2 data must remain persistently saved; later-layer/model-cycle metadata can be deleted after the model run cycle ends to avoid filling disk.
- Implemented/accepted the policy direction in `trading-storage`: Layer 1/2 data remains a persistent/compress-retain class, while dashboard/read-model metadata snapshots are treated as TTL-prunable metadata.
- Added `trading-storage/src/trading_storage/dashboard_snapshot_lifecycle.py` and wrapper `scripts/dashboard/prune_dashboard_snapshots.py` to plan/apply bounded deletion of only `storage/dashboard/read_models/*/snapshots/**/*.json` old dashboard metadata snapshots.
- Safety constraints: default dry-run; `--apply` required for deletion; preserves `latest.json`, schemas, dashboard index rows, Layer 1/2 persistent data, SQL data, model bodies, receipts, and manifests; summary records `layer_01_02_data_deleted=false` and `sql_mutation_performed=false`.
- Registered stable entrypoint `STORAGE_DASHBOARD_SNAPSHOT_PRUNE` in `trading-manager` migration `466_register_storage_dashboard_snapshot_pruner.sql`.
- Dry-run evidence reported `trading-storage/storage` around 141G with approximately 86.3GB eligible old dashboard metadata snapshots under the default policy.
- Verification passed before push: `trading-storage` 65 tests OK, compileall OK, prune dry-run OK, diff-check OK; `trading-manager` 275 tests OK, registry dry-run no pending, compileall OK, diff-check OK.
- Pushed commits: `trading-storage` `ce14a56` (`Add dashboard snapshot lifecycle pruning`) and `trading-manager` `30d7ba6` (`Register dashboard snapshot pruning`).
- Current pending decision: actual deletion of the ~86GB eligible dashboard snapshot metadata was not performed yet; requires explicit user confirmation such as “删”.

## Dashboard snapshot deletion hold during event-model redo
- Chentong clarified that because the event model is being redone, downstream models will also need regeneration, so existing later-layer/dashboard metadata snapshots should not be deleted now.
- Operational decision: keep dashboard/model-run metadata pruning dry-run only until the event-model redo and downstream regeneration cycle is complete/reviewed. The ~86GB dashboard snapshot prune candidates remain intentionally preserved for comparison/debug/regeneration evidence.

## Event model redo loop closed
- Closed the event-model redo as a bounded Layer 8 `EventRiskGovernor` / `EventIntelligenceOverlay`, not as promotion approval or execution authorization.
- `trading-model` added `event_model_closeout_report_v1` via `src/models/model_08_event_risk_governor/event_model_closeout.py` and `scripts/models/model_08_event_risk_governor/build_event_model_closeout_report.py`.
- Final judgment encoded: reject broad event alpha, standalone option-flow alpha, and signed earnings/guidance alpha until PIT expectation/current-prior comparison gates exist; earnings/guidance remains direction-neutral scouting context; diagnostic artifacts are preserved; storage deletion remains on hold.
- `trading-manager` added non-mutating `manager_event_model_regeneration_plan_v1` via `src/trading_manager_tasks/event_model_regeneration_plan.py` and `scripts/tasks/plan_event_model_regeneration.py`.
- Regeneration plan: preserve Layer 1/2 persistent data and valid base Layer 3-7 outputs, supersede old event-overlay/abnormal-activity-only Layer 8 artifacts, rebuild source/feature/model Layer 8 only after reviewed event-feed coverage, and keep dashboard/model-run deletion dry-run-only until reviewed regeneration closes.
- Registry migration added: `467_register_event_model_closeout_and_regeneration_plan.sql` registering `MODEL_08_EVENT_RISK_GOVERNOR_CLOSEOUT_REPORT_BUILD` and `MANAGER_PLAN_EVENT_MODEL_REGENERATION`.
- Verification passed: `trading-model` 144 tests OK, compileall OK, diff-check OK, closeout CLI JSON smoke OK; `trading-manager` 278 tests OK, registry dry-run no pending, compileall OK, diff-check OK, regeneration-plan CLI JSON smoke OK.
- Pushed commits: `trading-model` `8bea3ed` (`Close event model redo loop`) and `trading-manager` `e94b50b` (`Plan event model regeneration`). Repos clean afterward: `trading-model`, `trading-manager`, `trading-storage`, `trading-data` all `main...origin/main`.

## Storage file-lifecycle closeout pass completed
- Completed the remaining safe file/storage-management class work in one pass while preserving the event-regeneration deletion hold.
- `trading-storage` added `src/trading_storage/file_lifecycle_closeout.py` and `scripts/lifecycle/run_file_lifecycle_closeout.py` to chain artifact index, protected set, dry-run lifecycle plan, quarantine/recheck evidence, execution scaffold, optional single-file compressed-copy execution, and dashboard snapshot prune dry-run into one closeout receipt.
- Added protected reason `dashboard_latest_snapshot` so explicitly indexed dashboard `latest.json` rows are protected cleanly rather than relying on generic unknown metadata.
- Closeout contracts: `storage_file_lifecycle_closeout_v1` and `storage_file_lifecycle_closeout_summary_v1`; summary explicitly reports no original deletion, artifact-index mutation, quarantine move, SQL mutation, model activation, broker execution, account mutation, or dashboard snapshot deletion.
- Ran the real closeout with `--apply-compression`: artifact index contained 7 records, all protected; no compressed-copy writes were needed, no mutation occurred, and dashboard snapshot prune stayed dry-run with ~88.2GB candidate delete bytes preserved under the event-regeneration hold.
- `trading-manager` registered `STORAGE_FILE_LIFECYCLE_CLOSEOUT_RUN` via migration `468_register_storage_file_lifecycle_closeout.sql` and regenerated `scripts/registry/current.csv`.
- Verification passed: `trading-storage` 68 tests OK, compileall OK, diff-check OK; `trading-manager` 278 tests OK, registry dry-run no pending migrations, compileall OK, diff-check OK.
- Pushed commits: `trading-storage` `6d01a69` (`Add file lifecycle closeout runner`) and `trading-manager` `d6f1849` (`Register storage file lifecycle closeout`). `trading-storage`, `trading-manager`, `trading-model`, and `trading-data` were clean on `main...origin/main` afterward.
- Remaining intentionally blocked: dashboard/model-run metadata deletion, original artifact deletion, quarantine moves, SQL archive/detach/drop, and daemon execution until event-risk-governor regeneration/downstream review and separate mutation approval close.

## Fine-grained event-family governance accepted
- Chentong clarified that event-price association must be as fine-grained as practical, including splitting news into concrete mechanism-level event families rather than treating news as one broad bucket.
- Updated `trading-model/docs/100_event_family_scouting.md` with the event-family granularity rule: ingestion categories such as `macro_news`, `symbol_news`, `sec_filing`, and `earnings_guidance` are routing buckets, not modeling families; each narrow family needs its own packet and association study.
- Updated `trading-model/docs/102_event_layer_final_judgment.md` so event gates require mechanism-level family decomposition before price/path association analysis, rejecting broad mixed-news pooling even when it increases sample count.
- Updated `trading-manager/docs/81_decision.md` with D197: manager governance must require fine-grained event-family association analysis before model training/risk intervention/promotion review.
- Initial candidate splits recorded: earnings/guidance shells/results/guidance/narrative residuals; SEC filing families such as offering/dilution, buyback, M&A, insider/ownership, legal/regulatory, restatement, bankruptcy; symbol-news families such as product/customer, management, analyst, lawsuit, regulatory, supply-chain, capital allocation; sector/macro families such as sector regulation, commodity/input cost, demand shock, CPI, FOMC/rates, NFP, credit/liquidity; and abnormal-activity residual families.
- Pushed commits: `trading-model` `7b9ce34` (`Require fine-grained event families`) and `trading-manager` `f464c83` (`Record fine-grained event family governance`).

## Event-family batch catalog completed
- Ran Chentong's requested safe local batch for fine-grained event-family association readiness; no provider calls, model activation, broker/account mutation, SQL/storage deletion, or artifact deletion were performed.
- `trading-model` added `src/models/model_08_event_risk_governor/event_family_batch_catalog.py` and `scripts/models/model_08_event_risk_governor/build_event_family_batch_catalog.py`.
- Generated artifacts under `trading-model/storage/event_family_batch_catalog_20260516/`: `event_family_batch_catalog.json`, `event_family_batch_summary.json`, `event_family_batch_queue.csv`, `event_family_first_pass_packets.jsonl`, and `event_family_blocker_queue.csv`.
- Batch result: 29 fine-grained mechanism-level candidate families, 13 high priority and 16 normal; statuses are 2 scouting, 26 proposed, and 1 deferred_low_signal. Routing buckets are explicitly not modeling families.
- High-priority queue: `earnings_guidance_scheduled_shell`, `earnings_guidance_result_metrics`, `earnings_guidance_raise_cut_or_withdrawal`, `equity_offering_dilution`, `mna_transaction`, `legal_regulatory_investigation`, `accounting_restatement_or_fraud`, `bankruptcy_or_restructuring`, `sector_regulation_policy`, `cpi_inflation_release`, `fomc_rates_policy`, `credit_liquidity_stress`, and `option_derivatives_abnormality`.
- `trading-manager` registered `MODEL_08_EVENT_FAMILY_BATCH_CATALOG_BUILD` through migrations `469_register_event_family_batch_catalog.sql` and `470_register_event_family_batch_packet_and_blocker_outputs.sql`.
- Verification passed: `trading-model` 147 tests OK, compileall OK, diff-check OK; `trading-manager` 278 tests OK, registry dry-run no pending, compileall OK, diff-check OK.
- Pushed commits: `trading-model` `4f2b8a8` (`Add event family batch catalog`) and `trading-manager` `d705c79` (`Register event family batch catalog`).

## Event-price association readiness first slice completed
- Continued Chentong's approved safe local association-study scouting after the fine-grained event-family batch catalog.
- `trading-model` added `src/models/model_08_event_risk_governor/event_price_association_readiness.py` and `scripts/models/model_08_event_risk_governor/build_event_price_association_readiness.py`.
- Generated local artifacts under `trading-model/storage/event_price_association_readiness_20260516/`: `event_price_association_batch.json`, `event_price_association_family_readiness.csv`, `event_price_association_candidate_events.csv`, `event_price_association_price_labels.csv`, and `README.md`.
- First slice families: `equity_offering_dilution`, `legal_regulatory_investigation`, `cpi_inflation_release`, and `credit_liquidity_stress`.
- Result: 4 families, 8 candidate events, 10 exploratory CPI price labels from local January 2016 macro-calendar + ETF bar artifacts. CPI remains `underpowered_single_month_scouting_only`; equity offering/dilution remains blocked on offering-terms parser; legal/regulatory remains blocked on official-source/severity taxonomy; credit/liquidity stress remains blocked on stress-event standard. No association/promotion claim was made.
- Safety preserved: provider calls 0, model activation false, broker execution false, account mutation false, artifact deletion false.
- `trading-manager` registered `MODEL_08_EVENT_PRICE_ASSOCIATION_READINESS_BUILD` via migration `471_register_event_price_association_readiness.sql` and regenerated `scripts/registry/current.csv`.
- Verification passed: `trading-model` 151 tests OK, compileall OK, diff-check OK; `trading-manager` 278 tests OK, registry dry-run no pending migrations, compileall OK, diff-check OK.


## Event-price association readiness slice completed
- Continued the approved safe local event-family association scouting after the fine-grained family batch catalog.
- `trading-model` added `src/models/model_08_event_risk_governor/event_price_association_readiness.py`, CLI `scripts/models/model_08_event_risk_governor/build_event_price_association_readiness.py`, and tests for the readiness batch.
- Generated artifacts under `trading-model/storage/event_price_association_readiness_20260516/`: `event_price_association_batch.json`, `event_price_association_family_readiness.csv`, `event_price_association_candidate_events.csv`, `event_price_association_price_labels.csv`, and README.
- First batch covered high-priority families: `equity_offering_dilution`, `legal_regulatory_investigation`, `cpi_inflation_release`, and `credit_liquidity_stress`.
- Batch result: 4 families, 8 candidate events, and 10 CPI exploratory price labels. `cpi_inflation_release` is the only family with local event clocks plus price labels, but status remains `underpowered_single_month_scouting_only`; it is not association proof or promotion evidence.
- Other family blockers remain explicit: offering/dilution needs offering-terms parser and canonical filing/news events; legal/regulatory needs official-source precedence and severity taxonomy; credit/liquidity stress needs a stress event standard/severity ladder and contagion scope.
- Safety preserved: provider_calls=0, no model activation, no broker/account mutation, and no artifact deletion.
- `trading-manager` registered migration `471_register_event_price_association_readiness.sql` and registry key `MODEL_08_EVENT_PRICE_ASSOCIATION_READINESS_BUILD`.
- Verification passed: `trading-model` 151 tests OK, compileall OK, diff-check OK; `trading-manager` 278 tests OK, registry dry-run no pending, compileall OK, diff-check OK.
- Pushed commits: `trading-model` `e7a92bf` (`Add event price association readiness`) and `trading-manager` `c4fa588` (`Register event price association readiness`).
- Recommended next step: expand `cpi_inflation_release` across more local months and add matched controls before any risk/alpha claim.

## CPI inflation association-control readiness completed
- Continued Chentong's "跑吧" instruction after the first event-price readiness slice by running the CPI-focused safe local association/control readiness path.
- Local coverage inspection found Trading Economics calendar event artifacts only for `2016-01`, while ETF bars exist across many months. Therefore the slice built matched-control machinery and an explicit blocker instead of pretending multi-month CPI evidence exists.
- `trading-model` added `src/models/model_08_event_risk_governor/cpi_inflation_association_readiness.py`, CLI `scripts/models/model_08_event_risk_governor/build_cpi_inflation_association_readiness.py`, and tests.
- Generated artifacts under `trading-model/storage/cpi_inflation_association_readiness_20260516/`: `cpi_inflation_association_readiness.json`, `cpi_inflation_association_summary.json`, `cpi_inflation_events.csv`, `cpi_inflation_event_labels.csv`, `cpi_inflation_control_labels.csv`, `cpi_inflation_event_control_comparisons.csv`, and README.
- Result: 1 CPI calendar month (`2016-01`), 1 CPI event clock, 10 event labels, 120 same-month control labels, and 10 event/control comparisons. Status remains `underpowered_cpi_scouting_only`; blocker codes include insufficient local CPI calendar months, insufficient event count/labels, need official-source canonicalization, need market/sector/target-state controls, and need preaccepted surprise definition.
- Safety preserved: provider_calls=0, no model activation, no broker/account mutation, no artifact deletion.
- `trading-manager` registered `MODEL_08_CPI_INFLATION_ASSOCIATION_READINESS_BUILD` via migration `472_register_cpi_inflation_association_readiness.sql` and regenerated `scripts/registry/current.csv`.
- Verification passed: `trading-model` 155 tests OK, compileall OK, diff-check OK; `trading-manager` 278 tests OK, registry dry-run no pending migrations, compileall OK, diff-check OK.

## CPI release correlation conclusion completed
- Chentong clarified that only the final conclusion was needed: whether CPI data is correlated with stock prices and whether it is meaningful to include.
- Ran a follow-up diagnostic using FRED read-only CPI release dates and CPI/Core CPI observations plus already-local Alpaca ETF bars. No model activation, broker/account mutation, or artifact deletion occurred.
- Artifact: `trading-model/storage/cpi_release_correlation_study_20260516/` with `strict_summary.json`, `strict_cpi_release_summary.csv`, and `strict_cpi_release_event_rows.csv`.
- Strict sample: 131 CPI release dates from 2016-01 through 2026-05, 1,571 one-day ETF/event rows across liquid ETF/sector proxies with nearby non-event controls.
- Aggregate result: one-day return delta vs controls was effectively zero (-0.004 percentage points), one-day absolute-return delta effectively zero (+0.001 percentage points), and one-day path-range delta small (+0.097 percentage points). Realized CPI/Core CPI level/change correlations with forward returns were weak/unstable: one-day aggregate correlations roughly +0.05 to +0.09; 5-10 day correlations drifted mildly negative around -0.04 to -0.09 for MoM and -0.08 to -0.09 for YoY/core variants.
- Conclusion communicated basis: CPI release occurrence has weak event-risk/volatility/calendar-control relevance, but realized CPI level/change is not meaningful as standalone directional alpha. Include it only as a macro event-risk/control feature, not as a trading signal or promotion-ready event family.

## Abnormal CPI release diagnostic completed
- Chentong noted that normal CPI releases may dilute the relationship and asked to extract abnormal CPI cases and evaluate again.
- Built abnormal CPI release artifact from existing strict CPI release rows using expanding historical z-score excluding the current print, `abs(z) >= 1.5`, minimum 24 prior releases, over CPI MoM, Core CPI MoM, CPI YoY, and Core CPI YoY.
- Artifact: `trading-model/storage/cpi_abnormal_release_correlation_study_20260516/` with abnormal flags, event rows, summary CSV/JSON, and README.
- Sample: 131 total CPI release dates, 44 abnormal release dates, 87 normal release dates, 12 ETF/sector proxies. No provider calls in this slice, no model activation, no broker/account mutation, no artifact deletion.
- Result: abnormal releases have more event-risk/volatility relevance than normal releases, especially one-day path range. Abnormal aggregate one-day path-range delta vs controls was about +0.233 percentage points versus about +0.027 percentage points for normal releases. Abnormal YoY/Core YoY prints had stronger one-day path expansion, about +0.405 to +0.489 percentage points. Directional returns remained weak/unstable: abnormal one-day return delta about -0.012 percentage points overall, positive-return-delta share about 52%, and 5-10 day abnormal return deltas were negative.
- Conclusion: abnormal CPI is worth including as an abnormal macro-risk/volatility flag/control feature, but still not as standalone directional alpha.

## CPI actual-vs-forecast surprise diagnostic completed
- Chentong corrected the abnormal CPI definition: abnormal should mean actual data differs materially from forecast/consensus, not merely the realized CPI level being historically unusual.
- Acknowledged the correction and ran a CPI surprise diagnostic using public Investing.com CPI event pages for actual/forecast history (`cpi_mom`, `core_mom`, `cpi_yoy`, `core_yoy`) joined to local Alpaca ETF bars and nearby non-event controls.
- Artifact: `trading-model/storage/cpi_surprise_correlation_study_20260516/` with surprise release metrics, event rows, summary CSV/JSON, and README.
- Sample: 101 release dates from 2017-12 through 2026-05; large actual-vs-forecast surprise threshold tested at 0.1pp and 0.2pp. The meaningful large-surprise definition is `abs(actual - forecast) >= 0.2pp`, producing 33 release dates.
- Result for >=0.2pp surprises: aggregate one-day path-range delta about +0.465 percentage points and one-day absolute-return delta about +0.149 percentage points vs controls. Directional return remains conditional/weak: aggregate one-day return delta about -0.389 percentage points; hot surprises had stronger negative one-day return delta about -0.534 percentage points with positive-delta share about 34%; cooler surprises were less negative at about -0.216 percentage points with positive-delta share about 48%.
- Corrected conclusion: actual-vs-forecast CPI surprise is the right abnormality definition. It is meaningful enough to include as an abnormal macro-risk/surprise feature, especially for event-day volatility/path risk and conditional risk-off pressure after hot surprises. It is still not robust enough to be a standalone buy/sell alpha signal.

## TE CPI surprise route check completed
- Chentong clarified he only wanted to confirm Trading Economics has both expected and actual CPI data, then continue.
- Confirmed the existing `trading-data` Trading Economics visible-calendar feed schema includes `actual`, `consensus`, and `te_forecast` fields. A Jan 2017 monthly probe showed CPI rows populated with actual/consensus/te_forecast, e.g. Core Inflation Rate YoY and Inflation Rate YoY.
- Ran a bounded TE-only monthly visible-calendar check from 2016-01 through 2026-05 using the existing feed, with no training, model activation, broker/account mutation, or artifact deletion.
- Artifact: `trading-model/storage/te_cpi_surprise_correlation_study_20260516/`.
- Result: TE route produced expectation-populated CPI rows for 18 release dates / 36 metric rows, mostly 2016-2017 YoY rows. Later visible historical rows often retain `actual`/`previous` but blank `consensus`/`te_forecast` through this visible scrape route.
- Conclusion: TE is the preferred canonical route when expectation fields are populated (`actual - consensus`, fallback `actual - te_forecast`), but the current visible-calendar scrape is too sparse to replace the larger temporary Investing.com surprise-history diagnostic. Need a fuller TE expectation-history route before making TE the sole canonical study input.

## TE CPI surprise route verification pushed
- Completed the follow-up after Chentong confirmed he was only checking whether Trading Economics has expected and actual CPI fields.
- Formal interpretation: TE has the desired fields (`actual`, `consensus`, `te_forecast`), and the correct canonical surprise formula should be `actual - consensus`, falling back to `actual - te_forecast` when consensus is absent.
- Current TE visible-calendar historical scrape is sparse for expectation-populated CPI rows: 18 release dates / 36 metric rows, mostly 2016-2017 YoY rows; later rows often preserve `actual`/`previous` but blank `consensus`/`te_forecast` through this route.
- Artifact retained: `/root/projects/trading-model/storage/te_cpi_surprise_correlation_study_20260516/`.
- Conclusion: TE should be the canonical provider route once a fuller TE expectation-history path is available; until then, the Investing.com CPI surprise diagnostic remains temporary supporting evidence for the actual-vs-forecast relationship.
- Safety preserved: no model activation, no broker/account mutation, no artifact deletion.
- Verification passed: `trading-model` 155 tests OK, compileall OK, diff-check OK.
- Pushed commit: `trading-model` `c25a711` (`Check TE CPI surprise route`).
- Recommended next step: continue event-family batch/scouting work; treat CPI surprise as macro event-risk/control feature, not standalone alpha.

## Remaining event-family batch closeout completed
- Chentong asked to process the remaining event families in one pass.
- Implemented safe local all-family closeout in `trading-model`: `src/models/model_08_event_risk_governor/event_family_remaining_closeout.py`, CLI `scripts/models/model_08_event_risk_governor/build_event_family_remaining_closeout.py`, and tests.
- Generated artifacts under `trading-model/storage/event_family_remaining_closeout_20260516/`: `event_family_remaining_closeout.json`, `event_family_remaining_closeout_summary.json`, `event_family_remaining_closeout.csv`, `event_family_next_packet_queue.csv`, and README.
- Closeout result accounts for all 29 fine-grained families: 2 risk/control candidates only (`earnings_guidance_scheduled_shell`, `cpi_inflation_release`), 1 deferred-low-signal family (`option_derivatives_abnormality`), 20 families requiring family packets, 3 blocked by missing PIT expectation/comparable baselines, 2 blocked by missing residual-over-base-state definitions, and 1 blocked by missing liquidity/depth evidence.
- No family was promoted to standalone directional alpha. No provider calls, model activation, broker/account mutation, or artifact deletion occurred.
- `trading-manager` registered `MODEL_08_EVENT_FAMILY_REMAINING_CLOSEOUT_BUILD` via migration `473_register_event_family_remaining_closeout.sql`, updated `scripts/registry/current.csv`, and recorded decision D198 that remaining closeout is disposition, not promotion.
- Verification passed: `trading-model` 159 tests OK, compileall OK, diff-check OK; `trading-manager` 278 tests OK, registry dry-run no pending migrations, compileall OK, diff-check OK.
- Pushed commits: `trading-model` `c5b5517` (`Close remaining event family batch`) and `trading-manager` `1cf0f11` (`Register event family remaining closeout`).

## Event-family correlation interpretation communicated
- Chentong asked how the remaining event-family results looked and whether correlations were obvious.
- Communicated conclusion: no family currently has obvious standalone directional-alpha correlation. The clearest observed relationships are event-risk / volatility / path-range, especially CPI actual-vs-consensus/forecast surprise.
- CPI surprise remains the strongest current evidence: large surprises (`abs(actual - forecast/consensus) >= 0.2pp`) show meaningful 1-day path-range and absolute-return expansion, with hot surprises showing conditional risk-off pressure, but still not enough for standalone buy/sell alpha.
- CPI release occurrence and realized-abnormal CPI z-score remain weak calendar/risk-control features, not alpha.
- Earnings/guidance scheduled shell shows elevated path range around earnings/guidance windows, but direction is not meaningful without current comparable guidance and PIT expectation baselines.
- Option-derivatives abnormality remains deferred low-signal/noisy; most other families are not proven unrelated, but are blocked by missing family packets, PIT baselines, residual definitions, or liquidity/depth evidence.

## Event-family all-packet precondition completion pushed
- Chentong said to fill the missing event-family prerequisites first and only make a final conclusion after all families are supplemented.
- Implemented safe all-family precondition completion in `trading-model`: `src/models/model_08_event_risk_governor/event_family_precondition_completion.py`, CLI `scripts/models/model_08_event_risk_governor/build_event_family_precondition_completion.py`, and tests.
- Generated artifact `trading-model/storage/event_family_precondition_completion_20260516/` with `event_family_precondition_completion.json`, summary JSON, `event_family_scouting_packets.jsonl/csv`, `event_family_evidence_requirements.csv`, and README.
- Result: all 29 fine-grained EventRiskGovernor families now have maintained `event_family_scouting_packet_v1` precondition packets defining source precedence, point-in-time clocks, identity/measure fields, baseline requirements, controls, label windows, residual requirements, liquidity requirements, and early-stop gates.
- This removes the generic missing-packet governance blocker, but final association judgment remains withheld until empirical studies and remaining PIT expectation/comparable baseline, fuller TE expectation-history, residual-over-base-state, liquidity/depth, or revised option-abnormality evidence are completed.
- `trading-manager` registered `MODEL_08_EVENT_FAMILY_PRECONDITION_COMPLETION_BUILD` via migration `474_register_event_family_precondition_completion.sql`, updated `scripts/registry/current.csv`, and recorded decision D199 that packet completion precedes final judgment but is not promotion/evidence proof.
- Verification passed: `trading-model` targeted precondition tests OK, full 163 tests OK, compileall OK, diff-check OK; `trading-manager` 278 tests OK, registry dry-run no pending migrations, compileall OK, diff-check OK.
- Pushed commits: `trading-model` `f734d4f` (`Complete event family precondition packets`) and `trading-manager` `3cd8cae` (`Register event family precondition completion`).

## Event-family empirical coverage scan completed
- Built local-only empirical coverage/readiness scan for all 29 `EventRiskGovernor` families in `trading-model`.
- New code: `src/models/model_08_event_risk_governor/event_family_empirical_coverage.py`, script `scripts/models/model_08_event_risk_governor/build_event_family_empirical_coverage.py`, and tests `tests/test_event_family_empirical_coverage.py`.
- Artifact: `/root/projects/trading-model/storage/event_family_empirical_coverage_20260516/` with JSON summary, full coverage CSV/JSON, next-action CSV, and README.
- Scan used existing local artifacts only: 405 Alpaca news rows, 750 GDELT news rows, 24,764 SEC company-fact rows, 12 valid Trading Economics calendar rows with expectation fields, and 6,045 Alpaca bar completion receipts.
- Result: 5 families have existing empirical/scouting artifacts; 12 have local candidates requiring interpretation/dedup/matched-control studies; 7 have no local candidates under current source/parser coverage; 3 are blocked by PIT expectation/comparable baselines; 3 by residual detectors; 1 by liquidity/depth evidence; option derivatives abnormality remains deferred until definition revision.
- Safety: provider_calls=0; no model training/activation, broker/account mutation, artifact deletion, or final alpha/risk promotion.
- Registered manager script key `MODEL_08_EVENT_FAMILY_EMPIRICAL_COVERAGE_BUILD` via migration `475_register_event_family_empirical_coverage.sql`; added manager decision D200.
- Verification passed: `trading-model` targeted coverage tests 4 OK, full 167 tests OK, compileall/diff-check OK; `trading-manager` 278 tests OK, registry dry-run no pending, compileall/diff-check OK.
- Pushed commits: `trading-model` `80ea48c`; `trading-manager` `12d3e7f`.

## Event layer final judgment completed
- Chentong asked to do the remaining event-model conclusion in one pass because it affects event-model design.
- Built final current-cycle EventRiskGovernor judgment in `trading-model`:
  - code: `src/models/model_08_event_risk_governor/event_layer_final_judgment.py`
  - script: `scripts/models/model_08_event_risk_governor/build_event_layer_final_judgment.py`
  - tests: `tests/test_event_layer_final_judgment.py`
  - artifact: `/root/projects/trading-model/storage/event_layer_final_judgment_20260516/`
- Final posture: build `EventRiskGovernor / EventIntelligenceOverlay` as bounded risk/intelligence overlay; do not build/train/activate standalone event alpha under current evidence.
- Family disposition: 29 families covered; 2 accepted for risk/control only (`earnings_guidance_scheduled_shell`, `cpi_inflation_release`); 0 accepted for standalone directional alpha; 12 research-queue local candidates; 7 source/parser gaps; 3 PIT-baseline blockers; 3 residual-detector blockers; 1 liquidity/depth blocker; `option_derivatives_abnormality` deferred low-signal pending abnormality-definition revision.
- Allowed outputs: canonical event/lifecycle/evidence-quality/risk/uncertainty/path-gap/liquidity-review/block-cap/reduce-flatten-review/audit hints only. Prohibited: buy/sell/hold, directional alpha override, sizing, target exposure, option contract selection, order/broker/account mutation, automatic activation, destructive SQL, artifact deletion.
- Registered manager script key `MODEL_08_EVENT_LAYER_FINAL_JUDGMENT_BUILD` via migration `476_register_event_layer_final_judgment.sql`; added manager decision D201.
- Verification passed: `trading-model` final-judgment tests 4 OK, full 171 tests OK, compileall/diff-check OK; `trading-manager` 278 tests OK, registry dry-run no pending, compileall/diff-check OK.
- Pushed commits: `trading-model` `6445415`; `trading-manager` `82a945a`.

## All-family event association measurement completed
- Chentong pushed back that the first priority is measuring whether all event families have association, before model-governance conclusions.
- Built local all-family event/price association measurement in `trading-model`:
  - code: `src/models/model_08_event_risk_governor/event_family_all_association.py`
  - script: `scripts/models/model_08_event_risk_governor/build_event_family_all_association.py`
  - tests: `tests/test_event_family_all_association.py`
  - artifact: `/root/projects/trading-model/storage/event_family_all_association_20260516/`
- Artifact emits one row for all 29 event families and separates accepted measured associations from unaccepted local keyword/proxy screening signals, no-label data gaps, and required-precondition blockers.
- Accepted measured associations remain risk/control only: `cpi_inflation_release` = risk/volatility association, not directional alpha; `earnings_guidance_scheduled_shell` = scheduled path-risk association, not directional alpha.
- Local screening-only associations: 11 families show unaccepted local risk/path screening signal and 2 show unaccepted local directional-candidate signal, but these are not accepted for model use because canonical source/parser, interpretation/dedup, matched-control, PIT baseline, residual-detector, or liquidity/depth gates are missing.
- No clear local association in screening: `mna_transaction`, `sector_demand_shock`.
- No measurable local labels/data gap: `accounting_restatement_or_fraud`, `fomc_rates_policy`, `credit_liquidity_stress`, `geopolitical_or_fiscal_shock`.
- Required-precondition blockers: `earnings_guidance_result_metrics`, `earnings_guidance_raise_cut_or_withdrawal`, `nfp_employment_release`, `treasury_yield_curve_shock`, `price_action_pattern`, `residual_market_structure_disturbance`, `microstructure_liquidity_disruption`.
- `option_derivatives_abnormality` remains measured prior low-signal/current-definition-no-accepted-association.
- Registered manager script key `MODEL_08_EVENT_FAMILY_ALL_ASSOCIATION_BUILD` via migration `477_register_event_family_all_association.sql`; added manager decision D202.
- Verification passed: `trading-model` all-association tests 4 OK, full 175 tests OK, compileall/diff-check OK; `trading-manager` 278 tests OK, registry dry-run no pending, compileall/diff-check OK.
- Pushed commits: `trading-model` `4089439`; `trading-manager` `d734720`.

## Expanded event-family association screening universe completed
- Chentong asked to increase detection data and use more symbols before setting thresholds/grading.
- Expanded `trading-model` all-family event association screening from the initial 12 proxy symbols to all locally available Alpaca bar symbols for the screening month: 47 ETF/proxy symbols.
- Updated `src/models/model_08_event_risk_governor/event_family_all_association.py` to discover local bar symbols automatically and emit `event_family_expanded_stability.csv` as a preparation-only stability screen for the next threshold/grading step.
- Regenerated `/root/projects/trading-model/storage/event_family_all_association_20260516/`.
- Expanded stability counts: 2 accepted prior risk/control studies, 9 expanded-screening threshold-review candidates, 3 measured no-clear local stability rows, 3 thin unstable screening rows, 11 not-threshold-ready data/precondition gap rows, and 1 definition-revision-required row.
- Threshold-review candidates from expanded screening: `earnings_call_narrative_residual`, `equity_offering_dilution`, `buyback_or_capital_return`, `customer_contract_win_loss`, `management_change`, `analyst_rating_or_price_target_change`, `supply_chain_disruption`, `sector_regulation_policy`, `commodity_or_input_cost_shock`.
- Accepted current associations remain risk/control only: `cpi_inflation_release` and `earnings_guidance_scheduled_shell`; still zero accepted standalone directional-alpha families.
- Updated `trading-model` docs and `trading-manager` docs/decision D202 to make clear expanded proxy coverage is still screening evidence only and may feed threshold/grading review, not accepted model outputs.
- Verification passed: `trading-model` all-association tests 4 OK, full 175 tests OK, compileall/diff-check OK; `trading-manager` 278 tests OK, registry dry-run no pending, compileall/diff-check OK.
- Pushed commits: `trading-model` `f43ce90`; `trading-manager` `67e5b25`.

## Event-family threshold queue with no-correlation deletion completed
- Chentong said families already proven to have no correlation can be deleted directly.
- Implemented this as queue-scoped deletion, not physical evidence deletion: measured no-clear families are removed from the next active threshold/grading universe while audit artifacts and historical rows remain available.
- Added `trading-model` threshold/grading queue builder:
  - code: `src/models/model_08_event_risk_governor/event_family_threshold_grading.py`
  - script: `scripts/models/model_08_event_risk_governor/build_event_family_threshold_grading.py`
  - tests: `tests/test_event_family_threshold_grading.py`
  - artifact: `/root/projects/trading-model/storage/event_family_threshold_grading_20260516/`
- Queue-scoped deleted/retired no-clear families: `mna_transaction`, `product_launch_or_failure`, `sector_demand_shock`.
- Current-definition deletion: `option_derivatives_abnormality` is removed from threshold work under the current abnormality definition; broader family can be retested after a revised abnormality standard.
- Active threshold universe now has 11 families: accepted risk/control seeds `earnings_guidance_scheduled_shell` and `cpi_inflation_release`, plus threshold-review candidates `earnings_call_narrative_residual`, `equity_offering_dilution`, `buyback_or_capital_return`, `customer_contract_win_loss`, `management_change`, `analyst_rating_or_price_target_change`, `supply_chain_disruption`, `sector_regulation_policy`, `commodity_or_input_cost_shock`.
- Held thin/unstable, not deleted: `insider_or_ownership_change`, `legal_regulatory_investigation`, `bankruptcy_or_restructuring`.
- Blocked data/precondition families remain blocked, not deleted.
- `trading-manager` registered `MODEL_08_EVENT_FAMILY_THRESHOLD_GRADING_BUILD` via migration `478_register_event_family_threshold_grading.sql` and added decision D203.
- Verification passed: `trading-model` threshold tests 3 OK, full 178 tests OK, compileall/diff-check OK; `trading-manager` 278 tests OK, registry dry-run no pending, compileall/diff-check OK.
- Pushed commits: `trading-model` `ff2f882`; `trading-manager` `b5a3a6d`.

## Reverse price-anomaly event discovery completed
- Chentong proposed reversing the association direction: find abnormal price points first, then inspect nearby events for commonalities to establish relationships.
- Implemented local reverse discovery in `trading-model`:
  - code: `src/models/model_08_event_risk_governor/price_anomaly_event_discovery.py`
  - script: `scripts/models/model_08_event_risk_governor/build_price_anomaly_event_discovery.py`
  - tests: `tests/test_price_anomaly_event_discovery.py`
  - artifact: `/root/projects/trading-model/storage/price_anomaly_event_discovery_20260516/`
- Method: detect local 2016-01 price anomalies across available Alpaca ETF/proxy bars using 1-day absolute-return/path-range z-score threshold 1.25, then scan local event-family mentions within ±1 day and compare anomaly-hit rate to non-anomaly control observations.
- Result: 104 anomaly observations across 27 symbols; 23 event families appeared in nearby windows.
- Only current reverse-discovery candidate under the stronger enrichment rule: `legal_regulatory_investigation` with anomaly hit rate ~48.1%, control hit rate ~22.2%, delta ~25.9pp, lift ~2.17.
- Positive but not passing stronger rule: `sector_regulation_policy` and `cpi_inflation_release`; saturated/general families such as analyst/customer/macro mentions were not useful when control hit rates were near 100%.
- Registered manager script key `MODEL_08_PRICE_ANOMALY_EVENT_DISCOVERY_BUILD` via migration `479_register_price_anomaly_event_discovery.sql`; added manager decision D204.
- Verification passed: `trading-model` reverse-discovery tests 3 OK, full 181 tests OK, compileall/diff-check OK; `trading-manager` 278 tests OK, registry dry-run no pending, compileall/diff-check OK.
- Pushed commits: `trading-model` `e019111`; `trading-manager` `6182716`.

## Residual-anomaly event overlay route accepted
- Chentong clarified the intended production event-model route: use Layers 1-7 to analyze the market first, identify abnormal price/path/volume/liquidity/option behavior that remains unexplained, then use the event model to analyze those residual anomalies and determine which events may have caused or explained them.
- Final target remains EventRiskGovernor / EventIntelligenceOverlay as a coverage, correction, explanation, and warning layer, not standalone event alpha.
- Updated `trading-model/docs/09_layer_08_event_risk_governor.md` and `docs/100_event_family_scouting.md` to define the base-stack-first residual-anomaly workflow: Layers 1-7 explain normal context -> residual_anomaly_context -> Layer 8 event explanation/warning/correction overlay.
- Updated `trading-manager/docs/81_decision.md` with D205 and task docs to preserve governance boundary: Layer 8 may emit warnings, uncertainty/path-risk, block/cap/reduce/flatten-review, and human-review hints, but must not replace Layers 1-7, emit standalone directional alpha, directly buy/sell/hold, choose contracts, mutate broker/account state, or bypass manager review.
- Verification passed: `trading-model` 181 tests OK + diff-check; `trading-manager` 278 tests OK + registry dry-run no pending + diff-check.
- Pushed commits: `trading-model` `a36ced7`; `trading-manager` `a644e26`.

## Event observation-pool and strategy-promotion policy accepted
- Chentong refined the Layer 8 production architecture: historical training/research may scan all point-in-time events/news/filings/macro candidates to explain residual anomalies, but realtime trading should not continuously read/classify every event stream.
- Accepted operating split: use Layers 1-7 to find base-stack residual anomalies first; during research, search all events to identify possible causes; when a family repeatedly explains anomalies, add it to the reviewed EventRiskGovernor observation pool for realtime monitoring.
- Realtime event monitoring scope is now observation-pool-only: current active pool families are `cpi_inflation_release` and `earnings_guidance_scheduled_shell`; `legal_regulatory_investigation` is probationary because reverse price-anomaly discovery found enrichment but canonical interpretation/control work is still required.
- Strategy-layer promotion is allowed only after a family shows stable, predictive, high-correlation market reaction across splits, controls, base-stack residuals, and regimes, with no leakage and material improvement over the base strategy. Promotion changes the family from correction/explanation overlay into a strategy-decision input.
- Final promotion/demotion decision must be made by a script-emitted evidence packet plus agent review (`accept` / `defer` / `reject`); no family auto-promotes based only on a local screen.
- Implemented `trading-model` policy builder:
  - code: `/root/projects/trading-model/src/models/model_08_event_risk_governor/event_observation_pool_policy.py`
  - script: `/root/projects/trading-model/scripts/models/model_08_event_risk_governor/build_event_observation_pool_policy.py`
  - tests: `/root/projects/trading-model/tests/test_event_observation_pool_policy.py`
  - artifact: `/root/projects/trading-model/storage/event_observation_pool_policy_20260516/`
- Registered manager migration `/root/projects/trading-manager/scripts/registry/sql/schema_migrations/480_register_event_observation_pool_policy.sql` for `MODEL_08_EVENT_OBSERVATION_POOL_POLICY_BUILD`.
- Verification passed in the completed slice: `trading-model` targeted observation-pool tests 3 OK, full 184 tests OK, compileall/diff-check OK; `trading-manager` 278 tests OK, registry dry-run no pending, compileall/diff-check OK.
- Pushed commits: `trading-model` `7b56290`; `trading-manager` `a7d1076`.
- Next recommended implementation: build `residual_anomaly_event_discovery` that consumes Layers 1-7 residual-anomaly context instead of raw price-only z-scores, then searches all historical events for explanations and emits observation-pool candidates plus strategy-promotion evidence packets when warranted.

## Layer 8 residual-anomaly event discovery implemented
- Continued Chentong's safe Layer 8 implementation request: build code before service integration, add residual-anomaly event discovery, register manager entry, and avoid daemon startup, model activation/training, broker/account mutation, realtime service activation, destructive SQL, or artifact deletion.
- Implemented `trading-model` residual-anomaly discovery:
  - code: `/root/projects/trading-model/src/models/model_08_event_risk_governor/residual_anomaly_event_discovery.py`
  - script: `/root/projects/trading-model/scripts/models/model_08_event_risk_governor/build_residual_anomaly_event_discovery.py`
  - tests: `/root/projects/trading-model/tests/test_residual_anomaly_event_discovery.py`
  - artifact: `/root/projects/trading-model/storage/residual_anomaly_event_discovery_20260516/`
- Contract/output files include `residual_anomaly_event_discovery_v1`, `residual_anomaly_event_discovery_summary_v1`, residual rows CSV, event-family enrichment CSV, and `event_family_strategy_promotion_review_packet_v1` JSONL.
- Discovery starts from Layer 7 evaluation labels over the Layers 1-7 stack, not raw price-only z-scores. It scans nearby point-in-time event families to explain base-stack residuals and can emit observation-pool candidates or strategy-promotion review packets only when controls/evidence support it.
- Current empirical result: `3,555` residual anomalies were identified, but current Layer 7 labels are saturated with no non-residual controls. Therefore the artifact correctly reports `missing_non_residual_control_labels`, emits no observation-pool additions, and emits no strategy-promotion packets.
- `trading-manager` registered `MODEL_08_RESIDUAL_ANOMALY_EVENT_DISCOVERY_BUILD` via migration `/root/projects/trading-manager/scripts/registry/sql/schema_migrations/481_register_residual_anomaly_event_discovery.sql`, regenerated `scripts/registry/current.csv`, and added decision D207.
- Verification passed: `trading-model` 187 tests OK, compileall OK, diff-check OK; `trading-manager` 278 tests OK, registry dry-run no pending migrations, compileall OK, diff-check OK.
- Pushed commits: `trading-model` `b6d9bfe`; `trading-manager` `ad2e32d`. Both repos were clean/synced afterward.
- Next recommended action: build a disabled/dry-run service-side callable wrapper only after preserving the no-activation boundary; stronger residual discovery requires non-residual control labels.
