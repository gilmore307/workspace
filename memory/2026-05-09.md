
## Layer 1 historical backfill planner expanded to full ETF universe
- Chentong corrected the historical-training plan: run models layer by layer, starting with Layer 1 `MarketRegimeModel`, which needs the full Layer 1 ETF universe rather than a single-SPY provider smoke.
- Inspected `trading-storage/main/shared/market_regime_etf_universe.csv`: 47 total rows, with 22 `layer_01_market_regime` market-state ETFs and 25 `layer_02_sector_context` sector-observation ETFs.
- Updated `trading-manager` monthly backfill planning so `01_feed_alpaca_bars` expands into one monthly request per `layer_01_market_regime` ETF symbol, with symbol-specific request ids, parameter refs, expected output paths, timeframe metadata, and universe refs. Current 2016-01 plan now emits 28 total requests: 22 Layer 1 Alpaca bar requests plus 6 other historical source requests.
- Layer 1 symbols planned for `2016-01`: BITW, CPER, DBA, DBC, DIA, ETHA, FSOL, GLD, HYG, IBIT, IEF, IWM, LQD, QQQ, RSP, SHY, SLV, SPY, TLT, USO, UUP, VIXY.
- Updated materialization so SQL-loaded rows can infer symbol/timeframe from symbol-specific `parameter_ref` paths and the reviewed ETF universe. Layer 1 payloads now write under `storage/monthly_backfill_v1/alpaca_bars/<SYMBOL>/<MONTH>/task_key.json`.
- Verification passed in `trading-manager`: targeted monthly/payload/handoff tests 12 OK, full 72 tests OK, registry dry-run no pending migrations, compileall OK, `git diff --check` OK. Pushed commit `f300bce` (`Expand Layer 1 backfill over ETF universe`). Repo clean on `main...origin/main` afterward.

## Manager-owned Layer 1 historical-training batch preparation implemented
- Chentong pushed back that historical-training tasks must be manager-dispatched automatically rather than manually prompted one by one. Accepted correction: manager is the layer-by-layer historical-training scheduler/orchestrator; operator review should be for boundary approvals, not per-ETF task催促.
- Added `trading-manager/src/trading_manager_tasks/historical_training.py` and CLI `scripts/tasks/prepare_layer_one_historical_training.py`.
- The new entrypoint prepares Layer 1 `MarketRegimeModel` historical-training as one batch: plans all 22 reviewed Layer 1 ETF `01_feed_alpaca_bars` requests, materializes task-key payloads, validates `trading-data` handoff boundaries, and reports zero provider calls, zero dispatch, zero model activation, and zero broker execution.
- Added registry migration `265_register_layer_one_historical_training_preparation.sql`, registering `LAYER_ONE_HISTORICAL_TRAINING_BATCH` and `MANAGER_PREPARE_LAYER_ONE_HISTORICAL_TRAINING`; exported `scripts/registry/current.csv`.
- Tightened `live_call_approval_v1` batch validation so a reviewed approval cannot cover more requests than `max_requests`.
- Updated manager README/docs/scripts/tests docs to state that historical-training orchestration prepares whole layer batches and must not degrade into manual one-request-at-a-time prompting.
- Verification passed in `trading-manager`: 75 tests OK, registry dry-run no pending migrations, compileall OK, diff-check OK, and live batch CLI smoke with `--write-files-only` produced 22 requests and 22 handoff validations with no provider dispatch. Pushed commit `a9c12fc` (`Prepare Layer 1 historical training batch`). Repo clean afterward.

## trading-data feature script wrappers normalized
- Chentong noticed `trading-data/scripts/` only had feature wrappers for 01 and 02 while `src/data_feature/` already had accepted feature surfaces 01, 02, 03, 04, and 08.
- Root cause: early Layer 1/2 features used repo-local `scripts/generate_feature_*.py` compatibility wrappers, while later Layer 3/4/8 features used package console scripts from `pyproject.toml`, leaving the visible `scripts/` surface inconsistent.
- Fixed `trading-data` by adding thin wrappers: `scripts/generate_feature_03_target_state_vector.py`, `scripts/generate_feature_04_event_overlay.py`, and `scripts/generate_feature_08_option_expression.py`; updated `scripts/README.md` and `tests/test_layer_structure_catalog.py` so each owned feature package must have a matching script wrapper.
- Updated `trading-manager` registry migration `266_route_trading_data_feature_scripts_through_scripts_dir.sql` so `FEATURE_03_TARGET_STATE_VECTOR_GENERATE`, `FEATURE_04_EVENT_OVERLAY_GENERATE`, and `FEATURE_08_OPTION_EXPRESSION_GENERATE` point to the new `trading-data/scripts/` wrappers; regenerated `scripts/registry/current.csv` and updated registry tests.
- Verification passed: `trading-data` 33 tests OK, wrapper help smoke OK for 03/04/08, compileall OK, diff-check OK; `trading-manager` 75 tests OK, registry dry-run no pending migrations, compileall OK, diff-check OK.
- Pushed commits: `trading-data` `ec694b9` (`Add feature SQL script wrappers`) and `trading-manager` `f8af2a4` (`Route feature scripts through trading-data wrappers`). Both repos clean afterward.

## trading-data feature entrypoints consolidated on package CLIs
- Chentong questioned whether both `trading-data/src/data_feature/` and `trading-data/scripts/generate_feature_*.py` wrappers needed to exist. Accepted correction: keep `src/data_feature/` as the implementation boundary and use `pyproject.toml` package CLIs as the single callable surface; do not keep duplicate feature wrapper scripts.
- Removed feature wrapper scripts from `trading-data/scripts/`: `generate_feature_01_market_regime.py`, `generate_feature_02_sector_context.py`, `generate_feature_03_target_state_vector.py`, `generate_feature_04_event_overlay.py`, and `generate_feature_08_option_expression.py`.
- Updated `trading-data/scripts/README.md`, `src/data_feature/feature_01_market_regime/README.md`, and `tests/test_layer_structure_catalog.py` so owned feature surfaces must not keep duplicate `scripts/generate_feature_*.py` wrappers. Package CLIs remain: `trading-data-feature-01-market-regime`, `trading-data-feature-02-sector-context`, `trading-data-feature-03-target-state-vector`, `trading-data-feature-04-event-overlay`, and `trading-data-feature-08-option-expression`.
- Added `trading-manager` registry migration `267_consolidate_trading_data_feature_entrypoints_to_package_clis.sql`, regenerating `scripts/registry/current.csv` so `FEATURE_01/02/03/04/08_*_GENERATE` rows point to package CLIs and their `src/data_feature/.../__main__.py` implementation entrypoints.
- Verification passed: `trading-data` 33 tests OK, package feature module help smoke OK for 01/02/03/04/08, compileall OK, diff-check OK; `trading-manager` 75 tests OK, registry dry-run no pending migrations, compileall OK, diff-check OK.
- Pushed commits: `trading-data` `20f56b8` (`Consolidate feature entrypoints on package CLIs`) and `trading-manager` `d1268cc` (`Consolidate feature registry entrypoints`). Both repos clean afterward.

## trading-data scripts directory removed
- Chentong pointed out that after feature wrappers were removed, `trading-data/scripts/` itself was unnecessary.
- Removed the now-empty `trading-data/scripts/` boundary by deleting `scripts/README.md`.
- Updated `trading-data` README and docs to state that stable callable surfaces use package CLIs declared in `pyproject.toml`; the repository does not keep a top-level `scripts/` wrapper directory.
- Updated `feature_01_market_regime/generator.py` docstring to stop referring to a scripts wrapper.
- Verification passed: `trading-data` 33 tests OK, package feature module help smoke OK for 01/02/03/04/08, compileall OK, diff-check OK. Pushed `trading-data` commit `79d752c` (`Remove empty scripts boundary`). Repo clean afterward.

## Manager automation scheduler policy accepted
- Chentong clarified that `trading-manager` should automate the full model training/maintenance lifecycle from data acquisition through feature generation, model training, evaluation, promotion-review preparation, and maintenance, rather than sitting idle or requiring manual prompting per task.
- Accepted guardrail: historical modeling can run continuously and use concurrency, but must reserve host capacity for future live monitoring and realtime order systems, throttle based on server pressure, and pause/heavily throttle historical provider/modeling work during regular US equity market protection windows.
- Added `trading-manager/docs/98_automation_scheduler.md` defining the always-on scheduler policy, priority order, resource budget, market-hours protection window (`09:20-16:10 ET` regular US equity session protection), hard approval gates, and work-loop semantics.
- Added registry migration `268_register_manager_automation_scheduler_policy.sql`, regenerated `scripts/registry/current.csv`, and added test coverage for `MANAGER_AUTONOMOUS_SCHEDULER_POLICY`, `MANAGER_RESOURCE_BUDGET_POLICY`, `MANAGER_MARKET_HOURS_HISTORICAL_PAUSE_POLICY`, and `MANAGER_SCHEDULER_WORK_LOOP`.
- Added decision `D110 - Manager scheduler should automate historical training while protecting live capacity` to `trading-manager/docs/81_decision.md`; updated README/task/task-system/closeout docs.
- Verification passed: `trading-manager` 75 tests OK, registry dry-run no pending migrations, compileall OK, diff-check OK. Pushed `trading-manager` commit `9f1d8b3` (`Define manager automation scheduler policy`). Repo clean afterward.
