# 2026-05-07

## Layer 4 EventOverlayModel contract/metawork completed
- Chentong asked to focus only on Layer 4 and synchronize md files plus registry metawork.
- `trading-model` now has accepted Layer 4 EventOverlayModel docs for point-in-time `event_context_vector`: inputs, `source_04_event_overlay` boundary, upstream state refs, scope/sensitivity metadata, leakage rules, EventEncoder/EventContextMatcher/EventOverlayScorer route, 21 scalar score families, dominant impact scope as model-local enum audit field, labels, baselines, and boundaries.
- `trading-data` clarified `source_04_event_overlay` as one-row-per-event overview/index, not the full `event_context_vector`; Layer 4 combines those rows with event artifacts and upstream context states in `trading-model`.
- `trading-manager` added registry migrations `225_register_event_context_vector_score_families.sql` and `226_keep_event_context_vector_values_scalar.sql`, regenerated `scripts/registry/current.csv`, and updated `state_vector_value` rules/tests so only scalar Layer 4 score families are registered; `4_event_dominant_impact_scope_<horizon>` remains model-local audit/debug/routing.
- Verification passed: `trading-model` 70 tests + `git diff --check`; `trading-data` 15 tests + `git diff --check`; `trading-manager` 35 tests + registry dry-run no pending migrations + `git diff --check`.
- Pushed commits: `trading-model` 879b070, `trading-data` 5cabfb4, `trading-manager` 3599300 and follow-up 6a87448. All three repos clean after push.

## Layer 5 AlphaConfidenceModel contract/metawork completed
- Chentong asked to close Layer 4 and start Layer 5.
- `trading-model` now has accepted Layer 5 `AlphaConfidenceModel` docs in `docs/06_layer_05_alpha_confidence.md` and synchronized docs spine/index/taxonomy/task/decision files. Layer 5 consumes `target_context_state` plus `event_context_vector` and outputs `alpha_confidence_vector` with calibrated long/short confidence, direction strength, expected return/value, downside/tail/path risk, uncertainty, context support, event adjustment, and calibration quality.
- Layer 5 boundaries are explicit: it must not emit buy/sell/hold, final action, target exposure, position size, account-risk allocation, option contract, strike, DTE, delta, order type, or broker/account mutation. Layer 6 owns trading projection/target exposure; Layer 7 owns expression/final-action boundaries.
- `trading-data` clarified that `source_05_option_expression` is source-number 05 for option-expression inputs, not model Layer 5 `AlphaConfidenceModel`; AlphaConfidence/TradingProjection do not need symmetry-only `trading-data` bundles when no new source acquisition is required.
- `trading-manager` added migration `227_register_alpha_confidence_vector_score_families.sql`, regenerated `scripts/registry/current.csv`, and updated registry rules/kind docs/tests for Layer 5 scalar `state_vector_value` score families only. Registered horizons: `5min`, `15min`, `60min`, `390min`. Registered score families: `5_alpha_direction_confidence_score_<horizon>`, `5_alpha_direction_strength_score_<horizon>`, `5_alpha_expected_return_score_<horizon>`, `5_alpha_expected_value_score_<horizon>`, `5_alpha_downside_risk_score_<horizon>`, `5_alpha_tail_risk_score_<horizon>`, `5_alpha_path_stability_score_<horizon>`, `5_alpha_uncertainty_score_<horizon>`, `5_alpha_context_support_score_<horizon>`, `5_alpha_event_adjustment_score_<horizon>`, and `5_alpha_calibration_quality_score_<horizon>`.
- Verification passed before commit: `trading-model` 70 tests + `git diff --check`; `trading-data` 15 tests + `git diff --check`; `trading-manager` 35 tests + registry dry-run no pending migrations + `git diff --check`.
- Pushed commits: `trading-model` 8aa1809, `trading-data` 5b26703, `trading-manager` 6581a3e. All three repos clean after push.

## Layer 5 adjusted alpha-confidence contract implemented
- Chentong accepted the Layer 5 base-vs-adjusted split: base/unadjusted alpha from Layer 1/2/3 is diagnostics/research/audit/event-attribution only; final adjusted `alpha_confidence_vector` is the Layer 6-facing output.
- Updated `trading-model` docs for Layer 5 and neighboring architecture/taxonomy/decision/task docs. Final adjusted V1 contract is 9 score families across `5min`, `15min`, `60min`, `390min`: direction, strength, expected residual return, confidence, signal reliability, path quality, reversal risk, drawdown risk, and alpha tradability.
- Added `trading-manager` registry migration `228_align_layer_five_base_adjusted_contract.sql`, regenerated `scripts/registry/current.csv`, updated registry naming/kind docs and registry tests to the new 9-family contract plus base diagnostic config rows.
- Verification passed: `trading-model` 70 tests OK; `trading-manager` 35 tests OK; registry dry-run no pending migrations; stale old Layer 5 token grep clean outside immutable migration history; git diff checks clean.
- Pushed commits: `trading-model` f91c29c; `trading-data` 61117b7; `trading-manager` bf624a8. Closeout spot-check found all three repos clean on `main...origin/main`; active docs/current registry scan found no blocking Layer 5 TODOs or stale pre-adjusted token references outside immutable migration history.

## Layer 6 framing clarified
- Chentong questioned whether Layer 6 was only a transition before connecting confidence to trading operations.
- Clarified accepted framing: Layer 6 should relate Layer 5 alpha confidence to current/pending position state, target exposure, position gap, costs, and risk budget — not directly to buy/sell/hold operations.
- Preferred Layer 6 output concept shifted from `trading_signal_vector` to `position_projection_vector`; Layer 7 should relate projected holding/exposure state to expression choice and final offline action.
- Layer 6 external review accepted directionally: preserve `PositionProjectionModel` / `position_projection_vector`, define `target_exposure` as abstract risk exposure, add `effective_current_exposure = current_position_exposure + pending_exposure_size * pending_order_fill_probability_estimate`, add resolved projection handoff summary, prefer candidate exposure utility curve training over single hindsight-best exposure, split position-level friction from expression-specific friction hints, and add explicit invariants preventing buy/sell/hold/instrument/option-chain leakage into Layer 6.

## Layer 6 PositionProjectionModel contract/metawork completed
- Chentong approved landing the revised Layer 6 outline as `PositionProjectionModel` / `model_06_position_projection` / `position_projection_vector`, replacing the older trading-projection/signal framing.
- `trading-model` added `docs/07_layer_06_position_projection.md` and synchronized scope/context/Layer 5/taxonomy/decomposition/task/decision/architecture docs. Layer 6 is now target holding-state projection from final adjusted `alpha_confidence_vector` plus current/pending position, effective current exposure, position-level friction, portfolio exposure, and risk-budget context.
- Accepted Layer 6 V1 horizons: `5min`, `15min`, `60min`, `390min`. Accepted core score families: `6_target_position_bias_score_<horizon>`, `6_target_exposure_score_<horizon>`, `6_current_position_alignment_score_<horizon>`, `6_position_gap_score_<horizon>`, `6_position_gap_magnitude_score_<horizon>`, `6_expected_position_utility_score_<horizon>`, `6_cost_to_adjust_position_score_<horizon>`, `6_risk_budget_fit_score_<horizon>`, `6_position_state_stability_score_<horizon>`, and `6_projection_confidence_score_<horizon>`.
- Layer 6 invariants are explicit: target exposure is abstract normalized risk exposure, position gap is target exposure minus effective current exposure and not an execution instruction, and Layer 6 does not output buy/sell/hold/open/close/reverse, choose instruments, read option chains, choose strike/DTE/Greeks, route orders, or mutate broker/account state.
- `trading-manager` added registry migration `231_register_layer_six_position_projection_contract.sql`, regenerated `scripts/registry/current.csv`, updated model-layer naming rules, and updated registry tests for Layer 6 canonical names/fields.
- Verification passed: `trading-model` 70 tests OK + `git diff --check`; `trading-manager` 35 tests OK + registry dry-run no pending migrations + `git diff --check`; active docs/current-registry stale trading-projection/signal grep clean outside immutable migration history/test negative assertions.
- Pushed commits: `trading-model` d891265; `trading-manager` f7fee3a. Both repos clean on `main...origin/main` after push.

## Event overlay canonical dedup contract completed
- Implemented `source_04_event_overlay` canonical-event dedup fields in `trading-data`: `canonical_event_id`, `dedup_status`, `source_priority`, `coverage_reason`, and `covered_by_event_id`.
- Event IDs now use deterministic SHA-256 fallback instead of Python process-random `hash()`. Canonical rows self-reference; covered/duplicate rows must point to a canonical event. SEC/company/regulatory/official data rows outrank derivative news coverage; covered news remains evidence/attention context, not independent alpha-event count.
- Updated `trading-data` EventOverlay docs/model-input docs and SQL table DDL/backfill path for the new fields, plus tests for SEC canonical + derivative news covered behavior.
- Updated `trading-model` Layer 4 docs/decisions so `event_context_vector` consumes canonical-event identity and dedup status; duplicate news coverage cannot create duplicate event-presence/alpha-factor evidence.
- `trading-manager` added registry migrations `232_register_event_overlay_dedup_contract.sql` and `233_normalize_event_dedup_status_registry_note.sql`, regenerated `scripts/registry/current.csv`, and updated registry tests for the new identity/classification/text/config rows.
- Verification passed: `trading-data` 15 tests OK + `git diff --check`; `trading-model` 70 tests OK + `git diff --check`; `trading-manager` 35 tests OK + registry dry-run no pending migrations + `git diff --check`.
- Pushed commits: `trading-data` 259387d, `trading-model` e84297b, `trading-manager` b7c383b. All three repos clean on `main...origin/main` after push.

## Layer 7 UnderlyingActionModel design completed
- Completed accepted Layer 7 `UnderlyingActionModel` docs in `trading-model`, including new `docs/08_layer_07_underlying_action.md` and docs-spine sync across scope/context/decomposition/taxonomy/decisions/tasks/README.
- Layer 7 now owns offline direct stock/ETF `underlying_action_plan` / `underlying_action_vector`; Layer 8 owns `OptionExpressionModel`; `trading-execution` remains the broker-order/execution boundary.
- Added Layer 7 semantics to `src/models/state_vector_feature_registry.py` and tests.
- Added `trading-manager` migrations `234_register_layer_seven_underlying_action_contract.sql` and `235_restore_alpha_tradability_contract_path.sql`, regenerated `scripts/registry/current.csv`, and updated registry tests/rules.
- Verification passed: `trading-model` 70 tests OK, `trading-manager` 35 tests OK, registry dry-run no pending migrations, and `git diff --check` clean for both repos.
- Pushed commits: `trading-model` ddae4f4 and `trading-manager` c989dee.

## Layer 7 UnderlyingActionModel deterministic scaffold implemented
- Implemented local deterministic `model_07_underlying_action` package in `trading-model` with `contract.py`, `generator.py`, `evaluation.py`, package README, and tests.
- Scaffold emits offline `underlying_action_plan` / `underlying_action_vector`, accounts for pending-adjusted effective underlying exposure, hard/soft gates, planned action resolver, entry/price-path/risk-plan builders, and Layer 8 handoff fields.
- Tests cover increase-long pending exposure math, duplicate-plan prevention via pending exposure, `maintain` vs `no_trade`, bearish no-short route, short side-neutral price bounds, conservative close-then-reassess opposite exposure, hard-gate no-trade, and offline plan-quality labels.
- Verification passed: `PYTHONPATH=src python3 -m unittest discover tests` => 78 tests OK; `git diff --check` clean.
- Pushed commit: `trading-model` ce7ec1d. `trading-manager` unchanged and clean at c989dee.

## Trading-model Layer 4-6 deterministic scaffolds completed
- Implemented local deterministic scaffolds in `trading-model` for:
  - `src/models/model_04_event_overlay/` (`EventOverlayModel` -> `event_context_vector`), with point-in-time visible-event filtering, dedup discounting, target/scope relevance, impact-scope scoring, offline labels, and leakage assertions.
  - `src/models/model_05_alpha_confidence/` (`AlphaConfidenceModel` -> `alpha_confidence_vector`), with base-alpha diagnostics, event adjustment/override diagnostics, calibration/reliability/path-risk scoring, offline labels, and leakage assertions.
  - `src/models/model_06_position_projection/` (`PositionProjectionModel` -> `position_projection_vector`), with pending-adjusted effective exposure, alpha-to-target exposure projection, gap-aware adjustment cost, risk-budget compression, horizon resolution, offline utility labels, and leakage assertions.
- Updated docs/status for Layers 4/5/6 and `docs/80_task.md`; queued implementation gaps now move to Layer 8 / final decision-record work, while real point-in-time feed hardening and promotion evidence remain later production-readiness work.
- Verification passed in `trading-model`: `PYTHONPATH=src python3 -m unittest discover tests` => 88 tests OK; `git diff --check` clean.
- Pushed commit: `trading-model` `59c75b1` (`Implement deterministic layers four through six`).

## Layer 8 OptionExpressionModel deterministic scaffold completed
- Implemented `OptionExpressionModel` in `trading-model` under `src/models/model_08_option_expression/` with contract/generator/evaluation/README and fixture tests.
- Layer 8 outputs offline `option_expression_plan` and `expression_vector`; V1 expression types are `long_call`, `long_put`, and `no_option_expression`.
- Boundary locked: selected contract refs and constraints are model outputs only; no broker order type, route, time-in-force, final order quantity, broker order ids, send/cancel/replace flags, or account mutation fields.
- Added `docs/09_layer_08_option_expression.md`, updated architecture/task/vector docs, and extended `state_vector_feature_registry.py` for Layer 8 score semantics.
- Added `trading-manager` migration `236_register_layer_eight_option_expression_contract.sql` and regenerated `scripts/registry/current.csv` for `OPTION_EXPRESSION_PLAN`, `EXPRESSION_VECTOR`, Layer 8 score families, resolved fields, expression types, and boundary policy.
- Verification passed: `trading-model` 92 tests OK, `trading-manager` 35 tests OK, registry dry-run no pending migrations, and `git diff --check` clean in both repos.
- Pushed commits: `trading-model` `66e3c05`; `trading-manager` `1fbd0d9`.
- Next active model task: define final unified decision-record shape now that Layers 1-8 have accepted local deterministic scaffolds.

## Layer 8 accepted outline reconciliation completed
- Chentong objected that Layer 8 implementation started before sending the detailed outline. Acknowledged process error and treated the initial Layer 8 commit as draft until aligned with the accepted outline.
- External review accepted `OptionExpressionModel` as Layer 8: converts Layer 7 underlying path thesis into an offline option-expression plan, not alpha judgment or execution.
- Reconciled `trading-model` Layer 8 with accepted details: snapshot replay refs, pending option exposure guard, `maintain`/`no_trade` -> `no_option_expression`, stale quote/volume/OI/adjusted-contract hard filters, DTE range policy, richer selected contract payload, premium risk plan fields, expanded offline labels, and resolved fields including `8_resolved_selected_contract_ref` / `8_resolved_no_option_reason_codes`.
- Added `trading-manager` migration `237_refine_layer_eight_option_expression_accepted_outline.sql`, refreshed registry current, and registered snapshot refs, pending exposure context, DTE/delta policy, baseline ladder, and label-family config.
- Verification passed: `trading-model` 93 tests OK, `trading-manager` 35 tests OK, registry dry-run no pending migrations, and `git diff --check` clean in both repos.
- Pushed commits: `trading-model` `a8ec703`; `trading-manager` `f19c0c7`.

## Layer 8 accepted outline final tightening completed
- Finished Chentong's instruction to complete Layer 8 according to the accepted detailed outline after the draft reconciliation.
- Tightened `OptionExpressionModel` candidate handling in `trading-model`: added top-level and plan-level replay context, top-level `pending_option_exposure_context`, richer selected-contract payload fields, hard preferred-delta filter, target-range moneyness guardrail, per-candidate `hard_filter_fail_reason_codes`, and no-option reason summaries when all contracts fail hard filters.
- Updated `trading-model/docs/09_layer_08_option_expression.md` to document replay fields, candidate hard-filter diagnostics, delta hard filter, and coherent target-range moneyness guardrails.
- Added `trading-manager` migration `238_tighten_layer_eight_candidate_filter_policy.sql`; refreshed registry current and tests for candidate hard-filter diagnostics plus V1 moneyness guardrail policy.
- Verification passed: `trading-model` 94 tests OK, `trading-manager` 35 tests OK, registry dry-run no pending migrations, and `git diff --check` clean in both repos.
- Pushed commits: `trading-model` `34f8cd0`; `trading-manager` `633c2cb`.

## Trading-model stack design phase closed
- Chentong asked to close out the entire `trading-model` repository after Layer 8 completion.
- Added `/root/projects/trading-model/docs/94_model_stack_closeout.md` as the authoritative closeout receipt for Layers 1-8: accepted stack, no accepted Layer 9 inside `trading-model`, post-Layer-8 execution boundary, remaining production-hardening gaps, and verification receipt.
- Updated `docs/80_task.md`, `docs/81_decision.md`, `docs/90_system_model_architecture_rfc.md`, `docs/91_model_decomposition.md`, `docs/README.md`, and `docs/09_layer_08_option_expression.md` so the docs spine marks Layers 1-8 model design closed and removes stale implementation-pending wording.
- Verification passed: `trading-model` 94 tests OK, `git diff --check` clean, and stale grep for implementation-pending / old trading-projection tokens / layer_09 tokens returned no output.
- Pushed commit: `trading-model` `d4c064d` (`Close out model stack design phase`). Repo clean on `main...origin/main` after push.
# 2026-05-07

## Layer 4 EventOverlayModel contract/metawork completed
- Chentong asked to focus only on Layer 4 and synchronize md files plus registry metawork.
- `trading-model` now has accepted Layer 4 EventOverlayModel docs for point-in-time `event_context_vector`: inputs, `source_04_event_overlay` boundary, upstream state refs, scope/sensitivity metadata, leakage rules, EventEncoder/EventContextMatcher/EventOverlayScorer route, 21 scalar score families, dominant impact scope as model-local enum audit field, labels, baselines, and boundaries.
- `trading-data` clarified `source_04_event_overlay` as one-row-per-event overview/index, not the full `event_context_vector`; Layer 4 combines those rows with event artifacts and upstream context states in `trading-model`.
- `trading-manager` added registry migrations `225_register_event_context_vector_score_families.sql` and `226_keep_event_context_vector_values_scalar.sql`, regenerated `scripts/registry/current.csv`, and updated `state_vector_value` rules/tests so only scalar Layer 4 score families are registered; `4_event_dominant_impact_scope_<horizon>` remains model-local audit/debug/routing.
- Verification passed: `trading-model` 70 tests + `git diff --check`; `trading-data` 15 tests + `git diff --check`; `trading-manager` 35 tests + registry dry-run no pending migrations + `git diff --check`.
- Pushed commits: `trading-model` 879b070, `trading-data` 5cabfb4, `trading-manager` 3599300 and follow-up 6a87448. All three repos clean after push.

## Layer 5 AlphaConfidenceModel contract/metawork completed
- Chentong asked to close Layer 4 and start Layer 5.
- `trading-model` now has accepted Layer 5 `AlphaConfidenceModel` docs in `docs/06_layer_05_alpha_confidence.md` and synchronized docs spine/index/taxonomy/task/decision files. Layer 5 consumes `target_context_state` plus `event_context_vector` and outputs `alpha_confidence_vector` with calibrated long/short confidence, direction strength, expected return/value, downside/tail/path risk, uncertainty, context support, event adjustment, and calibration quality.
- Layer 5 boundaries are explicit: it must not emit buy/sell/hold, final action, target exposure, position size, account-risk allocation, option contract, strike, DTE, delta, order type, or broker/account mutation. Layer 6 owns trading projection/target exposure; Layer 7 owns expression/final-action boundaries.
- `trading-data` clarified that `source_05_option_expression` is source-number 05 for option-expression inputs, not model Layer 5 `AlphaConfidenceModel`; AlphaConfidence/TradingProjection do not need symmetry-only `trading-data` bundles when no new source acquisition is required.
- `trading-manager` added migration `227_register_alpha_confidence_vector_score_families.sql`, regenerated `scripts/registry/current.csv`, and updated registry rules/kind docs/tests for Layer 5 scalar `state_vector_value` score families only. Registered horizons: `5min`, `15min`, `60min`, `390min`. Registered score families: `5_alpha_direction_confidence_score_<horizon>`, `5_alpha_direction_strength_score_<horizon>`, `5_alpha_expected_return_score_<horizon>`, `5_alpha_expected_value_score_<horizon>`, `5_alpha_downside_risk_score_<horizon>`, `5_alpha_tail_risk_score_<horizon>`, `5_alpha_path_stability_score_<horizon>`, `5_alpha_uncertainty_score_<horizon>`, `5_alpha_context_support_score_<horizon>`, `5_alpha_event_adjustment_score_<horizon>`, and `5_alpha_calibration_quality_score_<horizon>`.
- Verification passed before commit: `trading-model` 70 tests + `git diff --check`; `trading-data` 15 tests + `git diff --check`; `trading-manager` 35 tests + registry dry-run no pending migrations + `git diff --check`.
- Pushed commits: `trading-model` 8aa1809, `trading-data` 5b26703, `trading-manager` 6581a3e. All three repos clean after push.

## Layer 5 adjusted alpha-confidence contract implemented
- Chentong accepted the Layer 5 base-vs-adjusted split: base/unadjusted alpha from Layer 1/2/3 is diagnostics/research/audit/event-attribution only; final adjusted `alpha_confidence_vector` is the Layer 6-facing output.
- Updated `trading-model` docs for Layer 5 and neighboring architecture/taxonomy/decision/task docs. Final adjusted V1 contract is 9 score families across `5min`, `15min`, `60min`, `390min`: direction, strength, expected residual return, confidence, signal reliability, path quality, reversal risk, drawdown risk, and alpha tradability.
- Added `trading-manager` registry migration `228_align_layer_five_base_adjusted_contract.sql`, regenerated `scripts/registry/current.csv`, updated registry naming/kind docs and registry tests to the new 9-family contract plus base diagnostic config rows.
- Verification passed: `trading-model` 70 tests OK; `trading-manager` 35 tests OK; registry dry-run no pending migrations; stale old Layer 5 token grep clean outside immutable migration history; git diff checks clean.
- Pushed commits: `trading-model` f91c29c; `trading-data` 61117b7; `trading-manager` bf624a8. Closeout spot-check found all three repos clean on `main...origin/main`; active docs/current registry scan found no blocking Layer 5 TODOs or stale pre-adjusted token references outside immutable migration history.

## Layer 6 framing clarified
- Chentong questioned whether Layer 6 was only a transition before connecting confidence to trading operations.
- Clarified accepted framing: Layer 6 should relate Layer 5 alpha confidence to current/pending position state, target exposure, position gap, costs, and risk budget — not directly to buy/sell/hold operations.
- Preferred Layer 6 output concept shifted from `trading_signal_vector` to `position_projection_vector`; Layer 7 should relate projected holding/exposure state to expression choice and final offline action.
- Layer 6 external review accepted directionally: preserve `PositionProjectionModel` / `position_projection_vector`, define `target_exposure` as abstract risk exposure, add `effective_current_exposure = current_position_exposure + pending_exposure_size * pending_order_fill_probability_estimate`, add resolved projection handoff summary, prefer candidate exposure utility curve training over single hindsight-best exposure, split position-level friction from expression-specific friction hints, and add explicit invariants preventing buy/sell/hold/instrument/option-chain leakage into Layer 6.

## Layer 6 PositionProjectionModel contract/metawork completed
- Chentong approved landing the revised Layer 6 outline as `PositionProjectionModel` / `model_06_position_projection` / `position_projection_vector`, replacing the older trading-projection/signal framing.
- `trading-model` added `docs/07_layer_06_position_projection.md` and synchronized scope/context/Layer 5/taxonomy/decomposition/task/decision/architecture docs. Layer 6 is now target holding-state projection from final adjusted `alpha_confidence_vector` plus current/pending position, effective current exposure, position-level friction, portfolio exposure, and risk-budget context.
- Accepted Layer 6 V1 horizons: `5min`, `15min`, `60min`, `390min`. Accepted core score families: `6_target_position_bias_score_<horizon>`, `6_target_exposure_score_<horizon>`, `6_current_position_alignment_score_<horizon>`, `6_position_gap_score_<horizon>`, `6_position_gap_magnitude_score_<horizon>`, `6_expected_position_utility_score_<horizon>`, `6_cost_to_adjust_position_score_<horizon>`, `6_risk_budget_fit_score_<horizon>`, `6_position_state_stability_score_<horizon>`, and `6_projection_confidence_score_<horizon>`.
- Layer 6 invariants are explicit: target exposure is abstract normalized risk exposure, position gap is target exposure minus effective current exposure and not an execution instruction, and Layer 6 does not output buy/sell/hold/open/close/reverse, choose instruments, read option chains, choose strike/DTE/Greeks, route orders, or mutate broker/account state.
- `trading-manager` added registry migration `231_register_layer_six_position_projection_contract.sql`, regenerated `scripts/registry/current.csv`, updated model-layer naming rules, and updated registry tests for Layer 6 canonical names/fields.
- Verification passed: `trading-model` 70 tests OK + `git diff --check`; `trading-manager` 35 tests OK + registry dry-run no pending migrations + `git diff --check`; active docs/current-registry stale trading-projection/signal grep clean outside immutable migration history/test negative assertions.
- Pushed commits: `trading-model` d891265; `trading-manager` f7fee3a. Both repos clean on `main...origin/main` after push.

## Event overlay canonical dedup contract completed
- Implemented `source_04_event_overlay` canonical-event dedup fields in `trading-data`: `canonical_event_id`, `dedup_status`, `source_priority`, `coverage_reason`, and `covered_by_event_id`.
- Event IDs now use deterministic SHA-256 fallback instead of Python process-random `hash()`. Canonical rows self-reference; covered/duplicate rows must point to a canonical event. SEC/company/regulatory/official data rows outrank derivative news coverage; covered news remains evidence/attention context, not independent alpha-event count.
- Updated `trading-data` EventOverlay docs/model-input docs and SQL table DDL/backfill path for the new fields, plus tests for SEC canonical + derivative news covered behavior.
- Updated `trading-model` Layer 4 docs/decisions so `event_context_vector` consumes canonical-event identity and dedup status; duplicate news coverage cannot create duplicate event-presence/alpha-factor evidence.
- `trading-manager` added registry migrations `232_register_event_overlay_dedup_contract.sql` and `233_normalize_event_dedup_status_registry_note.sql`, regenerated `scripts/registry/current.csv`, and updated registry tests for the new identity/classification/text/config rows.
- Verification passed: `trading-data` 15 tests OK + `git diff --check`; `trading-model` 70 tests OK + `git diff --check`; `trading-manager` 35 tests OK + registry dry-run no pending migrations + `git diff --check`.
- Pushed commits: `trading-data` 259387d, `trading-model` e84297b, `trading-manager` b7c383b. All three repos clean on `main...origin/main` after push.

## Layer 7 UnderlyingActionModel design completed
- Completed accepted Layer 7 `UnderlyingActionModel` docs in `trading-model`, including new `docs/08_layer_07_underlying_action.md` and docs-spine sync across scope/context/decomposition/taxonomy/decisions/tasks/README.
- Layer 7 now owns offline direct stock/ETF `underlying_action_plan` / `underlying_action_vector`; Layer 8 owns `OptionExpressionModel`; `trading-execution` remains the broker-order/execution boundary.
- Added Layer 7 semantics to `src/models/state_vector_feature_registry.py` and tests.
- Added `trading-manager` migrations `234_register_layer_seven_underlying_action_contract.sql` and `235_restore_alpha_tradability_contract_path.sql`, regenerated `scripts/registry/current.csv`, and updated registry tests/rules.
- Verification passed: `trading-model` 70 tests OK, `trading-manager` 35 tests OK, registry dry-run no pending migrations, and `git diff --check` clean for both repos.
- Pushed commits: `trading-model` ddae4f4 and `trading-manager` c989dee.

## Layer 7 UnderlyingActionModel deterministic scaffold implemented
- Implemented local deterministic `model_07_underlying_action` package in `trading-model` with `contract.py`, `generator.py`, `evaluation.py`, package README, and tests.
- Scaffold emits offline `underlying_action_plan` / `underlying_action_vector`, accounts for pending-adjusted effective underlying exposure, hard/soft gates, planned action resolver, entry/price-path/risk-plan builders, and Layer 8 handoff fields.
- Tests cover increase-long pending exposure math, duplicate-plan prevention via pending exposure, `maintain` vs `no_trade`, bearish no-short route, short side-neutral price bounds, conservative close-then-reassess opposite exposure, hard-gate no-trade, and offline plan-quality labels.
- Verification passed: `PYTHONPATH=src python3 -m unittest discover tests` => 78 tests OK; `git diff --check` clean.
- Pushed commit: `trading-model` ce7ec1d. `trading-manager` unchanged and clean at c989dee.

## Trading-model Layer 4-6 deterministic scaffolds completed
- Implemented local deterministic scaffolds in `trading-model` for:
  - `src/models/model_04_event_overlay/` (`EventOverlayModel` -> `event_context_vector`), with point-in-time visible-event filtering, dedup discounting, target/scope relevance, impact-scope scoring, offline labels, and leakage assertions.
  - `src/models/model_05_alpha_confidence/` (`AlphaConfidenceModel` -> `alpha_confidence_vector`), with base-alpha diagnostics, event adjustment/override diagnostics, calibration/reliability/path-risk scoring, offline labels, and leakage assertions.
  - `src/models/model_06_position_projection/` (`PositionProjectionModel` -> `position_projection_vector`), with pending-adjusted effective exposure, alpha-to-target exposure projection, gap-aware adjustment cost, risk-budget compression, horizon resolution, offline utility labels, and leakage assertions.
- Updated docs/status for Layers 4/5/6 and `docs/80_task.md`; queued implementation gaps now move to Layer 8 / final decision-record work, while real point-in-time feed hardening and promotion evidence remain later production-readiness work.
- Verification passed in `trading-model`: `PYTHONPATH=src python3 -m unittest discover tests` => 88 tests OK; `git diff --check` clean.
- Pushed commit: `trading-model` `59c75b1` (`Implement deterministic layers four through six`).

## Layer 8 OptionExpressionModel deterministic scaffold completed
- Implemented `OptionExpressionModel` in `trading-model` under `src/models/model_08_option_expression/` with contract/generator/evaluation/README and fixture tests.
- Layer 8 outputs offline `option_expression_plan` and `expression_vector`; V1 expression types are `long_call`, `long_put`, and `no_option_expression`.
- Boundary locked: selected contract refs and constraints are model outputs only; no broker order type, route, time-in-force, final order quantity, broker order ids, send/cancel/replace flags, or account mutation fields.
- Added `docs/09_layer_08_option_expression.md`, updated architecture/task/vector docs, and extended `state_vector_feature_registry.py` for Layer 8 score semantics.
- Added `trading-manager` migration `236_register_layer_eight_option_expression_contract.sql` and regenerated `scripts/registry/current.csv` for `OPTION_EXPRESSION_PLAN`, `EXPRESSION_VECTOR`, Layer 8 score families, resolved fields, expression types, and boundary policy.
- Verification passed: `trading-model` 92 tests OK, `trading-manager` 35 tests OK, registry dry-run no pending migrations, and `git diff --check` clean in both repos.
- Pushed commits: `trading-model` `66e3c05`; `trading-manager` `1fbd0d9`.
- Next active model task: define final unified decision-record shape now that Layers 1-8 have accepted local deterministic scaffolds.

## Layer 8 accepted outline reconciliation completed
- Chentong objected that Layer 8 implementation started before sending the detailed outline. Acknowledged process error and treated the initial Layer 8 commit as draft until aligned with the accepted outline.
- External review accepted `OptionExpressionModel` as Layer 8: converts Layer 7 underlying path thesis into an offline option-expression plan, not alpha judgment or execution.
- Reconciled `trading-model` Layer 8 with accepted details: snapshot replay refs, pending option exposure guard, `maintain`/`no_trade` -> `no_option_expression`, stale quote/volume/OI/adjusted-contract hard filters, DTE range policy, richer selected contract payload, premium risk plan fields, expanded offline labels, and resolved fields including `8_resolved_selected_contract_ref` / `8_resolved_no_option_reason_codes`.
- Added `trading-manager` migration `237_refine_layer_eight_option_expression_accepted_outline.sql`, refreshed registry current, and registered snapshot refs, pending exposure context, DTE/delta policy, baseline ladder, and label-family config.
- Verification passed: `trading-model` 93 tests OK, `trading-manager` 35 tests OK, registry dry-run no pending migrations, and `git diff --check` clean in both repos.
- Pushed commits: `trading-model` `a8ec703`; `trading-manager` `f19c0c7`.

## Layer 8 accepted outline final tightening completed
- Finished Chentong's instruction to complete Layer 8 according to the accepted detailed outline after the draft reconciliation.
- Tightened `OptionExpressionModel` candidate handling in `trading-model`: added top-level and plan-level replay context, top-level `pending_option_exposure_context`, richer selected-contract payload fields, hard preferred-delta filter, target-range moneyness guardrail, per-candidate `hard_filter_fail_reason_codes`, and no-option reason summaries when all contracts fail hard filters.
- Updated `trading-model/docs/09_layer_08_option_expression.md` to document replay fields, candidate hard-filter diagnostics, delta hard filter, and coherent target-range moneyness guardrails.
- Added `trading-manager` migration `238_tighten_layer_eight_candidate_filter_policy.sql`; refreshed registry current and tests for candidate hard-filter diagnostics plus V1 moneyness guardrail policy.
- Verification passed: `trading-model` 94 tests OK, `trading-manager` 35 tests OK, registry dry-run no pending migrations, and `git diff --check` clean in both repos.
- Pushed commits: `trading-model` `34f8cd0`; `trading-manager` `633c2cb`.

## Trading-model stack design phase closed
- Chentong asked to close out the entire `trading-model` repository after Layer 8 completion.
- Added `/root/projects/trading-model/docs/94_model_stack_closeout.md` as the authoritative closeout receipt for Layers 1-8: accepted stack, no accepted Layer 9 inside `trading-model`, post-Layer-8 execution boundary, remaining production-hardening gaps, and verification receipt.
- Updated `docs/80_task.md`, `docs/81_decision.md`, `docs/90_system_model_architecture_rfc.md`, `docs/91_model_decomposition.md`, `docs/README.md`, and `docs/09_layer_08_option_expression.md` so the docs spine marks Layers 1-8 model design closed and removes stale implementation-pending wording.
- Verification passed: `trading-model` 94 tests OK, `git diff --check` clean, and stale grep for implementation-pending / old trading-projection tokens / layer_09 tokens returned no output.
- Pushed commit: `trading-model` `d4c064d` (`Close out model stack design phase`). Repo clean on `main...origin/main` after push.

## Trading-model risk-cap and production-promotion status clarified
- Chentong asked whether every trade currently has a stop-loss / model-invalidation hard cap. Clarified that Layer 7 emits offline risk thesis fields (`stop_loss_price`, `thesis_invalidation_price`, `time_stop_minutes`, `risk_plan_reason_codes`) and Layer 8 emits long-option premium risk fields (`premium_stop_pct`, `premium_time_stop_minutes`, `planned_max_premium_at_risk_usd`, `max_loss_is_premium_paid_flag`), but these are not yet execution-enforced broker stops or mandatory hard caps.
- Recommended next decision-record / `trading-execution` invariant: any executable trade must include a `trade_risk_cap` with model invalidation price, hard stop or premium max-loss, `max_loss_usd`, `max_loss_pct`, `time_stop_at`, enforcement mode, and `cap_failure_action = reject_order`; otherwise the order must not be placed.
- Chentong asked whether per-layer promotion/calibration rules already exist. Clarified that governance rules do exist: `trading-model/docs/91_model_decomposition.md` requires the nine-part per-layer decomposition; `trading-model/docs/81_decision.md` D010/D014/D015 define model-governance artifacts, output/explainability/diagnostics split, and full promotion evidence packages; `trading-manager/scripts/registry/rules/kind-routing.md` keeps model outputs/evaluation/promotion/diagnostics model-owned unless separately registered as shared terms.
- Current promotion/calibration status: rules and evidence schema concepts exist, but Layers 1-8 have not all completed production promotion/calibration. Layer 1 has early promotion tooling; Layer 2 had a real-data review that was deferred, not approved; Layers 3-8 remain mostly fixture/local evidence. Next recommended phase is a narrow Layers 1-8 promotion-readiness checklist/status matrix, then dependency-ordered evidence completion from Layer 1 onward.

## Production readiness and trade-risk-cap contracts implemented
- Chentong said that since the route is settled, complete it together. Implemented the narrow production-hardening/control-plane slice without pretending any Layer 1-8 model is production-approved.
- `trading-model` added `docs/95_promotion_readiness.md`, `src/model_governance/promotion/readiness.py`, and `tests/test_promotion_readiness.py`; exported the helper through `model_governance.promotion`. The readiness contract requires dataset snapshot/split, labels, eval run, promotion metrics/candidate, thresholds, baseline comparison, split stability, leakage check, calibration report, and decision receipt before approval can even be considered. Current matrix: L1 evidence-gated, L2 deferred, L3-L8 evidence-pending.
- `trading-execution` added `docs/07_trade_risk_cap.md`, `src/trading_execution/risk_cap/validator.py`, and `tests/test_trade_risk_cap.py`. Any executable trade must carry a valid `trade_risk_cap`; missing/invalid caps force `reject_order`. Accepted modes: `broker_native_stop`, `risk_monitor_synthetic_stop`, `long_option_premium_defined_risk`.
- `trading-manager` added migration `239_register_promotion_readiness_and_trade_risk_cap.sql`, regenerated `scripts/registry/current.csv`, and registered shared terms/config rows for `MODEL_PROMOTION_READINESS_CHECKLIST`, `MODEL_PROMOTION_READINESS_STATUS_MATRIX`, `TRADE_RISK_CAP`, required fields, enforcement modes, and failure policy.
- Verification passed: `trading-model` 98 tests OK; `trading-execution` 11 tests OK; `trading-manager` 35 tests OK; registry dry-run no pending migrations; `git diff --check` clean in all three repos.
- Pushed commits: `trading-model` `ae81e0b`, `trading-execution` `91d518e`, `trading-manager` `872dd40`. All three repos clean on `main...origin/main` after push.
