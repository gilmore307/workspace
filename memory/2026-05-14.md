

## Runtime paused for incomplete Layer 4 event inputs
- Chentong correctly challenged that if news, earnings/SEC, macro calendar, and similar event sources are not connected into `source_04_event_overlay`, then Layer 4 and downstream model outputs are missing key inputs and must be treated as provisional/invalid.
- Immediately stopped `trading-manager-historical-scheduler.service`; service is inactive, so no further historical folds/model stages will be generated until event-source integration and invalidation/rebuild policy are fixed.
- Current SQL evidence: `trading_data.source_04_event_overlay` has 49,710 rows and only `event_category_type='equity_abnormal_activity'`; no `symbol_news`, `sector_news`, `macro_news`, `macro_data`, `sec_filing`, or earnings/filing-derived categories are present.
- Runtime impact check: 20 half-year folds exist through `2025-07_2025-12`; Layer 4-8 model-generation/evaluation/promotion/maintenance stages have 400 succeeded stage records that should be considered stale/provisional and regenerated after event source integration. Layer 1-3 market/sector/target substrate/model work is not invalidated by this event-source gap unless later source-contract changes touch their inputs.

## Runtime worker and dashboard throughput updates

- Fixed the dashboard detail layout so Evidence and Blockers share the details grid and the Blockers title stays inside its card. Pushed `trading-dashboard` commit `7d8455e`.
- Investigated whether 4 Month Ingest Workers were all active. The runtime was active, but the dashboard current-lane read model only exposed one worker because it did not recognize the later layer/month substrate lane heads. Fixed and pushed `trading-manager` commit `c8fac06`.
- Compared rough stage throughput: earlier single-thread windows were about 9.3-10.3 stage completions/min; the parallel window reached about 13.9 completions/min with bursts up to 5/sec, enough to show real concurrency but not a 4x improvement.
- Chentong decided to reduce the runtime to 3 Month Ingest Workers plus 1 Model Worker so two month-ingest rounds complete one 6-month fold group. Updated and pushed:
  - `trading-manager` `8450c65` (`Use three month ingest workers`)
  - `trading-storage` `ec0db5e` (`Add scheduler runtime throughput status`)
  - `trading-dashboard` `12664e1` (`Replace multitask threads with throughput`)
- Replaced the obsolete Current Status Multitask Threads surface with Runtime Throughput, including topology, fold cadence, completion rate, burst rate, observation window, idle/blocked count, and subordinate provider-thread detail.
- Confirmed the dashboard public URL at the time was `http://66.206.20.138:5173/`; Tailscale/local dashboard URLs included `http://100.104.174.7:5173/` and `http://100.104.174.7:5174/`.
