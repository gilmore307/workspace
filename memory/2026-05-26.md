# 2026-05-26

## Trading-manager registry cleanup and stable-id script routing

- Added and pushed `trading-manager` commit `2e73fb8 Register SQL output table inventory`: migration `679_register_sql_output_table_inventory.sql` registered canonical SQL output table names for M01/M02/M09, replay/fold/promotion evaluation, realtime/C01-C08/risk/runtime lifecycle execution, plus future-gated broker/account/position/reconciliation reserved names with no live broker/account mutation authorization. Verification passed: registry tests, migration/current checks, dry-run, compileall, and diff-check.
- After Chentong asked to review all registry entries for non-current concepts and mergeable duplicates, cleaned and pushed `trading-manager` commit `da9e340 Clean current registry concepts`: removed stale activation artifact, deprecated data config, old runtime timer, old earnings guidance scout, and old Layer 10 materializer rows; renamed `PROMOTION_REPLAY_REPLAY_WINDOW_POLICY` to `PROMOTION_REPLAY_WINDOW_POLICY`; normalized SQL table inventory naming; added shared canonical entries for `MODEL_DECISION_HORIZON_GRID`, `ABNORMAL_ACTIVITY_EVIDENCE_FAMILY_SET`, and `ACTIVITY_PRICE_RELATIONSHIP_PROOF_GATE_REQUIREMENT`; updated `current.csv`, SQL table naming rule, and registry tests. Verification passed including stale-key scans.
- After compaction, Chentong asked to scan global scripts so scripts use stable registry ids rather than body values. Followed the development startup rule, then changed `trading-manager` so scripts resolve registry values by `trading_registry.id` through new `src/trading_manager_tasks/registry_values.py`.
- Pushed `trading-manager` commit `d39ce72 Resolve script registry values by stable id`: converted direct registry body references in `scripts/docs/check_layer_tokens.py`, `scripts/tasks/rehearse_realtime_shadow_handoff.py`, `build_agent_storage_lifecycle_decision.py`, `prepare_model_worker_target_queue.py`, `materialize_layer_four_event_observation_inputs.py`, and `run_automation_scheduler_daemon.py`; added missing registry rows for MarketRegimeModel / DynamicRiskPolicyModel, M06 physical token, DynamicRiskPolicy state, and Layer 3-10 model_layer tokens; added regression tests so `scripts/tasks`, `scripts/docs`, and `scripts/contracts` fail if they hardcode registry payload/key values again.
- Verification for the stable-id script routing work passed: `PYTHONPATH=src python3 -m unittest discover tests` reported 353 tests OK; registry current/migration checks passed; registry dry-run had no pending migrations; `PYTHONPATH=src python3 scripts/docs/check_layer_tokens.py`, compileall, and diff-check passed. The `materialize_layer_four_event_observation_inputs.py` smoke returned expected `blocked`/exit 1 because local data was missing three event source classes, and its output structure was verified.

## Trading-manager registry line-by-line tightening

- Chentong asked for a direct line-by-line review of the too-long `trading-manager` active registry, with the goal of making it accurate, concise, and internally consistent rather than expecting him to inspect every row manually.
- Reviewed `scripts/registry/current.csv` and related migration/test rules for stale, generic, wrongly typed, historically descriptive, overly verbose, or inconsistent rows.
- Pushed `trading-manager` commit `5fd3064 Tighten current registry rows`: reduced `current.csv` from 1332 rows to 1324; removed generic non-trading registry rows (`CODEX`, `GITHUB`, `OPENCLAW`, `SQL`, `POSTGRESQL`, `SMB`, `TAILSCALE`, `TRADING_MANAGER_REGISTRY`); moved policy/list rows from `term` to `config`; cleaned sentence/list/equation-like payloads from `term`; normalized script runner command payloads to stable relative entrypoints; changed the manager systemd service registry row from `script` to `template`; removed active registry historical migration narration such as the 2026-04-28 registry cleanup row.
- Verification passed: `PYTHONPATH=src python3 -m unittest discover tests` reported 354 tests OK; registry snapshot/current checks passed; registry migration dry-run reported no pending migrations; compileall and `git diff --check` passed; worktree was clean after push.

## Trading-manager registry moved from migrations to current sync

- After compaction, Chentong said to cancel the registry `schema_migrations` pattern and maintain the registry like other SQL tables instead of stacking files such as `001_create_trading_registry.sql`.
- Followed the development startup rule and replaced the active registry maintenance model in `trading-manager`: current table definition now lives at `scripts/registry/sql/trading_registry.sql`, current registry data remains in `scripts/registry/current.csv`, synchronization entrypoint is `scripts/registry/sync_registry.py`, and DB/current validation entrypoint is `scripts/registry/check_registry_current_matches_db.py`.
- Removed the old active `schema_migrations` directory and `apply_registry_migrations.py`; updated docs, tests, and script references to use the current-table sync model.
- Synchronized the local registry database from `current.csv` and verified the DB snapshot matched.
- Pushed `trading-manager` commit `cc9040d Replace registry migrations with current sync`.
- Verification passed: `PYTHONPATH=src python3 -m unittest discover tests` reported 355 tests OK; registry dry-run reported 1324 rows OK; registry DB snapshot OK; docs spine, layer tokens, and contract examples checks passed; compileall and `git diff --check` passed.

## Trading-manager registry kind taxonomy cleanup

- Chentong asked whether the broad `template` kind was wrong after the registry moved to current-table sync. Reviewed the rows and confirmed the active `template` usage only covered checked-in systemd unit files while mixing policy/config semantics into `payload`.
- Pushed `trading-manager` commit `aa43108 Replace template kind with systemd units`: removed the broad `template` kind, added narrow `systemd_unit`, moved seven checked-in `.service/.timer/.path` unit rows to `systemd_unit`, kept stable ids, normalized `payload` to unit filenames, and moved cadence/operator/policy context into `note`.
- Hardened `scripts/registry/sync_registry.py` so it relaxes old enum/check constraints before importing `current.csv`, then reapplies current SQL constraints; this prevents future kind reclassification work from being blocked by stale DB constraints.
- Verification passed: `PYTHONPATH=src python3 -m unittest discover tests` reported 356 tests OK; registry DB snapshot, docs spine, layer tokens, contract examples, compileall, diff-check, and stale `template` scans passed.

## Trading-manager term dictionary boundary clarified

- Chentong asked whether the previously discussed system-wide dictionary for self-defined terms had a real shape yet, and noted that many `term` rows were not actually glossary terms. Reviewed `term` rows as a dictionary boundary problem rather than a generic registry bucket.
- Pushed `trading-manager` commit `cc6ff06 Clarify registry term dictionary boundary`: narrowed `term` to system-defined glossary concepts that require explanatory `note`, and moved 112 non-glossary stable ids into more precise kinds: 65 SQL table names to new `sql_table`, 19 statuses/reasons to `status_value`, 19 artifacts/contracts/receipts/plans to `artifact_type`, and 9 policy/config values to `config`.
- Added regression coverage so table names, state/reason values, artifact-like suffixes, and ordinary policy config rows do not drift back into `term`.
- Verification passed: `PYTHONPATH=src python3 -m unittest discover tests` reported 358 tests OK; registry DB snapshot, docs spine, layer tokens, contract examples, compileall, and `git diff --check` passed; worktree was clean after push.

## Trading-manager registry dictionary coverage expanded

- Chentong challenged `MANAGER_SCHEDULER_WORK_LOOP` and asked for another pass through Markdown-defined system terms. Re-screened active registry dictionary boundaries against current decision/scheduler/runtime docs.
- Pushed `trading-manager` commit `7f3150e Expand registry dictionary coverage`: renamed `MANAGER_SCHEDULER_WORK_LOOP` to `MANAGER_SCHEDULER_DECISION_LOOP` with stable payload `manager_scheduler_decision_loop`; migrated 34 non-dictionary rows out of `term` into `artifact_type`, `config`, or `status_value`; kept `MODEL_VECTOR_TAXONOMY` as a `term` while moving its doc path to `path`; and added 23 core Markdown-defined system concepts such as `manager_control_plane`, `current_table_synced_registry`, `reusable_foundation_catch_up`, `full_run_cycle_promotion_gate`, `execution_owned_runtime_lifecycle`, and `failure_attribution_boundary`.
- Added registry regression tests so `term` cannot hold path/file patterns or date/quarter artifact-like values, and so core system concepts documented in decision docs must be registered as `term`.
- Verification passed: `PYTHONPATH=src python3 -m unittest discover tests` reported 359 tests OK; registry DB snapshot, docs spine, layer tokens, contract examples, compileall, and `git diff --check` passed; worktree was clean.

## Promotion ownership cleanup before scheduler trial

- Chentong asked to finish remaining code tasks before any scheduler trial run. Did not start the scheduler, did not run one-shot smoke, and did not execute registry DB sync; only dry-run validation was used.
- Pushed `trading-manager` commit `8fc0c69 Fix promotion readiness registry ownership`: changed promotion request policy references away from stale `evaluation_promotion_activation_policy` to `evaluation_promotion_readiness_policy` plus `execution_runtime_model_lifecycle_policy`, and removed obsolete model-owned promotion decision/activation/rollback table registrations.
- Pushed `trading-model` commit `5d5972e Fix promotion activation ownership docs`: updated docs/source/scripts/tests so promotion readiness is evaluation-owned, runtime activation and active pointers are execution-owned, and manager remains a scheduler/request owner rather than the activation owner.
- Verification passed before stopping: `trading-manager` 359 tests OK with registry dry-run, compileall, and diff-check passing; `trading-model` 244 tests OK with compileall and diff-check passing. All `trading-*` repositories were clean. Next concrete action remains registry DB sync plus scheduler one-shot/service start only after Chentong approves the trial run.

## Registry sync and storage cleanup

- After Chentong asked whether formal operation could begin, read-only checks showed code was ready and scheduler readiness reported `service_runtime_ready: true`, but formal running was blocked because `trading-manager/scripts/registry/current.csv` no longer matched the local `trading_registry` DB after the promotion ownership cleanup.
- With Chentong's approval to sync, synchronized the local registry DB from `current.csv`: DB row count was `1344`, DB snapshot validation passed, and the synchronized `current.csv` snapshot was committed and pushed as `trading-manager` commit `bc97b8a Sync registry snapshot after DB update`. All `trading-*` repos were clean afterward. Scheduler one-shot smoke and service start were still not run yet.
- Chentong asked to clean previously accumulated data while protecting Trading Economics data because the TE subscription is gone and historical TE data cannot be reacquired. Cleaned only old dashboard read-model snapshots from `trading-storage/storage/06_dashboard_cache`, deleting `34,728` old snapshots and freeing about `9.25GB`; retained each read-model's latest 10 snapshots plus `latest.json`, schemas, index, SQL, source data, model data, and execution data.
- Added TE retention protection: pushed `trading-storage` commit `135ee92 Protect Trading Economics source retention` and `trading-manager` commit `eb1b44d Register Trading Economics retention protection`. The registry policy id is `TRADING_ECONOMICS_APPEND_ONLY_RETENTION_POLICY`; TE artifacts are classified as `keep_forever`, and existing TE payloads must not be deleted or pruned. Future handling should be append-only unless a verified restore-capable cold archive plan is explicitly built.
- Verification for the cleanup/protection work passed: `trading-storage` 118 tests OK, `trading-manager` registry tests OK, registry DB snapshot OK, and all `trading-*` repos were clean.

## Trading Economics source locations

- Confirmed TE data is Trading Economics calendar source data, not ThetaData. Protected TE footprints after cleanup were roughly: realtime `53M`, monthly backfill `8.2M`, and replay `4.0M`.
- Historical TE master copy is under `/root/projects/trading-storage/storage/01_source_data/monthly_backfill/trading_economics_calendar_web/`. It currently has month directories from `2016-01` through `2026-05` inclusive, 125 months total. Each month primarily contains `runs/.../saved/trading_economics_calendar_event.csv`, `runs/.../cleaned/trading_economics_calendar_event.jsonl`, and `completion_receipt.json`.
- Other TE copies: recent/realtime data lives at `/root/projects/trading-storage/storage/01_source_data/realtime/trading_economics_calendar_web/`; replay-fixed input windows live at `/root/projects/trading-storage/storage/01_source_data/replay/trading_economics_calendar_web/`.
- Recommended next project action recorded during the conversation: build a TE coverage inventory that lists each month's latest run, row count, and anomalous receipts before any future storage lifecycle decision touches these folders.

## Trading Economics canonical source consolidation

- Chentong clarified the desired storage boundary: event sources with original text/artifacts belong in `trading-storage`; TE is treated like event original evidence because it cannot be reliably reacquired, but it still must be materialized into the unified event SQL flow like other events.
- Consolidated active TE source artifacts so `01_source_data` now has a single active TE root: `/root/projects/trading-storage/storage/01_source_data/monthly_backfill/trading_economics_calendar_web/`.
- Merged old active TE `realtime` and `replay` source roots into the canonical TE root and preserved their originals under `_manifests/source_consolidation_20260526T074000/original_roots/` rather than deleting TE data.
- Canonical TE layout is now `YYYY-MM/runs/<run_id>/saved/trading_economics_calendar_event.csv`, with run-wide recent refresh manifests/receipts under `_manifests/recent_refresh_runs/<run_id>/`.
- Consolidation result recorded during the turn: `43,758` active TE rows across `2016-01` through `2026-06`; active CSV rows had `0` unparseable `event_time`; `201` old anomalous rows were isolated into `rejected_rows.csv`; latest event time was `2026-06-19T17:00:00-04:00`.
- Pushed `trading-storage` commit `91c18b4 Consolidate Trading Economics source layout` and `trading-data` commit `01c56a1 Store TE refresh manifests under canonical root`. Verification passed with `trading-storage` 119 tests OK, `trading-data` 72 tests OK, compileall, and diff-check.
- Manually triggered TE recent refresh once after consolidation. Run `te_recent_calendar_refresh_20260526T115151Z` succeeded with `status=0/SUCCESS`, one provider call, and `297` total rows: `84` rows into `2026-05` and `213` rows into `2026-06`. It wrote only under the canonical TE monthly root and did not write to the old `realtime` path.

## TE event SQL materialization and artifact paths

- Clarified storage/SQL boundary for downloaded data: structured market data such as bars/liquidity should primarily live in SQL after ingestion, with storage retaining manifests/receipts/audit as needed; event original evidence such as TE, SEC filings, and news/source snapshots can remain in storage while materialized event rows are written to SQL.
- `trading-data` now persists source artifact paths in `trading_data.source_10_event_risk_governor` via new `source_artifact_path`, while keeping external `reference` values stable to avoid changing event ids and duplicating existing TE events.
- Feature 10 reads now carry `source_artifact_path` forward from source event rows.
- Manager TE historical seed task policy now classifies TE artifacts as `keep_forever_append_only_source_evidence`, aligning with the TE append-only retention policy.
- Re-ran TE historical seed materialization into SQL: `source_10_event_risk_governor` wrote/updated `2929` rows, and DB check showed `2945 / 2945` TE source events had `source_artifact_path`, spanning `2016-01-06` through `2026-06-19`.
- Pushed `trading-data` commit `8312d0c Persist event source artifact paths` and `trading-manager` commit `f4cddc3 Keep TE source artifacts permanently`. Verification passed: `trading-data` 72 tests OK, `trading-manager` 359 tests OK, compileall, diff-check, and clean worktrees.

## Event calendar system and dashboard page

- Chentong asked to finish the broader event calendar system together with the previously discussed dashboard calendar page.
- Implemented `event_calendar_summary` in `trading-storage` as a dashboard read model and included it in the public dashboard refresh flow.
- Added the `trading-dashboard` Calendar page showing upcoming/recent events, Trading Economics refresh state, source mix, and calendar families.
- Registered `EVENT_CALENDAR_SUMMARY`, the Calendar dashboard page, and refresh script entries in `trading-manager`; synchronized the registry DB snapshot.
- Ran the real dashboard refresh successfully: `event_calendar_summary ready info` reported `485` events in the current window, with `353` upcoming and `132` recent.
- Restarted the dashboard service; it was running at `http://127.0.0.1:5173`.
- Pushed commits: `trading-storage` `a49f49e`, `trading-dashboard` `92b4d77`, and `trading-manager` `fe463cc`.
- Verification passed: `trading-storage` 121 tests OK plus compileall and diff-check; `trading-dashboard` 13 tests OK plus `npm run build` and diff-check; `trading-manager` 359 tests OK plus registry DB snapshot, compileall, and diff-check.

## Temporal Explorer / Timewheel correction

- Chentong clarified that the implemented simple Calendar page was not the previously discussed system. The intended UI is a Timewheel / Temporal Explorer: a vertical scroll wheel of dates/timeframes with synchronized market/session state on one side and event context on the other, plus a chart viewport and direct date/time jump.
- Replaced the simple calendar direction with a Temporal Explorer substrate. In `trading-data`, added SQL tables `calendar_day`, `calendar_market_session`, `calendar_scheduled_event`, `calendar_event_result`, `calendar_news_event_index`, and `chart_ohlcv_cache`, and installed them into PostgreSQL.
- Loaded real calendar substrate rows: `calendar_day` had `3834` rows, `calendar_market_session` had `11502` rows, and `calendar_scheduled_event` materialized `2951` rows from existing `source_10_event_risk_governor`. Result/news/chart-cache boundaries were kept explicit: no fake early-close data, no raw news text in calendar rows, and no scheduled-result leakage.
- In `trading-storage`, added `temporal_explorer_summary` as the dashboard read model and included it in the public dashboard refresh. A real refresh returned `ready` with `489` visible event markers; `chart_ohlcv_cache` remained `not_populated`, correctly signaling that OHLCV bars still need to be aggregated.
- In `trading-dashboard`, renamed the navigation/page from Calendar to Timewheel and changed the page structure to chart viewport, center vertical timewheel, left market/session lane, and right event lane. API check succeeded at `/api/read-models/temporal_explorer_summary/latest` with `temporal_explorer_summary ready 489 populated not_populated`.
- In `trading-manager`, registered `TEMPORAL_EXPLORER_SUMMARY`, `DASHBOARD_TIMEWHEEL_PAGE`, the six SQL tables, install/refresh scripts, and dictionary terms `TEMPORAL_EXPLORER` / `TIMEWHEEL`; registry DB synchronization/checks passed.
- Verification passed: `trading-data` 76 tests OK, `trading-storage` 123 tests OK, `trading-dashboard` 14 tests OK plus `npm run build`, and `trading-manager` 359 tests OK. Dashboard service was restarted and active.
- Pushed commits: `trading-data` `c089ad6`, `trading-storage` `d18f137`, `trading-dashboard` `1e6b6e7`, and `trading-manager` `e84f462`.
- Next concrete project action: aggregate existing bars into `chart_ohlcv_cache` so the Timewheel chart viewport can move from `not_populated` to real OHLCV visualization.

## Timewheel chart-axis refinement

- Chentong clarified the Timewheel should become the chart x-axis rather than a separate space-consuming wheel, lower subcharts such as volume should sit under the chart, and Market State should live on the Status page.
- Updated `trading-dashboard`: Status is now the owner-facing page name, Temporal Explorer uses an integrated chart/time axis with event markers on the axis, volume and event-density subcharts, a compact event/status strip, and updated docs.
- Updated `trading-storage`: `temporal_explorer_summary` now emits no left lanes, removes `market_state` from Timewheel lanes, moves market session/replay/event lanes into the event/status lane set, and labels the status read model as `Status Read Model`.
- Updated `trading-data` Temporal Explorer docs and `trading-manager` registry rows to match the current contract.
- Refreshed public dashboard read models and restarted `trading-dashboard-web.service`; runtime check returned `temporal_explorer_summary ready` with `489` events, `0` left lanes, and `6` right/event-status lanes.
- Verification passed: `trading-dashboard` build + 14 tests OK, `trading-storage` 123 tests OK, `trading-manager` registry snapshot + 359 tests OK, and `trading-data` diff-check OK.
- Pushed commits: `trading-dashboard` `da87291`, `trading-storage` `6a43fa1`, `trading-data` `0ee4997`, and `trading-manager` `a866f30`.

## Temporal Explorer lane population

- Chentong asked whether all Timewheel lanes could be completed after seeing Event Results/News Index empty and Model Event Markers/Replay State not connected.
- Implemented local, no-provider/no-broker/no-model-activation population for available data:
  - `trading-data` installer now materializes accepted release-result payloads from `source_10_event_risk_governor` into `calendar_event_result`.
  - `trading-data` installer now materializes accepted Alpaca/GDELT source10 rows into `calendar_news_event_index` as index metadata only, not full raw news bodies.
  - `trading-storage` Timewheel read model now connects model-marker lane to execution runtime active-model readiness and replay lane to `storage/05_replay_datasets`, returning `empty` instead of `not_connected` when connected sources have no rows.
- Ran the installer: `calendar_event_result` received `2199` source10 rows and `calendar_news_event_index` received `5569` source10 rows.
- Refreshed `temporal_explorer_summary`: status `ready`; visible current viewport counts were `scheduled_event=443`, `event_result=57`; lanes were Market Session populated, Scheduled Events populated, Event Results populated, News Index populated with no rows in current viewport, Model Event Markers empty, Replay State empty.
- Verification passed: `trading-data` 76 tests OK + compileall, `trading-storage` 123 tests OK + compileall, `trading-manager` registry snapshot + 359 tests OK, and `trading-dashboard` build + 14 tests OK.
- Pushed commits: `trading-data` `db33e7b`, `trading-storage` `ac5add2`, `trading-manager` `7d93c37`, and `trading-dashboard` `8a8bbb1`.

## Chart cache policy clarification

- After the Timewheel lanes were populated, the dashboard still showed `Visualization Cache Not Training Truth`, `Chart cache not populated yet`, and `Waiting for chart cache bars` because `trading_data.chart_ohlcv_cache` had no OHLCV rows yet; event markers were already using the same time axis independently of chart bars.
- Chentong asked whether chart data should be long-lived or generated/deleted on demand. Recommended policy: keep a long-lived chart-cache pyramid for core market instruments/timeframes, while keeping one-off fine-grained debug/replay windows temporary.
- Chentong clarified that `1min` data should itself be permanently saved. The agreed data flow is: provider/API -> normalized SQL `1min` source data -> aggregated `chart_ohlcv_cache` pyramid -> dashboard.
- Durable principle: `1min` normalized market/source data is the reusable lowest standard granularity and should be retained for core market/ETF/sector data; `10min / 30min / 1h / 1D / 1W` chart bars are derived from `1min`. The chart cache remains labeled visualization-only and must not become the model training or replay truth source.
- Next concrete project action: implement and run a chart-cache builder to aggregate existing SQL `1min` bars into `chart_ohlcv_cache` for core ETFs such as `SPY`, `QQQ`, `IWM`, `DIA`, and sector ETFs.

## Timewheel interactive controls and accepted-marker boundary

- Chentong clarified the Timewheel dashboard should show substrate statuses as a card above Temporal Chart, make Timeline Status calendar/date-selectable, add symbol selection to Temporal Chart, make frame buttons and the x-axis interactive, show Event Markers only for the selected time unit, and restrict markers to Layer 10 accepted event kinds.
- Updated `trading-storage` so `temporal_explorer_summary` no longer treats ordinary scheduled events, macro result rows, or news-index rows as chart markers. The marker list now emits only rows carrying accepted Layer 10/review status; current runtime correctly returns `0` visible markers because no model-generated accepted event families exist yet.
- Updated `trading-dashboard` Timewheel UI: substrate status cards are above the chart; Timeline Status is a selectable date/tick grid; Temporal Chart has symbol, frame, center-time, prev/next, wheel-scroll, and clickable x-axis controls; Event Markers displays only the selected frame unit with detailed fields when accepted events later exist.
- Updated docs and registry descriptions across `trading-dashboard`, `trading-storage`, `trading-data`, and `trading-manager`; refreshed `temporal_explorer_summary` and restarted `trading-dashboard-web.service`.
- Verification passed: `trading-storage` 123 tests OK + compileall + diff-check, `trading-dashboard` build + 14 tests OK + diff-check, `trading-data` compileall + diff-check, `trading-manager` registry dry-run/sync/check + 359 tests OK + diff-check. API check showed `events=0`, status lanes populated/empty as expected, and chart symbols `DIA/IWM/QQQ/SPY`.
- Pushed commits: `trading-storage` `3ca6ee3`, `trading-dashboard` `388ab05`, `trading-data` `e83a68e`, and `trading-manager` `221cbbf`.

## Dashboard Timewheel cleanup and Data catalog label correction

- Fixed `trading-dashboard` compact calendar clipping after Chentong reported the Timewheel date grid was too tall/cut off. Date cells no longer render empty placeholder rows, remain compact around `40px`, and status/event text is combined onto one conditional line. Verification passed: `npm run build`, `npm test` 14 tests OK, `git diff --check`, and headless Chrome confirmed 42 date cells with no overflow. Pushed `trading-dashboard` commit `2c4a363 Fix compact calendar clipping`.
- Chentong noticed Data still showed old labels such as `trading_model.model_01_market_regime`. Fixed dashboard Data catalog display labels so user-facing source/feature/model names use canonical `mNN_<domain>_<stage>` style, while internal queries still point at legacy physical SQL table names until new `mNN_*` physical tables exist. Verification passed: `npm test` 15 tests OK, `npm run build`, `git diff --check`, API returned `trading_model.m01_market_regime_model_generation`, and dashboard service was active. Pushed `trading-dashboard` commit `85cc44d Use canonical Data table labels`.
- Chentong observed Temporal Chart Center already acts as the calendar, so `Timeline Status` was redundant. Removed the `Timeline Status` month calendar panel from Timewheel, kept the Temporal Chart center controls, symbol/frame controls, x-axis, and event details, and updated docs. Verification passed: `npm test` 15 tests OK, `npm run build`, `git diff --check`, headless Chrome confirmed no `Timeline Status` text and `Temporal Chart` still present. Pushed `trading-dashboard` commit `86a9ef8 Remove Timewheel timeline status calendar`.

## Non-TE old data deletion and TE source boundary clarification

- Chentong asked to delete old non-TE data because existing bars/frame data were not uniformly `1min`. After an explicit confirmation phrase, OpenClaw permanently deleted the scoped non-TE old data from SQL and storage. SQL results included `trading_data.source_01_market_regime = 0`, `trading_data.source_03_target_state = 0`, `calendar_news_event_index = 0`, feature/model/cache tables cleared, and `source_10_event_risk_governor` left with only TE rows at that moment. Storage deletion removed non-TE old source artifacts such as Alpaca bars/liquidity/news, GDELT, OKX, SEC financials, target/source runtime artifacts, and non-TE event evidence, about `1.28GB`. Dashboard read models were refreshed and the dashboard service restarted active. Deletion manifests were written under `/root/projects/trading-storage/storage/90_lifecycle/runtime/non_te_old_data_delete/`.
- Scope correction: Chentong clarified “only keep TE” meant the retained canonical source should be the TE source data in `trading-storage`, specifically data scraped from the Trading Economics website, not SQL/materialized TE derivatives. OpenClaw initially interpreted the confirmed list broadly and deleted SQL old bars/non-TE rows too; Chentong then clarified that deleting SQL was acceptable because macro data should now source from storage TE data rather than the TE website or SQL as source. Durable lesson: for deletion requests involving “source data,” distinguish canonical source artifacts, derived SQL materializations, runtime manifests, and dashboard/read-model/cache layers before executing, even after a confirmation phrase.
- Current TE source boundary as of the latest turn: the intended single canonical TE source is `/root/projects/trading-storage/storage/01_source_data/monthly_backfill/trading_economics_calendar_web/`.
- `trading_data.source_10_event_risk_governor` remaining TE rows are derived SQL materialized/index rows, not the unique source, and can be deleted if Chentong confirms. They are restorable from the monthly TE CSV route/materialization flow.
- `storage/01_source_data/runtime/source_10_event_risk_governor` is not TE original source data; it contains historical seed runtime receipts/manifests and can be deleted if Chentong wants a single TE source root.
- `storage/02_control_plane/runtime/te_calendar/historical_seed/filtered_artifacts` is another derived/filtered TE copy, not the canonical source, and should be removed if enforcing a single TE source.
- Open decision waiting for Chentong after compaction: confirm whether to delete all three derived TE ranges and refresh dashboard:
  - storage runtime: `storage/01_source_data/runtime/source_10_event_risk_governor`
  - storage filtered/control-plane TE copy: `storage/02_control_plane/runtime/te_calendar`
  - SQL TE materializations: `trading_data.source_10_event_risk_governor` TE rows, `trading_data.calendar_scheduled_event` TE rows, and `trading_data.calendar_event_result` TE rows
- Risk noted before deletion: dashboard Temporal Explorer currently reads SQL calendar tables, so deleting SQL TE materializations will make event calendar lanes temporarily empty until dashboard/read-model refresh logic is adjusted to read or rematerialize from the canonical TE monthly source.
- Chentong confirmed the deletion and clarified this is the correct state: macro TE data is not yet part of the Layer 10 attention/event-risk pool, so it should not appear in Temporal Explorer. OpenClaw permanently deleted the derived SQL TE rows and derived storage/runtime copies, refreshed dashboard read models, and recorded the durable boundary that the only canonical TE source-data home is `/root/projects/trading-storage/storage/01_source_data/monthly_backfill/trading_economics_calendar_web/`.

## Formal trading-system pre-operation Codex review

- Chentong asked for a final formal pre-operation review across all trading repositories using Codex CLI review mode.
- Reviewed maintained files across `trading-dashboard`, `trading-data`, `trading-evaluation`, `trading-execution`, `trading-manager`, `trading-model`, and `trading-storage`; review outputs were saved under `/tmp/trading_formal_review_<repo>.md`.
- Formal-operation conclusion: do not enter production/formal operation yet. Blocker/high findings exist in `trading-storage`, `trading-manager`, `trading-data`, `trading-model`, `trading-execution`, and `trading-evaluation`; `trading-dashboard` had only a medium manual-refresh drift.
- Main blocker classes: storage lifecycle retention can delete reviewed archives and unresolved alert snapshots; manager fatal-error handoff and error-repair permissions are unsafe; data/model contracts drift between documented `mNN_*` contracts and legacy physical tables; model dry-run/cleanup scripts can mutate/drop too broadly; execution can report ready despite missing Layer 4 realtime source coverage and can emit broker-submission approval while broker execution is globally closed; evaluation replay/promotion can accept failed or under-specified evidence.
- Codex CLI read-only sandbox prevented some unittest/build gates that need temporary/write paths, but the execution repo tests passed in sandbox with `/dev/shm`, and several findings were confirmed by direct static/probe evidence rather than failed tests alone.

## Formal review blocker repair batch completed

- Repaired and pushed the blocker/high findings from the formal pre-operation review across the trading repositories.
- `trading-data`: removed old SQL event/calendar dependencies from Temporal Explorer and corrected default data/feature labels toward canonical `m01/m02/m03/m09/m10_*` naming. Deleted creation/reads for `calendar_scheduled_event`, `calendar_event_result`, `calendar_news_event_index`, and `source_10_event_risk_governor` in that path. Commit pushed: `9d62d0b Remove event SQL from temporal explorer`.
- `trading-storage`: hardened lifecycle/public-dashboard behavior so local archives are no longer deleted, unresolved `issue_refs` prevent dashboard snapshot deletion, and public dashboard refresh defaults to failure on partial refresh unless `--allow-partial-success` is explicit. Commit pushed: `f348b55 Harden storage lifecycle and dashboard refresh`.
- Storage follow-up decision remains open: `.gitignore` now exposes `/storage/`, leaving about `22,643` untracked storage files and roughly `308M` data visible as `?? storage/`. These were intentionally not committed or pushed because they are externalized raw/cache data and need a storage inventory plus sensitive-data scan before any GitHub publication decision.
- `trading-manager`: fixed scheduler daemon and stage executor truthy parsing for `MANAGER_AGENT_ERROR_AUTOCALL`; constrained automatic repair handoff to use the `$server-error-repair` skill boundary rather than broader agent permissions. Commit pushed: `9996f1d Fix scheduler error handoff truthy flags`.
- `trading-execution`: added Alpaca paper broker boundary with paper-only route and no live-money broker mutation; confirmed Alpaca paper endpoint/credentials are separate from live trading; realtime feature readiness now rejects placeholder context refs unless fixture/shadow paths explicitly allow them. Commit pushed: `59b3e56 Add paper broker and context readiness gates`.
- `trading-evaluation`: required replay simulated fills to pass the execution gate, stopped promotion bootstrap from bypassing gate failure, and tightened frozen replay dataset validation. Commit pushed: `e8493f6 Require execution gates in replay evaluation`.
- `trading-model`: made model DB cleanup dry-runs reject non-`trading_model` schemas, prevented target substrate review dry-runs from writing DB state, and made price anomaly discovery tests independent of real storage bars. Commit pushed: `aa1e96c Harden model governance and discovery guards`.
- `trading-dashboard`: manual refresh now refreshes the Temporal Explorer read model. Commit pushed: `b60394f Refresh temporal explorer read model manually`.
- Verification completed after the repair batch: storage `125 tests OK`, manager `359 tests OK`, execution `104 tests OK`, evaluation `38 tests OK`, data `77 tests OK`, model `245 tests OK`, dashboard `15 tests OK`, dashboard `npm run build` OK, manager registry dry-run OK, and relevant repos passed `compileall` / `git diff --check`.
- Recommended next action after compaction: inspect `trading-storage/storage/` with a data inventory and sensitive-data scan, then decide whether any of the newly visible storage artifacts should be tracked, archived elsewhere, or ignored.

## Canonical TE source data tracked

- After the formal repair batch, Chentong told OpenClaw to proceed with the storage follow-up. OpenClaw limited Git publication to the one canonical Trading Economics source-data home: `/root/projects/trading-storage/storage/01_source_data/monthly_backfill/trading_economics_calendar_web/`.
- `trading-storage` now tracks `2,402` canonical TE source files from that path only. Excluded generated/runtime/cache/storage areas remain out of Git, including `_manifests/`, `runtime/`, `realtime/`, `replay/`, `source_outputs/`, `02_control_plane/`, `03_model_artifacts/`, `04_execution_artifacts/`, `06_dashboard_cache/`, and `90_lifecycle/`.
- Added `.gitattributes` in `trading-storage` so TE CSV files are stored as original source data and Git does not normalize CRLF or CSV empty trailing fields in a way that changes source bytes.
- High-confidence sensitive-value scan of the tracked TE source set did not find API keys, tokens, passwords, private keys, DSNs, or similar secret material.
- Pushed `trading-storage` commit `5a6a3e4` (`Track canonical Trading Economics source data`) to `origin/main`.

## TE canonical source cleanup and website route retirement

- Completed the final TE source-boundary cleanup after tracking the canonical Trading Economics source-data root.
- Deleted `source_url` / `tradingeconomics.com` fields from canonical TE storage data so tracked source files no longer retain live website URLs.
- Retired the TE website/live-fetch path: removed the systemd unit, changed data pipeline behavior so it parses existing storage source data only, and hardened the automatic repair skill boundary so future server-error repair does not restore website scraping for TE.
- Removed `event_calendar_summary` and `Timeline Status` from storage, dashboard, and manager registry surfaces. The `event_calendar_summary` API now returns `404`; `temporal_explorer_summary` remains `ready`.
- Confirmed Layer 10 / Temporal Explorer no longer reads SQL TE macro-calendar tables. TE macro data is storage-only source material until Layer 10 explicitly promotes relevant events into the event-risk/attention pool.
- Durable TE source boundary after cleanup: the only canonical TE source-data home is `/root/projects/trading-storage/storage/01_source_data/monthly_backfill/trading_economics_calendar_web/`. SQL tables, runtime/control-plane copies, dashboard read models, and explorer caches are derived or retired materializations, not TE source of truth.
- Verification passed: TE storage source scan found no `source_url` / `tradingeconomics.com`; full-repo scan found no active `event_calendar_summary` / `Timeline Status`; relevant tests/builds/registry sync/diff-checks passed across touched repos.
- Pushed commits: `trading-storage` `68f0d63`, `trading-data` `22f5fdf`, `trading-manager` `b3ba3df`, `trading-dashboard` `9a1cbe7`, `trading-model` `8338f30`, `trading-execution` `d777fe0`, and workspace `27ae75f`.
- `trading-manager-historical-scheduler.service` was intentionally left `inactive`; starting it would resume formal historical scheduling.

## TE recent/future refresh restored and formal scheduler started

- Correction after Chentong challenged the phrase "no longer access TE website": the intended boundary is not that TE acquisition is retired. Trading Economics remains the acquisition source for recent/future macro-calendar data and release schedules, but old per-row website URLs are not durable source references.
- Restored bounded TE recent/future calendar refresh while preserving the source boundary: live TE fetch writes only to canonical storage at `/root/projects/trading-storage/storage/01_source_data/monthly_backfill/trading_economics_calendar_web/`, does not persist `source_url` or `tradingeconomics.com` URLs, and does not write SQL Layer10/event-calendar tables.
- Reinstated `trading-data-te-calendar-refresh.service` and `.timer`; enabled/started the timer. A manual service run succeeded with run_id `te_recent_calendar_refresh_20260526T191155Z`, row_count `298`, and refreshed `2026-05`/`2026-06` TE source buckets. Dashboard reported `Trading Economics Calendar Source available true`.
- Pushed TE refresh restoration commits: `trading-data` `e76c4f9` (`Restore bounded TE recent calendar refresh`), `trading-manager` `82c7103` (`Restore TE recent refresh planning`), `trading-storage` `d315a50` (`Show active TE calendar source refresh`), and later `trading-storage` `8bcd060` (`Record TE calendar refresh service run`).
- Durable Layer10 meaning clarified with Chentong: TE rows are raw/source facts. Replay/evaluation should first discover model fragility around event windows; then Layer10 should promote selected event classes such as CPI/FOMC/NFP into the observation/attention pool via interpretation and evidence, rather than admitting raw TE calendar rows automatically.
- Chentong clarified that "正式开跑" includes model services. OpenClaw started formal historical/model scheduling: `trading-manager-historical-scheduler.service` is now `active + enabled`. Recent observed decision was `decision_status=executed`, `reason_code=autonomous_provider_stage_executed`, selected work `layer_01_market_regime.data_acquisition`, worker month `2016-04`, and `provider_calls=4`.
- Safety state at startup: `broker_execution_performed=false`, `model_activation_performed=false`, and `storage_lifecycle_mutation_performed=false`. OpenClaw did not enable broker live orders or model activation.
- Two startup blockers were repaired before formal scheduling could run: the systemd `PYTHONPATH` was missing `trading-data/src`, causing `data_feed` import failure; and manager dynamic import/provider pipeline loading was unsafe under concurrent execution, causing intermittent `build_context` / `NoneType.__dict__` errors.
- Verification for the scheduler fix passed with relevant `trading-manager` tests (`43` selected tests OK) and `git diff --check`; six trading repos were clean at that point. Pushed `trading-manager` commit `ae24b80` (`Fix historical scheduler provider imports`).

## Realtime trading disabled until a promoted active model config exists

- Chentong clarified that before any usable promoted model group exists, realtime trading should be stopped because there is no runnable model.
- Stopped and disabled realtime execution units:
  - `trading-execution-realtime-monitor-loop.service`
  - `trading-execution-realtime-runtime-check.path`
  - `trading-execution-realtime-runtime-check.service`
  - `trading-execution-realtime-runtime-check.timer`
- Historical/modeling scheduler, TE calendar refresh timer, and dashboard read-model refresh timer remained active/enabled.
- Chentong then asked to make the rule durable: live/realtime trading may be enabled only when a usable promoted active model config exists.
- `trading-execution` now enforces this with `run_realtime_trading_runtime_check.py --require-active-model-config`; absence of an active model config exits `2` with `waiting_for_promoted_model`.
- `trading-execution-realtime-monitor-loop.service` now has an `ExecStartPre` gate, so even manual service enabling fails closed without an active model config.
- Verification passed: `tests.test_realtime_trading_runtime` + `tests.test_model_lifecycle` (`18` tests OK), CLI smoke for no-active-model returned exit `2`, systemd unit sync/daemon-reload completed, realtime units stayed inactive/disabled, and historical scheduler stayed active/enabled.
- Pushed `trading-execution` commit `1336c91` (`Gate realtime trading on active model config`).

## TASK-C7651B95 / ERR-000001 automatic repair gap closed

- Chentong asked why `TASK-C7651B95` was not automatically repaired and why Discord showed a different error number (`ERR-000001`).
- Root cause: `erragent_e3390e96ee0df733` had been generated with the `server-error-diagnosis` skill and allowed only safe dry-run / non-destructive checks. Codex diagnosed and patched the Layer 2 sector context feature generation `NameError` caused by missing `OUTPUT_TABLE` import, but did not complete the repair loop: no commit/push, no rerun of the original `--write` stage, and no workflow-state transition from `failed` to `succeeded`.
- Closed the repair manually:
  - `trading-data` fixed the sector context feed artifact source table issue and pushed commit `0227c1e` (`Fix sector context feed artifact source table`).
  - `trading-manager` changed automatic server-error agent requests to use the repair skill boundary and pushed commit `8dbd2c2` (`Use repair skill for server error agent requests`).
  - `trading-manager` clarified Discord alert identifier labels and pushed commit `a2b7333` (`Clarify server error alert identifiers`).
- Reran the original `2016-02` `layer_02_sector_context.feature_generation` write stage successfully; result included `feature_rows_written: 4480`, `broker_execution_performed: false`, and `model_activation_performed: false`.
- Updated the workflow state to `succeeded`, refreshed the dashboard read model, and restarted `trading-manager-historical-scheduler.service`; scheduler returned active.
- Identifier clarification: `ERR-000001` is the trading-manager local error catalog number; `TASK-C7651B95` is an outer OpenClaw/Discord task identifier not found in local trading artifacts. Future Discord alerts should display `Manager Error No: ERR-...` and `Agent Request: erragent_...` to avoid conflating manager error IDs with outer task IDs.
- Verification passed: `trading-manager` agent error tests (`11` OK), `trading-data` sector context tests (`7` OK), `py_compile`, `git diff --check`, and scheduler active check.

## Server-error repair closure rule hardened

- Chentong rejected half-finished automatic server-error repair: a local patch plus dry-run is not meaningful if the original failed operation remains failed.
- Durable operating rule recorded for future server-error agents: `repaired_verified` requires code/config fix commit+push when a maintained-code change is made, rerun or formally reconcile the original failed operation, runtime/error/read-model state closed back to success, and success evidence from successful receipt/log/state rather than the original failed receipt.
- Hardened both OpenClaw and Codex CLI `server-error-repair` skill copies so automatic repair agents use the same closure definition.
- Recorded Chentong's preference in `USER.md`: server-error agents should repair to closure when safely possible, including commit/push, original-operation rerun/reconcile, and runtime/error/read-model update; a local patch plus dry-run is incomplete unless the remaining retry is explicitly blocked outside the agent boundary.
- `trading-manager` automatic server-error requests already route to `server-error-repair` rather than the narrower diagnosis-only skill.

## Incomplete-month historical task guard and dashboard wording

- Chentong challenged a dashboard card showing `Feature Generation / 2025-06 / Layer 1 / Now` because historical-data tasks must never analyze the current in-progress calendar month.
- Investigation showed the card was for `2025-06`, which is a completed historical month, not `2026-06`; however the dashboard label `Now` was misleading and made the task look like current-month analysis.
- Confirmed no `2026-05` or `2026-06` workflow state was present at that point, and scheduler code used an America/New_York month cutoff allowing only fully completed months.
- Stopped `trading-manager-historical-scheduler.service` and intentionally left it inactive pending Chentong confirmation.
- Added/verified guard tests so Month Ingest Worker selection skips incomplete months for historical/modeling work.
- Dashboard wording changed from `Now` to `Active` / `Active historical period`; dashboard read model refreshed and service restarted.
- Pushed commits: `trading-manager` `416d025` (`Guard month ingest workers from incomplete months`) and `trading-dashboard` `ec01884` (`Clarify active historical task labels`).
