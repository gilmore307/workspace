
## 2026-05-05 Pre-compaction flush — V2.2 legacy Layer 3 cleanup completed

- User asked to delete obsolete legacy surfaces, including original Layer 3, and asked whether the prior promotion rules can continue.
- Completed cross-repo cleanup and pushed commits:
  - `/root/projects/trading-model` commit `4ba3b33`: deleted legacy `src/models/model_03_strategy_selection/` and `tests/test_strategy_selection_families.py`; active docs/contracts/tests now center Layer 3 on `TargetStateVectorModel` / `target_state_vector` and remove obsolete `StrategySelection` / `TradeQuality` active peer wording.
  - `/root/projects/trading-data` commit `e332a23`: deleted legacy `src/data_feature/feature_03_strategy_selection/`, `src/data_source/source_03_strategy_selection/`, `scripts/generate_feature_03_strategy_selection.py`, and old strategy-selection feature test; active Layer 3 data boundary is `source_03_target_state` + `feature_03_target_state_vector`.
  - `/root/projects/trading-manager` commit `3d132f4`: added registry migration `199_retire_obsolete_layer_three_strategy_selection.sql`; removed active registry rows `FEATURE_03_STRATEGY_SELECTION`, `SOURCE_03_STRATEGY_SELECTION`, `FEATURE_03_STRATEGY_SELECTION_GENERATE`, and `STRATEGY_SELECTION_FEATURE_REQUEST`; removed `source_03_strategy_selection` from active field applies_to references; refreshed `scripts/registry/current.csv`.
- Verification passed:
  - `trading-model`: `PYTHONPATH=src python3 -m unittest discover tests` => 65 tests OK.
  - `trading-data`: `PYTHONPATH=src python3 -m unittest discover tests` => 12 tests OK.
  - `trading-manager`: `PYTHONPATH=src python3 -m unittest discover tests` => 35 tests OK.
  - `trading-manager`: `python3 scripts/registry/apply_registry_migrations.py --dry-run` => no pending migrations.
  - Active docs/src/tests/registry current/rules grep for obsolete Layer 3 terms returned no output after excluding immutable migration-history SQL.
- Promotion decision communicated to user: keep the previous promotion governance/evidence framework (`model_dataset_snapshot`, `model_dataset_split`, `model_eval_label`, `model_eval_run`, `model_promotion_metric`, `model_promotion_candidate`, agent review/defer-or-approve, thresholds/baselines/stability/no-leakage), but update the layer-specific promotion gates to V2.2 semantics. Layer 3 promotion now evaluates `TargetStateVectorModel` / `target_state_vector` against market-only and market+sector baselines, identity-safe anonymous target vectors, direction-neutral tradability/path outcomes, and must not promote old strategy-selection, trade-quality, alpha confidence, position sizing, final action, option contract, or strategy/action variant behavior.

## Layer 1 V2.2 cleanup completed
- Split `trading-storage/main/shared/market_regime_etf_universe.csv` and `market_regime_relative_strength_combinations.csv` with authoritative `model_layer` values `layer_01_market_regime` / `layer_02_sector_context`.
- `trading-data` now filters Layer 1 feature generation and Layer 2 sector-context generation by `model_layer`; holdings source uses `layer_02_sector_context` for ETF holdings candidate preparation.
- `trading-model` finalized Layer 1 V2.2 `market_context_state` public outputs and retired old `1_*_factor`/`1_transition_pressure` fields from active downstream contract; old factor names remain internal signal groups/evidence sources.
- `trading-manager` added migrations `200_finalize_layer_one_v22_scope_and_outputs.sql`, `201_update_market_context_state_v22_note.sql`, and `202_update_layer_one_support_artifact_notes.sql`; regenerated `scripts/registry/current.csv`.
- Verification passed: `trading-storage` compileall/diff-check; `trading-data` 12 tests; `trading-model` 65 tests plus Layer 1 script help/evaluation dry-run; `trading-manager` 35 tests plus registry dry-run no pending migrations.
- Pushed commits: `trading-storage` 515425f, `trading-data` 6e45754, `trading-model` b6341fe, `trading-manager` 995c241.

## V2.2 target semantics stale active references cleaned
- User asked whether the repos had been changed to V2.2 target semantics.
- Verification confirmed core Layer 3 active semantics are `TargetStateVectorModel` / `target_state_vector`, with obsolete strategy-selection surfaces removed from active registry/code/tests except immutable migration/decision history and valid downstream-exclusion prose.
- Found and cleaned two stale active references:
  - `trading-data` removed retired `source_03_strategy_selection` and `feature_03_strategy_selection` console scripts from `pyproject.toml`; commit `368e1ed` pushed.
  - `trading-model` removed deleted `src/models/model_03_strategy_selection/` from README and marked Layer 3 as the TargetStateVector contract/workspace; commit `d5c56f1` pushed.
- Verification passed: `trading-data` 12 tests OK, pyproject script scan OK, `trading-model` 65 tests OK, active grep for `model_03_strategy_selection` / `source_03_strategy_selection` / `feature_03_strategy_selection` / `StrategySelection` returned no output after excluding immutable migration SQL.

## Layer 2 V2.2 direction-neutral cleanup completed
- User asked to clear Layer 2 todos and update registry + markdown files.
- `trading-model` finalized active `SectorContextModel` V2.2 direction-neutral implementation: generator now emits signed `2_sector_relative_direction_score`, direction-neutral trend/tradability/transition-risk scores, separate `2_sector_handoff_state` and `2_sector_handoff_bias`, coverage/data-quality/state-quality fields, and no active `2_selection_readiness_score` / `2_trend_certainty_score` output. Evaluation now uses signed-label handling only for direction/support factors, absolute labels for direction-neutral quality/tradability, and long/short bias handoff alignment metrics.
- Fresh database generation succeeded: `generate_model_02_sector_context.py` generated 3250 primary rows plus explainability/diagnostics into `trading_model.model_02_sector_context*`.
- Real database evaluation/review was run and persisted as deferred: handoff gates and leakage gates passed, but production promotion remains blocked by baseline/stability gates (`minimum_baseline_improvement_abs=-0.2988`, `maximum_stability_correlation_range=1.5602`, `minimum_stability_sign_consistency=0.3333`). Persisted review decision `mpdec_debd0cfb74825b84` for candidate `mpcand_a8c01abbeed14816`; no activation was made.
- `trading-manager` added registry migration `205_finalize_layer_two_v22_direction_neutral_contract.sql`, registering Layer 2 V2.2 shared fields, handoff state/bias values, and direction-neutral promotion threshold config; regenerated `scripts/registry/current.csv`.
- Docs/tasks updated across `trading-model`, `trading-manager`, `trading-data`, and `trading-storage` to mark Layer 2 boundary alignment complete and record the deferred promotion blocker honestly.
- Verification passed: diff-check for modified repos; `trading-model` 65 tests OK plus compileall/evaluation dry-run; `trading-manager` 35 tests OK plus registry dry-run no pending migrations; `trading-data` 12 tests OK. Pushed commits: `trading-model` 780cd9b, `trading-manager` 99eca39, `trading-data` 66f0a2a, `trading-storage` 458815c.

## Layer 2 V2.2 direction-neutral cleanup completed
- User asked to clear all Layer 2 TODOs and remember to update registry and MD files.
- `trading-model` finalized `SectorContextModel` V2.2 direction-neutral semantics: signed `2_sector_relative_direction_score`, trend quality/stability, transition risk, market-context support, breadth confirmation, dispersion/crowding, liquidity/tradability, separate `2_sector_handoff_state` and `2_sector_handoff_bias`, handoff rank/reason codes, eligibility, state quality, coverage, data quality, and evidence count.
- Layer 2 evaluation/promotion moved away from old readiness/long-only semantics to direction-neutral handoff checks: selected long/short bias counts, selected bias alignment rate, selected average absolute label, selected abs-label lift vs blocked, baseline improvement, split stability, and leakage checks.
- Real DB regeneration/evaluation was run for `trading_data.feature_02_sector_context` + `trading_model.model_02_sector_context`: 3250 model rows, 4030 feature rows, 7874 labels, 6040 selected handoff rows, leakage violations 0, selected bias alignment 0.5543, selected abs-label lift vs blocked 0.00849. Promotion review was persisted as correctly deferred because baseline/stability thresholds did not all pass (`maximum_stability_correlation_range` 1.5602 > 1.5, `minimum_baseline_improvement_abs` min -0.2988 < 0, `minimum_stability_sign_consistency` min 0.3333 < 0.66).
- `trading-manager` added registry migration `205_finalize_layer_two_v22_direction_neutral_contract.sql`, regenerated `scripts/registry/current.csv`, and registered Layer 2 V2.2 fields, handoff state/bias terms, and promotion threshold config.
- MD/doc updates completed across `trading-model`, `trading-manager`, `trading-data`, and `trading-storage`, including clearing completed Layer 2 active tasks and documenting that old `2_selection_readiness_score`/`2_selection_candidate_state` are compatibility aliases only, not durable downstream contract fields.
- Verification passed before final response: `trading-model` 65 tests OK, `trading-manager` 35 tests OK, `trading-data` 12 tests OK, registry dry-run no pending migrations, repo status checks clean for touched project repos.
- Pushed commits: `trading-model` 780cd9b, `trading-manager` 99eca39, `trading-data` 66f0a2a, `trading-storage` 458815c.

## Layer 3 Target State Vector implementation completed
- User asked to continue/complete `layer3-trading-data-implementation` after the subagent timed out; recovered manually and finished the Layer 3 target-state data/model/registry/doc path.
- `trading-data` implemented `source_03_target_state` and `feature_03_target_state_vector`: deterministic source normalizer, non-null merge handling for bar/liquidity evidence, natural key `target_candidate_id + timeframe + timestamp`, optional symbol retained only as source/audit/routing metadata, required `available_time`, JSONB feature block writer keyed by `target_candidate_id + available_time + target_state_vector_version`, storage DDL alignment, console scripts `trading-data-source-03-target-state` and `trading-data-feature-03-target-state-vector`, docs and tests.
- `trading-model` implemented the anonymous target candidate builder, target state vector generator, evaluation/promotion scaffold, scripts, promotion thresholds config, docs, and tests. Layer 3 remains bounded to `TargetStateVectorModel` / `target_state_vector`; it does not emit strategy selection, alpha confidence, final action, sizing, option contract, or execution outputs. Model-facing vectors reject raw ticker/company/symbol identity and downstream/action fields; routing/audit metadata may retain symbol refs separately.
- `trading-manager` added registry migrations `207_register_layer_three_target_state_entrypoints.sql` and `208_correct_layer_three_data_entrypoint_paths.sql`, registering stable Layer 3 source/feature/model generation, evaluation, and review entrypoints; regenerated `scripts/registry/current.csv`.
- Verification passed: `trading-data` 15 tests OK, `trading-model` 68 tests OK, `trading-manager` 35 tests OK, Layer 3 model scripts expose `--help`, and registry dry-run reports no pending migrations.
- Production promotion remains intentionally deferred until real-data Layer 3 evidence proves baseline improvement, split stability, no leakage, sufficient rows/labels, and reviewed approval.
- Pushed commits: `trading-model` d9fecd3, `trading-data` b04aa30, `trading-manager` 8f0e714.

## Layer 3 acceptance pass completed
- Ran a no-expansion Layer 3 acceptance pass after implementation completion.
- Confirmed no active subagents remained, registered Layer 3 script paths exist, all Layer 3 source/feature/model CLI help commands work, and `trading-manager` registry dry-run reports no pending migrations.
- Scanned active docs for stale Layer 3 draft/legacy strategy-selection wording; fixed one stale `trading-model/docs/README.md` line from draft to accepted Layer 3 contract wording.
- Targeted verification passed: `trading-model` Layer 3/anonymous candidate contract tests 9 OK; `trading-data` Layer 3 source/feature/sql tests 6 OK.
- Pushed doc-index cleanup commit: `trading-model` 10f78ee. Project repos clean afterward: `trading-model` 10f78ee, `trading-data` b04aa30, `trading-manager` 8f0e714, `trading-storage` 458815c.

## Layer 3 TargetStateVector registry value coverage completed
- User asked to put all state-vector values into the registry.
- `trading-manager` added migrations `209_register_complete_target_state_vector_values.sql` and `210_restore_sector_context_state_canonical_path.sql`, regenerated `scripts/registry/current.csv`, and expanded `tests/test_trading_registry.py` coverage.
- Registry now covers all accepted TargetStateVector V1 block/group payloads, score patterns, derived model payloads, synchronized windows, and fixed enum/window values: coverage check expected 58, missing 0.
- `SECTOR_CONTEXT_STATE` remains canonically owned by the Layer 2 contract while also marked as carried in Layer 3 `sector_state_features` / `target_state_vector` usage.
- `trading-model` updated `target_state_vector_contract.md` status from draft to accepted implementation/evaluation scaffold with production promotion still pending evidence/review.
- Verification passed: `trading-manager` registry tests 33 OK, full tests 35 OK, registry dry-run no pending migrations; `trading-model` targeted Layer 3 tests 9 OK; `trading-data` targeted Layer 3 tests 6 OK; diff checks clean.
- Pushed commits: `trading-manager` af3ea5d and `trading-model` c5facc0. `trading-data` remains b04aa30 and `trading-storage` remains 458815c. No subagents were used; active/recent subagent list was empty after completion.

## TargetStateVector state-vector value kind added
- User suggested creating a dedicated registry kind for values inside the state vector.
- `trading-manager` added dedicated `state_vector_value` kind for reviewed model state-vector contract tokens: block/group names, score-family names, diagnostic/derived payload names, window values, and enum/policy values that live inside the vector contract rather than ordinary table fields.
- Added kind docs at `scripts/registry/kinds/state_vector_value.md`, updated `scripts/registry/rules/kind-routing.md`, and added migrations `211_add_state_vector_value_kind.sql` + `212_remove_retired_data_derived_from_state_vector_value_constraint.sql`.
- Reclassified 60 TargetStateVector V1 payload/window/enum rows to `kind=state_vector_value` while preserving stable ids and leaving physical row metadata/ref fields as field-like kinds. `SECTOR_CONTEXT_STATE` remains canonically a Layer 2 `term` carried by Layer 3.
- Verification passed: `trading-manager` registry migrations applied/exported, dry-run reports no pending migrations, and `PYTHONPATH=src python3 -m unittest tests/test_trading_registry.py` passed 33 tests.
