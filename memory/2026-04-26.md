
## ThetaData provider term registered
- User identified ThetaData as the intended options-data provider for chain timeline, quote, trade, OHLC, Greeks, and related options datasets.
- ThetaData credential handling is special: credentials eventually need a `creds.txt` file beside `ThetaTerminalv3.jar` with email on line 1 and password on line 2. User said to defer that implementation/credential layout for now and only register ThetaData.
- Implemented `trading-main` commit `c4c3065`:
  - migration `012_register_thetadata_provider.sql` registers `THETADATA` as a `term` row applying to `trading-data`;
  - no ThetaData secret alias/config was registered yet.
- Implemented `trading-data` commit `d51c5bb` documenting ThetaData as the current options-data provider term and marking connector/JAR/creds.txt placement as a deferred source-connector decision.
- Verification passed: trading-main markdown links, migration dry-run no pending, Python unittest, ThetaData registry assertion; trading-data markdown links and check that no ThetaData secret alias/credential details were added.
