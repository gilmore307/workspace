
## Source secrets consolidated to one JSON file per source
- User corrected the secret layout: one provider/source should use one JSON secret file, not separate files/config aliases for each credential field. Parsing named fields belongs in `REGISTRY_LOAD_SECRET_TEXT_BY_CONFIG_ID_HELPER` / `SecretResolver.load_secret_text_by_config_id`.
- Consolidated local secret material outside Git:
  - OKX source alias `okx` -> `/root/secrets/okx.json`, JSON keys `api_key`, `secret_key`, `passphrase`.
  - GitHub source alias `github` -> `/root/secrets/github.json`, JSON key `pat`.
  - `/root/secrets/registry.json` now maps each source alias to its source-level JSON file with non-secret field descriptions.
  - Removed old split secret directories `/root/secrets/okx/` and `/root/secrets/github/` after consolidating values.
- Updated local git credential helper scripts under `/root/.config/openclaw/bin/` to read the GitHub PAT from `/root/secrets/github.json` instead of `/root/secrets/github/pat`; git auth smoke test passed with `git ls-remote`.
- Implemented `trading-main` commit `4a52621`:
  - migration `009_consolidate_source_secret_aliases.sql` deletes split OKX config rows and adds `OKX_SECRET_ALIAS` -> `okx` with path `/root/secrets/okx.json`;
  - adds `GITHUB_SECRET_ALIAS` -> `github` with path `/root/secrets/github.json`;
  - registers source-secret JSON field names `api_key`, `secret_key`, `passphrase`, and `pat` as `field` rows with `applies_to=source_secret_json`;
  - updates `SecretResolver.load_secret_text_by_config_id(config_id, field_name=None)` to return either raw JSON text or one named JSON string field.
- Implemented `trading-data` commit `84618c2` to replace split OKX aliases in docs with source-level `OKX_SECRET_ALIAS` / `okx` and `/root/secrets/okx.json` field names.
- Updated workspace `TOOLS.md` to make one JSON file per source/provider the standing secret-storage rule.
- Verification passed: no raw OKX secret values in checked repos/workspace; no old split OKX aliases in current docs/current CSV; trading-main migration dry-run no pending; Python unittest; markdown links; GitHub auth smoke.
