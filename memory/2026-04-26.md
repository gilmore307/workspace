
## Provider documentation URLs registered in provider term paths
- User asked whether registry `path` can store documentation website addresses for data sources because they will be consulted frequently.
- Adopted split locator convention:
  - provider `term` row `path` stores the canonical public documentation URL;
  - source-secret `config` row `path` continues to point at local `/root/secrets/<source>.json` files.
- Implemented `trading-main` commit `b155d62`:
  - migration `014_add_provider_documentation_paths.sql` sets provider documentation URLs for `OKX`, `ALPACA`, `THETADATA`, `FRED`, `CENSUS`, `BEA`, and `BLS`;
  - updated registry docs and `term` kind boundary to document provider documentation path usage.
- Implemented `trading-data` commit `bdb054a`:
  - data-source docs now list documentation paths for registered providers;
  - source-secret docs now explicitly distinguish provider documentation paths from secret config JSON paths.
- Verification passed: markdown links, registry migration dry-run no pending, Python unittest, provider documentation path assertions, secret config path assertions, and raw credential grep checks.

## Compaction checkpoint: trading-data provider registration state
- Current data-provider/source registry direction:
  - provider `term` rows may use `path` for canonical public documentation URLs;
  - source-secret `config` rows keep `path` pointed at local source JSON files under `/root/secrets/<source>.json`.
- Registered provider documentation paths now include OKX, Alpaca, ThetaData, FRED, Census, BEA, and BLS.
- Source-level credential aliases currently configured include `okx`, `alpaca`, `fred`, `census`, `bea`, and `bls`; ThetaData intentionally has no secret alias yet because its `ThetaTerminalv3.jar`/`creds.txt` placement is deferred.
- Latest pushed commits after provider documentation path work:
  - `trading-main`: `b155d62`
  - `trading-data`: `bdb054a`
  - workspace memory/docs notes: `126210b`
- Continue to avoid printing or committing raw provider API keys or credentials. When adding providers, register only provider terms, documentation URLs, source-level aliases, and reusable JSON field names unless a connector boundary has been accepted.

## U.S. Treasury Fiscal Data provider registered
- User provided U.S. Treasury Fiscal Data API documentation URL and noted the API may not need a key.
- Verified official docs page loads and states the API is open and does not require a user account or token.
- Implemented `trading-main` commit `e3bc017`:
  - migration `015_register_us_treasury_fiscal_data_provider.sql` registers provider term `US_TREASURY_FISCAL_DATA`;
  - provider term `path` is `https://fiscaldata.treasury.gov/api-documentation/`;
  - no source-secret/config alias was registered.
- Implemented `trading-data` commit `59825dd` documenting U.S. Treasury Fiscal Data as an open/no-key provider source for federal finance datasets including debt, revenue, spending, interest rates, and savings bonds.
- Verification passed: markdown links, registry migration dry-run no pending, Python unittest, Treasury registry assertions, and raw credential grep checks.

## Web-discovered calendar and ETF holdings source rules registered
- User specified three source-acquisition rules for `trading-data`:
  - FOMC calendar should come from the official Federal Reserve page.
  - Official macro release calendars should be found through web search and then confirmed against official government/issuing-agency pages.
  - ETF holdings constituents and weights should come from issuer websites or issuer-published holdings files.
- Implemented `trading-main` commit `38eb8e0`:
  - migration `016_register_web_discovered_source_terms.sql` registers `FOMC_CALENDAR`, `OFFICIAL_MACRO_RELEASE_CALENDAR`, and `ETF_ISSUER_HOLDINGS` as `term` rows;
  - `FOMC_CALENDAR.path` is `https://www.federalreserve.gov/monetarypolicy/fomccalendars.htm`;
  - no secret/config aliases were registered for these sources.
- Implemented `trading-data` commit `11b0068` documenting source-of-truth rules and connector requirements: preserve source URL, retrieval timestamp, publication/effective/as-of date when available, file/page format, official/secondary status, fixture sanitization, and live-call guardrails.
- Verification passed: markdown links, registry migration dry-run no pending, Python unittest, registry assertions for the three terms, and raw credential grep checks.

## Manager-driven historical data workflow formalized
- User clarified the intended `trading-data` workflow:
  - current data acquisition is historical only; realtime data belongs to `trading-execution` later;
  - `trading-manager` initiates data acquisition by giving `trading-data` a self-contained task key file;
  - `trading-data` uses the task key to invoke the specified historical acquisition script with the provided parameters;
  - cleaned outputs are written to the specified storage SQL destination once contracts are accepted;
  - a task completion receipt recording task status/evidence should live in `trading-storage`.
- Added suggested script bundle boundaries:
  - `alpaca_bars`;
  - `alpaca_market_events` for quote/trade/news;
  - `thetadata_option_1m_bundle` for chain timeline, quote, trade, OHLC, Greeks, and open interest at 1m;
  - `thetadata_option_snapshot_bundle` for requested-time snapshot + OI + Greeks;
  - `okx_bars`;
  - `macro_release_bundle`;
  - `calendar_discovery`;
  - `etf_holdings`.
- Implemented `trading-main` commit `d236c9f` registering workflow terms: `HISTORICAL_DATA_ACQUISITION`, `DATA_TASK_KEY_FILE`, and `DATA_TASK_COMPLETION_RECEIPT`.
- Implemented `trading-data` commit `466d7d9` documenting the manager-driven historical task-key workflow, task key requirements, acquisition script bundles, SQL output target boundary, and completion receipt requirements.
- Implemented `trading-manager` commit `d514100` documenting manager responsibility for issuing task key files and observing completion receipts.
- Implemented `trading-storage` commit `400a4e6` documenting storage responsibility for SQL destination contracts and durable completion receipt storage/reference formats.
- Verification passed across affected repos: markdown links, diff checks, registry migration dry-run no pending, Python unittest, registry assertions, and raw credential grep checks.

## Macro release bundles split by release event
- User corrected the previous `macro_release_bundle` planning shape: macro data from FRED, Census, BEA, BLS, Treasury, and official agency pages has different publication times and is usually not consumed simultaneously.
- Updated `trading-data` commit `592d391`:
  - replaced broad `macro_release_bundle` planning with `macro_release_<release_key>`;
  - documented that macro acquisition should be split by release event / release family / publication time / cadence;
  - macro data may be grouped only when records are published together or intentionally consumed as one release package;
  - task keys should preserve release key, source agency, publication timestamp/window, covered period, revision/vintage behavior, source URL, and SQL target.
- Verification passed: markdown links, diff check, docs assertions, and raw credential grep check.
