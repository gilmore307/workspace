
## Alpaca source-level credentials registered
- User provided Alpaca paper API credentials and endpoint for stock/ETF bars, quotes, trades, and news data acquisition.
- Stored secret material outside Git in `/root/secrets/alpaca.json` with JSON keys `api_key`, `secret_key`, and `endpoint`; updated `/root/secrets/registry.json` alias `alpaca` to point to that file.
- Implemented `trading-main` commit `3c432e3`:
  - migration `011_register_alpaca_source_secret.sql` registers term `ALPACA`, config `ALPACA_SECRET_ALIAS` -> `alpaca` with path `/root/secrets/alpaca.json`, and field `SOURCE_SECRET_ENDPOINT` -> `endpoint` with `applies_to=source_secret_json`;
  - docs/tests updated so `endpoint` is part of the reusable source-secret JSON key set;
  - SecretResolver verified against the local Alpaca JSON without printing secret values.
- Implemented `trading-data` commit `b6a080e` documenting Alpaca as the first stock/ETF data provider config surface.
- Updated workspace `TOOLS.md` to include Alpaca source-level JSON alias and the `endpoint` key convention.
- Verification passed: markdown links, registry migration dry-run no pending, Python unittest, Alpaca source JSON assertions, and raw Alpaca key grep checks for repos/workspace.
