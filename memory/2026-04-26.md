
## OKX credential metadata moved into source JSON
- User asked why `OKX_ALLOWED_IP_ADDRESS` and `OKX_API_KEY_REMARK_NAME` stayed as separate registry config rows instead of being included in the source JSON.
- Agreed and tightened the rule: even non-secret metadata that belongs to a source credential bundle should live in that source-level JSON file.
- Updated `/root/secrets/okx.json` to include fields: `api_key`, `secret_key`, `passphrase`, `allowed_ip_address`, `api_key_remark_name`.
- Updated `/root/secrets/registry.json` field descriptions for OKX accordingly.
- Implemented `trading-main` commit `71587c5`:
  - migration `010_move_okx_metadata_into_source_secret_json.sql` deletes `OKX_ALLOWED_IP_ADDRESS` and `OKX_API_KEY_REMARK_NAME` config rows;
  - registers `SOURCE_SECRET_ALLOWED_IP_ADDRESS` -> `allowed_ip_address` and `SOURCE_SECRET_API_KEY_REMARK_NAME` -> `api_key_remark_name` as field rows with `applies_to=source_secret_json`;
  - updates `OKX_SECRET_ALIAS` note to list all five JSON keys;
  - extends tests to resolve `allowed_ip_address` through `SecretResolver.load_secret_text_by_config_id(config_id, field_name)`.
- Implemented `trading-data` commit `bca1021` to document that OKX credential metadata lives in `/root/secrets/okx.json` under the same `OKX_SECRET_ALIAS` source-level alias.
- Verification passed: trading-main markdown links, migration dry-run no pending, Python unittest, OKX source JSON metadata assertions, raw secret grep; trading-data markdown links and raw secret/old-row-name grep.
