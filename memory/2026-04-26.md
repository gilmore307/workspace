## OKX access config stored and registered
- User provided OKX API credentials for crypto data acquisition and trading, plus OpenClaw server allowlisted public IPv4 `66.206.20.138` and OKX API key remark `OpenClaw`.
- Stored secret material outside Git under `/root/secrets/okx/`:
  - `okx/api-key` -> `/root/secrets/okx/api-key`
  - `okx/secret-key` -> `/root/secrets/okx/secret-key`
  - `okx/passphrase` -> `/root/secrets/okx/passphrase`
- Updated `/root/secrets/registry.json` with the three OKX aliases. Do not copy or commit the actual credential values.
- Implemented `trading-main` commit `bf24bf9`:
  - added migration `008_register_okx_access_config.sql`;
  - added payload format `ipv4_address`;
  - registered term `OKX`;
  - registered config rows `OKX_API_KEY_SECRET_ALIAS`, `OKX_SECRET_KEY_SECRET_ALIAS`, `OKX_PASSPHRASE_SECRET_ALIAS`, `OKX_ALLOWED_IP_ADDRESS`, and `OKX_API_KEY_REMARK_NAME`;
  - registry rows contain only aliases/non-secret metadata, not API key/secret/passphrase values.
- Implemented `trading-data` commit `72a9122`:
  - documented OKX as the first registered crypto provider config surface;
  - documented aliases and non-secret metadata in `docs/08_data_sources.md`;
  - updated context/tasks/decision/memory for OKX provider status.
- Verification passed: trading-main markdown links, migration dry-run no pending, Python unittest, registry assertions; trading-data markdown links and secret-value grep checks. Secret values were not found in repo files.
