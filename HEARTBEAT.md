# HEARTBEAT

- Review recent work for new shared names, repo identifiers, path fragments, config defaults, or templates that belong in `trading-main/registry/`.
- If Codex introduced temporary names, surface them for OpenClaw registration review and update relevant skills or docs when the naming rule changed.
