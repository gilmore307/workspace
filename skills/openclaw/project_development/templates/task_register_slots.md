# Task Register Slots

Formal machine-readable task-register schema is not fixed in this template yet.

If a project formalizes it, align field names with `universal-catalog`.
If the register records `task_lifecycle_state`, use the default catalog-registered vocabulary unless the project docs define an override.
Until then, record only the minimum task state the project actually uses.
